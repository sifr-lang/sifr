# sifr.resource - Deterministic cleanup helpers


class NullContext[T]:
    value: T

    def __enter__(self) -> T:
        return self.value

    def __exit__(self) -> None:
        pass


def nullcontext[T](own value: T = None) -> NullContext[T]:
    return NullContext(value)
