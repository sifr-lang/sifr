# Private _sifr.process native declaration module.


class ProcessError(Error):
    message: str


@rust(sifr_stdlib.process.process_run)
def process_run(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output)
def process_output(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin: bytes,
    has_stdin: bool,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_text)
def process_output_text(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin: bytes,
    has_stdin: bool,
    encoding: str,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_timeout)
def process_output_timeout(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin: bytes,
    has_stdin: bool,
    timeout: float,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_shell_run)
def process_shell_run(script: str) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_shell_output)
def process_shell_output(
    script: str,
    stdin: bytes,
    has_stdin: bool,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_shell_output_text)
def process_shell_output_text(
    script: str,
    stdin: bytes,
    has_stdin: bool,
    encoding: str,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_shell_output_timeout)
def process_shell_output_timeout(
    script: str,
    stdin: bytes,
    has_stdin: bool,
    timeout: float,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_stdout)
def process_output_stdout(handle: str) -> Result[bytes, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_stderr)
def process_output_stderr(handle: str) -> Result[bytes, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_stdout_text)
def process_output_stdout_text(handle: str) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_stderr_text)
def process_output_stderr_text(handle: str) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_status)
def process_output_status(handle: str) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_output_timed_out)
def process_output_timed_out(handle: str) -> bool:
    ...


@rust(sifr_stdlib.process.process_output_close)
def process_output_close(handle: str) -> None:
    ...


@rust(sifr_stdlib.process.process_spawn)
def process_spawn(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin_mode: str,
    stdout_mode: str,
    stderr_mode: str,
) -> Result[int, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_child_stdin)
def process_child_stdin(handle: int) -> Result[int, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_child_stdout)
def process_child_stdout(handle: int) -> Result[int, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_child_stderr)
def process_child_stderr(handle: int) -> Result[int, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_pipe_read_all)
def process_pipe_read_all(handle: int) -> Result[bytes, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_pipe_read)
def process_pipe_read(handle: int, max_bytes: int) -> Result[bytes, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_pipe_reader_close)
def process_pipe_reader_close(handle: int) -> Result[None, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_pipe_write_all)
def process_pipe_write_all(handle: int, data: bytes) -> Result[None, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_pipe_close)
def process_pipe_close(handle: int) -> Result[None, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_wait)
def process_wait(handle: int) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_kill)
def process_kill(handle: int) -> Result[None, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_terminate)
def process_terminate(handle: int) -> Result[None, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_run)
async def process_async_run(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin_mode: str,
) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_run_timeout)
async def process_async_run_timeout(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin_mode: str,
    timeout_seconds: float,
) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_output)
async def process_async_output(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin_mode: str,
    stdin: bytes,
    has_stdin: bool,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_output_timeout)
async def process_async_output_timeout(
    program: str,
    args: list[str],
    env: list[str],
    cwd: str,
    has_cwd: bool,
    stdin_mode: str,
    stdin: bytes,
    has_stdin: bool,
    timeout_seconds: float,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_shell_run)
async def process_async_shell_run(script: str) -> Result[list[int], ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_shell_output)
async def process_async_shell_output(
    script: str,
    stdin: bytes,
    has_stdin: bool,
) -> Result[str, ProcessError]:
    ...


@rust(sifr_stdlib.process.process_async_shell_output_timeout)
async def process_async_shell_output_timeout(
    script: str,
    stdin: bytes,
    has_stdin: bool,
    timeout_seconds: float,
) -> Result[str, ProcessError]:
    ...
