# Private _sifr.runtime native declaration module.


class DiagnosticError(Error):
    message: str


@rust(sifr_stdlib.runtime_observability.emit_diagnostic)
def runtime_emit_diagnostic(
    level: str,
    target: str,
    name: str,
    message: str,
) -> Result[None, DiagnosticError]:
    ...
