# Reference: m26_2
# Source issue: phase26-type-system-soundness-execution.md
def accepts_mixed(values: list[int | str]) -> int:
    return len(values)

def main():
    ints: list[int] = [1, 2, 3]
    size: int = accepts_mixed(ints)
    print(size)
