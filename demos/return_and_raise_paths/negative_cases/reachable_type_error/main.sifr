# Reference: return_and_raise_paths
# return_and_raise_paths negative case
# Reachable branch return mismatch should still be rejected.

def classify(flag: bool) -> int:
    if flag:
        return 1
    else:
        return 'bad'

def main():
    print(classify(False))
