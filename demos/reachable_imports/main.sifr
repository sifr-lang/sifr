# Reference: reachable_imports
# reachable_imports import-closure discovery demo
# Only modules reachable from this entrypoint should participate in frontend analysis.

from helper import compute

def main():
    print('reachable_imports import-closure discovery demo:')
    print(compute(6))
