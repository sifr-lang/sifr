# Reference: recursive_types
# Reference: compiler-feature-history
# feature surface: Recursive Types Demo
# Showcases self-referential types with automatic Box<T> wrapping

# === Linked List ===
class LinkedNode:
    val: int
    next: LinkedNode | None

    def __init__(self, val: int, next: LinkedNode | None):
        self.val = val
        self.next = next

# === Binary Tree ===
class TreeNode:
    val: int
    left: TreeNode | None
    right: TreeNode | None

    def __init__(self, val: int, left: TreeNode | None, right: TreeNode | None):
        self.val = val
        self.left = left
        self.right = right

def main():
    # Build a linked list: 1 -> 2 -> 3 -> None
    n3: LinkedNode = LinkedNode(3, None)
    n2: LinkedNode = LinkedNode(2, n3)
    n1: LinkedNode = LinkedNode(1, n2)
    print(n1.val)  # 1

    # Build a binary tree:
    #       1
    #      / \
    #     2   3
    left: TreeNode = TreeNode(2, None, None)
    right: TreeNode = TreeNode(3, None, None)
    root: TreeNode = TreeNode(1, left, right)
    print(root.val)  # 1
