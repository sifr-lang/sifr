# Reference: type_checking
# type-checking-sample demo
# Validates that frontend type-checking succeeds on a normal typed program.

def identity(x: int) -> int:
    return x

def main():
    value: int = identity(17)
    print("type_checking frontend-only check path demo:")
    print(value)
