# m24_5 analysis consolidation regression matrix demo
# Exercises nested conditionals and early-return paths used in consolidation parity checks.

from helper import evaluate

def main():
    print('m24_5 analysis consolidation regression matrix demo:')
    print(evaluate(10))
    print(evaluate(0))
