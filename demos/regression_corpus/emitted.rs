// src/main.rs
fn main() {
    println!("regression_corpus fixedbugs and crashes corpus demo");
    println!("fixedbugs index links issue -> root cause -> executable regression");
    println!("crashes index keeps unresolved sentinels visible until promotion");
}
