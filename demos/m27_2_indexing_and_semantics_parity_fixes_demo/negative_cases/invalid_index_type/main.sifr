# Reference: m27_2
# Source issue: phase27-runtime-safety-and-diagnostics-execution.md
# negative path: invalid index type must fail with a deterministic diagnostic.

def main():
    items: list[int] = [1, 2, 3]
    _ = items["0"]
