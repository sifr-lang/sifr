# Reference: m_adhoc_1
# Source issue: ad-hoc-entrypoint-compilation-unification-and-dependency-metadata-closure-execution.md
# Milestone adhoc 1 demo:
# rooted entrypoint compilation still follows the reachable local import closure.
from helper import render


def main():
    print(render())
