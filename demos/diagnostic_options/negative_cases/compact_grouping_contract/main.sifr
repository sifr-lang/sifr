# Reference: m27_6
# Source issue: phase27-runtime-safety-and-diagnostics-execution.md
# negative path:
# repeated diagnostics must stay deterministic in compact mode with stable grouping.

def main():
    a: int = "a"
    b: int = "b"
    c: int = "c"
    d: int = "d"
    e: int = "e"
    f: int = "f"
    g: int = "g"
    h: int = "h"
