# Reference: diagnostic_options
# negative path:
# `--diagnostic-format <unknown>` must fail with exit code 2 before semantic work.

def main():
    print("unknown format should fail before this program is analyzed")
