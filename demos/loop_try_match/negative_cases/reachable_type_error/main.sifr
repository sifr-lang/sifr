# Reference: loop_try_match
# loop_try_match negative case
# Reachable type mismatch in a nested branch should fail frontend validation.

def broken(flag: bool) -> int:
    if flag:
        return 1
    else:
        return 'bad'

def main():
    print(broken(False))
