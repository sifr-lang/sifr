# Reference: mode_consistency
# mode_consistency parity regression matrix demo
# Primary positive fixture used by the parity matrix across check/build/run/test.

from helper import value

def main():
    print('mode_consistency parity regression matrix demo:')
    print(value(1))
