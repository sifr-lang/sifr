# Reference: compiled_expressions
# compiled-expressions-sample negative-path demo
# Must fail in lowering/type-checking with an explicit return-type mismatch diagnostic.

def bad() -> int:
    return "oops"

def main():
    print(bad())
