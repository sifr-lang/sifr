# Demo: codegen_intrinsic_migration (registry scaffold)
# This demo exercises canonical intrinsic-backed math operations and the public pow wrapper.
from sifr.math import sqrt, floor, ceil, pow, atan2, isfinite

def main():
    base: float = 9.0
    root: float = sqrt(base)
    rounded_down: int = floor(3.9)
    rounded_up: int = ceil(3.1)
    powered: float = pow(2.0, 3.0)
    rounded: int = 0
    try:
        converted_rounded: int = round(3.6)
        rounded = converted_rounded
    except ValueError:
        assert False
    angle: float = atan2(1.0, 1.0)
    finite: bool = isfinite(powered)

    print(f"root = {root}")
    assert str(f"root = {root}") == "root = 3"
    print(f"rounded_down = {rounded_down}")
    assert str(f"rounded_down = {rounded_down}") == "rounded_down = 3"
    print(f"rounded_up = {rounded_up}")
    assert str(f"rounded_up = {rounded_up}") == "rounded_up = 4"
    print(f"powered = {powered}")
    assert str(f"powered = {powered}") == "powered = 8"
    print(f"rounded = {rounded}")
    assert str(f"rounded = {rounded}") == "rounded = 4"
    print(f"angle_positive = {angle > 0.0}")
    assert str(f"angle_positive = {angle > 0.0}") == "angle_positive = true"
    print(f"finite = {finite}")
    assert str(f"finite = {finite}") == "finite = true"
