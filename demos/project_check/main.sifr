# Reference: project_check
# project_check project-aware check parity demo
# Valid local imports in a multi-file project should pass in `sifr check` and `sifr run`.

from helper import area_like

def main():
    print('project_check project-aware check parity demo:')
    print(area_like(3.0))
