# m24_1 negative case
# Reachable type mismatch in a nested branch should fail frontend validation.

def broken(flag: bool) -> int:
    if flag:
        return 1
    else:
        return 'bad'

def main():
    print(broken(False))
