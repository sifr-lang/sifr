# Reference: diagnostic_exit_codes
# diagnostic_exit_codes cross-mode diagnostic and exit behavior demo
# Use with `check`, `build`, and `run` to validate frontend diagnostic parity.

from helper import doubled

def main():
    print('diagnostic_exit_codes cross-mode diagnostic and exit behavior demo:')
    print(doubled(21))
