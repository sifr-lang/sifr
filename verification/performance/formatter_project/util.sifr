def collect(values: dict[str, list[int]]) -> Result[list[int], str]:
    match len(values):
        case 0:
            return Err("missing")
        case _:
            items: list[int] = values["items"]
            return Ok([item for item in items if item > 0])
