{
  "schema_version": 1,
  "profile": "python-interop-live",
  "status": "policy-only",
  "required_resource_classes": [
    "container-runtime",
    "network",
    "platform-specific"
  ],
  "offline_profiles": [
    "create-pr",
    "merge",
    "nightly",
    "release"
  ],
  "offline_forbidden_suites": [
    "live-policy",
    "live-examples"
  ],
  "container_runtime": {
    "provider": "testcontainers",
    "docker_required": true,
    "image_pull_policy": "explicit-profile-only",
    "cleanup_required": true,
    "timeout_seconds": 1800
  },
  "live_suites": [
    "live-policy",
    "live-examples"
  ],
  "live_examples": {
    "provider": "testcontainers",
    "services": [
      "redis",
      "postgres",
      "kafka-compatible",
      "pubsub-compatible",
      "aws-compatible-sns",
      "aws-compatible-sqs",
      "aws-compatible-sns-sqs"
    ],
    "sifr_sources_required": true,
    "structured_skip_when_docker_unavailable": true
  },
  "result_statuses": [
    "policy-passed",
    "live-passed",
    "structured-skip",
    "live-failed"
  ],
  "artifact_root": "target/verification/areas/python_interop",
  "notes": "This policy gate is intentionally non-containerized. Any live dependency suite must use this profile/resource contract. Service aliases map to concrete live examples as redis, postgres, kafka-compatible, pubsub-compatible over LocalStack SNS-to-SQS fanout, aws-compatible-sns over LocalStack SNS delivery to SQS, aws-compatible-sqs over LocalStack SQS, and aws-compatible-sns-sqs as the shared LocalStack topology."
}
