# ============================================================
# Test 5: Import from multiple stdlib modules
# ============================================================

from sifr.math import sqrt
from sifr.json import json_dumps

def main():
    print(sqrt(4.0))
    print(json_dumps("hello"))
