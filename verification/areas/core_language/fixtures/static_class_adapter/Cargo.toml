[package]
name = "static-class-adapter"
version = "0.0.0"
edition = "2024"

[dependencies]
sifr_runtime = { path = "../../../../../crates/sifr_runtime", features = ["structural"] }

[package.metadata.sifr]
manifest = "sifr.toml"

[workspace]
