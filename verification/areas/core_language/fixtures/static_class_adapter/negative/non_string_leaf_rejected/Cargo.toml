[package]
name = "static-class-adapter-non-string-negative"
version = "0.0.0"
edition = "2024"

[dependencies]
static-class-adapter = { path = "../.." }

[package.metadata.sifr]
manifest = "sifr.toml"

[workspace]
