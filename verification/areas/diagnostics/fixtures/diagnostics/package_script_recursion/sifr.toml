[package]
name = "package_script_recursion"
edition = "2026"
sifr-version = ">=0.3,<0.4"

[source]
root = "src"

[scripts]
dev = { command = "run", args = ["other"] }
other = { command = "check", args = [] }
