[package]
name = "package-untrusted-backend"
version = "0.1.0"
edition = "2024"

[package.metadata.sifr]
manifest = "sifr.toml"

[dependencies]
rust-helper = { path = "rust-helper" }

[lib]
path = "src/lib.rs"

[workspace]
