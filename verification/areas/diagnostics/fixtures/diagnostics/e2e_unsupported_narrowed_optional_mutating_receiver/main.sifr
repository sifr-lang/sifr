# expect-error: SIFR-OWN-0014

class Helper:
    items: list[int]

    def bump(mut self) -> None:
        self.items.append(1)


def mutate() -> None:
    maybe: Helper | None = Helper([])
    if maybe is not None:
        maybe.bump()
