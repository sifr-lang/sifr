# Network HTTP Dependency Audit

Status: closed, audited through network/HTTP readiness implementation. Generated dependency snapshots are resolver-backed by `crates/sifr_stdlib_manifest/tests/network_http_dependency_snapshots.rs`; current lockfile deltas are recorded below.

| Crate family | Accepted features | Sifr abstraction hiding crate | Typed error mapping | Panic/unsafe audit | License/MSRV/binary/platform impact | Deterministic tests and conformance | Maintenance signal |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `tokio` 1.53.1 | Provider baseline `macros`, `rt`, `sync`, `time`, `process`, `signal` plus network-owned `net`, `io-util`; no `full`, `rt-multi-thread`, `fs`, `parking_lot`, or `tokio_unstable` | `_sifr.net`/`_sifr.tls`/HTTP intrinsics and generated current-thread runtime support | `NetError`, `TlsError`, and `HttpError` carry timeout/I/O/cancellation/closed evidence as stable messages | No public Tokio handles; user data paths must map I/O results without `unwrap`/`expect`; no network-owned process/signal API is introduced | Existing workspace dependency; workspace Rust is 1.98; network feature adds socket/I/O feature surface and inherits provider process/signal features | async-network capability loopback TCP/DNS/cancellation fixtures and generated dependency snapshots | Mature Rust async runtime; already provider substrate |
| `tokio-util` 0.7.18 | conditional `rt`, `io-util`, `time` only | internal cancellation/I/O helpers | lower-level errors mapped before escaping | token/codec/compat types forbidden in public API | Conditional production dependency only if async-network capability/HTTP-transport capability proves need | async-network capability/HTTP-transport capability helper-specific loopback tests if enabled | Mature Tokio ecosystem helper crate |
| `bytes` 1.12.1 | default std | internal zero-copy buffer representation behind Sifr `bytes` | size/copy failures map to owner API errors such as `BodyError` or `HttpError` | `bytes::Bytes` never exposed; slicing must be bounds-checked | Small common dependency; production only for HTTP transport bodies in the current generated snapshots | HTTP-transport capability chunk ownership/equality/redaction fixtures | Widely maintained HTTP ecosystem primitive |
| `tracing` 0.1.44 | `default-features = false`, `std`; no `attributes` | Sifr observability hooks and structured event schema | event fields include typed error kind/cause; no exception strings as control flow | no subscriber/exporter/global setup in substrate | Existing workspace dependency; low binary impact without subscriber | deterministic span/event capture in tests if subscriber is dev-only | Mature Rust observability facade |
| `socket2` 0.6.4 | default only; no `all`; currently not emitted in generated production snapshots | async-network capability socket-option builder internals if a future accepted option requires it | unsupported/host-specific options map to `NetError` with host-option evidence | no raw socket constants or descriptors exposed | Conditional; host rows required for every option before activation | host-matrix fixtures required for accepted socket options if enabled | Maintained low-level socket crate |
| `metrics` 0.24.6 | conditional facade only | optional metrics hook schema | emission failures are not user control flow | no recorder/exporter/global install | Deferred until network/HTTP readiness schema approval | deterministic counter/histogram-name tests if enabled | Common facade; exporter crates deferred |
| `url` 2.5.8 | std only; no `serde`, no `expose_internals` | `sifr.url.Url`, `UrlQuery`, `UrlError` | parse/validation/provider-blocked states map to `UrlError` | wrapper rejects non-ASCII host before accidental IDNA until text/i18n sign-off | Workspace dependency and generated dependency snapshot use 2.5.8 | RFC/CPython URL fixtures; non-ASCII host blocked fixture | Maintained URL parser with standards focus |
| `percent-encoding` 2.3.2 | std/default | `sifr.url` percent helpers | invalid percent and provider-blocked encodings map to `UrlError` | no local codec registry; named encodings call text/i18n | Small dependency | percent encode/decode property and regression fixtures | Stable Rust URL ecosystem crate |
| `rustls` =0.23.43 | `aws_lc_rs` is the default provider. RFC 9149 ticket requests have explicit bounds. Ring, FIPS, compression, early data, and custom providers are disabled. | Sifr keeps the config and stream internals in `sifr.tls`. | `TlsError` and `CertificateError` contain stable TLS, certificate, and lower-transport error messages. | Sifr does not expose raw Rustls types. Handshake and verification errors do not cause a panic. | The TLS capability records the CMake toolchain, cross-compilation, binary size, and host behavior. | The deterministic CA, self-signed, and mTLS loopbacks pass. The exact client and server ticket-policy test passes. | Maintained TLS stack |
| `tokio-rustls` 0.26.4 | default `aws_lc_rs`; no early-data/compression | `TlsStream`, `TlsReadHalf`, `TlsWriteHalf` | I/O and TLS failures mapped to `TlsError` with deterministic lower-layer evidence | synchronized split implementation uses accepted utilities, not bespoke session sharing | follows Rustls/aws-lc-rs platform impact | TLS read/write/flush/close_notify/split fixtures | Standard Tokio/Rustls adapter |
| `rustls-platform-verifier` 0.7.0 | default features disabled unless host proof requires one | `TlsClientConfig.default_verified()` root strategy | verifier failures map to `CertificateError` | no direct `webpki-roots`, local file, env, or best-effort fallback; verifier-managed transitives include platform/native-root crates such as `webpki-root-certs`, `rustls-native-certs`, `security-framework`, `openssl-probe`, and target-gated helpers | TLS capability host matrix required for macOS/Linux/Windows | invalid cert, platform-host behavior, explicit-root tests | Maintained platform verifier crate |
| `rustls-pemfile` 2.2.0 | std/default | PEM config loader for Sifr TLS config | parse failures map to TLS config errors | no certificate display parser; no x509-parser | Small config-only dependency | malformed PEM and key/cert mismatch fixtures | Rustls project crate |
| `http` 1.5.0 | std/default | `sifr.http` method/status/version/header/head types | `HeaderError`, `ProtocolError`, `HttpError` | crate types hidden; all validation passes through Sifr rules | Small common HTTP type dependency | URL/header/cookie capability/HTTP-transport capability header/status/method fixtures, including the registered `QUERY` method | Maintained Hyper ecosystem crate |
| `http-body` 1.1.0 | default | internal body trait adapter only | `BodyError` and `HttpError` carry body limit/reset/cancellation evidence as stable messages | public body streams remain Sifr-owned | Small HTTP dependency | HTTP-transport capability body streaming/limit/reset fixtures | Maintained HTTP ecosystem crate |
| `http-body-util` 0.1.5 | default-features false | bounded full-body adapters | adapter errors mapped before public boundary | no public adapter types | Conditional; no unused channel/stream feature | HTTP-transport capability body-limit fixtures | Maintained HTTP ecosystem helper |
| `hyper` 1.11.0 | `http1`, `http2`, `client`, `server`; no `full`, unstable `tracing`, FFI, nightly | internal HTTP transport drivers | `HttpError`, `ProtocolError`, and `BodyError` carry transport/TLS/protocol/body evidence as stable messages | Hyper types hidden; malformed user input maps to typed errors | HTTP-transport capability records generated dependency/binary impact | HTTP/1.1 framing and HTTP/2 loopback plus malformed protocol fixtures | Maintained production HTTP stack |
| `hyper-util` 0.1.20 | conditional `tokio` first; more only with proof; no pools/proxy/full/server-auto | internal adapter helper only | adapter errors mapped to transport/protocol errors | no public lifecycle/shutdown/type dependency | Conditional; requires `hyper_util_necessity.md` | HTTP-transport capability proof artifact and loopback fixtures if enabled | Maintained Hyper companion crate |
| `h2` 0.4.19 | automatic production DATA-frame budget; an explicit low budget in the direct protocol fixture | HTTP/2 conformance internals | stream/frame errors map to `ProtocolError`/`HttpError` | no public h2 types; limits wrap resource behavior | direct dependency retained for protocol-level regression coverage; lockfile coherence required | SETTINGS, frame-budget, flow control, PING, RST_STREAM, GOAWAY, HPACK fixtures | Maintained HTTP/2 crate used by Hyper |
| `cookie` | rejected for this capability; no production crate is emitted | Sifr-owned cookie-header parser | invalid cookie syntax maps to `HeaderError` | no persistence, signing, encryption, or percent-decoded text | no production dependency | URL/header/cookie capability header-level cookie fixtures | Future cookie jar/signing capability may re-evaluate a crate |
| `tower-service` 0.3.3 | crate only; no full `tower` | internal capability handoff handoff shape | service failures map to Sifr HTTP errors | trait never appears in public API | Small dependency | HTTP-transport capability/network/HTTP readiness handoff adapter tests if used | Stable minimal service trait |
| Ring 5 `tokio-test`, `proptest`, `rcgen`, `tracing-subscriber` | These dependencies are only for development, tests, and demos. TLS fixtures lock `rcgen` to 0.14.9. | Test harnesses and demos. | Test-only errors. | Production snapshots do not contain these dependencies. | These crates are not production dependencies. | The network and HTTP fixtures use these crates. TLS runtime tests use `rcgen` only through `dev-dependencies` and assert canonical `ExplicitNoCa` DER. | Common fixture crates. |

## Rejected Or Deferred Direct Dependencies

| Dependency | Terminal state | Rationale | Revisit rule |
| --- | --- | --- | --- |
| `hickory-resolver` | deferred-to-future-capability | TCP connect/address resolution uses `tokio::net::lookup_host`; custom resolver records and DNS policy are outside this substrate. | Future resolver issue must define record APIs, cache behavior, DNSSEC stance, host support, and deterministic fixtures. |
| `x509-parser` | deferred-to-future-capability | Public certificate subject/issuer/SAN display is outside TLS capability; TLS errors carry typed verification evidence and fingerprints only. | Future certificate-inspection issue must consume text/i18n Unicode/display decisions. |
| `webpki-roots` | rejected | Production verification uses platform roots; deterministic tests use explicit in-memory roots. No fallback trust store. | Only a platform-verifier blocker with a security validation may revisit. |
| `reqwest`, `ureq`, `isahc`, `surf` | rejected | High-level HTTP clients belong to the future Sifr-native HTTP client capability. | Future client capability must build on this substrate, not wrap a policy-heavy client crate as stdlib. |
| `axum`, `warp`, `actix-web`, `rocket`, `tower-http` | rejected | Server framework behavior belongs to capability handoff. | capability handoff may evaluate implementation helpers only behind Sifr-owned public APIs. |
