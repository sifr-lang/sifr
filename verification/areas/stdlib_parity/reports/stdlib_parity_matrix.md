# Capability 30 Parity Matrix and Waiver Inventory

Canonical columns for every module behavior row:

- `module`
- `behavior`
- `status` (`done` | `open`)
- `classification` (`parity` | `intentional-diff` | `unsupported`)
- `rationale`
- `owner`
- `tracking_issue`
- `revisit_rule`
- `evidence`

| module | behavior | status | classification | rationale | owner | tracking_issue | revisit_rule | evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| env | missing-key behavior without explicit default | done | intentional-diff | CPython exposes `os.getenv(key)` directly; Sifr currently exposes explicit no-default path as `getenv_opt(key)` because imported-function default arguments are not yet applied at call sites | python-parity execution loop | `python-parity reliability execution record` | Revisit when imported-function default-argument lowering is implemented | `stdlib/sifr/env.sifr`, `crates/sifr/tests/e2e/pass/cpython_env_subset.sifr`, `demos/env/main.sifr` |
| env | invalid env keys (`""`, contains `"="`) for set/get paths | done | intentional-diff | CPython can raise on invalid environment names; Sifr safety rules forbids panic/exception control-flow and keeps invalid-key handling panic-free (`None`/no-op) | python-parity execution loop | `python-parity reliability execution record` | Revisit if typed stdlib error modeling for env is introduced | `crates/sifr/tests/e2e/pass/cpython_env_subset.sifr`, `crates/sifr_codegen/src/intrinsics/env.rs`, `demos/env/main.sifr` |
| bytes | first-class encode/decode/hex/search/prefix subset | done | parity | CPython-derived behavior is implemented on the first-class `bytes` type and validated with canonical vectors and effectful-receiver checks | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython bytes object-model surface is promoted into approved scope | `crates/sifr_codegen/src/methods/bytes.rs`, `crates/sifr/tests/e2e/pass/cpython_bytes_subset.sifr`, `crates/sifr/tests/e2e/pass/bytes_helpers.sifr` |
| bytes | mutable `bytearray` and subclass-heavy object-model features remain outside the approved surface | done | intentional-diff | Sifr keeps immutable first-class bytes with checked constructors and methods; it does not expose the removed list-backed transition module | python-parity execution loop | `python-parity reliability execution record` | Revisit only if a distinct mutable binary-buffer type is approved | `crates/sifr_codegen/src/methods/bytes.rs`, `crates/sifr/tests/e2e/pass/bytes_hex_and_binary_io.sifr` |
| base64 | base64/base32/base16 encode-decode behavioral subset | done | parity | CPython-derived base64 subset behaviors are validated with canonical vector fixtures | python-parity execution loop | `python-parity reliability execution record` | Revisit when additional CPython option flags/surfaces are brought into approved scope | `stdlib/sifr/base64.sifr`, `crates/sifr/tests/e2e/pass/cpython_base64_subset.sifr`, `demos/base64/main.sifr` |
| base64 | decode error signaling uses `Result[..., ParseError]` rather than CPython exceptions | done | intentional-diff | Sifr safety rules uses typed Result/Option adaptation instead of exception control flow | python-parity execution loop | `python-parity reliability execution record` | Revisit if typed stdlib error-class expansion changes base64 error model | `stdlib/sifr/base64.sifr`, `crates/sifr/tests/e2e/pass/cpython_base64_subset.sifr`, `crates/sifr/tests/e2e/pass/parse_safety_error_paths.sifr` |
| hashlib | constructor/object update/copy/hexdigest/file_digest behavioral subset | done | parity | CPython-derived hashlib API/object-model subset is validated in canonical vector fixtures and demo | python-parity execution loop | `python-parity reliability execution record` | Revisit when additional digest algorithms/options are promoted into approved scope | `stdlib/sifr/hashlib.sifr`, `crates/sifr/tests/e2e/pass/cpython_hashlib_api_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_hashlib_object_model_subset.sifr`, `demos/hashlib/main.sifr` |
| hashlib | `digest()` returns bytes while the distinct `hexdigest()` operation returns hexadecimal text; sha3/shake constructors are explicit unsupported placeholders | done | intentional-diff | The canonical object model is bytes-native and keeps typed unsupported results for algorithms outside the approved dependency set | python-parity execution loop | `python-parity reliability execution record` | Revisit when sha3/shake runtime support is approved | `stdlib/sifr/hashlib.sifr`, `crates/sifr/tests/e2e/pass/bytes_hashing_and_base64.sifr`, `crates/sifr/tests/e2e/pass/cpython_hashlib_api_subset.sifr` |
| math | CPython-derived numeric semantic subset (transcendentals, IEEE helpers, aggregate numerics) | done | parity | Canonical vector fixtures validate stabilized numeric surface and special-value handling for approved scope | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython corpora are promoted from subset to full-surface coverage | `stdlib/sifr/math.sifr`, `crates/sifr/tests/e2e/pass/cpython_math_semantic_corrections_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_math_missing_surface_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_math_consolidated.sifr`, `demos/math/main.sifr` |
| math | safe adaptation for unsupported/invalid numeric semantics uses deterministic non-throwing rules | done | intentional-diff | Sifr numeric wrappers avoid exception control flow and use deterministic boolean/NaN-safe adaptation for invalid tolerance and dimension mismatch cases in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit if typed numeric error modeling is introduced for math APIs | `stdlib/sifr/math.sifr`, `crates/sifr/tests/e2e/pass/cpython_math_semantic_corrections_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_math_missing_surface_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_math_consolidated.sifr` |
| statistics | mean/median/variance/stdev plus dispersion and regression helper subset | done | parity | Canonical CPython-derived fixture and demo validate central tendency, spread, mode, quantiles, covariance, correlation, and linear regression behavior in approved scope | python-parity execution loop | `python-parity reliability execution record` | Revisit when weighted/decimal/fractional CPython statistics surfaces are promoted into scope | `stdlib/sifr/statistics.sifr`, `crates/sifr/tests/e2e/pass/cpython_statistics_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_statistics_consolidated.sifr`, `demos/statistics/main.sifr` |
| statistics | invalid-input semantics use typed `StatisticsError` instead of CPython exception taxonomy | done | intentional-diff | Sifr safety rules uses typed error adaptation and deterministic error-return behavior for empty/invalid datasets | python-parity execution loop | `python-parity reliability execution record` | Revisit if typed stdlib error hierarchy alignment expands for statistics module | `stdlib/sifr/statistics.sifr`, `crates/sifr/tests/e2e/pass/cpython_statistics_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_statistics_consolidated.sifr` |
| bisect | insertion-point and sorted-insert subset (`bisect_left`, `bisect_right`, `insort_left`, `insort_right`) | done | parity | Canonical CPython-derived fixture and demo validate insertion-point behavior for empty, duplicate, and generic comparable inputs plus deterministic in-place insertion behavior | python-parity execution loop | `python-parity reliability execution record` | Revisit when optional-parameter parity (`lo`, `hi`, `key`) is promoted into approved scope | `stdlib/sifr/bisect.sifr`, `crates/sifr/tests/e2e/pass/cpython_bisect_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_bisect_consolidated.sifr`, `demos/bisect/main.sifr` |
| bisect | CPython's optional `key` parameter is out of approved scope | done | intentional-diff | The canonical API keeps `lo` and `hi` bounds and one mutating insertion model; callers copy explicitly before insertion when they need to preserve the source list | python-parity execution loop | `python-parity reliability execution record` | Revisit when key-function support is scheduled | `stdlib/sifr/bisect.sifr`, `crates/sifr/tests/e2e/pass/cpython_bisect_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_bisect_consolidated.sifr` |
| heapq | heap ordering and selection subset (`heapify`, `heappush`, `heappop`, `nsmallest`, `nlargest`) | done | parity | Canonical CPython-derived fixture and demo validate min-heap ordering, pop order determinism, and top-k selection behavior for approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython heapq helper surface is promoted into scope | `stdlib/sifr/heapq.sifr`, `crates/sifr/tests/e2e/pass/cpython_heapq_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_heapq_consolidated.sifr`, `demos/heapq/main.sifr` |
| heapq | empty-pop adaptation diverges from CPython exception behavior | done | intentional-diff | Sifr keeps empty operations panic-free (`None`); heapify, push, pop, replace, and push-pop otherwise use one explicit mutating model | python-parity execution loop | `python-parity reliability execution record` | Revisit when typed stdlib error hierarchy is scheduled | `stdlib/sifr/heapq.sifr`, `crates/sifr/tests/e2e/pass/cpython_heapq_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_heapq.sifr`, `crates/sifr/tests/e2e/pass/stdlib_heapq_consolidated.sifr` |
| string | constants and `capwords` whitespace-normalized subset | done | parity | Canonical CPython-derived fixture and demo validate constants plus `capwords` behavior across repeated spaces and CPython whitespace classes (`space`, `tab`, `newline`, `carriage-return`, `vertical-tab`, `form-feed`) | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader `string` helper surface is promoted into approved scope | `stdlib/sifr/string.sifr`, `crates/sifr/tests/e2e/pass/cpython_string_subset.sifr`, `demos/string/main.sifr` |
| string | CPython optional `capwords(sep=...)` parameter and broader module helpers remain out of approved subset | done | intentional-diff | Current approved scope keeps `capwords(s)` and constants only; optional separator API and additional helpers are tracked for future expansion | python-parity execution loop | `python-parity reliability execution record` | Revisit when `string` scope expansion is scheduled in later implementation passes | `stdlib/sifr/string.sifr`, `crates/sifr/tests/e2e/pass/cpython_string_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_string.sifr` |
| textwrap | wrapping/filling/dedent/indent/shorten subset with deterministic whitespace normalization | done | intentional-diff | Sifr normalizes mixed whitespace classes to single spaces before wrapping/filling/shortening; this keeps deterministic safety-focused behavior but can differ from CPython's richer TextWrapper tokenization for mixed whitespace inputs | python-parity execution loop | `python-parity reliability execution record` | Revisit when richer CPython-compatible tokenization and TextWrapper option expansion are scheduled | `stdlib/sifr/textwrap.sifr`, `crates/sifr/tests/e2e/pass/cpython_textwrap_subset.sifr`, `demos/textwrap/main.sifr` |
| textwrap | optional TextWrapper parameters and configurable predicates remain out of approved subset; width safety is enforced for `wrap`/`fill` only | done | intentional-diff | Current approved scope keeps core helpers and defaults, with Sifr-safe deterministic behavior and no partial option emulation | python-parity execution loop | `python-parity reliability execution record` | Revisit when TextWrapper options (`initial_indent`, `subsequent_indent`, `drop_whitespace`, `placeholder`, custom predicate) are scheduled | `stdlib/sifr/textwrap.sifr`, `crates/sifr/tests/e2e/pass/cpython_textwrap_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_textwrap.sifr` |
| fnmatch | deterministic case-sensitive wildcard subset (`*`, `?`) for `fnmatch`, `filter`, and `filterfalse` | done | parity | Canonical CPython-derived fixtures validate the one matching operation and its two distinct list-selection operations | python-parity execution loop | `python-parity reliability execution record` | Revisit when bracket character classes and escaping semantics are promoted into approved scope | `stdlib/sifr/fnmatch.sifr`, `crates/sifr/tests/e2e/pass/cpython_fnmatch_subset.sifr`, `demos/fnmatch/main.sifr` |
| fnmatch | bracket character classes (`[seq]`, `[!seq]`) and platform-normalized case-folding behavior are out of approved subset | done | intentional-diff | Current scope keeps deterministic case-sensitive matching with `*`/`?` only and avoids platform-specific `normcase` behavior | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython fnmatch syntax and platform-dependent normalization are scheduled | `stdlib/sifr/fnmatch.sifr`, `crates/sifr/tests/e2e/pass/cpython_fnmatch_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_fnmatch.sifr` |
| re | regex search/sub/findall/split/fullmatch plus flag-aware search subset | done | parity | Canonical CPython-derived fixture and demo validate approved regex helper surface with deterministic Result-to-typed-value adaptation via explicit `try` handling in parity vectors | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader Pattern/Match object-model coverage is promoted into approved scope | `stdlib/sifr/re.sifr`, `crates/sifr/tests/e2e/pass/cpython_re_subset.sifr`, `demos/regex/main.sifr` |
| re | advanced CPython regex surface (groups/captures/backrefs/verbose tokenization and full parser-compat edges) remains outside approved subset | done | intentional-diff | Current scope stabilizes core helper APIs and safety-aligned error adaptation without claiming full CPython `re` engine parity | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython `test_re.py` parity expansion is scheduled with explicit grouping/capture semantics | `stdlib/sifr/re.sifr`, `crates/sifr/tests/e2e/pass/cpython_re_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_re.sifr` |
| collections | first-class generic `set[T]`, `Counter`, `from_list`, and `deque` behavioral subset | done | parity | Canonical fixtures and demos validate built-in set construction/mutation/operations, counter aggregation, and bounded deque behavior | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader `collections` object-model coverage is promoted into approved scope | `stdlib/sifr/collections.sifr`, `crates/sifr/tests/e2e/pass/cpython_collections_subset.sifr`, `demos/collections/main.sifr` |
| collections | advanced CPython collections surface (`defaultdict` object model, `namedtuple`, `OrderedDict`, `ChainMap`, `UserDict`, `UserList`, `UserString`) remains out of approved subset | done | intentional-diff | Current scope stabilizes built-in set, counter, and deque behavior and leaves the broader CPython collections ecosystem for later capability expansion | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython `test_collections.py` feature classes are scheduled | `stdlib/sifr/collections.sifr`, `crates/sifr/tests/e2e/pass/cpython_collections_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_collections.sifr` |
| itertools | sequence-combinator subset (`chain`, `repeat`, `take`, `flatten`, `pairwise`, `batched`, `islice`, `accumulate`, `compress`, `dropwhile`, `takewhile`, `filterfalse`, `zip_longest`, `count_from`, `cycle`) | done | parity | Canonical CPython-derived fixture and demo validate deterministic helper behavior and typed error adaptation for `batched(n <= 0)` | python-parity execution loop | `python-parity reliability execution record` | Revisit when iterator-lazy object-model parity is promoted into scope | `stdlib/sifr/itertools.sifr`, `crates/sifr/tests/e2e/pass/cpython_itertools_subset.sifr`, `demos/itertools/main.sifr` |
| itertools | advanced CPython iterator object-model surface (`tee`, `groupby`, `product`, `permutations`, `combinations`, lazy iterator protocol parity) remains out of approved subset | done | intentional-diff | Current scope keeps deterministic list-backed helpers and avoids partial lazy-iterator emulation in this capability | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython iterator object-model parity is scheduled | `stdlib/sifr/itertools.sifr`, `crates/sifr/tests/e2e/pass/cpython_itertools_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_itertools.sifr` |
| json | `loads` and `dumps` primitive roundtrip subset with typed decode errors | done | parity | Canonical CPython-derived fixture and demo validate parse/serialize behavior for approved primitive JSON subset and panic-free `JSONDecodeError` signaling for invalid inputs | python-parity execution loop | `python-parity reliability execution record` | Revisit when richer typed JSON object model and serializer options are promoted into approved scope | `stdlib/sifr/json.sifr`, `crates/sifr/tests/e2e/pass/cpython_json_subset.sifr`, `demos/json/main.sifr` |
| json | CPython `dumps` wrapper options (`indent`, `sort_keys`, custom encoder hooks) and object-model rich adaptation are out of approved subset | done | intentional-diff | Current scope exposes one typed `dumps` family and avoids partial option emulation until typed JSON model expansion is scheduled | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython `json` option surface and structured typed model are scheduled | `stdlib/sifr/json.sifr`, `crates/sifr/tests/e2e/pass/cpython_json_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_json.sifr` |
| datetime | `timedelta` arithmetic and core `datetime`/`date`/`time`/`timezone` helpers with `now` and `from_timestamp` subset | done | parity | Canonical CPython-derived fixture and demo validate approved temporal helper semantics, ISO formatting, and typed invalid-timestamp rejection for out-of-range values | python-parity execution loop | `python-parity reliability execution record` | Revisit when richer CPython datetime object-model methods are promoted into approved scope | `stdlib/sifr/datetime.sifr`, `crates/sifr/tests/e2e/pass/cpython_datetime_subset.sifr`, `demos/datetime/main.sifr` |
| datetime | advanced CPython timezone/locale/calendar semantics (`tzinfo` subclasses, aware/naive conversions, fold, microseconds, full strftime/strptime parity) are out of approved subset | done | intentional-diff | Current scope keeps deterministic local-time helpers and an approved subset of constructors/formatters without claiming full CPython timezone/calendar parity | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython datetime/timezone semantics are scheduled | `stdlib/sifr/datetime.sifr`, `crates/sifr/tests/e2e/pass/cpython_datetime_subset.sifr`, `crates/sifr/tests/e2e/pass/cpython_datetime.sifr` |
| io | file read/write/open helper subset (`read_text`, `write_text`, `append_text`, `exists`, `open` with `read`/`write`/`readline`/`readlines`/binary helpers) | done | parity | Canonical CPython-derived fixture and demo validate panic-free I/O behavior for approved open-mode subset and typed `IOError` adaptation on missing paths/mode failures | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython `io` object-model classes are promoted into approved scope | `stdlib/sifr/io.sifr`, `crates/sifr/tests/e2e/pass/cpython_io_subset.sifr`, `demos/io/main.sifr` |
| io | advanced CPython `io` hierarchy (`TextIOWrapper`, buffering controls, encoding/newline/error options, seek/tell semantics) remains out of approved subset | done | intentional-diff | Current approved scope keeps deterministic intrinsic-backed file-handle helpers and avoids partial emulation of CPython's full stream hierarchy | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython `io` class hierarchy and option surface are scheduled | `stdlib/sifr/io.sifr`, `crates/sifr/tests/e2e/pass/cpython_io_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_io.sifr` |
| csv | row/CSV parse-format helpers and object wrappers (`reader`, `writer`, `DictReader`, `DictWriter`, `reader_from_path`, `writer_to_path`) | done | parity | Canonical CPython-derived fixture and demo validate deterministic delimiter-split/join behavior, object wrapper row flows, and typed `IOError` adaptation for missing-file paths in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython dialect and quoting parity is promoted into approved scope | `stdlib/sifr/csv.sifr`, `crates/sifr/tests/e2e/pass/cpython_csv_subset.sifr`, `demos/csv/main.sifr` |
| csv | advanced CPython CSV dialect/quoting surface (`quotechar`, `escapechar`, `doublequote`, `lineterminator`, `Sniffer`, strict RFC-4180 edge semantics) remains out of approved subset | done | intentional-diff | Current approved scope intentionally keeps simple comma-delimited helpers without partial dialect emulation to preserve deterministic behavior | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython dialect and quoting semantics are scheduled | `stdlib/sifr/csv.sifr`, `crates/sifr/tests/e2e/pass/cpython_csv_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_csv_objects.sifr` |
| os | filesystem and process helper subset (`run_command`, `getcwd`, `listdir`, `mkdir`, `rmdir`, `remove_file`, `rename`, `stat`, `is_file`, `is_dir`, `which`, `disk_usage`, `getpid`, `cpu_count`, constants) | done | parity | Canonical CPython-derived fixture and demo validate deterministic command/process wrappers and typed `IOError` adaptation for failing filesystem paths in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython process-management and env-modification parity surfaces are promoted into approved scope | `stdlib/sifr/os.sifr`, `crates/sifr/tests/e2e/pass/cpython_os_subset.sifr`, `demos/os/main.sifr` |
| os | advanced CPython OS surface (`fork`/`exec` families, signals, uid/gid and permission mutation APIs, full environment/process-group management) remains out of approved subset | done | intentional-diff | Current approved scope intentionally keeps deterministic, portable wrappers and avoids partial emulation of host-specific low-level process APIs | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython low-level process and platform-specific OS APIs are scheduled | `stdlib/sifr/os.sifr`, `crates/sifr/tests/e2e/pass/cpython_os_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_os_intrinsics.sifr` |
| pathlib | path helper subset and `Path` object operations (`basename`, `dirname`, `extension`, `stem`, `is_absolute`, `join_path`, `with_name`, `with_suffix`, `exists`/`is_file`/`is_dir`, `read_text`, `write_text`, `mkdir`, `touch`, `unlink`, `rmdir`, `resolve`, `iterdir`, `glob`, `rglob`) | done | parity | Canonical CPython-derived fixture, demo, and regression vectors validate deterministic path parsing, object-method behavior, `glob`/`rglob` wildcard + hidden-entry semantics, missing-directory empty-result behavior for globs, and typed `IOError` adaptation for missing-path reads in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython pathlib semantics and path subclassing behaviors are promoted into approved scope | `stdlib/sifr/pathlib.sifr`, `crates/sifr/tests/e2e/pass/cpython_pathlib_subset.sifr`, `crates/sifr/tests/e2e/pass/pathlib_glob_semantics.sifr`, `demos/pathlib/main.sifr` |
| pathlib | advanced CPython pathlib surface (`PurePath`/`PosixPath`/`WindowsPath` subclass hierarchy, drive/root semantics, symlink resolution nuances, permission/stat rich API, URI conversions) remains out of approved subset | done | intentional-diff | Current scope intentionally keeps a deterministic cross-platform subset and avoids partial emulation of platform-specific path-class hierarchy semantics | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython pathlib class hierarchy and platform-specific behaviors are scheduled | `stdlib/sifr/pathlib.sifr`, `crates/sifr/tests/e2e/pass/cpython_pathlib_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_pathlib_class.sifr` |
| glob | approved subset for deterministic filename expansion in a directory (`glob(directory, pattern)`) with `*`/`?` wildcard matching and CPython-style hidden-file filtering | done | parity | Canonical CPython-derived fixture and demo validate deterministic sorted matches, hidden-file filtering parity (`.*` needed to match hidden names), and missing-directory behavior returning empty list in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader pathname semantics are promoted into approved scope | `stdlib/sifr/glob.sifr`, `crates/sifr/tests/e2e/pass/cpython_glob_subset.sifr`, `demos/glob/main.sifr` |
| glob | advanced CPython glob surface (single-argument pathname expansion with embedded separators, `recursive=True` and `**`, `root_dir`, `dir_fd`, lazy `iglob`, full character-class/exclude semantics) remains out of approved subset | done | intentional-diff | Current scope intentionally keeps deterministic directory-plus-pattern subset without partial emulation of CPython's full pathname-expansion and iterator APIs | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython glob API surface is scheduled | `stdlib/sifr/glob.sifr`, `crates/sifr/tests/e2e/pass/cpython_glob_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_glob.sifr` |
| tempfile | approved tempfile subset (`mktemp_path`, `mkstemp`, `mkdtemp`) with deterministic temp-root placement, retry-based collision handling, and cleanup-safe behavior | done | parity | Canonical CPython-derived fixture and demo validate created file/dir existence, unique path generation, temp-root placement via `gettempdir()`, and panic-free cleanup flow in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython tempfile object APIs are promoted into approved scope | `stdlib/sifr/tempfile.sifr`, `crates/sifr/tests/e2e/pass/cpython_tempfile_subset.sifr`, `demos/tempfile/main.sifr` |
| tempfile | API shape is safety-adapted (`mkstemp`/`mkdtemp` accept `prefix` only, return path string, no file-descriptor tuple, no `suffix`/`dir` optional args) | done | intentional-diff | Current approved scope keeps deterministic path-returning helpers with no raw descriptor exposure and no partial emulation of CPython optional-argument matrix | python-parity execution loop | `python-parity reliability execution record` | Revisit when tempfile API expansion and descriptor-level semantics are scheduled | `stdlib/sifr/tempfile.sifr`, `crates/sifr/tests/e2e/pass/cpython_tempfile_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_tempfile.sifr` |
| tempfile | advanced CPython tempfile surface (`TemporaryFile`, `NamedTemporaryFile`, `SpooledTemporaryFile`, `TemporaryDirectory`, fd-level semantics, delete-on-close and context-manager lifecycle nuances, mode/encoding buffering matrix) remains out of approved subset | done | intentional-diff | Current scope intentionally keeps deterministic path-returning helpers and avoids partial emulation of CPython's file-object lifecycle and descriptor-level behaviors | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython tempfile object model and lifecycle semantics are scheduled | `stdlib/sifr/tempfile.sifr`, `crates/sifr/tests/e2e/pass/cpython_tempfile_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_tempfile.sifr` |
| shutil | approved subset (`copy`, `move_file`, `rmtree`, `which`, `disk_usage`) with deterministic file/tree mutation behavior and typed `IOError` propagation for missing-path failures | done | parity | Canonical CPython-derived fixture and demo validate copy/move/tree-removal behavior, command lookup, and disk-usage tuple-shape adaptation for approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader shutil object/options surface is promoted into approved scope | `stdlib/sifr/shutil.sifr`, `crates/sifr/tests/e2e/pass/cpython_shutil_subset.sifr`, `demos/shutil/main.sifr` |
| shutil | CPython naming/options and return-shape differences are safety-adapted (`move_file` name for Rust keyword conflict, no optional argument matrix, `disk_usage` as `list[int]` instead of named tuple) | done | intentional-diff | Current approved scope keeps deterministic wrappers and explicit safety-aligned data-shape adaptation instead of partial emulation of CPython option-rich APIs | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython shutil option surface and richer return object model are scheduled | `stdlib/sifr/shutil.sifr`, `crates/sifr/tests/e2e/pass/cpython_shutil_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_shutil_intrinsics.sifr` |
| logging | approved subset (`Logger`, `basicConfig`, `getLogger`, `FileHandler`, `Formatter`, level constants, helper log functions) with deterministic level filtering and panic-free handler behavior | done | parity | Canonical CPython-derived fixture and demo validate level filtering, global-level propagation, formatter substitution, and non-crashing behavior for invalid file targets in approved scope | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader logging hierarchy/configuration surface is promoted into approved scope | `stdlib/sifr/logging.sifr`, `crates/sifr/tests/e2e/pass/cpython_logging_subset.sifr`, `demos/logging/main.sifr` |
| logging | advanced CPython logging surface (handler hierarchy, filters, propagation tree, format styles, `dictConfig`, `NullHandler`, adapters, and full record metadata lifecycle) remains out of approved subset | done | intentional-diff | Current scope intentionally keeps deterministic lightweight logger/handler subset and avoids partial emulation of CPython's full logging framework | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython logging framework parity is scheduled | `stdlib/sifr/logging.sifr`, `crates/sifr/tests/e2e/pass/cpython_logging_subset.sifr`, `crates/sifr/tests/e2e/pass/stdlib_logging_enhanced.sifr` |
| time | approved subset (`time`, `sleep`, `perf_counter`, `monotonic`, `strftime`, `strptime`, `gmtime`, `localtime`) with deterministic formatting/parsing behavior | done | parity | Canonical CPython-derived fixture and demo validate monotonic counter progression, epoch formatting/parsing behavior, and ISO conversion helpers in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython `time` APIs (tuple/object model and optional argument matrix) are promoted into approved scope | `stdlib/sifr/time.sifr`, `crates/sifr/tests/e2e/pass/cpython_time_subset.sifr`, `demos/time/main.sifr` |
| time | safety-adapted invalid-input handling (`sleep` invalid durations no-op; out-of-range epoch conversion returns empty string; no tuple-return object model; `perf_counter`/`monotonic` currently mapped to wall-clock epoch seconds) diverges from CPython exception/object behavior | done | intentional-diff | Sifr safety rules forbids user-triggerable runtime panics and exception-driven control flow in this subset, so invalid timing inputs are handled panic-free with deterministic outputs while high-resolution monotonic guarantees are deferred | python-parity execution loop | `python-parity reliability execution record` | Revisit when typed time error modeling and CPython tuple/object parity expansion are scheduled | `stdlib/sifr/time.sifr`, `crates/sifr_codegen/src/intrinsics/time.rs`, `crates/sifr/tests/e2e/pass/cpython_time_subset.sifr` |
| timeit | approved subset (`default_timer`, `timeit`, `repeat`) with deterministic callable loop execution and non-negative elapsed outputs | done | parity | Canonical CPython-derived fixture and demo validate timer progression, repeat shape semantics, and callable execution over configured iteration counts in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython `Timer` object model and CLI helpers are promoted into approved scope | `stdlib/sifr/timeit.sifr`, `crates/sifr/tests/e2e/pass/cpython_timeit_subset.sifr`, `demos/timeit/main.sifr` |
| timeit | safety-adapted timing semantics (`default_timer` currently follows `time.perf_counter` wall-clock mapping; elapsed values are clamped to non-negative to prevent backward-clock artifacts) diverge from CPython high-resolution monotonic expectations | done | intentional-diff | Sifr safety rules prioritizes deterministic panic-free behavior for approved wrapper subset while true high-resolution monotonic guarantees remain tied to future timer backend expansion | python-parity execution loop | `python-parity reliability execution record` | Revisit when monotonic timer backend parity is expanded for `time`/`timeit` modules | `stdlib/sifr/timeit.sifr`, `stdlib/sifr/time.sifr`, `crates/sifr/tests/e2e/pass/cpython_timeit_subset.sifr` |
| platform | approved subset (`system`, `machine`, `node`, `release`, `version`, `processor`) with deterministic non-empty host metadata wrappers | done | parity | Canonical CPython-derived fixture and demo validate CPython-style `system()` naming (`Linux`/`Darwin`/`Windows`) and non-empty platform metadata in approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython `platform` helpers (`platform()`, `uname()`, python build metadata) are promoted into approved scope | `stdlib/sifr/platform.sifr`, `crates/sifr/tests/e2e/pass/cpython_platform_subset.sifr`, `demos/platform/main.sifr` |
| platform | advanced CPython platform surface (full `uname_result` object model, libc/processor probing heuristics, distro-specific aliases, and python-build metadata helpers) remains out of approved subset; release/version values use deterministic fallback when host commands are unavailable | done | intentional-diff | Current scope intentionally keeps safe wrapper primitives and avoids partial emulation of CPython's larger probing stack while guaranteeing panic-free non-empty outputs | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython platform probing/object-model parity is scheduled | `stdlib/sifr/platform.sifr`, `crates/sifr_codegen/src/intrinsics/platform.rs`, `crates/sifr/tests/e2e/pass/cpython_platform_subset.sifr` |
| uuid | approved subset (`uuid4`, `UUID` object helpers, `uuid_from_hex`) with canonical UUID text shape, typed parse validation, and version/variant semantics | done | parity | Canonical CPython-derived fixture and demo validate UUID4 shape/version bits, canonical lowercase normalization, constructor/parse validation, and object helper behavior for approved subset | python-parity execution loop | `python-parity reliability execution record` | Revisit when broader CPython uuid constructors (`bytes`, `int`, `fields`, `URN` variants) and namespace UUID helpers are promoted into approved scope | `stdlib/sifr/uuid.sifr`, `crates/sifr/tests/e2e/pass/cpython_uuid_subset.sifr`, `demos/uuid/main.sifr` |
| uuid | advanced CPython uuid surface (namespace constructors `uuid1/uuid3/uuid5`, rich constructor overloads with bytes/int/fields, `SafeUUID` metadata, and full variant taxonomy) remains out of approved subset; direct `UUID(...)` constructor is a lightweight passthrough while strict validation is provided by `uuid_from_hex(...)` | done | intentional-diff | Current scope intentionally stabilizes panic-free UUID4 plus canonical parse/object helpers without partial emulation of CPython's broader constructor and namespace matrix | python-parity execution loop | `python-parity reliability execution record` | Revisit when full CPython uuid constructor and namespace API parity is scheduled | `stdlib/sifr/uuid.sifr`, `crates/sifr_codegen/src/intrinsics/uuid.rs`, `crates/sifr/tests/e2e/pass/cpython_uuid_subset.sifr` |
