# fixture: ecosystem_backend_certification
# package-example: axum
# required-crate: axum
# execution-kind: cargo-probe
# expected-result: package-example
class PackageExampleError(Error):
    message: str

@rust(bridge.backend.route_probe, panic=map_error(bridge.backend.map_panic))
def axum_router(route: str) -> Result[str, PackageExampleError | RustPanicError]: ...

def verify_axum_package() -> Result[str, PackageExampleError | RustPanicError]:
    sample_result: Result[str, PackageExampleError | RustPanicError] = axum_router("/health")
    return sample_result
