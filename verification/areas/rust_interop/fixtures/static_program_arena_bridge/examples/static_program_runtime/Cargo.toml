[workspace]
resolver = "2"

[package]
name = "static_program_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
sifr_runtime = { path = "../../../../../../../crates/sifr_runtime", features = ["structural"] }

[package.metadata.sifr]
manifest = "sifr.toml"
