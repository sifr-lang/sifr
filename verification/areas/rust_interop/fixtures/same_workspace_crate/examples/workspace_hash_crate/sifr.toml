# fixture: same_workspace_crate
# scenario-example: workspace_hash_crate

[package]
name = "workspace-hash-consumer"
edition = "2026"
sifr-version = ">=0.3,<0.4"

[source]
root = "src"

[rust]
direct-crate-bindings = true

[trust]
rust-no-panic = ["workspace_hash.hash", "workspace_hash.hash_pair"]
