[workspace]
members = [
  "rust/workspace_hash",
]
resolver = "2"

[package]
name = "workspace-hash-consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
workspace_hash = { path = "rust/workspace_hash" }

[package.metadata.sifr]
manifest = "sifr.toml"
