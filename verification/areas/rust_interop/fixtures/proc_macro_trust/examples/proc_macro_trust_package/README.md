# fixture: proc_macro_trust
# scenario-example: proc_macro_trust_package

This scenario models package-level trust for Rust procedural macros and
build-script code. `sifr.toml` explicitly trusts `serde_derive` as a proc macro
provider and `prost-build` as a build-script crate before the bridge can decode
generated bytes.
