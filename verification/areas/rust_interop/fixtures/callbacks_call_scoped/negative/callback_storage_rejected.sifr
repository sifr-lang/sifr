# fixture: callbacks_call_scoped
# evidence: negative/callback_storage_rejected
# evidence-status: passing
# execution-kind: runtime-observed
# expected-result: diagnostic
# expected-diagnostic: SIFR-RUST-CB-0001
class EventError(Error):
    message: str

@rust(bridge.callbacks.store_for_later)
def store_for_later(handler: Callable[[str], None]) -> Result[None, EventError | RustPanicError]: ...

def sample_event_callback(event: str) -> None:
    return None

def verify_callback_storage_rejected() -> None:
    try:
        stored: None = store_for_later(sample_event_callback)
    except EventError:
        return None
    except RustPanicError:
        return None
    return None
