[workspace]
resolver = "2"

[package]
name = "call-scoped-callback-runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
sifr_runtime = { path = "../../../../../../../crates/sifr_runtime" }

[package.metadata.sifr]
manifest = "sifr.toml"
