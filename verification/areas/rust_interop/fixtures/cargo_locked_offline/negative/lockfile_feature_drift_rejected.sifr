# fixture: cargo_locked_offline
# evidence: negative/lockfile_feature_drift_rejected
# evidence-status: passing
# execution-kind: cargo-probe
# expected-result: diagnostic
# expected-diagnostic: SIFR-RUST-CARGO-0001
# fixture-cargo: indexmap feature policy requires a lockfile update
@rust(locked_bridge.cached_hash, panic=trusted_no_panic)
def cached_hash(input: bytes) -> uint32: ...

def verify_lockfile_feature_drift_rejected() -> uint32:
    feature_drift_result: uint32 = cached_hash(b"feature-drift")
    return feature_drift_result
