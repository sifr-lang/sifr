[workspace]
members = [
  "rust/locked_bridge",
]
resolver = "3"

[package]
name = "locked-offline-cache"
version = "0.1.0"
edition = "2024"

[dependencies]
locked_bridge = { path = "rust/locked_bridge" }

[package.metadata.sifr]
manifest = "sifr.toml"
