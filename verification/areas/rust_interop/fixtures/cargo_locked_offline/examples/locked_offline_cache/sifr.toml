# fixture: cargo_locked_offline
# scenario-example: locked_offline_cache

[package]
name = "locked-offline-cache"
edition = "2026"
sifr-version = ">=0.3,<0.4"

[source]
root = "src"

[rust]
bridge-version = 1
direct-crate-bindings = true

[trust]
rust-no-panic = ["locked_bridge.cached_hash", "locked_bridge.lockfile_generation"]
