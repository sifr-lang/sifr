# fixture: shared_bridge_crate
# evidence: positive/stable_runtime_types_only
# evidence-status: passing
# execution-kind: cargo-probe
# expected-result: pass
@rust(sifr_shared_hash_bridge.digest, panic=trusted_no_panic)
def digest(input: bytes) -> bytes: ...

def verify_stable_runtime_types_only() -> bytes:
    digest_result: bytes = digest(b"sifr-rust-interop")
    return digest_result

def main() -> None:
    digest_result: bytes = verify_stable_runtime_types_only()
    assert len(digest_result) == 8
    print(len(digest_result))
