[workspace]
members = [
  "rust/sifr_shared_hash_bridge",
]
resolver = "3"

[package]
name = "shared-hash-consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
sifr_shared_hash_bridge = { path = "rust/sifr_shared_hash_bridge" }

[package.metadata.sifr]
manifest = "sifr.toml"
