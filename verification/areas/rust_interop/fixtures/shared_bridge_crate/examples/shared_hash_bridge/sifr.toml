# fixture: shared_bridge_crate
# scenario-example: shared_hash_bridge

[package]
name = "shared-hash-consumer"
edition = "2026"
sifr-version = ">=0.3,<0.4"

[source]
root = "src"

[rust]
direct-crate-bindings = true

[trust]
rust-no-panic = [
  "sifr_shared_hash_bridge.digest",
  "sifr_shared_hash_bridge.digest_hex",
]
