# fixture: ecosystem_cli_certification
# package-example: tracing
# required-crate: tracing
# execution-kind: cargo-probe
# expected-result: package-example
class PackageExampleError(Error):
    message: str

@rust(bridge.cli.parse_and_trace, panic=map_error(bridge.cli.map_panic))
def tracing_info(args: list[str]) -> Result[str, PackageExampleError | RustPanicError]: ...

def verify_tracing_package() -> Result[str, PackageExampleError | RustPanicError]:
    sample_result: Result[str, PackageExampleError | RustPanicError] = tracing_info(["sifr", "--mode", "check"])
    return sample_result
