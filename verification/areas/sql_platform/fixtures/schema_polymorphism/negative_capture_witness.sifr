from sifr.sql import SqlSchema
from sifr.sql.requirements import has_users


def captured_query[S: has_users.Schema](own schema: SqlSchema[S], email: str):
    def build() -> Template:
        return schema.sql(t"SELECT id FROM public.users WHERE email = {email}")

    return build()
