# Text/I18n Dependency Decisions

Status: M3 in progress. Encoding versions were checked during M0/M1; Unicode core versions and generated Unicode 17.0.0 tables were locked during M2 on 2026-06-06; segmentation is aligned to Unicode 17.0.0 during M2.5. Locale IDs, formatting, plural rules, and collation use ICU4X 2.2 compiled data during M3.

| Family | Crate(s) | Sifr abstraction | Unicode/version alignment | Panic/unsafe audit | Typed error mapping | License/MSRV/binary/platform impact | Deterministic tests | Supply-chain signal |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Encoding | `encoding_rs 0.8.35` | Hidden behind `sifr.encoding.Encoding`, static label registry, `Encoder`, `Decoder`, and text I/O adapters. | WHATWG encoding data from crate; Unicode version not used as Sifr Unicode table source. | M1 scans wrapper and emitted code; no user bytes may reach `unwrap`/`expect`/`panic`; `unsafe` audit limited to crate boundary. | `DecodeError`, `EncodeError`, recoveries, exhausted-state errors. | `(Apache-2.0 OR MIT) AND BSD-3-Clause`, MSRV 1.36; default `alloc`; no CJK fast encode features in M1 to bound size. | Tier 0/Tier 1 alias, BOM, malformed byte, incremental, handler fixtures. | Maintained WHATWG-compatible Rust crate used by Gecko ecosystem. |
| Normalization and Unicode core data | `unicode-normalization 0.1.25`, `unicode_names2 3.1.0`, generated first-party Unicode 17.0.0 tables | Hidden behind `sifr.unicode.normalize`, `is_normalized`, scalar property APIs, lookup/name APIs, and `case_fold`; compiled into `sifr_runtime` only through its opt-in `unicode` Cargo feature. | M2 uses Unicode 17.0.0 across normalization (`unicode-normalization::UNICODE_VERSION`), names (`unicode_names2 3.1.0` docs/data), and generated `UnicodeData.txt` / `EastAsianWidth.txt` / `CaseFolding.txt` tables. Version skew blocks release. | M2 scans wrapper/property integration and generated tables; generated tables are data-only, marked `@generated`, and regenerated by `scripts/generate_unicode_tables.py`. | `UnicodeDataError` for invalid forms, invalid scalar arity, missing names, missing numeric values, and lookup misses. | `unicode-normalization`: `MIT OR Apache-2.0`, MSRV 1.36. `unicode_names2`: `(MIT OR Apache-2.0) AND Unicode-DFS-2016`, MSRV 1.65. Generated Unicode data is Unicode data-file derived, increases source size in an excluded generated file only, and does not affect non-Unicode generated projects because `sifr_runtime/unicode` is emitted only for `sifr.unicode` users. | `text_i18n_unicode_core.sifr` covers normalization, properties, numeric values, lookup/name, unassigned defaults, missing-value errors, and full case folding. | `unicode-rs` normalization family plus maintained `unicode_names2`; first-party generated tables use Unicode Consortium UCD files directly. |
| Segmentation | `unicode-segmentation 1.13.3` | Hidden behind owned `graphemes`, `grapheme_indices`, `words`, and `word_boundaries`; compiled into `sifr_runtime` only through its opt-in `unicode` Cargo feature. | M2.5 uses `unicode_segmentation::UNICODE_VERSION == (17, 0, 0)`, aligned with M2 normalization/properties/case data. The runtime unit test asserts this version alignment. | M2.5 wrapper uses crate iterators over valid Rust `str`; byte offsets are paired with segment text and no slicing is performed from user-provided offsets. Sentence boundaries and streaming cursors are deferred. | Segmentation APIs are infallible on valid `str`; no typed error is required for owned grapheme/word iteration. | `MIT OR Apache-2.0`, MSRV 1.85; compatible with workspace Rust 1.93+; optional runtime feature keeps non-Unicode generated projects lean. | `text_i18n_unicode_segmentation.sifr` covers combining marks, emoji ZWJ sequences, regional indicators, punctuation, whitespace, mixed-script words, and segment-bearing boundary offsets. | `unicode-rs` family, UAX #29 implementation, README changelog records Unicode 17.0.0 support in the 1.13.x line. |
| Locale IDs | `icu_locale 2.2.0` / `icu_locale_core 2.2.0` | Hidden behind `sifr.i18n.LocaleId`, `parse_locale`, canonicalization, and likely-subtag expansion/minimization. | ICU4X 2.2 locale data; `LocaleCanonicalizer::new_extended` and `LocaleExpander::new_extended` provide deterministic canonicalization and likely-subtag behavior. | Runtime wrapper parses user locale strings through ICU4X and returns typed errors; no user-controlled locale string reaches panic paths. | `LocaleIdError`. | `Unicode-3.0`, MSRV 1.86; compatible with workspace Rust 1.93+; cross-platform pure Rust. | `text_i18n_locale_formatting.sifr` plus runtime unit tests cover `EN-US`, `zh-CN`, `zh-Hans-CN`, and invalid locale text. | Unicode/ICU4X official project. |
| Number formatting | `icu_decimal 2.2.0` + data | Hidden behind object-scoped `NumberFormatter`. | Uses ICU4X 2.2 compiled decimal data; emitted `sifr_runtime` enables the `i18n` feature only for `sifr.i18n` users. | Runtime parses decimal strings through ICU decimal input so trailing-zero-sensitive data is preserved; malformed values map to `FormatError`. | `FormatError`. | `Unicode-3.0`, MSRV 1.86; compiled data is opt-in through generated Cargo features. | Bangla decimal fixture formats `1000007` as `১০,০০,০০৭`; invalid decimal fixture maps to typed error. | Unicode/ICU4X official project. |
| Date/time formatting | `icu_datetime 2.2.0` + data | Hidden behind object-scoped `DateTimeFormatter` with explicit style and date/time parts. | Uses ICU4X 2.2 compiled date/time data with explicit locale preferences and `YMDT` fieldsets. | Runtime range-checks date/time integer parts before constructing ICU input values; invalid style/date/time maps to `FormatError`. | `FormatError`. | `Unicode-3.0`, MSRV 1.86; compiled data is opt-in through generated Cargo features. | Stable fixed-input fixture formats `2025-01-15T16:09:35` with explicit `en-US` medium style and checks non-empty output. | Unicode/ICU4X official project. |
| Plural rules | `icu_plurals 2.2.0` + data | Hidden behind object-scoped `PluralRules`; M4 translation plural lookup will consume the same explicit plural substrate. | Uses ICU4X 2.2 compiled plural data shared with M3; no process-global locale state. | Runtime parses plural operands from decimal text and maps unsupported rule types or malformed operands to `PluralRulesError`. | `PluralRulesError`, future M4 `TranslationError`. | `Unicode-3.0`, MSRV 1.86; compiled data is opt-in through generated Cargo features. | English cardinal `1`/`2` and ordinal `2` fixtures cover `one`/`other`/`two`; invalid rule type fixture maps to typed error. | Unicode/ICU4X official project. |
| Collation | `icu_collator 2.2.0` + data | Hidden behind object-scoped `Collator` with explicit locale and strength. | Uses ICU4X 2.2 compiled collation data; accepted behavior is limited to supported strengths and deterministic locale data. | Runtime rejects unsupported strengths with `FormatError` and maps ICU construction failures; compare returns `-1`, `0`, or `1`. | `FormatError`. | `Unicode-3.0`, MSRV 1.86; compiled data is opt-in through generated Cargo features. | Spanish traditional collation orders `pollo` after `polvo` while English primary collation orders it before `polvo`. | Unicode/ICU4X official project. |
| Translation catalogs | local `.mo` parser | Hidden behind `Bundle`/`Translator` backend importer. | Catalog-declared charset decoding consumes M1 encoding substrate; no Unicode table source. | M4 parser rejects malformed lengths, offsets, plural metadata, and never evaluates general expressions. | `CatalogParseError`, `TranslationError`. | First-party code; file-size guardrail applies; no host-specific dependencies. | CPython `.mo` magic/version/corrupt/endian/plural/context/fallback fixtures. | Local audited parser because backend scope is narrow and security-sensitive. |
| Modern message formatting | none | API leaves backend-neutral room only. | n/a | n/a | n/a | No dependency in this phase. | Deferred fixtures only. | Deferred until a dedicated backend issue. |
