# ============================================================
# Test 7: sifr.time
# ============================================================

from sifr.time import time_now, sleep

def main():
    t: float = time_now()
    print(t > 0.0)
    sleep(0.01)
    print("slept ok")
