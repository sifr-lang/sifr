# Migration registry for native standard-library surfaces during the sysroot
# and stdlib toolchain workstream. This file is intentionally broad at the baseline inventory stage and is
# deleted or reduced to a retained compiler-language-glue allowlist by the final intrinsic-registry cleanup stage.

schema_version = 1
workstream = "sifr_sysroot_stdlib_toolchain"
created_for_stage = "baseline_inventory"
certification_reference = "verification/areas/rust_interop/data/rust_interop_compatibility_matrix.json"

[[surface]]
id = "_sifr.math"
public_modules = ["sifr.math", "sifr.statistics"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/math"
final_owner = "crates/sifr_stdlib via stdlib/_sifr/math.sifr private Rust interop declarations"
reason = "Stateless numeric stdlib leaves are stdlib implementation, not language semantics."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "generated-program stdlib crate creation and private stdlib interop context"
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.crypto"
public_modules = ["sifr.base64", "sifr.hashlib", "sifr.random", "sifr.secrets", "sifr.tempfile"]
current_owner = "mixed: hash helpers and base64/base32 encoders, decoders, and options via crates/sifr_stdlib private declarations; random helpers still use compiler intrinsic fallback plus direct generated dependencies"
final_owner = "crates/sifr_stdlib for base encodings, hash, UUID/random-facing helpers; crates/sifr_runtime only for language/runtime state that remains shared"
reason = "Stateless encodings and hashes are stdlib leaves; random module state requires explicit retained or migrated ownership."
certification_state = "mixed-stateless-supported-resource-state-needs-review"
can_move_before_runtime_certification = false
migration_blocker = "Random module state ownership and any resource-like global state must be reviewed against runtime certification evidence."
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.bytes"
public_modules = ["sifr.bytes", "sifr.hashlib"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/bytes.rs"
final_owner = "compiler language glue for first-class bytes plus crates/sifr_stdlib for stdlib-facing helpers"
reason = "First-class bytes representation is language-owned; helper conversion leaves should not stay in generic stdlib intrinsic dispatch."
certification_state = "retained-compiler-glue-plus-migratable-helpers"
can_move_before_runtime_certification = true
migration_blocker = "Split language-owned bytes representation from stdlib helper functions."
deletion_stage = "final retained allowlist"

[[surface]]
id = "_sifr.collections"
public_modules = ["sifr.collections"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/collections"
final_owner = "compiler language glue for built-in collection representation plus crates/sifr_stdlib for CPython-shaped collection helpers"
reason = "Core collection layout is compiler-owned, but defaultdict/set helper leaves are stdlib behavior."
certification_state = "retained-compiler-glue-plus-migratable-helpers"
can_move_before_runtime_certification = true
migration_blocker = "Separate language collection primitives from stdlib helper behavior."
deletion_stage = "final retained allowlist"

[[surface]]
id = "_sifr.regex"
public_modules = ["sifr.re"]
current_owner = "private stdlib Rust interop declarations backed by crates/sifr_stdlib regex feature; sifr.pathlib retains a separate compiler-special direct regex dependency for glob lowering"
final_owner = "crates/sifr_stdlib regex feature via stdlib/_sifr/regex.sifr"
reason = "Regex matching is a stateless stdlib leaf backed by an implementation crate."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "none for sifr.re; path-glob regex usage is tracked under the filesystem/path surface"
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.uuid"
public_modules = ["sifr.uuid"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/uuid.rs with direct rand/uuid dependency planning"
final_owner = "crates/sifr_stdlib uuid feature via stdlib/_sifr/uuid.sifr"
reason = "UUID generation and namespace UUID helpers are stateless stdlib leaves backed by implementation crates."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "generated-program stdlib crate creation and private declaration interop context"
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.toml"
public_modules = ["sifr.tomllib"]
current_owner = "migrated to stdlib/_sifr/toml.sifr private Rust interop backed by crates/sifr_stdlib/src/toml.rs"
final_owner = "crates/sifr_stdlib toml feature via stdlib/_sifr/toml.sifr"
reason = "TOML parsing is fallible stdlib behavior, not compiler semantics."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "none; private stdlib interop owns flat token payload reconstruction"
deletion_stage = "migrated-to-sysroot-stdlib"

[[surface]]
id = "_sifr.html"
public_modules = ["sifr.html"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/html.rs"
final_owner = "crates/sifr_stdlib html feature via stdlib/_sifr/html.sifr"
reason = "HTML escaping is a stateless stdlib leaf."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "generated-program stdlib crate creation and private declaration interop context"
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.calendar"
public_modules = ["sifr.calendar"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/calendar.rs"
final_owner = "crates/sifr_stdlib calendar feature via stdlib/_sifr/calendar.sifr"
reason = "Calendar calculations are stateless stdlib leaves."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "generated-program stdlib crate creation and private declaration interop context"
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.platform"
public_modules = ["sifr.platform"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/platform.rs"
final_owner = "crates/sifr_stdlib platform feature via stdlib/_sifr/platform.sifr"
reason = "Host platform queries are stdlib behavior; deterministic host support remains validated by platform matrices."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "Host matrix evidence must stay attached to migrated implementation."
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.datetime"
public_modules = ["sifr.datetime"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/datetime.rs with chrono dependency planning"
final_owner = "crates/sifr_stdlib datetime/calendar feature via stdlib/_sifr/datetime.sifr"
reason = "Datetime formatting/conversion is stdlib implementation backed by pinned sysroot dependencies."
certification_state = "can-move-before-runtime-certification"
can_move_before_runtime_certification = true
migration_blocker = "generated-program stdlib crate creation and private declaration interop context"
deletion_stage = "stateless-native-leaf migration"

[[surface]]
id = "_sifr.time"
public_modules = ["sifr.time", "sifr.timeit", "sifr.datetime", "sifr.random"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/time.rs with chrono dependency planning"
final_owner = "crates/sifr_stdlib time feature, with sleep/runtime behavior retained only where language runtime semantics require it"
reason = "Clock formatting and counters are stdlib behavior; sleep interacts with runtime scheduling and needs certification review."
certification_state = "mixed-stateless-supported-runtime-sensitive"
can_move_before_runtime_certification = false
migration_blocker = "Async/runtime interaction for sleep and monotonic resources must align with runtime certification evidence."
deletion_stage = "fallible-data-text migration"

[[surface]]
id = "_sifr.json"
public_modules = ["sifr.json"]
current_owner = "crates/sifr_stdlib json feature reached through _sifr.json private Rust interop declarations; sifr_runtime::json retains shared integer policy primitives"
final_owner = "crates/sifr_stdlib json feature backed by sifr_runtime only for shared integer primitives"
reason = "JSON parsing/serialization is fallible stdlib behavior; shared exact-int value helpers may remain runtime-owned."
certification_state = "supported-through-bridge"
can_move_before_runtime_certification = true
migration_blocker = "Runtime shared integer policy primitives remain recorded here until final registry cleanup decides whether this row is removed or reduced to retained policy ownership notes."
deletion_stage = "fallible-data-text migration"

[[surface]]
id = "_sifr.logging"
public_modules = ["sifr.logging"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/logging.rs plus generated global logging state"
final_owner = "crates/sifr_stdlib runtime-observability feature where stdlib-owned; retained compiler/runtime glue only if final retained allowlist permits it"
reason = "Global logging state is a stateful stdlib/runtime-observability surface and must not be lost between runtime and stdlib ownership decisions."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Global state and runtime observability ownership must be certified or explicitly retained before deletion."
deletion_stage = "runtime-state migration"

[[surface]]
id = "_sifr.encoding"
public_modules = ["sifr.encoding"]
current_owner = "migrated to stdlib/_sifr/encoding.sifr private Rust interop backed by crates/sifr_stdlib/src/encoding.rs; sifr_runtime::encoding retains shared text codec primitives"
final_owner = "crates/sifr_stdlib encoding feature backed by sifr_runtime only for shared text primitives"
reason = "Text codec operations are fallible stdlib behavior."
certification_state = "supported-through-bridge-requires-error-shaping"
can_move_before_runtime_certification = true
migration_blocker = "none; private stdlib interop uses a ParseError message bridge and public Sifr wrappers preserve DecodeError/EncodeError API shape"
deletion_stage = "migrated-to-sysroot-stdlib"

[[surface]]
id = "_sifr.unicode"
public_modules = ["sifr.unicode"]
current_owner = "compiled private Rust interop declarations backed by crates/sifr_stdlib unicode feature and sifr_runtime::unicode helpers"
final_owner = "crates/sifr_stdlib unicode feature backed by sifr_runtime only for shared Unicode data primitives"
reason = "Unicode normalization/segmentation/name lookup is fallible stdlib behavior with pinned data dependencies."
certification_state = "supported-through-bridge-requires-error-shaping"
can_move_before_runtime_certification = true
migration_blocker = "none; private stdlib interop uses a ParseError message bridge and public Sifr wrappers preserve UnicodeDataError API shape"
deletion_stage = "migrated-to-sysroot-stdlib"

[[surface]]
id = "_sifr.i18n"
public_modules = ["sifr.i18n"]
current_owner = "compiled private Rust interop declarations in stdlib/_sifr/i18n.sifr targeting crates/sifr_stdlib/src/i18n.rs"
final_owner = "crates/sifr_stdlib i18n feature backed by sifr_runtime only for shared locale/data primitives"
reason = "Locale and gettext helpers are fallible stdlib behavior with bounded diagnostic requirements."
certification_state = "supported-through-bridge-requires-error-shaping"
can_move_before_runtime_certification = true
migration_blocker = "none; private stdlib interop uses a ParseError message bridge and public Sifr wrappers preserve LocaleIdError/FormatError/PluralRulesError/CatalogError API shapes"
deletion_stage = "migrated-to-sysroot-stdlib"

[[surface]]
id = "_sifr.url"
public_modules = ["sifr.url"]
current_owner = "private stdlib Rust interop declarations backed by crates/sifr_stdlib url feature; HTTP helpers in url_http.rs remain compiler-owned until the HTTP/resource surface migrates"
final_owner = "crates/sifr_stdlib url feature via stdlib/_sifr/url.sifr"
reason = "URL validation/building is fallible stdlib behavior."
certification_state = "supported-through-bridge-requires-error-shaping"
can_move_before_runtime_certification = true
migration_blocker = "none for sifr.url; HTTP/resource URL-adjacent helpers are tracked under their own runtime/resource surface"
deletion_stage = "fallible-data-text migration"

[[surface]]
id = "_sifr.compress"
public_modules = ["sifr.gzip", "sifr.zipfile"]
current_owner = "private stdlib Rust interop declarations backed by crates/sifr_stdlib gzip and zipfile features"
final_owner = "crates/sifr_stdlib gzip and zipfile features via stdlib/_sifr/compress.sifr"
reason = "Compression and archive operations are fallible stdlib behavior."
certification_state = "supported-through-bridge-requires-error-shaping"
can_move_before_runtime_certification = true
migration_blocker = "none"
deletion_stage = "migrated-to-sysroot-stdlib"

[[surface]]
id = "_sifr.fs"
public_modules = ["sifr.io", "sifr.os", "sifr.pathlib", "sifr.shutil", "sifr.tempfile", "sifr.glob", "sifr.json", "sifr.hashlib"]
current_owner = "compiler preamble and intrinsic registry: file_handles, io, os, pathlib, open_text_handles"
final_owner = "crates/sifr_stdlib fs feature through resource interop; compiler retains only language-owned file-handle glue if explicitly allowed"
reason = "Filesystem and file handles are resource-shaped stdlib surfaces."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Resource-shaped opaque handles and lifecycle semantics must be certified before migration."
deletion_stage = "filesystem-resource migration"

[[surface]]
id = "_sifr.process"
public_modules = ["sifr.process"]
current_owner = "compiler intrinsic registry and preamble: process, process_async, process_child_lifecycle, process_pipes"
final_owner = "crates/sifr_stdlib process feature backed by certified resource/async interop and sifr_runtime substrates"
reason = "Process children, pipes, environment, and process state are resource-shaped stdlib surfaces."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Opaque handle, async, cancellation, pipe lifecycle, and shutdown behavior need certification evidence."
deletion_stage = "process-resource migration"

[[surface]]
id = "_sifr.sys"
public_modules = ["sifr.sys", "sifr.env", "sifr.os"]
current_owner = "compiler intrinsic registry: sys, env, os"
final_owner = "crates/sifr_stdlib process/env/platform features, with compiler retaining only entrypoint argv/exit language glue if required"
reason = "Environment and process metadata are stdlib behavior; process exit may remain language/runtime glue."
certification_state = "mixed-stdlib-leaf-plus-runtime-sensitive"
can_move_before_runtime_certification = false
migration_blocker = "Separate environment queries from process exit and command execution resource behavior."
deletion_stage = "process-resource migration"

[[surface]]
id = "_sifr.signal"
public_modules = ["sifr.signal"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/signal.rs"
final_owner = "crates/sifr_stdlib signals feature backed by certified runtime state interop"
reason = "Signal observation is stateful runtime behavior."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Signal lifecycle and cancellation semantics need runtime certification evidence."
deletion_stage = "runtime-state migration"

[[surface]]
id = "_sifr.runtime"
public_modules = ["sifr.runtime"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/runtime.rs with metrics/tracing dependency planning"
final_owner = "retained compiler-language glue plus sifr_runtime observability where tied to language runtime semantics"
reason = "Runtime diagnostic emission is language/runtime observability, not ordinary stdlib leaf behavior."
certification_state = "retained-compiler-language-glue"
can_move_before_runtime_certification = false
migration_blocker = "final retained allowlist decision."
deletion_stage = "final retained allowlist"

[[surface]]
id = "_sifr.task"
public_modules = ["sifr.task"]
current_owner = "compiler preamble and intrinsic registry: task/task_context/task_scope runtime helpers"
final_owner = "retained compiler-language task glue plus sifr_runtime substrates"
reason = "Task scopes, cancellation, and async control-flow are language runtime semantics."
certification_state = "retained-compiler-language-glue"
can_move_before_runtime_certification = false
migration_blocker = "final retained allowlist decision."
deletion_stage = "final retained allowlist"

[[surface]]
id = "_sifr.net"
public_modules = ["sifr.net"]
current_owner = "compiler intrinsic registry and preamble: net plus sifr_runtime::net calls"
final_owner = "crates/sifr_stdlib net feature backed by certified resource/async interop and sifr_runtime substrates"
reason = "Network handles are resource-shaped async stdlib surfaces."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Opaque network handle lifecycle and async runtime certification."
deletion_stage = "network-tls-resource migration"

[[surface]]
id = "_sifr.tls"
public_modules = ["sifr.tls"]
current_owner = "compiler intrinsic registry and preamble: tls plus sifr_runtime::tls calls"
final_owner = "crates/sifr_stdlib tls feature backed by certified resource/async interop and sifr_runtime substrates"
reason = "TLS config and stream handles are resource-shaped async stdlib surfaces."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Opaque TLS handle lifecycle, async runtime, and certificate resource certification."
deletion_stage = "network-tls-resource migration"

[[surface]]
id = "_sifr.http"
public_modules = ["sifr.http"]
current_owner = "compiler intrinsic registry and preamble: url_http plus sifr_runtime::http calls"
final_owner = "crates/sifr_stdlib http feature backed by certified resource/async interop and sifr_runtime substrates"
reason = "HTTP transport is resource-shaped async stdlib behavior."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "HTTP handle/transport lifecycle and async runtime certification."
deletion_stage = "http-resource migration"

[[surface]]
id = "_sifr.python"
public_modules = ["sifr.python", "sifr.python_core"]
current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/python.rs plus sifr_runtime::python calls"
final_owner = "crates/sifr_stdlib python feature backed by certified Python object/resource/callback interop and sifr_runtime substrates"
reason = "Python objects, buffers, callbacks, and context managers are resource-shaped interop surfaces."
certification_state = "future-owned-by-runtime-resource-certification"
can_move_before_runtime_certification = false
migration_blocker = "Opaque handles, callbacks, zero-copy views, and Python resource lifecycle certification."
deletion_stage = "python-resource migration"

[[surface]]
id = "generated-runtime-integer-glue"
public_modules = []
current_owner = "compiler codegen entrypoints, module constants, SifrInt render helpers, and generated imports of sifr_runtime::SifrInt"
final_owner = "retained compiler-language glue backed by sifr_runtime"
reason = "Exact integer representation and generated bridge helpers are language semantics."
certification_state = "retained-compiler-language-glue"
can_move_before_runtime_certification = false
migration_blocker = "final retained allowlist decision."
deletion_stage = "final retained allowlist"
