# Review: Phase 32 milestone_async_2 — async main Result bootstrap slice

**Reviewer:** agent
**Date:** 2026-05-09
**Branch:** phase32-async-main-result
**Changes:** +3 files, +42 lines

## Verdict: SATISFIED

The slice is acceptable to open as a PR. No blockers.

---

## 1. E2E fixture validates async Result main under runtime bootstrap

**Finding:** The fixture at `crates/sifr/tests/e2e/pass/async_main_result_bootstrap.sifr` correctly exercises the full stack:

```sifr
async def main() -> Result[None, ValueError]:
    await task.sleep(0.0)
    return None
```

Combined output from `cargo run -q -p sifr -- emit` confirms the bootstrap emits:
- `#[tokio::main(flavor = "current_thread")]` — private Tokio runtime attached
- `async fn main() -> Result<(), ValueError>` — `Result[None, E]` lowered to `Result<(), E>` per `sifr_type_to_rust_type` (`Type::None` → `RustType::Unit`)
- `return Ok(());` — `HirStmt::Return { value: Some(HirExpr::NoneLiteral) }` lowered via `try_lower_simple_return_stmt` lines 4271-4287, which specifically handles `Result[None, _]` by wrapping in `Ok(())`

The fixture runs end-to-end: the e2e harness discovers it lexicographically, compiles it, runs the binary, and asserts zero exit code. The `task.sleep(0.0)` also exercises the runtime primitive.

**Status:** Acceptable.

---

## 2. Codegen assertion is sufficient and stable

**Finding:** `test_async_result_main_entrypoint_keeps_result_return` at `lib_codegen_tests.rs:3596-3615` asserts three properties:

1. `#[tokio::main(flavor = "current_thread")]` present — bootstrap attribute
2. `async fn main() -> Result<(), ValueError>` present — signature preserved
3. `return Ok(());` present — none-to-unit wrap in return

All three assertions are stable:
- The Tokio attribute is inserted by `annotate_async_main_entrypoint` (lib.rs:758-783) and is not conditional on return type
- The signature is generated by `sifr_type_to_rust_type` (preamble.rs:20-23) which recursively maps `Type::Result(Type::None, err)` → `RustType::Result(RustType::Unit, err)`
- The `Ok(())` return is generated by `try_lower_simple_return_stmt` (lower_stmt.rs:4271-4287) which has a dedicated branch for `HirExpr::NoneLiteral` / `is_none_type` when return type is `Result[None, _]`

No fragile substring matches; all assertions target structural Rust code patterns that won't shift with formatting.

**Status:** Acceptable.

---

## 3. Docs accurately reflect implementation status

**Finding:** Three doc files updated:

- `internal_docs/phases/32_async_ecosystem.md` (line 384-385): States "Locked async `main() -> Result[None, E]` bootstrap coverage; generated Rust preserves `Result<(), E>` under the private Tokio entrypoint" and "Added positive validation fixture: `async_main_result_bootstrap.sifr`" — accurate.
- `internal_docs/roadmap.md` (line 63): Updated description to include "async-result entrypoints" — accurate.
- No changes needed to `internal_docs/async_concurrency_model.md` — the model doc already shows `async def main() -> Result[None, Error]` as the canonical primary pattern (lines 31-54); the implementation is consistent with it.

**Status:** Acceptable.

---

## 4. Pre-PR validation confirmed

User reported all local validations passed:
- `cargo fmt --check`
- `cargo test -q -p sifr_codegen async_result_main`
- E2E suite with fixture manifest
- `cargo run -q -p sifr -- run` on fixture
- `scripts/run_all_tests.sh --profile quick`

**Status:** Acceptable.

---

## No blockers. Ready to open PR.