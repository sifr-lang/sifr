# Native Pydantic Sifr PS2 — agent Review Pass 1

Exact base: `01c43b9cd67df6174b44fbbf7d2328ac5a831cb7`

Exact candidate: `7b8df434706161e17fe0fade071d80fab09effea`

Draft PR: [#3114](https://github.com/sifr-lang/sifr/pull/3114)

I reviewed the exact diff `01c43b9cd..7b8df4347` (133 files) plus the unchanged structural contract in `internal_docs/rust_interop_architecture.md`, and reproduced three items directly with the candidate-built compiler (`target/debug/sifr`, clean worktree at `7b8df4347`).

What is genuinely in place: the atomic `bridge-version` cutover (manifest field, `RustInteropConfig`, plan/cache fields, package-graph digest, sysroot `Some(1)`, fixture matrix marker, `_scenario_checks`/`_scenario_registry`/`_matrix_inventory`/`check_fixture_matrix`/`bridge_check.py`, deleted `bridge_version_mismatch` fixture and its matrix/tier/claim rows, docs and architecture rewrites) with an explicit removed-field diagnostic (`sifr_fields.rs:155`, `diag/mod.rs:169`) bound to a driver test; the `@rust.structural` bare-marker grammar with argument/duplicate/target/owner diagnostics; the position validator; the generic probe with mutation-backed negative evidence; the leaf identity crate; the opaque-resource-backed `re.Pattern` with both `compile`/`compile_flags` fallible; and non-tautological positive fixture assertions (constructed values `root/a,b/boxed/tail` differ from the projected input).

Verdict: NOT SATISFIED

Blocking findings:

- regression: `crates/sifr_codegen/src/structural_impl_codegen.rs:61` (`Type::Newtype` treated as structurally supported) with `crates/sifr_codegen/src/class_emitter.rs:635` (unconditional `emit_structural_record_impls`). Violates the milestone requirement that the compiler gain only general capabilities without breaking existing programs, and the file-wide rule that generated code must build. Concrete reason: impls are emitted for every ordinary record class, and a field whose type is a newtype class is declared supported, but newtype classes are excluded from impl generation (`class.newtype_inner.is_none()` at line 35), so the emitted impls reference traits that do not exist for that Rust type. Reproduced on a plain program with no Rust interop (`class UserId(int)`, `class Item: id: UserId`): `sifr run` fails with `SIFR-BUILD-0005` / `E0277: the trait bound 'UserId: StructuralType' is not satisfied` (also `StructuralConstruct`, `StructuralProject`). Required correction: make the codegen support predicate exactly match the set of types that receive impls — reject `Type::Newtype` (or emit the `Refined` implementations the contract's `StructuralKind::Refined` reserves) and align it with `supports_structural_bridge_type_inner` in `crates/sifr_lowering/src/lower/type_bounds.rs:107`, which already returns false for newtypes.
- in-scope omission: `crates/sifr_codegen/src/structural_impl_codegen.rs:192` (`recursive_reference(0)` only for a direct self-name match) and `:233` (default branch emitting `<T as StructuralType>::shape_identity()`). Violates the accepted contract's “numbered recursive back-references” and “Generated nominal implementations return the compiler-precomputed result produced by the same crate”; also leaves “every concrete `ShapeIdentity`” and the identity algorithm version out of the interop cache identity (`rust_interop_cargo_inputs.rs` records neither, and no compiler crate depends on `sifr_structural_identity`). Concrete reason: identity is composed at program runtime, not precomputed, and only depth-0 self-reference is numbered, so a mutually recursive pair emits `A::shape_identity → B::shape_identity → A::shape_identity` with no cycle encoding. Reproduced via `sifr emit` on `class A: b: B | None` / `class B: a: A | None` (`/tmp/mr.rs:8` and `:52`); both types are accepted by the `Structural` bound (`type_bounds.rs:139` returns true on a revisit), so a legal structural call on them is an unbounded recursion — a stack-overflow abort the mandatory silent panic boundary cannot catch. Required correction: compute the canonical identity in the compiler through `sifr_structural_identity` with a numbered back-reference stack covering mutual cycles, emit the precomputed bytes via `ShapeIdentity::from_bytes`, and feed the concrete identities plus `ALGORITHM_VERSION` into the interop cache inputs.
- in-scope omission: `crates/sifr_lowering/src/lower/rust_interop_structural.rs:77-91` (return validation) and the absence of any panic/trust check in `validate_structural_function_contract`. Violates the accepted contract clause “A structural return must be inside `Result` with an ordinary error and `RustPanicError`; a structural call cannot use a no-panic trust waiver or a `panic = "abort"` generated-build profile”, and the milestone's “correct capability, ownership, and panic behavior”. Concrete reason: only the `Result` shape is checked. Reproduced: `sifr check` reports “no errors found” for `@rust.structural` + `@rust(bridge.structural.observe, panic=trusted_no_panic)` returning `Result[str, BridgeError]` — the mandatory outer silent boundary that the contract requires over the backend target, source methods, generated construction/projection, visitor calls and callback invocation is waived with no diagnostic. Required correction: reject a structural declaration whose `Result` error side omits `RustPanicError`, and reject `panic=trusted_no_panic` / an abort generated-build profile on structural declarations, with targeted positive and negative tests.

Follow-up findings:

- suggestion: `stdlib/sifr/re.sifr:205` — `Pattern.finditer` still routes through `_finditer_materialize(self._pattern, ...)`, recompiling the pattern per call while every other method uses the opaque handle, so “compiling once” holds for six of seven methods.
- suggestion: `verification/areas/rust_interop/fixtures/structural_bridge_calls/examples/structural_bridge_runtime/src/bridges/structural.rs:186` — `into_source::<T>` sets `shape: T::shape_identity()`, so the root shape comparison in the runtime-observed positive evidence is self-satisfying; mismatch is only covered by the `sifr_runtime` unit test. A deliberately wrong-shape source in the fixture would make that evidence direction non-tautological.
- suggestion: `crates/sifr_codegen/src/rust_interop_direct.rs:352-361` — `rust_function_path` now returns `structural_target_path(...)` on both branches of the `BRIDGE_ROOT` test; the branch is dead and can be collapsed.
- suggestion: `crates/sifr_lowering/src/lower/type_bounds.rs:28` — `Structural` is recognized as a builtin bound purely by name, so a locally declared `Structural` (or one not imported from `sifr.meta`) is silently treated as the compiler-owned marker.
- pre-existing issue: the `readonly_check_doctor` 120-second timeout in the create-PR Python lane reproduces on current `main` and is tracked by #3110; not a PS2 defect and not counted here.
