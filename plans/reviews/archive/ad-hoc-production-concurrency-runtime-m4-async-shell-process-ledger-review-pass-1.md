PASS

Both ledger updates accurately reflect the merge of PR #2393 ("Add async shell process APIs"):

- `issues/ad-hoc-production-concurrency-runtime-platform-substrate-execution.md:437` - updates the M4 async shell process APIs entry from "in progress" to the correct PR link (#2393), matching the established format of sibling M4 entries.
- `verification/stdlib/concurrency_runtime_m4_process_traceability.md:5` - moves "async shell process APIs are under review" to "async shell process APIs merged in PR #2393", preserved at the same position in the chronological merge list (after PR #2390, before the "remaining M4 subprocess lifecycle gaps are pending" tail), and the overall M4 milestone remains "In progress" which is consistent with the issues ledger still listing M4 as in progress.

No stale status, wrong PR number, or missing ledger surface. Acceptable to merge.
