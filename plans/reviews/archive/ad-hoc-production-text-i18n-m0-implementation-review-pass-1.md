FAIL

Blocking findings (must be remediated before M1 opens):

1. **Required negative import fixtures for `encodings` and `locale` are missing.** Phase doc M0 scope (issues/ad-hoc-production-text-i18n-platform-substrate.md:362, 678, 696) requires bare-import diagnostics for every CPython text/i18n root, and `cpython_stdlib_reserved_suggestion` (crates/sifr_stdlib/src/lib.rs:144) maps all five (`codecs`, `encodings`, `unicodedata`, `locale`, `gettext`). Only three have fixtures (`crates/sifr/tests/e2e/fail/bare_cpython_text_i18n_imports.sifr`, `bare_cpython_unicodedata_import.sifr`, `bare_cpython_gettext_import.sifr`). Remediation: add `bare_cpython_encodings_import.sifr` and `bare_cpython_locale_import.sifr` fail fixtures that expect `SIFR-IMPORT-0008` and that the suggested redirect text resolves to `sifr.encoding` and `sifr.i18n` respectively; also add a regression case for the dotted form (`from codecs.utf_8 import …`) or explicitly document dotted forms as out of M0 scope.

2. **Itemized unsupported Python-shaped surface classifications absent.** Phase doc Resolved M0 Decisions (issues/ad-hoc-production-text-i18n-platform-substrate.md:696) requires `setlocale`, `localeconv`, `strcoll`, `strxfrm`, `codecs.register`, `codecs.unregister`, `codecs.register_error`, `gettext.install`, global `_`, public `encodings.*`, and CPython C APIs to each have an `unsupported-with-diagnostic`/`deferred-to-phase-X`/`rejected` row with CPython evidence and a regression fixture (Required Tracking Artifacts at line 668 reiterates this). `verification/stdlib/text_i18n_substrate_inventory.md` only has a single bare-import row and the JSON has a single bare-import line in `unsupported_python_shaped_surfaces`. Remediation: add per-API rows to the inventory (md + json) with terminal state, stability, CPython evidence path/line, and link to a regression fixture (or to the bare-import fixture set if that is the chosen evidence) for each named surface above.

3. **Dependency decision records are incomplete.** Phase doc Rust Ecosystem First (issues/ad-hoc-production-text-i18n-platform-substrate.md:232) requires each crate family record to include: Sifr abstraction hiding crate types, Unicode/version alignment, panic/unsafe audit, typed error mapping into Sifr variants, license/MSRV/binary-size/platform impact, deterministic local test strategy, and supply-chain/maintenance signal. M0 DoD at line 387 makes this an M1 gate. The dependency table in `verification/stdlib/text_i18n_substrate_inventory.md` and `.json` only carries crate/version/feature flags/state (+ license/rust_version in JSON). Remediation: extend each row (md + json) with the missing required fields, or split each crate into its own dependency decision record under `verification/stdlib/` (one per crate family) and reference it from the inventory.

4. **Exact Tier 0/Tier 1 alias table not recorded.** Phase doc Encoding Support Tiers (issues/ad-hoc-production-text-i18n-platform-substrate.md:257) requires M0 to "record the exact Tier 0/Tier 1 alias table". The inventory only states the policy ("canonical labels plus WHATWG labels covered by `encoding_rs`") without enumerating accepted aliases. Without this table, M1 reviewers cannot verify the static registry. Remediation: add an explicit alias table to `verification/stdlib/text_i18n_substrate_inventory.md` (and a structured array to the JSON) listing every accepted label/alias per Tier 0/Tier 1 encoding, with the WHATWG-or-canonical justification per entry.

5. **`open(...)` and mode-literal diagnostic wording not captured.** Phase doc M0 scope (issues/ad-hoc-production-text-i18n-platform-substrate.md:377) requires "diagnostic wording for explicit-encoding-required text `open(...)` and literal-mode-required `open(...)`" as an M0 decision. Neither `verification/stdlib/text_i18n_substrate_inventory.md`/`.json` nor `verification/platform/platform_contract.md` records the exact diagnostic strings or reserved codes. M1 must implement these diagnostics and they must be derived from M0. Remediation: add diagnostic codes + canonical message text to the inventory (e.g., reserve `SIFR-IO-NNNN` for missing-encoding text open, and a separate code for nonliteral mode), with the human-text wording and the suggested-fix text as a row.

6. **No-global-state policy not recorded as M0 evidence.** Phase doc Objective (issues/ad-hoc-production-text-i18n-platform-substrate.md:27-28), Architecture Principles "Locale And I18n Without Process Globals" (lines 295-302), and Quality Contract (line 674) all assert "no unsynchronized process-global mutation". The platform contract's Ownership And Lifetime table (verification/platform/platform_contract.md:29-37) does not call this out, and neither inventory file records it. Remediation: add a "No Global State" row or section to `verification/platform/platform_contract.md` and a matching entry to `platform_contract.json` (e.g., a `policies` block) stating: no registry mutation, no process-global locale, no `gettext.install`/global `_`, no implicit locale defaults; cite which surfaces enforce it. The text/i18n inventory should reference that policy.

7. **External review PASS missing on the platform contract.** Phase doc M0 DoD (issues/ad-hoc-production-text-i18n-platform-substrate.md:386) and `verification/stdlib/text_i18n_m0_traceability.md:15` require an external review `PASS` on the shared platform contract before M1 opens. `reviews/ad-hoc-production-text-i18n-m0-implementation-review-pass-1.md` is empty (0 lines). Remediation: after items 1-6 are closed, record this review's outcome (with verdict and remediation links) in that file and add the link to `issues/ad-hoc-production-text-i18n-platform-substrate-execution.md` under Planning Reviews before flipping the M0 checklist box.

Non-blocking observations:

A. `BareStdlibMatch::exact_embedded_module_exists` is set to `false` for the reserved CPython text/i18n roots, which causes `crates/sifr_lowering/src/lower/import_diagnostics.rs:88-94` to append "; no embedded sifr.codecs module exists" to the diagnostic. The suggestion already redirects to `sifr.encoding`, so the trailing "no embedded sifr.codecs module exists" can read as contradictory. Consider suppressing the trailing note for reserved redirects, or rewording it to "no embedded `sifr.codecs` module exists; use the production redirect above".

B. Dotted CPython forms (`codecs.foo`, `encodings.utf_8`, etc.) bypass `cpython_stdlib_reserved_suggestion` because the function only checks the exact bare module name; they fall through to the generic unknown-import path. Either widen the reserved match to handle `<reserved>.…` forms or explicitly document the M0 scope as "bare root imports only".

C. Three platform-golden entries (`http_body_text_after_text_i18n.sifr`, `subprocess_text_explicit_encoding.sifr`, `locale_host_limited_formatting.sifr`) are referenced in `verification/platform/golden/manifest.json` but the `.sifr` source files do not exist; they are skipped because `blocked_until` is non-empty. When their providing milestones close and `SIFR_PLATFORM_CLOSED_MILESTONES` includes them, the runner will fail with "file not found" rather than a substrate diagnostic. Either add stub files now (with `@cpu_heavy/@blocking_io` annotations and the expected stdout lines) or add a manifest-level note that the source files are landed in the milestone PRs that close the gate.

D. The substrate inventory uses `deferred-to-phase-adapter`, `deferred-to-phase-encoding-expansion`, and `deferred-to-phase-message-format-backends` as concrete instantiations of the contract's `deferred-to-phase-X` template. There is no "adapter", "encoding-expansion", or "message-format-backends" phase recorded elsewhere. Either link each instantiation to a tracked issue/phase doc or reword to `deferred-to-adapter-phase`/`deferred-to-follow-up-issue` consistently with the issue's existing prose.

E. `scripts/run_all_tests.sh` now treats the platform-golden suite as a hard merge-gate step. Any future change to the SIFR-IMPORT-0008 wording will break the merge gate via the `expected_diagnostic_contains` substring match; consider pinning to the diagnostic code only ("SIFR-IMPORT-0008") and dropping the brittle module-name substring, or adding a comment explaining the coupling.
