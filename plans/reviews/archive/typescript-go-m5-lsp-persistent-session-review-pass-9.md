SATISFIED. The branch is ready for PR.

**Verification of the six M5 blocker requirements:**

1. **DocumentStore scrubbed of per-document analysis** — `crates/sifr_lsp/src/document_store.rs:1-3` drops the `AnalysisHost`/`AnalysisSnapshot`/`FileId`/`FrontendInput`/`SourcePath`/`SourceText` imports; the `DocumentAnalysis` struct, `rebuild()` method, and `with_host()` method are removed (was lines 41-46/175-176/185-216 in the diff). `state.rebuild()` calls in `change_full`/`change_incremental`/`save` (was lines 91/115/124) are gone. `documents_mut`/`uri_map`/`source_map`/`file`/`load_diagnostics` are also gone from `DocumentStore`. Grep confirms zero remaining `AnalysisHost::open_single_file` or `FrontendMode::SingleFile` references in the file.

2. **Session owns persistent analysis state** — `crates/sifr_lsp/src/session.rs:13` adds `analysis: LspAnalysisWorkspace`; the new `crates/sifr_lsp/src/analysis_workspace.rs:11-18` defines `LspAnalysisWorkspace` as `BTreeMap<String, LspDocumentAnalysis>` where `LspDocumentAnalysis` persists an `Option<AnalysisHost>` per URI. `Session::open_document`/`change_full`/`change_incremental`/`save_document`/`close_document` (lines 42-92) update the workspace and `LspDocumentAnalysis::update` (lines 100-123) reuses the existing host via `upsert_overlay_document` rather than rebuilding.

3. **Open/change/save flow through WorkspaceSession overlays** — `crates/sifr_analysis/src/host/overlay_updates.rs:7-18` adds `AnalysisHost::open_single_file_overlay` which creates a single-file `WorkspaceSession`, calls `session.upsert_overlay(path, uri, version, source, None)`, and `session.reload()?`. `AnalysisHost::upsert_overlay_document` (lines 20-35) calls `self.session.upsert_overlay(...)` and `self.session.reload()?` on the same `WorkspaceSession` — keeping unsaved text in the overlay layer rather than rebuilding from disk. The new test `session.rs:183-208` (`open_document_analysis_uses_unsaved_overlay_text`) writes a bad-on-disk string and asserts diagnostics come from the editor overlay text.

4. **Snapshot + version publication identity** — `Session::with_document_analysis` (`session.rs:110-127`) captures `before_version`, runs the operation through `self.analysis.with_document`, then re-reads `after_version` and returns `LspError::request_cancelled` on mismatch. The inner `LspDocumentAnalysis::with_host` (`analysis_workspace.rs:134-159`) captures `host.snapshot()` before the operation and rejects via `host.is_snapshot_current(&snapshot)` after — which compares both `WorkspaceSnapshot.revision` and analysis `graph`/`source` revisions (`implementation.rs:120-123`). All 14 request files (symbols, completion, hover, navigation, formatting, code_action, semantic_tokens, inlay_hint, selection_range, folding_range, type_hierarchy, diagnostics) and all commands (`explain_diagnostic`, `generated_rust_preview`, `run_tests`) plus `document_diagnostics` route through this single boundary.

5. **M5 remains serialized; scheduler/cancellation deferred** — `with_document_analysis` and `with_host` both take `&mut self` for the lifetime of the closure, with borrows properly scoped so the document version is re-read after the analysis closure returns. Grep across `sifr_lsp/src` finds no new `async`/`tokio`/`spawn`/`debounce`/`priority` keywords — only the pre-existing `cancel_request` and `request_cancelled` (which now also serves the snapshot/version stale path). `scheduler.rs` is still 19 lines (`lane_for_method` only) and `request_queue.rs` still 35 lines (pending-id set + shutdown flag). `lsp_server.md:78-81` explicitly states both remain shallow and "not yet a cancellation-token registry".

6. **Docs, guardrail, tests, tracker reflect M5** — 
   - `internal_docs/architecture.md:276` adds the M5 row.
   - `internal_docs/lsp_server.md:61-85` swaps the "Current M4 Compiler-Service Caveats" section for "Current M5 Compiler-Service State" with the new ownership layout.
   - `internal_docs/tooling_analysis.md:62-69` adds a "TypeScript-Go architecture transfer M5 implementation" section.
   - New `internal_docs/typescript_go_architecture_transfer_m5_lsp_persistent_session.md` documents session-owned analysis, overlay updates, and snapshot/version publication identity.
   - `internal_docs/typescript_go_architecture_transfer_m1_guardrails.md:122/143-147/166-170/183-184` updates the "Current LSP Reality" section to historical framing, adds an M5 update paragraph, and flips "M5 must update" to "M5 updated" in both required-doc-snippet and forward-looking sections.
   - `verification/tooling/check_typescript_go_m1_guardrails.py:19-20, 119, 199-229` flips the LSP-current-state check: it now requires `DocumentStore` to NOT contain `AnalysisHost::open_single_file`/`FrontendMode::SingleFile`/`with_host`, and requires `Session` to contain `analysis: LspAnalysisWorkspace` / `with_document_analysis` / `LspAnalysisWorkspace::default()` and `analysis_workspace.rs` to contain `open_single_file_overlay` / `upsert_overlay_document` / `load_diagnostics`.
   - `issues/ad-hoc-typescript-go-compiler-architecture-transfer.md:14` adds the M5 tracker row; lines 59-77 add the M5 validation list.
   - `issues/ad-hoc-typescript-go-compiler-architecture-transfer-execution.md:193-201` adds the M5 validation log entries (cargo test for sifr_lsp/sifr_analysis/sifr_frontend, e2e-skip, lsp_protocol_smoke/stress + self-tests, M1 guardrails + self-test, fmt/diff-check, file-size + package-manager guardrails, clippy -D warnings, `run_all_tests.sh --profile quick` at 227.66s).
   - New tests `session.rs:183-246` cover the overlay-fed open path and change-keeps-analysis-in-session path; `analysis_workspace.rs` is a focused 164-line file under the 900-line file-size guardrail.

No blockers; the branch is ready for PR.
