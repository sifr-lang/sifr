## Review — Phase 40 / M40.5 protected stable publication production wiring, pass 5 (exact PR #3045 head)

**Identity verified.** Local `HEAD`, `origin/codex/phase-40-milestone-40-5-stable-publication-wiring`, and PR #3045 `headRefOid` are all `a5c9a2ce873b6a3f65b142c803bca61b191abbbf`. Merge base with `origin/main` is `47c837a4b`; the diff is one commit, 43 files, +3651/−124, `MERGEABLE`/`CLEAN`. Working tree is clean except two 0-byte untracked review artifacts (below). Diff is Phase-40 distribution/release governance only — no Rust, no Rust-interop, no Python-interop source.

**Independently reproduced** (not reused evidence): `distribution_release` `full` + `stable-prepare` + `stable-publish-primitives` + `stable-publication` = **63 variants, 0 failures** (governance 14/14, incident-recovery 9/9, protected-drill, stable-prepare 7/7, primitives 4/4, stable-publication 9/9). Both workflow contract cases pass standalone and inside the run. `coverage_matrix` 5/5. Runner `--self-test` all pass including release-report precondition/production. Both workflows parse as YAML; all `scripts/distribution/*.sh` pass `bash -n`; `git diff --check` clean; `file-size guardrails: PASS (2924 files, limit 900)` with `release-publication.yml` at 899 and `stable_publish_selftest.py` at 892.

### Prior actionable findings — all eight closed, verified in the committed tree

| Pass-N finding | Closure verified at |
|---|---|
| Marketplace path executable | `release-publication.yml:174-186` SHA-pinned `setup-node@49933ea5` (node 22, cache keyed to `stable-source/editor_integrations/vscode/package-lock.json`) + `npm ci --ignore-scripts` with `GH_TOKEN: ""`, before any secret step; `VSCE_BIN` refused unless `-x` at `run_stable_publication.sh:99` **and** `publish_marketplace_extension.sh:46`; no `npx` anywhere |
| Governance-code symmetry / pinning | prepare runs `governance-source` @ `github.sha` (`release-publication-prepare.yml:267-274, 337-393`); publish root checkout is unpinned-ref = `github.sha`, and `run_stable_publication.sh:107-116` asserts `HEAD == workflow_commit` **and** `origin/main == workflow_commit`, with `--workflow-ref` regex-pinned to `refs/heads/main` at `:77` |
| Immutable per-attempt sign-off | `stable-release-signoff-${version}-attempt-${run_id}-${run_attempt}.json` (`:399,406`); `materialize_stable_signoff` has no clock/random input, so the same-name `cmp` branch is a true no-op |
| Marketplace raw-byte verification + tests | `publish_marketplace_extension.sh:113-118` re-verifies the raw `Microsoft.VisualStudio.Services.VSIXPackage` bytes and package `publisher`/`name`/`version` before `mv` to `--verified-out`; negative drift test at `stable_publish_selftest.py:203-214` (server bytes replaced, non-zero exit, output absent); GitHub-release adapter has duplicate-`initial` and remote-drift rejections; public smoke executes end-to-end |
| Paginated assets | `fetch_governance:156`, `upload_or_verify_governance:189` (`--paginate --slurp …?per_page=100`), `publish_stable_release.py:159-185` pages explicitly |
| Protected-main ancestry | `:117-129` derives `candidate_source_commit` from `prepare-summary.source.commit` (which `stable_prepare.py:130` binds via `_require_checkout(source_root, plan["source_commit"])`) and loops `merge-base --is-ancestor` over source **and** evidence against freshly fetched `refs/remotes/origin/main`, with `fetch-depth: 0` on the root checkout; executed fake-git test `stable_orchestrator_selftest.py:15-103` asserts the exact message |
| Narrow secret exposure | `:92-98` captures `site_token`/`marketplace_pat` as non-exported locals then `unset SITE_TOKEN VSCE_PAT`; each is reintroduced only command-scoped (`:227,328,368,372` site; `:259` Marketplace; `:382` all-cleared smoke); `publish_marketplace_extension.sh:88` re-clears for `vsce`; `run_stable_public_smoke.sh:132-141` clears all three for the dispatcher `sh` and installed `sifr` |
| Negative schema parity | `schema_contracts.py:149-167`. I confirmed **non-vacuity independently**: deleting the prepare `allOf` makes `publication_state: activated` + `mode: initial` accept, and relaxing the sign-off `repository` `const` makes `example.invalid/site` accept; the real schemas reject both and the positive fixtures stay valid |

### Fresh audit — no actionable finding

Ordering holds and is pinned: revalidate → approver resolution → site-identity verify → `publish_stable_release.py` → Marketplace → refetch+revalidate → snapshot upload → post-snapshot stale-lease `cmp` → the single `--clobber` on `channels.json` → activated-digest check → facts → site dispatch → poll → public smoke → sign-off → retained assets. `--clobber` appears exactly once in the orchestrator and never in `publish_stable_release.py`. Custody is exact-source/no-rebuild: staged assets and the published VSIX come from the refetched `stable-assets` tree; `npm ci` installs only the publisher. `mode=initial` fails closed on an existing release **or** tag (`publish_stable_release.py:57`), resume byte-compares by immutable asset ID with `text=False` and rejects unknown/drifted assets, and the release/tag object must resolve to the exact source commit. `publication_state` correctly splits pending (reserve + mutate) from activated (verify, no second mutation), with `next_generation > proposed_generation` enforced in `validate_stable_prepare_summary:422`; the generation-scoped site-facts asset is byte-stable across attempts, so `upload_or_verify_governance` converges. `poll_site_release_run.sh:66-96` correlates on `display_title` (unique per `run_id-run_attempt`) plus `head_sha`, emits `jq -cnS` bytes that match `canonical_json_bytes` exactly (trailing newline, sorted, compact — I checked `common.py:165-169`), and writes `--result-out` only on terminal success. `resolve_distinct_approvers` requires a `stable-release` approval by a login other than the initiator, resolved before any mutation. Permissions: top-level `contents: read`, re-granted only on `publish` (`stable-release` environment for non-preview), `drill` and `prepare` remain `contents: read` with no publication secrets; concurrency is `sifr-release-index` for all non-drill modes. Every unvalidated dispatch input path is fail-closed (bad `workflow_ref`, unmerged commit, absent `SITE_TOKEN`, absent `VSCE_PAT` with an absent Marketplace version, `parser.error` → exit 2 throughout). Registration is consistent and deduplicated across `manifest.json`, `runner.py` (`include_stable_publication`), merge/nightly/release profiles, the profile-assignment matrix, `REQUIRED_SUITES`, and the gate inventory; `create-pr` keeps its pre-existing empty `distribution_release` row.

I found **no** path that activates stable before exact release and Marketplace state.

**Python timeout follow-up (item 4):** `plans/issues/active/python-interop-readonly-inspection-timeout.md` is truthfully scoped — states 18/19 variants passed, names the exact failing capability and 120-second boundary, cites the isolated reproduction, explicitly records that no timeout, waiver, suppression, or Phase 40 code change was made, and uses a capability-based filename. Nothing in the diff touches Python interop. The ledger's matching entry agrees. Ledger figures I could measure (63 variants, 899 lines, 7/7, 4/4, 9/9, 20 artifacts, 6 uploads) all match.

### Non-blocking observations (all carried forward; none new to this wave)

- Post-publication public smoke runs only on the `ubuntu-24.04` host (`x86_64-unknown-linux-gnu`); M40.5's "every supported target" DoD and the exit gate still rest on the unchecked ledger items ("Publish or verify write-once assets… and post-publication smoke", "Exercise resume, stale generation, burned generation, rollback…"). This wave does not claim them.
- Preview/bootstrap governance-asset inventory (`release-publication.yml:419,671`) is still unpaginated `gh release view channels --json assets`; pre-existing, fail-closed (spurious 422), and now slightly worse in practice since per-attempt sign-off assets grow the `channels` release per attempt.
- Marketplace server-side re-signing remains the one open first-publish risk by design: if Microsoft repackages the upload, raw `VSIXPackage` bytes will never equal `marketplace.vsix_sha256` and the loop at `publish_marketplace_extension.sh:90-105` exhausts. Worth one live-Gallery validation before GA.
- `release-publication.yml` is at 899 of the 900-line cap; the next step in that file forces decomposition.
- Because prepare and publish share one run and one `sifr-release-index` lease, the lease is held across the human approval window — and `origin/main == workflow_commit` means `main` advancing during that window aborts the run (fail-closed, re-dispatchable).
- Three `mktemp -d` scratch dirs in the orchestrator (`verify`, `live_after_snapshot`, `activated`) sit outside the `EXIT` trap; harmless on an ephemeral runner.
- Secret narrowing is pinned by literal substring, not by execution assertion; a `test -z "${GH_TOKEN:-}"` in the existing fake `vsce`/dispatcher would make it behavioral.
- `test_public_smoke_adapter` uses a fake dispatcher and fake `sifr`, so the real receipt-discovery path isn't executed. I verified it by reading instead: `generate_version_installer.sh:582-590` puts the manifest at `${install_root}/install.json`, and `self_update_receipt.rs:99-107` finds it from the `bin/` parent without needing `HOME` — matching the already-merged `run_schema_bootstrap_public_smoke.sh:92-97` precedent, which also omits `HOME` on the `sifr` call.
- Untested-but-shared: the evidence-commit iteration of the ancestry loop, `publish_stable_release.py:143` `target_commitish` equality on resume, and `_recover_realized_mutation`'s `sha256_file` on a hypothetical directory matching `SNAPSHOT_RE`.
- `plans/reviews/active/phase-40-milestone-40-5-stable-publication-wiring-review-pass-5-exact-pr-head.{md,agent.log}` are 0-byte and untracked, so outside the PR. I modified no file.

SATISFIED
