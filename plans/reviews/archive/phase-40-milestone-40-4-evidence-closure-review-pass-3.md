## Round 3 review — Phase 40 Milestone 40.4 evidence/closure slice

No files modified. I read archived passes 1–2, the full working diff against `origin/main` (3 docs files, +94/−0), the four untracked review artifacts, and re-verified every digest and numeric claim against the surviving artifacts.

### Pass-2 findings — closure status

| # | Pass-2 finding | Status |
|---|---|---|
| 1 | Archived docs-qualification command couldn't regenerate the recorded id | **Closed & verified.** `plans/reviews/archive/phase-40-milestone-40-4-exact-source-evidence.md:42-61` now records the real invocation (`qualify_stable_documentation.py --source-root /private/tmp/sifr-phase40-release-source --source-commit 7242e473… --out …/local/qualification-documentation.json`; all three flags exist at `scripts/distribution/qualify_stable_documentation.py:229-231`), the producer-owned result path, and both digests. I recomputed: result file `…/target/verification/areas/documentation-stable-qualification-results.json` = `038b0eabc1c1…` (matches the recorded `result_sha256` and the `docs-7242e4737b1e-038b0eabc1c1` id per `:113,178-181`), report at the recorded `--out` path = `aea9f6f9c8a2…`. Both match exactly. |
| 2 | "four-metric set" undercounted the retry | **Closed & verified.** `plans/phases/adhoc_performance_budget_host_variance.md:67` now reads "four-benchmark, five-metric set"; archive `:87` says "five metrics across four benchmarks". The 22:51 log emits exactly 5 regressions over 4 benchmarks (`check-project-004`, `check-single-file-001`, `diagnostic-non-regression-002` medians, `lsp-query-003` median **and** p95), and the set differs from the 22:45 attempt. |
| 3 | Immediate retry and later end-to-end spike logs had no digests | **Closed & verified.** Archive `:87-98` records `af2331d5b0e3…` and `4b4b752cee1f…`; recomputed against `/tmp/sifr-phase40-performance-7242-rerun.log` and `/tmp/sifr-phase40-release-profile-7242-final.XXXXXX.log` — both exact. |

All six pass-1 findings remain closed (40.5 checklist items at issue doc `:290-296`, phase scope `:947-958`, DoD `:1038-1043`; "required … fields are absent" wording; "in a local canonical report"; archived artifacts non-empty).

### Independently re-verified this round
- **Chronology and every performance number.** mtimes: 22:45 first release-profile attempt → 22:51 retry → 22:56 `c17` control → 23:00 standalone pass → 23:15 later end-to-end → 23:43 canonical pass. From the six benchmark evidence files: first attempt exactly **three** medians (3053.6/1415.8/1420.4 vs 1357.5/1334.1/1336.0), standalone pass `variants=8 failures=0` with key medians 1276.8/1279.2/1281.1, later attempt 3 medians again with max sample **3880.394 ms** = "3.88 seconds", canonical run medians 1296.8/1278.5/1276.2 = archive's "1.276, 1.278, 1.297" with bench evidence `6c9b3f10…` (recomputed, matches, and is correctly attributed to the *final canonical* invocation). Every regression line reads `waiver_status=no_waiver`.
- **First-attempt lane list is accurate** (`adhoc:61-67`): coverage, core, diagnostics, CPython differential, python-interop `variants=25`, rust-interop `variants=10` consumed gate, frontend guardrails, developer-tooling `variants=48`, documentation `variants=2` all `status=pass` before `performance_budget_checks status=fail`.
- **New 40.5 bootstrap plan is feasible with no v1 path**, not just aspirational: `scripts/distribution/generate_channel_metadata.sh:18,52-56` already *requires* omitting `--stable-release` for preview metadata and forbids it, so a truthful alpha/beta-only schema-v2 `preview` generation is producible without a migration, fallback, or synthesized digest — matching phase doc `:947-958`.
- **Scope guards intact.** Diff is docs/evidence only — no `crates/**`, no baseline/threshold/waiver/profile-manifest file. Every `schema-v1` mention is a prohibition or an observation of external state; no v1 reader, no stable mapping, no GA activation, no Rust-interop implementation. All five `milestone_40_4` boxes unchecked, issue status still "In progress" — no closure claim. Milestone ownership is explicit for both the epoch bootstrap and the endpoint override (40.5), and for the stale-diagnostic defect (this ad-hoc follow-up's Scope, `adhoc:90-95`).

### Remaining actionable finding

**1. LOW — two newly asserted results still have no archived digest, the same class pass-2 finding 3 enforced.**
`plans/phases/adhoc_performance_budget_host_variance.md:61-67` asserts the 22:45 first release-profile attempt's lane-by-lane pass list and its "three check/diagnostic medians exceeded their budgets"; its log is `/tmp/sifr-phase40-release-profile-7242.XXXXXX.log`, SHA-256 `babdace21ecbecf5d07f3b997ee01cfe028f6a6e58b473136d76ad6cc2678aca`, which appears nowhere in the archive. `:73-75` attributes "key check/diagnostic samples clustered around 1.27–1.31 seconds" to the post-cleanup standalone run; that run's archived log (`04e8182b…`) contains only `variants=8, failures=0` and no medians, so the numbers come from `target/performance/evidence/bench-1785182272-87135.json`, SHA-256 `2bf8a8eb589cffcdd70741afd43811543af79dbc64ed8235331efe3622e8279f` — also unarchived. Both claims are *true* (I recomputed them), but the opening observation of the new `PERF-HOST` narrative and its recovery measurement are reproducible only because two `/tmp` files happen to survive, which is exactly what phase doc `:1112-1118` ("the exact commands and result artifacts") and the previous round required. Fix is two digest lines in `plans/reviews/archive/phase-40-milestone-40-4-exact-source-evidence.md:63-107`.

Non-blocking notes: `plans/reviews/active/phase-40-milestone-40-4-evidence-closure-review-pass-3.md` is 0 bytes — this pass's own placeholder, must be populated before commit. The archive still records reviewed head `95d5e2bbb` and source `7242e473…` without noting the merge rebased onto `6ffb34432`; I re-confirmed the patches are byte-identical, so coverage is intact. "1.27–1.31 seconds" understates the standalone run's single 1.328 s sample, but "clustered around" fairly covers 13 of 15 samples.

VERDICT: NOT SATISFIED
