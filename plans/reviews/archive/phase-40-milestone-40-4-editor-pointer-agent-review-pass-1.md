# Review: `sifr-lang/editor-integrations` pointer portion — Phase 40 / milestone_40_4

## Exact state

| Item | Value |
|---|---|
| Reviewed repo | `/Users/yaseralnajjar/.codex/worktrees/af6f/codebase/editor_integrations` (`sifr-lang/editor-integrations`, branch `codex/stable-vscode-release`) |
| **Exact HEAD** | `34da355fa47d51a8ceb753a44c2230b2d29e6cb9` ("Point VS Code extension at stable release package") |
| Range reviewed | `a980835e6986…..34da355fa47d…` |
| Diff | `vscode` gitlink only — 1 file, +1/−1 (`346a93cc…` → `273fd5d3…`) |
| Working tree | clean in all three repos before and after; no tracked file modified. Build artifacts `out/`/`dist/` inside the consumed checkout were regenerated by the verification gate itself (both `.gitignore`d); the packaging-drift experiment ran on a `/tmp` copy, since deleted |

## Checks run

**Pointer integrity**
| Check | Result |
|---|---|
| Only change in range is the `vscode` gitlink | PASS — `git diff --stat` = `vscode \| 2 +-`; no other submodule, no `.gitmodules`, no source change |
| New gitlink = sifr-vscode PR #12 merge commit | PASS — `273fd5d3ebc958124c3151647e2b61136a3ddb06` = "Merge pull request #12 from sifr-lang/codex/stable-compiler-range" |
| Merge contains reviewed package head `7e20bf58…` | PASS — parents are `346a93cc…` (prior pointer) and `7e20bf58cbef6af2ccdcffe09d45e1e558ab8446`; `git merge-base --is-ancestor` confirms ancestry |
| Merge tree ≡ reviewed head tree | PASS — `git diff 7e20bf58 273fd5d3` empty; the merge adds no code beyond the reviewed head |
| Package review pass 2 approved | PASS — `plans/reviews/archive/phase-40-milestone-40-4-vscode-package-agent-review-pass-2.md:66` = `APPROVED` |
| No publication surface anywhere in editor-integrations | PASS — repo is 11 tracked files (`README.md`, `emacs/`, `helix/`, `neovim/`, `syntaxes/`, `zed/`, `vscode` gitlink); grep for `vsce publish` / `VSCE_PAT` / `ovsx` / `marketplace.visualstudio` / `open-vsx` → no match |

**Package recheck from the consumed checkout (`editor_integrations/vscode` @ `273fd5d3`)**
| Check | Result |
|---|---|
| `npm ci --dry-run` | "up to date" — lockfile consistent |
| `npm run lint` (incl. self-checking drift-mutation table) | PASS |
| `npm run typecheck` | PASS |
| `npm test` / `npm run test:extension` | PASS / PASS |
| `npm audit` | **0 vulnerabilities** |
| Metadata coherence | `version 0.2.0`, `sifrCompilerCompatibility ">=0.1.0,<0.2.0"` (canonical comma form), same range in `README.md:34` and `CHANGELOG.md:6`; range literal matches main-repo consumers `incident_recovery_selftest.py:237,723,726` |
| Clean packaging (seeded `out/src/stale.js` + stale `dist/sifr-vscode-0.1.7.vsix`, `/tmp` copy) | PASS — `npm run package` removed both; **exactly one** `dist/sifr-vscode-0.2.0.vsix`; `out/src` = 7 compiled sources; VSIX has no nested `.vsix` and no `dist/` entry |
| Dependency bumps | `vscode-languageclient ^9.0.1→^10.1.0`, `@types/vscode ^1.90.0→~1.91.0`, `@vscode/vsce ^3.2.1→^3.9.2`; tree net −18 packages; `LogOutputChannel` migration confined to the two channels the client consumes |
| `check_vscode_extension.py` (package check) | PASS |
| `check_vscode_extension_rules.py` (rules check) | **FAIL** |

**Milestone-required suite at this pointer**

`uv run --project verification --locked python -m sifr_verify areas run --area developer_tooling --suite editor-release`
→ `variants=6, failures=1, blocking_failures=1` — **RED**.

## Findings

### 1. BLOCKING (integration) — this pointer bump turns the milestone's required `editor-release` suite red; no remediation exists in any working tree

**Evidence.** The pointer moves the consumed package from `engines.vscode: "^1.90.0"` (at `346a93cc…`) to `"^1.91.0"` (at `273fd5d3…`, `package.json:23-24`). The consumer asserts exact equality:

- `verification/areas/developer_tooling/vscode_extension_rules.json` → `"minimum_vscode_engine": "^1.90.0"`
- `verification/areas/developer_tooling/check_vscode_extension_rules.py:101-103` → `if engines.get("vscode") != extension["minimum_vscode_engine"]: failures.append(...)`

Observed: `VS Code extension rules: FAIL — package.json VS Code engine does not match rules`, and the required gate at `plans/issues/active/phase-40-stable-channel-ga-execution.md:189` reports 1 blocking failure. The prior pointer was green on this case; the bump is the sole cause.

Main-repo tracked status is `M editor_integrations` **only** — `vscode_extension_rules.json` is unmodified. So as staged today, the landing is red, not merely pending.

The engine value itself is correct (it is the sanctioned remediation of package pass-1 finding 3, matching LC10.1's own `engines.vscode`). The defect is coordination: the paired main-repo change must bump `minimum_vscode_engine` to `^1.91.0` in the same landing, and it currently does not.

### 2. MEDIUM — pass 2's waiver rests on a phase-doc exception that does not exist

Pass 2 line 56 declines to count the rules mismatch against the package "under the phase doc's sanctioned upstream-first two-PR exception." Greps of `plans/issues/active/phase-40-stable-channel-ga-execution.md` for `two-PR`, `upstream-first`, `paired PR`, `pointer PR`, `second PR`, and `exception` return no such sanction (the only `exception` hit is line 170, "raw-exception," unrelated). The waiver is therefore not governed by the phase doc, which leaves finding 1 unmitigated rather than deferred by policy. This is a recording gap, not a code defect — but it removes the basis on which the red gate would otherwise be tolerated.

### 3. LOW (informational, unchanged from pass 2) — `String.includes` metadata gate misses additive drift

`scripts/lint.js:82-108` asserts `readmeText.includes(\`Version \\\`${version}\\\`\`)`, so a *stale* line is caught but an *additional* one is not: appending a second `Version \`0.1.5\` … \`0.1.0-beta.12\`` line to README still lints clean (independently reproduced on the `/tmp` copy). An anchored single-match on the Compatibility section would close it. Not actionable for this pointer.

### 4. TRIVIAL (informational, pre-existing) — VSIX ships `extension/.gitignore`

`unzip -Z1` on the built `0.2.0` VSIX lists `extension/.gitignore` (369 entries total); `.vscodeignore` does not exclude it. Pre-existing, unchanged by this range.

### Confirmed clean

No unrelated pointer or source changes in the reviewed range. The reviewed-head → merge-commit chain is exact and adds nothing. No `.vsix` has ever been committed to sifr-vscode (`git log --all --diff-filter=A` over history). `.github/workflows/ci.yml` in the consumed checkout stops at `npm run package` — no Marketplace surface. The main repo's `release-qualification.yml:172-196` reads `sifrCompilerCompatibility` from the consumed `package.json`, globs for exactly one VSIX, and records `submodule_commit` from `editor_integrations` — all satisfied at this pointer. Main-repo `sifr-vscode-0.1.0.vsix` references (`qualification_fixture.py:241`, `qualification_selftest.py:80`, `schema_contracts.py:189`) are synthetic fixtures, not assertions about the real package version.

## Verdict

Finding 1 is an actionable integration issue attributable to this exact pointer: the milestone's own required suite is red at `34da355`, and the fix is absent from every working tree rather than merely queued behind a documented exception.

NOT APPROVED
