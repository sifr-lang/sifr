> Archived as the satisfied exact-implementation-head review.

## VERDICT: SATISFIED

**Reviewed:** `ddcd7e3d656e39a2b00727a7ce6ac775fa823f1e` — confirmed identical to `refs/heads/codex/phase40-bootstrap-index-recovery` on `origin` and to PR #3061 `headRefOid` (state OPEN, base `main`). Base `origin/main` = `94a5fec67b7bef51cae0034c84386c57d9ff1785`. Full PR diff: 15 files, +1620/−39 (both commits reviewed). The dirty submodule/corpora paths (`third_party/ruff`, `editor_integrations`, `leetcode`) are working-tree only — not in the PR. No files modified by this review.

### Pass-1 findings independently verified closed

**F1 — recovery provenance (CLOSED).** Schema `$defs/recovery` requires exactly the seven fields (`run_id`, `run_attempt`, `initiator`, `approval_policy`, `approvers`, `failed_site_run_id`, `site_run_id`) with `additionalProperties: false`, and the top-level `allOf` else-branch adds `{"not": {"required": ["recovery"]}}` so the alpha stage rejects it. `_validate_recovery` (`schema_bootstrap.py:317-357`) enforces positive ints, non-empty initiator, `validate_approval_policy`, `_validate_approvers` bound to the recovery initiator, and `site_run_id != failed_site_run_id`. `materialize_bootstrap_evidence:388-399` now fails closed both ways: attested path (`legacy_index_path is None`) requires `recovery`, exact-bytes path forbids it. I instrumented `expect_failure` and confirmed all 10 recovery mutations plus both new gate cases fail with the *intended* messages, not incidental ones (e.g. `bootstrap recovery: attested legacy identity requires durable recovery provenance`, `recovery provenance is forbidden when exact legacy bytes are present`, `$.recovery.site_run_id: must differ from the failed correlated site run`). Workflow population verified end-to-end: I replayed the exact `jq -cnS` invocation with realistic `decision.json`/`site-run.json` — output is byte-canonical per `canonical_json_bytes` (so `load_json_strict(require_canonical=True)` in the materializer accepts it) and passes `_validate_recovery` under the *real* `single-maintainer-waiver` mode. `FAILED_SITE_RUN_ID` is in the job env; `recovery-approval-decision.json` carries `approval_policy` via `--include-policy`; `publication/site-run.json` `.run_id` is written by `poll_site_release_run.sh` only on a successful correlated run. Contract pins `--recovery-json`, both source files, and the four schema fragments.

**F2 — durable custody and deadlines (CLOSED).** Both `download-artifact` steps are gone; `grep -rn "original-prepare\|publication-prepare"` in the recovery workflow returns nothing, and the contract now *forbids* that artifact fragment. I downloaded the still-live original artifact and `cmp`'d it: `plans/releases/schema-bootstrap-recovery/prepare-summary-30443929353-1.json` is **byte-identical** to the artifact's `summary.json`, digest `f45c012c17d2908bc2ef227f202e1037343c63d1f1881ca7913f22628f62a086`, and canonical. `prepare_schema_bootstrap_recovery.sh:83-84` gates that file against `--expected-prepare-summary-sha256`, so the durable copy cannot be swapped. All 11 dispatch inputs (plus dispatcher digests and default channel) are recorded in the ad-hoc credential issue, the Phase 40 issue ledger, and the phase doc. Deadlines verified live against the API: waiver `expires_at` = `2026-08-27T00:00:00Z`, artifact `expires_at` = `2026-08-28T10:46:13Z` — exactly as documented.

**F3 — partial-identity coverage (CLOSED).** All six required cases exist and, per the instrumented run, fail in `_resolve_legacy_identity` for the right reason: sha-only / size-only / neither → `recovery requires its exact attested SHA-256 and byte size`; zero / negative / `True` → `legacy channels.json.size_bytes: must be a positive integer`. The bool case is genuinely caught by the explicit `isinstance(..., bool)` guard.

**F4 — exact-bytes boundary pinned (CLOSED).** Contract asserts `release-publication.yml` contains `--legacy-index publication/current-channels.json` exactly twice and neither attested flag, and that each attested flag appears exactly once in the recovery workflow. Repo-wide grep confirms: the two `--legacy-index` call sites are `release-publication.yml:590,799`; `--legacy-index-sha256`/`--legacy-index-size-bytes` appear only in `schema-bootstrap-recovery.yml:469,471` (plus the argparse declarations and the contract itself), and `--recovery-json publication/recovery.json` only at `:472`.

**Pass-1 non-blocking observation (CLOSED).** `STABLE_SITE_FACTS_SHA256: none` is now in the revalidation heredoc, and I fetched the live failed site run log (`30445065348`, 1031 lines) and confirmed **all 12** expected lines are present — so the added assertion binds the last dispatched input from the log without spuriously failing closed.

### Validation I reran
- `governance.schema_bootstrap_selftest` → PASS
- `cases/schema_epoch_bootstrap_workflow_contract.sh` → exit 0
- `PYTHONPATH=. verification/areas/distribution_release/runner.py --suite full` → `variants=67, failures=0, blocking_failures=0, non_blocking_failures=0`
- `scripts/check_file_size_guardrails.py` → PASS (2959 files, limit 900)
- YAML parse of `schema-bootstrap-recovery.yml`, `bash -n` on `prepare_schema_bootstrap_recovery.sh` → clean
- Live state re-confirmed: `channels` assets are still only `channels.json`, `channels-generation-1.json`, `schema-v2-bootstrap-alpha-0.1.0-alpha.2.json` (final evidence absent, write-once path still open); site run `30445065348` still `completed/failure` with the pinned `head_sha`/`display_title`.
- Consumer sweep: `validate_bootstrap_evidence` is the single validator for this artifact (`release_governance.py:315`, `schema_contracts.py`); no second exact-key list would reject the new `recovery` field on read-back.

### Non-blocking observations
1. `validate_bootstrap_evidence` can only treat `recovery` as *optional* for `preview-index` — published evidence records the same `legacy_index` const in both paths, so a standalone validator cannot tell which path produced it. The required-when-attested direction is enforced only at materialization time (correct, but worth knowing that re-validating a published payload won't re-prove it).
2. `recovery_evidence()` fixture uses `distinct-reviewer`/`none`, while the live recovery will emit `single-maintainer-waiver`. I verified that mode validates by simulating the real jq output; a waiver-mode fixture would cover it in-suite.
3. Recovery still depends on the failed site run's **log** for the input re-grep, and Actions logs have their own retention (~2026-10-26 for a 2026-07-28 run) — later than the binding 2026-08-27 waiver deadline, so not a live risk, but that dependency isn't listed alongside the recorded deadlines.
4. `schema_bootstrap.py` (`_require_exact_bootstrap_membership`) and `schema_bootstrap_selftest.py` contain cosmetic reflows of pre-existing lines unrelated to the remediation; no lint lane in `run_all_tests.sh` requires them.
