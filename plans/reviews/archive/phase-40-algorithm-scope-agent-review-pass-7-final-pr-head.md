## Review — PR #3037, exact head `95d5e2bbb`

**Identity / tree.** `gh pr view 3037` → `headRefOid = 95d5e2bbb0d2c4064df9902f1a075013bdbf4f21`, branch `codex/phase-40-release-algorithm-scope`, base `main`, `MERGEABLE`; local `HEAD` matches. `git merge-base HEAD origin/main` == `c17f3c7d1ea1ed97ca125eb7a43344b30cf9413b`, so the 18-file diff I reviewed is the whole PR. Tracked tree clean; only untracked path is the zero-byte pass-7 placeholder. I wrote to no repository file — all mutation probing ran in `/tmp/p7probe` (a `git archive` copy, now deleted).

**The correction (`47967b84c..95d5e2bbb`) is exactly what pass 6 required, and I re-derived it rather than trusting it.**
- Table cell now reads `| Under-declare PAM nightly to match release | rc=0 (not its input) | **rc=1** nightly omits required suite and redundant release_suite |`. Reproduced: `coverage_matrix.py` → **rc=0** (`coverage matrix ok … strict=yes`, both with and without `SIFR_COVERAGE_MATRIX_STRICT=1`); `profile_assignment_matrix.py` → **rc=1** emitting *both* `nightly omits required suite algorithmic_compatibility:representative-subset` and `release_suite is declared without a profile assignment divergence`. The cell and its error summary are accurate.
- Conclusion now splits ownership correctly: deletion-path closure independent of the assignment matrix, under-declaration owned by `profile_assignment_matrix.py`, both in the same blocking readiness suite. I re-ran the other table rows: drop 3 divergence fields → cm rc=1 *and* pam rc=1; drop fields + delete PAM row → cm rc=1, pam rc=0; expired expiry → cm rc=1; unindexed `ALG-CORPUS` → cm rc=1; repointed index link → cm rc=1 "record target does not exist"; removed `release_divergence_record` → cm rc=1. Every remaining cell holds.
- Ledger entry under `milestone_40_4` is truthful: it names the attribution error as pass 6's finding, does not claim pass 6 approved, and its fail-closed rationale checks out — `coverage_matrix_readiness` and `profile_assignment_matrix` are both cases in the `readiness` suite (`expect_exit_code: 0`), and `coverage_matrix:readiness` is selected by all four profiles including `release`.
- Pass-6 archived report is internally consistent with the fix applied; its `PROFILE_ASSIGNMENT_MATRIX_PATH` unused-constant observation is real but pre-existing (introduced in #2649), not this PR's.

**Constraints reconfirmed at head.** `nightly.json` untouched — `algorithmic_compatibility: ["leetcode-full","taxonomy-smoke"]`; surface row `status: "blocking"`, `release_suite` = representative-subset + taxonomy-smoke, record `ALG-CORPUS` (indexed in `plans/phases/index.md`, target exists), expiry `2026-10-31`. Release still pins the corpus: `validate_profile_manifest` enforces `expected_fixture_count` 411 == actual glob 411, 12 representative rows all `expected_classification: PASS`, 12/12 required categories, and intersection with the 20 preserved slugs is **empty**. The 20 slugs are byte-identical (no slug line in the diff); restoration of `leetcode-full` is closeout scope + acceptance criterion, not a Phase 40 prerequisite. No `crates/**`, `demos/**`, or `stdlib/**` change — no Rust-interop implementation, no demo, and the no-phase-number/name demo rule is preserved. Checks green: `guarantees=13 surfaces=34 temporary_rows=0 strict=yes`, `rows=17`, self-tests `cases=24`, taxonomy ok; largest touched file 732 lines.

No actionable findings.

APPROVED
