> Archived after all four findings were remediated for review pass 2.

## Phase 40 schema-bootstrap post-index recovery — milestone review pass 1

**Reviewed commit:** `eecd5e34c359d05ec478e71e3bc8981ad4c389b2` (local `HEAD` == remote PR #3061 head, identical).
**Base:** `origin/main` = `94a5fec67b7bef51cae0034c84386c57d9ff1785`.
**Mergeability:** clean — `MERGEABLE` / `mergeStateStatus: CLEAN`, 11 files, +1230/−13.

### Independent verification performed (no files modified)

- Live state confirmed: run `30443929353` (`preview-release.yml`, `head_sha 94a5fec6…`, attempt 1, completed/failure, actor `yaseralnajjar`); its step ledger shows `Replace only canonical channels.json` **success**, `Dispatch and await exact site workflow` **failure**, and `Run protected public schema-bootstrap smoke` / `Retain final protected schema-bootstrap evidence` **skipped**. Site run `30445065348` (`release-site.yml`, `head_sha ff472f2a…`, title `Sifr site release 30443929353-1`, failure). `channels` release assets = `channels.json`, `channels-generation-1.json`, `schema-v2-bootstrap-alpha-0.1.0-alpha.2.json` — final evidence genuinely absent.
- **End-to-end reproduction of `prepare_schema_bootstrap_recovery.sh` against live public custody succeeded**: reproduced plan `979d469cb216…`, site facts `f3f03dd9366d…`, index `04edacb8ef64…` (live == generation-1 snapshot), alpha evidence `e6ee4f9ac780…`, all four dispatcher digests **byte-identical to the inputs actually dispatched** to the failed site run, and the beta asset map identical to the original prepare summary (`f45c012c17d2…`).
- Confirmed the failed site run's log really contains the greppable `KEY: value` input echo the revalidation step relies on (all 11 grepped lines present).
- Dry-ran `materialize_schema_bootstrap_evidence.py` with the new attested-legacy path: produces a schema-valid `preview-index` evidence payload; `require_opaque_legacy_identity` pins the attestation to `71b32439…`/105, and `_resolve_legacy_identity` rejects both-forms and non-`preview-index` stages.
- Approval chain: waiver digest `b9630cc0…` matches the pinned constant, `allowed_operations` includes `bootstrap-index`, `require_unexpired=True`, `resolve_approval_decision` fails closed on an empty history; original decision resolves to `single-maintainer-waiver`/`yaseralnajjar`.
- Contract case passes; `schema_bootstrap_selftest` passes; `check_file_size_guardrails.py` PASS (largest new file 502 lines); `bash -n` clean on both new scripts and all 8 embedded workflow scripts (heredoc terminator is column-0 after YAML block-scalar de-indent — no `<<-` bug).
- Boundary review: recovery cannot create/rewrite a release or tag (`gh release create`/`--clobber` absent and contract-forbidden), cannot allocate or write a snapshot, never uploads `channels.json`; write-once upload is guarded by a pre-dispatch "asset already exists" check plus non-clobbering `gh release upload` and post-upload `cmp`; `concurrency: sifr-release-index` matches `release-publication.yml`, so it serializes against index mutation; prepare is credential-free (`contents: read`), `SITE_TOKEN` is scoped to two steps of the approved job; `inputs.*` never reach shell interpolation.

## NOT SATISFIED — 4 findings

### F1 — MEDIUM (provenance/evidence): published final evidence attributes the public smoke and site publication to the failed run and records nothing about the recovery
**Where:** `.github/workflows/schema-bootstrap-recovery.yml:455-489` (`Retain final protected schema-bootstrap evidence`), contract `verification/areas/distribution_release/governance/schema_bootstrap.py:160-177` (`require_exact_keys`).
**Failure mode:** the recovery materializes `schema-v2-bootstrap-generation-1.json` with `run_id/run_attempt/initiator` = the *original* run and `public_smoke` = four `pass` records produced in the *recovery* run. The write-once public epoch record therefore asserts that run `30443929353` attempt 1 completed the public smoke and site publication — that run concluded `failure` and its smoke/evidence steps are `skipped` (verified above). The evidence has no field for the recovery run id/attempt, the recovery's separate `stable-release` approvers/policy, or the recovered site run id; `recovery-approval-decision.json` and `site-run.json` exist only in a 30-day artifact, and the step summary dies with the run logs. An auditor validating the canonical generation-1 evidence sees a self-contradictory chain with no in-band pointer to what actually published it. Because the top-level schema uses `require_exact_keys`, no such field can be added without a schema change.
**Required correction:** extend the bootstrap-evidence schema with a validated `recovery` object (recovery `run_id`, `run_attempt`, `initiator`, `approval_policy`, `approvers`, `failed_site_run_id`, recovered `site_run_id`), require it exactly when the attested-legacy path is used and forbid it otherwise, populate it in the recovery workflow from `recovery-approval-decision.json` + `publication/site-run.json`, and pin it in the workflow contract and selftest.

### F2 — MEDIUM (recovery/operational ledger): the only supported completion path has an unrecorded hard expiry and three unrecoverable dispatch inputs
**Where:** `plans/issues/active/ad-hoc-sifr-site-production-credentials.md`, `plans/issues/active/phase-40-stable-channel-ga-execution.md`; consumed by `.github/workflows/schema-bootstrap-recovery.yml:167-172` and `:311-315` (`download-artifact` of `publication-prepare-<id>-<attempt>`).
**Failure mode:** both jobs hard-require artifact `publication-prepare-30443929353-1`, which expires **2026-08-28T10:46:13Z** (verified via the artifacts API; `retention-days: 30` in `release-publication-prepare.yml:588`), and the pinned waiver expires **2026-08-27T00:00:00Z**. Meanwhile `expected_prepare_summary_sha256` (`f45c012c17d2908bc2ef227f202e1037343c63d1f1881ca7913f22628f62a086`), `expected_plan_sha256` (`979d469cb216…`) and `expected_site_facts_sha256` (`f3f03dd9366d…`) appear **nowhere** in `plans/`, `internal_docs/` or `docs/` (grepped) — the first is obtainable only from that expiring artifact. The path is also blocked on an external human action (Cloudflare secrets), and the ad-hoc issue records no deadline. If the artifact expires, generation-1 final evidence becomes permanently unproducible (write-once) with no documented alternative, leaving the live epoch half-evidenced.
**Required correction:** record all eleven exact dispatch inputs (including the three digests above) and the binding `2026-08-27`/`2026-08-28` deadlines in the ad-hoc credential issue and the Phase 40 ledger; durably archive the original prepare-summary bytes in-repo (or under a non-expiring release asset) so the artifact download is not the sole custody, or document the explicit fallback if the deadline is missed.

### F3 — LOW (test coverage): partial attested-identity arguments are never exercised
**Where:** `verification/areas/distribution_release/governance/schema_bootstrap.py:463-467` vs `…/schema_bootstrap_selftest.py:541-628`.
**Failure mode:** the milestone requires partial identity arguments to fail closed, and `_resolve_legacy_identity` has that branch ("recovery requires its exact attested SHA-256 and byte size") plus the positive-integer guard, but the new selftest helpers only ever set **both** values (or both plus the path). Digest-only, size-only, neither-and-no-path, and non-positive size are all uncovered; a future refactor could default the size (e.g. to `LEGACY_INDEX_SIZE_BYTES`) and silently turn attestation into an unchecked claim with the suite still green.
**Required correction:** add `expect_failure` cases for sha-without-size, size-without-sha, neither (with `legacy_index_path=None`), and `legacy_index_size_bytes <= 0`/bool.

### F4 — LOW (contract): nothing pins the original bootstrap paths to the exact-bytes legacy proof
**Where:** `verification/areas/distribution_release/cases/schema_epoch_bootstrap_workflow_contract.sh:208-224`; original callers `.github/workflows/release-publication.yml:590` and `:799`.
**Failure mode:** the library guard only blocks the attested path for non-`preview-index` stages, so the original `bootstrap-index` step could later be switched from `--legacy-index publication/current-channels.json` to `--legacy-index-sha256/--legacy-index-size-bytes` — losing the requirement that the mutating run actually possessed the opaque v1 bytes — and no assertion would fail (the contract only checks that the digest string and `= "105"` appear somewhere in the file).
**Required correction:** assert in the contract that `release-publication.yml` contains `--legacy-index publication/current-channels.json` for both evidence steps and contains neither `--legacy-index-sha256` nor `--legacy-index-size-bytes`, and that `--legacy-index-sha256` appears only in `schema-bootstrap-recovery.yml`.

### Non-blocking observation
`STABLE_SITE_FACTS_SHA256: none` is the one dispatched input not directly re-grepped from the failed site log (all others are). It is transitively constrained — `dispatcher_default_channel: beta` is inside the verified `publication_facts_sha256` payload, and the site workflow validated the pairing — so this is not a gap in binding; adding the line would make the "all dispatched inputs" claim literal.

Scope respected: no compiler, demos, algorithms, or Rust/Python interop material was reviewed. I did not run the full `create-PR` profile; I relied on your recorded result plus the focused re-runs above (contract case, bootstrap selftest, file-size guardrail, shell/YAML syntax, live reconstruction).
