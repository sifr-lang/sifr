All verification is complete and the repository is untouched (probes ran in `/tmp/m10rev7`; the 21 untracked entries are identical to session start; no `.snap.new` artifacts; `third_party/ruff` left as found). Here is the pass-7 review.

---

# M10 Milestone Review — Pass 7 (branch `codex/m10-milestone-closure` + working tree, PR #2990, agent high, read-only)

**Summary: every blocker from passes 2–6 is verifiably closed, every validation claim independently reproduces, and the pass-6 remediation survives adversarial probing across ~25 fresh shapes — mut-borrow arguments now borrow the original place (`&mut self.stock`, never `&mut <clone>`) in every reachable configuration, and immutably-rooted arguments fail at check with SIFR-OWN-0005. The one silent-loss shape I found (mutating-method *receivers* that are class fields) is a pre-existing defect reproducible on the shipped pre-M10 release, outside this diff's causation. The milestone meets the bar.**

**Methodology.** I read the milestone issue, review passes 2–6, the complete working-tree remediation diff (38 files, ~786 insertions, 4 new source files, 11 new fixtures) and the surrounding implementation (`class_method_receiver_analysis.rs`, `parameter_conventions.rs`, `field_and_stdlib_rewrites.rs`, `recursive_exprs.rs`, `stmt_expr_method_and_question_mark.rs`, `queries_impl.rs`, `mutating_methods.rs`, `class_type_collection.rs`, `class_body_lowering.rs`, signature registries, `error_contracts.rs`, `try_error_carrier.rs`, `class_upcasts.rs`, `source_names.rs`, `union_identity.rs`, scope/binding predicates). The committed `origin/main...HEAD` surface was whole-diff-reviewed in passes 2–6; I re-inspected every area this remediation touches. I rebuilt the debug compiler from the exact working tree, reran every claimed suite and gate myself, and ran ~27 behavioral probes in `/tmp/m10rev7` with the pre-M10 installed `~/.sifr/bin/sifr` as behavioral reference. No repository file was modified; the empty untracked `...pass-7.md` placeholder was not written to.

## Pass-6 blocker verification — closed, adversarially rechecked

The blocker (field-access arguments to class-method `mut` parameters cloned at the call site) is fixed structurally, not just for the reported shapes. The remediation threads the parameter convention end-to-end: class type collection now constructs `FunctionType` with explicit per-parameter conventions (`class_type_collection.rs`), HIR lowering retains the declared `mut` and registers the binding mutability (`class_body_lowering.rs:423-435`), call checking rejects immutably-rooted mut arguments (`mutating_methods.rs::reject_immutable_method_mut_borrow_arguments`), mutation analysis marks the *root* of any mut-borrow argument (`queries_impl.rs::expression_root_name`), and both codegen method-call paths (expression context in `recursive_exprs.rs`, statement context in `stmt_expr_method_and_question_mark.rs`) arm clone suppression per-argument via `method_mut_arg_needs_field_clone_suppression`. I verified `self` is consistently excluded from parameter lists at every layer (collection, HIR, `Class::method` signature registry, the check-side zip, both codegen zips — with a defensive self-strip in the codegen fallback), so no off-by-one exists.

Behavioral confirmation (fresh debug compiler; expected value in parens):

- **Exact pass-6 shapes**: `self.helper.merge(self.stock)` under inferred `&self` → **2** ✓, emitted `fn refill(&mut self) { self.helper.clone().merge(&mut self.stock); }` — the mut argument is the original place; explicit `mut self` → **2** ✓ with `&mut self` honored; forwarding through a free-function `mut` parameter → **2** ✓; a local's field → **2** ✓.
- **Adjacent shapes**: nested self chain `self.inner.stock` → 2 ✓ (`&mut self.inner.stock`, no clone); three-level chain through a `mut` free parameter → 2 ✓; nested field of a `mut` *method* parameter → 2 ✓; `&mut self` inference through delegation (`self.do_refill()`) → 2 ✓; keyword-reordered mut arguments (`merge_n(other=self.stock, n=9)`) → correct ✓; expression-context calls with return values (`return self.helper.merge_count(self.stock)`, and nested in `print(... + 1)`) → mutation lands and value is right ✓; calls inside loops → 3 ✓; two mut arguments in one call → both land ✓; two sequential calls → 3 ✓.
- **Fail-closed/check behavior**: field of a non-`mut` free parameter, field of a non-`mut` method parameter, and a direct immutable-parameter name (pass-6's non-blocking finding 2) are all rejected at **check** with SIFR-OWN-0005 pointing at the argument ✓. Index-rooted arguments are rejected at check (unproven index type mismatch) ✓.
- **Suppression is scoped and non-sticky**: a value read of the same field immediately after a mut-arg call still clones (snapshot mutation does not leak back; original prints 2, snapshot 3) ✓; an argument reading the same root alongside a mut arg fails closed at rustc (E0502), never silently ✓. The suppression counter is incremented per-argument and restored if unconsumed in both emitters.
- **Prescribed regressions all exist**: the pass fixture covers inferred `self`, explicit `mut self`, and `mut`-parameter forwarding with asserted values; the fail fixture is accepted by the e2e fail harness (ran it: ok) and reports SIFR-OWN-0005 at column 20; the four new lowering unit tests and the three codegen clone/suppression unit tests plus two `collect_mutated_vars` tests are present and pass.

## Passes 2–5 blockers — still closed

Same-basename `CsvError | CfgError` union channels dispatch to the correct handler natively in **both** directions (fresh probe); builtin, mixed, and aliased union channels build and run (fixtures pass natively); imported same-basename `Error` is not a catch-all, with `handler_is_catch_all` keyed on `is_builtin_error_base` identity at all consumer sites; the basename tail-match upcast fallback stays deleted with its negative unit test; `_sifr.python.PythonError` remains off the global-identity list (`assert_ne!` guard) and the ignored package-mode identity-collision driver test passes explicitly (17.9s); `PythonError` bridge-field injection is gated on contract shape plus actual interop declarations (both `python_error_*` fixtures pass natively); runtime-python feature detection covers both namespaces with unit coverage; union identity keys are order-insensitive; nested/free `mut`-parameter chain fixtures pass; borrowed field reads still clone (value semantics intact).

## Validation claims — all independently confirmed

`sifr_codegen` 866, `sifr_lowering` 783 (+1 ignored), `sifr_type_system` 114, `sifr_driver` 358 (+33 ignored) — exact match; the ignored `PythonError` identity package test passes explicitly; all 11 new fixtures build and run natively with exit 0; the e2e fail harness accepts the new fail fixture; `cargo fmt --check`, `cargo clippy --workspace -- -D warnings`, HIR maintainability guardrails, and the file-size guardrail (PASS, 2720 files, cap 900) all pass.

## Non-blocking findings (fix-forward)

1. **MEDIUM (pre-existing, silent — file an urgent follow-up issue)** — a self-mutating user method called on a **class-typed field receiver** clones the receiver, discarding the receiver-side mutation: `self.helper.bump()` where `bump` appends to `self.items` prints 0 instead of 1 on **both** this tree and the pre-M10 installed release, so it predates M10 and is independently user-reachable today. With M10's mut-arg feature the same clone co-occurs in newly compilable programs: `self.helper.absorb(self.stock)` where `absorb` mutates both `self` and the mut arg emits `self.helper.clone().absorb(&mut self.stock)` — the argument mutation lands correctly, the receiver's own mutation is lost (pre-M10 this program failed closed only because the feature didn't exist). The diff does not touch the receiver-lowering path (verified by identical p20 behavior on both compilers), and the fix is clean: without the clone, `self.helper.absorb(&mut self.stock)` is legal Rust (disjoint field borrows). Same classification precedent as pass-5's finding 2; it is still a core-guarantee violation and should be fixed with the existing root-name suppression machinery immediately after closure.
2. **LOW — two new fail-closed check/build parity gaps in the same mutability-analysis follow-up bucket** (consistent with pass-6 findings 2–3): (a) a mut argument alongside another argument reading the same root (`merge_n(self.stock, len(self.stock.items))`) passes check and fails at rustc E0502; (b) a **proven-index** mut argument (`self.helper.merge(self.crates[0])` under `if len(self.crates) > 0`) passes check and fails at rustc E0282 (the unproven form is correctly rejected at check). Both fail closed, never silently.
3. **LOW** — `crates/sifr_runtime/src/python.rs` remains at exactly the 900-line cap (carried since pass 3; still unaddressed).
4. **LOW** — carried cosmetic items: same-basename diagnostic qualification is partial on type-checker-formatted paths; `is_builtin_error_base` is a shape match rather than an identity tag.

## Verdict

The full M10 remit — nominal identity and same-basename types, union Result carriers/aliases/ordering, exact catch-all identity, `PythonError` source/runtime collision behavior, feature gating, check/build parity in the error/identity domain, ownership safety of the newly activated method-mutation surface, generated Rust quality, tests, diagnostics, and module decomposition — is closed and holds up under adversarial probing. Every prior blocker has fixtures, unit tests, and reproducible probes behind it; every validation claim reproduces exactly; the remaining findings are either pre-existing defects reproducible on the shipped release or fail-closed parity gaps, all catalogued for follow-up. Before merge, run the authoritative gate (`scripts/run_all_tests.sh`) as the standard merge-gate step, and file the finding-1 follow-up issue.

VERDICT: SATISFIED
