# Terminal review — PR #3094 @ `335814db4` (base `9c99ef43b`)

Read-only: I made **no** repository modifications, spawned no subagents/reviewers, and ran no Cargo/Sifr builds, tests, corpus sweeps, validation, or performance probes. All conclusions come from `git`, `gh`, the committed tree, the supplied create-pr log, and the JSON artifacts. (`…pass-7.md` exists as a 0-byte untracked placeholder; I did not write to it.)

## Findings

**None.** No blocking findings and no non-blocking findings.

## Prior findings — all closed

- **Pass 1** (no accept-side coverage for the widened non-name-rooted base) — closed at `85be31b17`: `index_and_slice_arguments_accept_disjoint_unresolved_bases` plus `crates/sifr/tests/e2e/pass/class_field_dynamic_index_base_disjoint.sifr` (51 lines) and its manifest entry.
- **Pass 2** (nested-subscript inner operand untested) — closed at `0b6bebdfc` for the `Index` arm (`nested_index_argument_collects_inner_index_footprint`, `nested_index_and_slice_arguments_accept_disjoint_inner_indexes`).
- **Pass 3 finding 1** (inert slice half: `Receiver.replace` had body `pass`, so no overlap check ran) — closed at `a813b9971`: `Receiver` now carries `values: list[int]` and `replace` does `self.values = values` (`method_receiver_footprint_tests.rs:180,183-184`), making the receiver `MutableBorrow` and the slice statement at `:199` a live check.
- **Pass 3 finding 2** (no nested reject under the `Slice` arm, leaving `footprint.rs:121` unpinned against the hoist-behind-early-return refactor) — closed at `a813b9971`: `nested_slice_argument_collects_inner_index_footprint` (`:148-172`) pins `other[self.receiver.index][:1]` to `OWN_DOUBLE_MUTABLE_BORROW` at the full argument range; the outer object's root resolves to `other` (`Dynamic(other)`, non-matching), so the `self`-rooted inner index is reachable **only** through `:121`.
- **Pass 4** — SATISFIED at `a813b9971`, zero findings; head code/tests are byte-identical to it (`git diff a813b9971 335814db4` = review records only).
- **Pass 5 finding 1** (stale PR Validation/Review section) — closed: body now records the green create-pr retry at `01bf3aff1…` (exit 0, all blocking steps/budgets green, E2E 140/140, signature `ac6d879686517f2c`), the transient `lsp-protocol-smoke` exit-timeout (`returncode -9`), and its 6/6 focused retry.
- **Pass 5 finding 2** (`953` in the pass-4 record) — closed at `1f06b0e53`: `…pass-4.md:34` now reads `954 passed/1 ignored`, matching log line 1529 (`test result: ok. 954 passed; 0 failed; 1 ignored`).
- **Pass 6 finding 1** (pass-5 guardrail line counts `279`/`254`) — closed **in the same commit that records it**, `335814db4`: the sole line-edit outside the new pass-6 file is `…pass-5.md:28` → *"`footprint.rs` 278, `method_receiver_places.rs` 684, `mod.rs` 252, new test module 205, fixtures 51/18"*. Verified against the tree at head: `footprint.rs` **278**, `mod.rs` **252**, `method_receiver_places.rs` 684, test module 205, fixtures 51/18 — all match, all under the 900-line cap.

## Evidence verified at this exact head

- **Identity.** `headRefOid = 335814db428b60998dc0e7ef9b3f446a84bdc370` = local HEAD; `state: OPEN`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. `git diff 01bf3aff1 335814db4` = 3 files, all `plans/reviews/active/…` (pass-4 one-line count fix, new pass-5, new pass-6 + the pass-5 count fix) — **record-only**, so the code-identical create-pr evidence produced at `01bf3aff1` remains authoritative. Working tree has no tracked modifications.
- **Fix re-derived, not inherited.** `extract_place` (`method_receiver_places.rs:398-429`) has no `Index`/`Slice` arm, so both subscript forms always reach `footprint.rs:104`/`:111`; `root_binding_id` (`:614-622`) walks only `Name`/`FieldAccess`/`Index`/`Slice` and returns `None` through a `MethodCall`, which is exactly the hole — pre-fix the base contributed nothing. The two added `collect_footprint(object, …)` calls (`footprint.rs:108`, `:121`) close it. `Footprint::Dynamic(root)` is still pushed ahead of each recursion and stays load-bearing (`:20` matches any same-root place, so recursion can never widen a name-rooted base; for a resolvable `FieldAccess` base it yields the strictly narrower place). Recursion is on strict sub-expressions (terminates, cost linear in nesting depth), duplicates are inert under the existence test at `:18-21`, and neither arm has a `_` fallback.
- **Soundness direction.** The change only *adds* footprints, so it cannot cause a missed rejection. The new rejections it does add (`self.owner.update(self.pick().values[0])`, `…(other[self.owner.index][0])`) are the same policy already applied to non-subscript arguments and match what rustc itself would reject for a whole-`self` borrow overlapping a `&mut self.owner` field borrow — closing a path that could otherwise leak a rustc borrow error.
- **Coverage matrix, both arms, both directions.** Unresolved base: reject index (unit test 1 + `mutable_receiver_dynamic_index_base_overlap.sifr`, `col=28` independently recomputed — `index($0,"self.pick") == 28`), reject slice (test 2), accept (test 3 + the pass fixture asserting `apply() == 7`, `owner.value == 7`, `owner.values == [7]`). Nested subscript: reject index (test 4), reject slice (test 5), accept with both halves live (test 6). All six green in the authoritative run: log lines 1135, 1137, 1138, 1143, 1144, 1146.
- **Gate claims accurate.** `create-pr.latest.json`: 24/24 `lane_steps` `status=pass`; 161 `case_timings`, zero non-pass; `hardening_summary {blocking_failures: 0, failures: 0, non_blocking_failures: 0, variants: 6}`; `time.real_seconds = 1309.02`; sole advisory `warm wall-time budget exceeded` against a non-enforcing 5-min warm target. Log: `report_signature=ac6d879686517f2c`, `140 pass tests completed (140 passed, 0 failed)`, `file-size guardrails: PASS (3077 files, limit 900 lines)`, `lowering maintainability guardrails: PASS`, `test result: ok. 954 passed; 0 failed; 1 ignored`. Artifact/log pairing confirmed: `sha256` of the first 302530 bytes of the supplied log == `sha256` of the canonical `create-pr.latest.log` (`74187db2…`), so the supplied log is a byte-exact superset of the run that produced the JSON.
- **Manifest and corpus.** `create_pr_e2e_manifest.json` holds 140 `fixture_names`, all unique, including `class_field_dynamic_index_base_disjoint`. `crates/sifr/tests/e2e/fail` is exactly 566 `.sifr` files, matching the reported 566/566.
- **PR body claims.** Every figure in the Validation section is corroborated (6 focused tests, 954/1, 566/566, 140/140 + signature, transient LSP failure and 6/6 retry). Review section's remaining statement — *"A terminal zero-finding review round is pending before readiness"* — is accurate as of this pass.

## Considered and dismissed (disclosed, not findings)

- **PR body's Review section does not enumerate round 6.** Every claim present is accurate, round 6's sole finding is closed at head, and the body explicitly flags a terminal zero-finding round as pending — a merger's readiness picture is complete and correct. This is an omission, not a stale or false claim (same standard pass 6 applied to the omitted wall time).
- **Pass-6's finding text quotes the pass-5 record as `279`/`254`, which the tree no longer says.** The record is explicitly scoped `@ 1f06b0e53` and documents a defect closed in the very commit that lands it — normal review-record history, exactly the reasoning pass 6 used for pass-5's quotation of `953`.
- **Passes 5 and 6 characterize all 24 lane steps as `budget_status=pass`, while `documentation_checks` and `sysroot_release_certification` have `budget_status: null`.** Neither appears in `lane_step_budgets`, i.e. no per-step budget is defined rather than one failing; the underlying assertion (every step passed, zero budget violations) is correct, and neither step touches this diff.
- **The new fail fixture is not exercised by the create-pr lane** (`test_e2e_fail` lives in the `full` profile); the identical source is pinned by unit test 1, which does run in-lane, and the fixture is covered at the merge gate. Profile design, not a diff defect.
- **No lane runs `cargo clippy`** (`grep -rn "cargo clippy" scripts/` is empty; it is an AGENTS.md developer/CI-doc command). The PR body's clippy claim rests on the author's focused run — pre-existing tooling design, and the diff is two lines plus a `#[cfg(test)]` module.
- Root-only coarseness of `Footprint::Dynamic` and `Place{self}` over-approximation from a `self`-rooted `MethodCall` base (pre-existing policy, now applied uniformly to subscripts); duplicate `Dynamic` pushes on nested subscripts; `range_for` duplication (idiom in 13 other test modules); non-alphabetical manifest placement (`BTreeSet`-loaded); `values` field-name reuse across fixture classes; missing `__init__`/never-instantiated classes in unit sources and the fail fixture; `warm wall-time budget exceeded` (advisory only); absence of a `plans/roadmap.md`/issue edit (matches precedent `ac31b0908`); untracked `.claude.log` companions for passes 1–4 (none are tracked, so no record-convention gap).

**SATISFIED**
