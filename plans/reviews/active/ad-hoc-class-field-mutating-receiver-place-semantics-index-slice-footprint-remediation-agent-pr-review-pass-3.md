## Review: class-field receiver-place index/slice footprint remediation — pass 3 @ `0b6bebdfc` (base `9c99ef43b`)

I made **no modifications** to any repository file, and ran **no** Cargo build/test/clippy, Sifr compilation, corpus sweep, performance probe, broad validation, or subagent/reviewer. (`plans/reviews/active/…-pass-3.md` exists as a 0-byte untracked placeholder; I did not write to it.) All conclusions come from reading the diff at this exact head and the surrounding code.

### Diff surface at this head
- `crates/sifr_lowering/src/lower/method_receiver_places/footprint.rs:108`, `:121` — the two `collect_footprint(object, …)` additions (byte-identical to passes 1–2).
- `crates/sifr_lowering/src/lower/method_receiver_footprint_tests.rs` (new, 173 lines; +62 vs. pass 2) + `mod.rs:91-92`.
- `crates/sifr/tests/e2e/fail/mutable_receiver_dynamic_index_base_overlap.sifr` (18 lines).
- `crates/sifr/tests/e2e/pass/class_field_dynamic_index_base_disjoint.sifr` (51 lines) + one manifest entry.
- Two `plans/reviews/active/…pass-1/2.md` records.

### Pass-2 finding — verified closed for the Index arm, partially closed for the Slice arm

**1. `nested_index_argument_collects_inner_index_footprint` is a live, mutation-sensitive reject.** (`method_receiver_footprint_tests.rs:113-143`)
I traced `other[self.receiver.index][0]` against the traversal. `extract_place` (`method_receiver_places.rs:398-428`) has no `Index` arm, so both subscript levels fall into `footprint.rs:104`. Outer level: `root_binding_id` (`method_receiver_places.rs:614-622`) walks `Index → Name(other)` → `Dynamic(other)`, which does not match the `self`-rooted receiver (`footprint.rs:20`); its own `index` is `IntLiteral(0)` → nothing. **The only path to the inner index is the new `:108` recursion**, which re-enters the `Index` arm on `other[self.receiver.index]` and there collects `Place{self, [Coordinator.receiver, Receiver.index]}` via the `extract_place` fast path at `:99`. That prefix-overlaps the receiver place `Place{self, [Coordinator.receiver]}` under `places_overlap` (`:24-28`). Delete `:108` and the footprint is exactly `[Dynamic(other)]` → accepted. So the reported mutation result (removing only the `Index`-arm line makes this test fail, restoring it makes all five pass) is exactly what the code predicts. Receiver proving holds: `Receiver.update` mutates `self.value` → `MutableBorrow` (`method_receiver_analysis.rs:60-75` + `body_contains_receiver_mutation`), which propagates to `Coordinator.conflict_index` through the dependency fixpoint (`:46-58`), so `prove_mutable_place(self.receiver)` succeeds. The reported range is `arg_ranges[0]` (`report_overlap` → `same_call_place_conflict` → `SIFR-OWN-0002`, `registry.rs:112`), i.e. the **full** argument range, matching the `range_for(source, "other[self.receiver.index][0]")` assertion; the needle occurs exactly once.

**2. Test sources are type-correct enough that overlap, not an unrelated diagnostic, drives each result — and the two nested tests mutually corroborate this.** Test 5 (`:145-173`) asserts *zero* errors via `.expect(…)` on source containing the same dynamic tuple subscript `other[self.source.index]` in both index and slice form. Since that compiles clean, the identical construct in test 4 cannot be contributing a spurious type diagnostic; combined with test 4's `primary_range` pin, the `SIFR-OWN-0002` there is genuinely the overlap. (The `.any()` assertion style matches the sibling convention at `method_receiver_analysis_tests.rs:203-234`, and the `"{errors:?}"` message is a genuine improvement over tests 1–2.)

**3. Original fix, negatives, fixtures, conventions, adjacent paths — all still correct.** `Dynamic(root)` is still pushed ahead of each recursion and remains load-bearing (it subsumes every same-root `Place`, so the recursion can never *widen* a name-rooted base; for a resolvable `FieldAccess` base it yields the strictly narrower place). Coverage of `Index`/`Slice` sub-expressions is now total — those variants carry only `object` + `index`/`start`/`stop`/`step` (`sifr_ir/src/hir_nodes.rs:711-745`); I re-walked every remaining `collect_footprint` arm against the full `HirExpr` definition (`:606-818`) and no variant drops a sub-expression (`Call`/`ConstructorCall`/`SuperCall` callees are `String`s, `EnumVariant` has no payload, and the `match` has no `_` arm). Recursion is on strict sub-expressions; duplicate footprints are inert under the existence test at `:18-21`. Fail fixture `col=28` is correct (8 indent + `return ` = 15 + `self.update(` = 12 → argument at 28) and the harness matches code + column only, no line (`fixture_compilation.rs:7-16`, `harness_model.rs:474-505`). `tests/e2e/fail/` is 566 files, matching the reported 566/566. Manifest: `class_field_dynamic_index_base_disjoint` appears exactly once, `fixture_names` is 140 unique entries with no `count` field and no hardcoded count anywhere in `scripts/`/`verification/`, so the +1 needs no companion edit. `expression_overlaps`/`places_overlap` have callers in only two files repo-wide, both in this module, so no adjacent traversal needs the same patch. File sizes: `footprint.rs` 278, `method_receiver_places.rs` 684, `mod.rs` 252, test module 173, fixtures 51/18 — all under 900; the new module (rather than appending to the 846-line `method_receiver_analysis_tests.rs`) is the right split, and the local `range_for` duplicate matches the idiom in 13 other test modules.

---

### Findings

**1. Non-blocking — the slice half of `nested_index_and_slice_arguments_accept_disjoint_inner_indexes` is inert: no overlap check ever runs on the slice argument, so the test's name and its `.expect` message claim coverage it does not provide.**
`crates/sifr_lowering/src/lower/method_receiver_footprint_tests.rs:156-157` (the `replace` body), asserted at `:167` and `:171-172`

`Receiver.replace` is declared with body `pass`. `body_contains_receiver_mutation` (`classes/class_semantics.rs:10-20`) therefore reports no mutation, `replace` has no callees to inherit mutability from via the fixpoint at `method_receiver_analysis.rs:46-58`, and `"replace"` is not a fixed trait name (`classes/parameter_conventions.rs:27-35`) — so its receiver convention stays `SharedBorrow` and `receiver_target` is `None` (`method_receiver_places.rs:113`). Independently, the argument is `own values: list[int]`, and `own` is `Own`/`Immutable` → `is_mut_borrow()` is `false` (`sifr_type_system/src/types/definitions.rs:428-430`, `:467-470`), so `prove_mutable_arguments` returns `None` for it (`method_receiver_places.rs:170-176`) and `mutable_arg_places[0]` is `None`.

Consequently, for `self.receiver.replace(other[self.source.index][:1])` at `:167`, **none** of the four overlap loops in `validate_call_overlaps` fires: the `MutableReceiverTarget::Place` loop (`:216-223`) and the `SpecializedIndexedStorage` loop (`:224-234`) need a receiver target; the shared-borrow-receiver loop (`:236-247`) and the pairwise mutable-argument loop (`:250-276`) both need a `MutableArgumentTarget::Place`. `expression_overlaps` is never called on that argument, so the statement lowers cleanly no matter what the `Slice` arm at `footprint.rs:121` does. The test's whole slice contribution is dead weight; only the `update` line at `:168` genuinely guards the nested accept path, and it exercises the `Index` arm only.

This is a regression against the pattern the same file already uses correctly: in `index_and_slice_arguments_accept_disjoint_unresolved_bases`, `Owner.replace` does `self.values = values` (`:97-98`), which makes it `MutableBorrow` and makes its slice statement at `:105` a live check.

Suggested closure: mirror `:97-98` — add a `values: list[int]` field to `Receiver` and change `replace`'s body to `self.values = values`. The test still passes: the receiver place becomes `Place{self, [Coordinator.receiver]}` and the slice argument's footprint is `Dynamic(other)`, `Place{other}`, `Place{self, [Coordinator.source, Source.index]}` — disjoint under `places_overlap`.

**2. Non-blocking — the nested-subscript shape has no reject-side coverage under the `Slice` arm, leaving `footprint.rs:121` unpinned against exactly the refactor pass 2 hypothesized.**
`crates/sifr_lowering/src/lower/method_receiver_places/footprint.rs:121`

Line `:121` itself is pinned by `slice_argument_traverses_unresolved_base_footprint` (`method_receiver_footprint_tests.rs:47-75`), but only through a base whose `root_binding_id` is `None` (`self.pick().values`). The specific regression pass 2 warned about — hoisting the recursion behind an early return when the root *does* resolve, e.g. `if let Some(root) = root_binding_id(object) { push(Dynamic(root)) } else { collect_footprint(object, …) }` — would keep that test green (root `None` → recursion still taken) while silently dropping every nested case where the outer object is itself a subscript with a resolvable root. Test 4 pins that scenario for the `Index` arm; nothing pins it for the `Slice` arm, and fixing finding 1 does not help, because the accept-side test passes under either behavior. Note this is not merely symmetric decoration: `Dynamic(other)` is pushed and does not match, so the `self`-rooted inner index is reachable only through `:121`.

Suggested closure: one additional reject test in the new module — `self.receiver.replace(other[self.receiver.index][:1])` with a self-mutating `replace` (per finding 1) — asserting `OWN_DOUBLE_MUTABLE_BORROW` at `range_for(source, "other[self.receiver.index][:1]")`. No fixture or manifest change needed.

---

Non-findings I considered and dismissed: the coarse `Footprint::Dynamic` root-only match causing `self.b.update(self.a.items[0])` to be rejected (pre-existing at `footprint.rs:20`, unchanged and unreachable-by this diff — every new test deliberately roots dynamic bases at a parameter, not `self`); `Call { func: String }` not contributing the callable binding to the footprint (pre-existing, unrelated to index/slice, and any such binding is a local rather than a `self` projection); the unused `own values` parameter in test 5's `pass`-bodied `replace` (accepted by the compiler — it is finding 1's *cause*, not an independent issue); duplicate `Dynamic(root)` pushes on nested subscripts (inert under the existence semantics at `:18-21`); `range_for` returning only the first needle occurrence (every needle used is unique in its source); the manifest entry's non-alphabetical placement (names are `BTreeSet`-loaded, `fixture_compilation.rs:100-107`, so order is not semantic); the `values` field-name reuse across `Inner`/`Owner` in the pass fixture (overlap turns on `Coordinator.owner` vs `Coordinator.source`); the missing `__init__` in the fail fixture (never instantiated, and the unit test pins the same source's range precisely); and the `SpecializedIndexedStorage` receiver-index path (pre-existing, out of scope).

On the stated evidence: the exact-head unit/fail-corpus/fixture/lint results are consistent with everything I traced statically. The create-PR profile remains unclaimed, so a final evidence review is still required before merge, independent of the two findings above.

**NOT SATISFIED** — two non-blocking actionable findings. The pass-2 finding is fully closed on the `Index` arm in both directions (reject and accept), but the slice statement added alongside it is inert, so the `Slice` arm's nested shape is guarded in neither direction.
