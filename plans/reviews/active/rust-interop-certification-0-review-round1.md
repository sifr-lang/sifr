## Review: `certification_0` working tree

Read-only. I ran only checks/self-tests and read-only cargo commands (`cargo fetch --locked --offline`, `cargo clippy -p sifr_rust_interop_catalog`, guardrail scripts); no files were edited, staged, or committed.

**What verifies clean.** Both new rows are honest: `future-owned-by-separate-phase`, `future_owner` set, both directions `planned`, tier/execution pairs legal (2/`runtime-observed`, 4/`runtime-observed`), fixtures at schema v2 with `future-owned` / `future-owned-diagnostic` results matching the existing `ecosystem_backend_certification` convention. The five contract-only claims are preserved and narrowed in prose only (`rust_interop_compatibility_matrix.json:326,338,363,375,387` — notes now say "contract-only … does not certify crate-backed runtime"), with unchanged `category`/`execution_kind`. All recorded counts reproduce exactly: 36/36/36 rows-manifests, 47 passing / 25 planned (both matrices), 17/5/1/13 categories, 13/4/10/9 execution kinds, 44 crate aliases, 23 claims, 5 suites / 10 cases. Profile selection is byte-identical across create-pr/merge/nightly/release, and `required_rust_interop_suites()` (`profiles.py:191`) derives from the manifest, so the all-profiles contract is mechanically enforced (runner self-test: 8/8 pass). Area run: 10 variants, 0 failures. `cargo fmt --check`, `clippy` on the new crate, file-size and HIR guardrails, `git diff --check` all pass.

### Findings

**1. HIGH — the new blocking offline-cache assertion depends on a step nothing in the repo executes.**
`_crate_catalog.py:39-51` shells out to `cargo fetch --locked --offline` with `CARGO_NET_OFFLINE=true` inside the `matrix` suite, which is blocking in all four profiles. The remedy the README names does not run: `setup_command: "cargo fetch --locked"` appears only in the four profile JSONs and `verification/schemas/profile.schema.json:94,103` — `grep -rn setup_command` finds no runner or script that reads it, and `profile_runner.py` never invokes it. All 44 catalog deps are `optional` with no feature enabling them, so `cargo build` / `cargo test` / `cargo clippy --workspace` never download the 367 packages this change added to `Cargo.lock` (462 → 829). Failure scenario: a developer (or the CI job at `.github/workflows/local-first-validation.yml:52`, a fresh runner with no cache action) whose registry cache was populated by builds runs `scripts/run_all_tests.sh --profile create-pr`; the matrix suite fails with `Rust interop crate catalog is not cacheable offline`, and because the lane forces `CARGO_NET_OFFLINE=true` (`profile_runner.py:252`) it cannot self-heal. Reproduced with a temp `CARGO_HOME`. `README.md:58-59` states the opposite ("makes the ordinary profile setup `cargo fetch --locked` populate it"), so the failure has no documented fix.

**2. MEDIUM — the budget-headroom claim measures a different quantity than the budget enforces.**
`README.md:140-145` records "3,500 ms for all ten cases" against the 5,000 ms blocking budget at `create-pr.json:15`. That figure is the sum of per-case `elapsed_ms` (I measure 3,524 ms); the budget is enforced on `rust_interop_checks` *step wall time*, which for the same run was 3.80 s — ~1.2 s headroom, not 1.5 s. The previous text it replaced quoted step-measured numbers (3,244 / 3,479 ms), so this is a metric regression, and it is unverified: the checklist item that runs the profiles is still unchecked (`certification.md:183-184`). The `cargo fetch` subprocess (0.44 s warm here) makes step time vary with cache/index state.

**3. MEDIUM — the issue now contradicts the Phase 40 doc it edited on who registers the suite.**
The change moves registration ownership to `certification_0` (`plans/phases/40_...md:56-62`, `:399-402`, `:417-423`), but `certification.md:221-224` ("Phase 40 **must register** the eventual `stable-candidate` suite…") and `:228-232` ("**Confirm Phase 40** `milestone_40_0` registers…") were left as-is. Whoever executes `milestone_40_0` gets conflicting instructions from the two governing documents.

**4. MEDIUM — README suite count is wrong after the change.** `README.md:126-128`: "select all **four** currently registered suites" — five are registered (`manifest.json`) and five are selected in each profile. The same edit added "currently registered" while leaving "four".

**5. MEDIUM — stable-claim doc gating covers one table, not "public stable docs".**
`check_stable_support_claims.py:23-50,105-121` parses only the marker block in `docs/rust-interop.mdx`. Nothing scans the rest of that file (e.g. the `## Zero-Copy And Views` examples at `:186-225`, which show Arrow/DLPack round-trips backed only by contract-only rows) or the other surfaces the issue's Stable Release Constraint names at `:404-408` (installer metadata, release checklist, package authoring guide). A future-owned row advertised in prose passes every gate. Separately, the dedicated overclaim rule at `:123-133` is unreachable independently of the exact-match rule at `:120-121`: any doc/data disagreement already fails equality, so the mutation case at `:248-253` proves table equality, not the overclaim rule the checklist credits.

**6. MEDIUM-LOW — self-test does not cover the "public-doc omissions" it is credited with.**
`certification.md:176-178` claims the self-tests reject public-doc omissions. The six cases (`check_stable_support_claims.py:202-254`) cover missing row, future-owned, execution-scope drift, extra doc row, and overclaim — none covers a claim present in `stable_support_claims.json` but absent from the doc table, nor `capability` drift, duplicate ids, or `schema_version` / source-path drift. Omission is caught only by the generic equality failure, untested.

**7. MEDIUM-LOW — feature-policy freezing is partial, and the crate's stated purpose is unenforced.**
`_crate_catalog.py:115-124` checks `default-features` only when the policy dict carries a `default_features` key, plus a hardcoded `if crate == "candle"` at `:116-117`. So `rusqlite {"features":["bundled"]}` and `tracing-subscriber {"features":["env-filter"]}` (`check_fixture_matrix.py:142-160`) leave `default-features` unpinned — flipping it in `Cargo.toml` passes. Nothing rejects a `[features]` table on the catalog crate (e.g. `default = ["arrow"]`), which would make ordinary workspace lanes compile the deferred graph — the exact outcome the crate exists to prevent (`Cargo.toml:8-10`).

**8. LOW — the non-Cargo policy assertions are self-referential.** `_validate_non_cargo_policies` (`_crate_catalog.py:132-149`) verifies only that `[package.metadata.sifr-rust-interop]` literally contains `candle-backend = "cpu-only"` and `prost-build-generated-output = "deterministic"`. No behavior is checked, and the values are not derived from `EXPECTED_FEATURE_POLICIES` (`candle {"backend":"cpu-only"}`, `prost-build {"generated_output":"deterministic"}`), so the two declarations can drift apart with all gates green.

**9. LOW — offline proof is at the workspace layer only.** Direct-crate generated builds in `Project`/`SingleFile` mode replace crates.io with the sysroot vendor tree (`cargo_manifest.rs:57-70`, `entrypoint.rs:308-315`); 23 of the 44 catalog packages are absent from the tracked `vendor/` (`arrow`, `polars`, `datafusion`, `candle-core`, `ndarray`, `memmap2`, `bytemuck`, `reqwest`, `rusqlite`, …), while every crate the currently-passing direct-crate rows use is present. Package-mode probes use `PackageOwned` (no replacement) so this is not a defect today, but `README.md:62-65` ("the single cache authority") and the entry criterion "cacheable by the repository's locked/offline setup" (`certification.md:69-70`) are broader than what was proven — and the catalog's `=` pins are not propagated into generated temp packages, so exact-version offline resolution is not established for downstream rows.

**10. LOW — duplicated README paragraph.** `README.md:56-59` and `:62-65` state the same fact twice with different wording ("pins all 44 matrix crate aliases…" / "pins every required matrix crate…"), sandwiching `:60-61`.

**11. LOW — "generated from" is inaccurate.** `docs/rust-interop.mdx:77` says the table "is generated from" the JSON; `_public_table` (`check_stable_support_claims.py:137-148`) is only used by the self-test — there is no `--write`/generate mode. The table is hand-maintained and validated.

**12. LOW — empty review artifact.** `plans/reviews/active/rust-interop-certification-0-review-round1.md` is 0 bytes, while the item's canonical evidence rules (`certification.md:87`) require a review artifact.

**13. LOW — public docs never name the two new rows.** `certification.md:206-208` asks for them in "architecture/public docs"; `internal_docs/rust_interop_architecture.md:751-756,778-782` names both, but `docs/rust-interop.mdx:80-83` only alludes to "their separate crate-backed runtime rows".

**14. LOW — headroom on a near-cap file.** `check_fixture_matrix.py` is 880/900 lines after +19; each of `certification_1`–`13` must extend `REQUIRED_FIXTURES`/`REQUIRED_CRATES` there, so the 900-line cap is ~1 row-PR away (AGENTS.md: refactor by responsibility rather than growing an oversized module). Nit in the same area: `_crate_catalog.py:88-89` has a single blank line between module-level functions.

Not defects: the shared-`failures` early return before `cargo fetch` (`_crate_catalog.py:37-38`) is intentional fail-fast; reuse of `SIFR-RUST-ZC-0001` for `advanced_data_runtime_matrix` matches `arrow_record_batch`/`advanced_data_matrix`; the hardcoded `/tmp/sifr-rust-interop.bin` in `examples/memmap2.sifr:12` is inert while the row is planned, though it will need the issue's temp-dir hermeticity rules when `certification_7` executes it.

Actionable findings: 14.

NOT SATISFIED
