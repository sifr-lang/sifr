## Audit result — certification_5 (driver / fixture / runtime)

**1. Two distinct mandatory tests genuinely build and execute.** Both `test_build_opaque_resource_lifecycle_runtime` and `test_build_opaque_resource_alias_rejection_runtime` (`crates/sifr_driver/src/tests/package_rust_interop_build_tests.rs:522`, `:556`) `include_str!` the actual fixture evidence (`:69`, `:72`), install it as the package entry point (`:153`), derive the entry point from `cargo metadata --locked --offline` (`:120`), build via `build_cached_package_project`, and execute the produced binary (`:178`). No prose, no handwritten surrogate. Provenance forbids a shortcut here: runtime-observed evidence must carry `sifr-evidence: executes-runtime-observed` (`_provenance_checks.py:400`), and the new outcome rule requires the observed state to come from `built_package_output`/`observed_*state(&output)` rather than a string literal (`_rust_test_outcomes.py:83-95`, self-tests at `:343-369`). Negative direction is declared `runtime-error-state`/`runtime-observed`, not a diagnostic, and the metadata matches (`fixture.json:6-7`) — consistent with the `close_after_use` precedent.

**2. Real operations, bounded, local-only.** `open()` binds three ephemeral `127.0.0.1:0` listeners *before* spawning any task and owns the handles first (`resources.rs:209-226`); reqwest with `.no_proxy()` (`:362`), bundled rusqlite on a unique temp path (`:368-376`, `:654`), redis RESP `PING` and tokio-postgres `simple_query("SELECT 1")` against in-process protocol servers (`:469-484`, `protocols.rs:35`, `:129`). Every connect, operation, socket read/write, and join is timeout-bounded (`OPERATION_TIMEOUT` `:17`, `IO_TIMEOUT` `protocols.rs:8`, `timed()` `:245`). No external host is contacted.

**3. Lifecycle assertions are real.** Owned `close=async_close` receiver enforced in the driver (`opaque_validation.rs:55-56`) and covered by `package_rust_interop_opaque_async_close_policy_requires_owned_receiver` (`rust_interop_opaque_contract_tests.rs:50`). Runtime asserts: stable double close via `closed`/`already-closed` (`resources.rs:519-526`, `:537`, `:560`), temp-DB removal with post-condition re-check (`:106-126`) plus RAII fallback (`:129`), exact poison redaction `== "Rust bridge panicked"` (`:509`), and zero tracked tasks after cleanup (`:578`). The Sifr side asserts the full summary string (`positive/resource_close_aclose_matrix.sifr:48`) and the driver test asserts stdout exactly and stderr empty (`package_rust_interop_build_tests.rs:541-552`). Alias rejection is genuinely a post-close operation attempt: `Handle::clone` copies the slot but shares the `Rc<RefCell<ResourceState>>` (`interop.rs:387-398`, `resources.rs:52`), so the alias reaches `ensure_open` and fails (`:418`, `:642`) — and both docs and README state exactly that ("the certified alias rejection is the shared resource's closed-state check", `docs/rust-interop.mdx`; `README.md:17`). No Sifr-level clone policy is declared.

**4. Negative protocol branches are meaningful and cleaned up.** Malformed RESP frame and PG post-startup shutdown (`protocols.rs:57-65`, `:170-174`) are probed and required to be rejected (`resources.rs:287-291`); both harness tasks are joined before the result is consumed (`:282-286`), and the PG probe joins its own connection task (`:339-350`). A server-side stall surfaces as a bounded error, not a hang. Scope limits are stated honestly in three places (README `:21`, compatibility notes, `internal_docs/rust_interop_architecture.md`): only exercised frames, `CLIENT SETINFO` disabled, client-library-internal tasks explicitly outside the counter, no general server compliance.

**5. Provenance/counts/claims match.** Verified computationally: 36 compatibility rows, 36 manifests, categories 18/9/1/8, execution kinds 13/4/10/9, 56 passing + 16 planned, 44 crate aliases, 60 package examples, 15 scenario examples, 28 stable claims — all identical to the post-item inventory. `stable_support_claims.json` drops the row from `runtime_deferrals` and adds it as `supported-through-bridge`/`runtime-observed`; `docs/rust-interop.mdx` updates the deferral sentence and the generated table in lockstep. Scenario manifest is guarded by a 13-mutation self-test (`_scenario_opaque_resources.py:122-214`) plus 22 required tokens (`_scenario_checks.py:73-99`).

**6. Round-6 findings are closed.** `sysroot_interop.rs:348-355` now declares `def close(own self) -> Result[None, FileHandleError]` with a declared error class, and sets `close_method.declaration.consumes_receiver = true` (`:366-372`). Consuming fixture declarations were corrected to `own` in every affected row (`close_after_use`, `opaque_handle_tokenizer`, `opaque_resource_core`, `async_runtime_core`, `callback_subscription_core`); the checked-in stdlib is unaffected because its only opaque classes use `close=drop` (`stdlib/_sifr/python.sifr:33`, `:38`).

**7. Panic/hang/leak sweep.** No `RefCell` borrow is held across an `await` (`resources.rs:416-458`, `:540-557`); the deliberate panic is confined to `catch_unwind_silently` with a silent-hook boundary (`:496`, `interop.rs:341`); temp packages go to `env::temp_dir()`; the fixture `target/` tree is `.gitignore`d (`**/target/`) and excluded from both the test copy (`:96`) and the self-test copytree (`_scenario_opaque_resources.py:109`).

### Findings

- **Unneeded relaxation of a placeholder-evidence guard.** `verification/areas/rust_interop/checks/_binding_helpers.py:91-132` now permits a sole `pass` body inside an `@rust.opaque` class. No `.sifr` file in `verification/areas/rust_interop/**` contains a bare `pass` (verified by grep), so this milestone ships a weakened placeholder check with zero consumers, plus a ~40-line indentation heuristic and five self-test cases to maintain. Revert it, or state which fixture requires it.
- **Leftover scratch probe directory in the working tree.** `.cert5probe/` (5 hand-written `t*.sifr` probe files, dated during this milestone's review) sits untracked at the repo root. It is not covered by `.gitignore`, so it is one `git add -A` away from landing. Delete it before merge.

Neither affects the correctness or integrity of the certified evidence.

VERDICT: SATISFIED
