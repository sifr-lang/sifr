All verification is complete. Here is the review.

---

# M16 Frozen Whole-Diff Review — Raw API Ergonomics On Shared Ownership (Pass 5)

**Reviewer:** Independent milestone-closure review (Fable High, fresh pass)
**Range:** `3f974f33b` → `403d71020` (6 commits), PR #2996 — HEAD confirmed at `403d710208c0bf9c30a74c2723a23ca66277a68b`
**Scope reviewed:** the complete 60-file diff (+1580/−100) read end to end — lowering (`direct_validation.rs`, `python_raw_object_methods.rs`, `method_call_arguments.rs`, `methods_lambdas_and_comprehensions.rs`, `regular_calls.rs`, `return_lowering.rs`, `workload_annotations.rs`, `compiler_intrinsics.rs`, `python_interop.rs`), IR (`hir_nodes.rs`), codegen (`python_raw_api_codegen.rs`, intrinsic registry, all four emitter call sites), runtime (`coroutine_ops.rs`, `recursive_ops.rs`, `object_ops_tests.rs`), stdlib (`object_bridge.rs`, `stdlib/sifr/python.sifr`), driver tests, LSP regression tests, all four fixtures, demo, docs, both certification inventories, capability metadata, the full performance remediation (`lsp_query_bench.py`, `run_benchmarks.py`, manifest, trend, `performance_budgets.md`), and the contract documents (AGENTS.md, the plan ledger, all four prior review artifacts — re-verified against source and behavior, not taken on trust). No files were modified.

## Verdict summary

The pass-4 remediation is precisely and completely delivered, and every finding from passes 1–4 is independently confirmed closed at the actual head. The whole-diff re-read plus full local re-execution surfaced no new actionable defect at any severity. Both M16 acceptance criteria and all safety/ownership/canonical-conversion invariants hold.

## Prior-findings re-verification (all four passes, against the head, independently executed)

- **Pass-1 MINOR-1 (polluted async fixtures): closed.** I compiled all three fail fixtures with a freshly built compiler at head. Each emits **exactly one** diagnostic at the exact annotated span: `SIFR-PYCONV-0001` at `python_raw_unsupported_generic_conversion.sifr:6:12` ("`set[int]` is not in the declaration conversion set"), `SIFR-ASYNC-0003` at `python_raw_typed_conversion_requires_offload.sifr:7:12` (`from_value`), and `SIFR-ASYNC-0003` at `python_raw_object_method_requires_offload.sifr:7:18` (`Object.get_attr`). Both async fixtures use canonical `await task.sleep(0.0)` with no bogus import.
- **Pass-1 MINOR-2 (stale architecture status): closed.** `internal_docs/architecture.md:54` carries the M16 contract sentences (typed `from_value`/`to_value`/`kwarg` through the same declaration conversion authority, compiler-known checked `Object.get_attr`/`get_item`/`call`/`call_method` on the canonical sealed object, ordinary automatic release, raw coroutine submission to the application-owned loop) — and the restored "LSP environment status goes through the same Cargo package graph…" sentence is true at head because the product was fixed, not the doc.
- **Pass-3 MAJOR-1 and MAJOR-2 (anti-conservative LSP input gate): closed.** `git diff 3f974f33b..403d71020 -- crates/sifr_lsp/src/python_declarations.rs` is **zero lines** — the product file is byte-identical to the pre-M16 base, so environment resolution is unconditional and the gate cannot exist at head. Both discriminating regressions are present and pass: `workspace_member_python_requirements_are_validated` now writes a **pure** root `sifr.toml` before asserting `SIFR-PYTRUST-0005` (`python_declaration_tests.rs:539-543`), and the new `misplaced_bridge_root_is_reported_for_an_otherwise_pure_package` asserts `SIFR-PYIMP-0002` for a pure manifest with a root-level `python_bridges/` (`python_declaration_tests.rs:579-604`). Both pass inside the 26/26 suite I ran at head. Pass 4's live gate-reapply discrimination experiment binds unchanged: neither `python_declarations.rs` nor `python_declaration_tests.rs` was touched by the final commit.
- **Pass-4 MINOR-1 (dead didOpen temp package + overclaiming ledger sentence): closed.** `benchmark_source` is now scenario-aware (`lsp_query_bench.py:274,285-290`): for `lsp.did_open_diagnostics` it yields the original source path before any `TemporaryDirectory` is created, so no dead locked package is constructed, while mode validation (package-needs-manifest, isolated-rejects-manifest, unknown-mode) still fires **before** the shortcut. I proved all of this by direct invocation: the didOpen/isolated context yields the original path with no temp package; the warm-isolated context yields a path inside a temp package containing exactly `sifr.toml`, `Cargo.toml`, frozen `Cargo.lock`, and `src/{main.sifr,lib.rs}`, cleaned up on exit; package mode is a byte-identical passthrough; all four error paths raise loudly. The ledger now states the exception explicitly (`plans/issues/active/…interop.md:2314-2317`): "the didOpen diagnostics sync case deliberately retains its historical package-less temporary directory and no longer constructs an unused second package" — accurate at head, and the paragraph's earlier general "isolated cases execute in a minimal temporary locked Sifr/Cargo package" sentence is qualified within the same paragraph exactly as pass 4 prescribed.
- **Pass-4 MINOR-2 (undocumented `workspace_mode` contract): closed.** `internal_docs/performance_budgets.md:83-91` now records everything pass 4 required: `workspace_mode` is mandatory on every `lsp-query` case with the `isolated`/`package` enum, validation cross-checks the mode against the sibling `sifr.toml`, isolated warm/cold cases run in a deterministic minimal temporary locked Sifr/Cargo package to prevent upward attachment to the repository workspace, package cases run the checked-in `lsp_workspace` fixture in place, and the didOpen exception is stated with its rationale (measured operation is document publication, not package analysis). Every claim matches the shipped `validate_case` (`run_benchmarks.py:267-279`) and harness behavior I executed.

## Benchmark integrity (challenged, no weakening found)

- The whole-range manifest diff is exactly 18 added `workspace_mode` lines (14 `isolated`, 4 `package` — cold-start, workspace-diagnostics, references, rename on `lsp_workspace/`); no warmups, iterations, timeouts, thresholds, or budget ids changed. `baselines.json`, `budgets.json`, and `lsp_query_budget_ids.md` are untouched across the entire range. The `trend/current.json` diff is exactly the `manifest_sha256` line, and I recomputed `sha256(benchmark_manifest.json)` = `f2e180e4…` — it matches.
- `validate_manifest` runs `validate_case` on every load (`run_benchmarks.py:200`), so the new required field and two-way manifest/directory cross-check gate every real run, not just `--validate-only`. `--validate-only` (65 cases), `--self-test`, and `check_trend_policy.py` all pass at head; both scripts `py_compile` clean.
- I ran the shipped harness end to end at head: `lsp.did_open_diagnostics … isolated` (samples 524→44 ms cold-first under a debug binary, structurally healthy) and `lsp.diagnostics … isolated` (4.48/4.88 ms per sample under a **debug** binary — fully consistent with the recorded 4.009–4.02 ms release medians and the 71.5–73.1 MiB RSS). didOpen measurement is byte-identical to its historical behavior in all three intermediate states, so existing baselines remain honest; headroom only increased for the formerly repo-attached cases, which the ledger discloses.

## M16 acceptance criteria and invariants (reassessed at head)

Since the pass-2-satisfied head `e3377da62`, the only crate change in the entire range is +31 lines of LSP **test** code (the two pass-3 regressions) — every compiler/runtime/stdlib/driver product file is code-identical to states that were exhaustively executed in passes 1–2 (native package tests, 537 fail fixtures, create-PR gate 19/19 interop and 131/131 core). On top of that inherited evidence I independently re-executed the primary claims at head:

- **One conversion authority, no second model.** `validate_raw_conversion_intrinsic` (`direct_validation.rs:9-38`) validates `from_value` args, `kwarg` values, and the `to_value` Result-ok type through the exact declaration predicate `is_direct_type`; codegen (`python_raw_api_codegen.rs`) generates exclusively through the declaration converters `input_conversion`/`output_value_expr` and the existing `__sifr_declaration_object_argument`/`__sifr_declaration_object_result` helpers; the registry entries are `(None, None)` so no alternate lowering exists.
- **One sealed identity, checked errors.** The four raw methods are a closed compiler-known set gated on `is_python_object_contract()`; they hard-require the validated five-field `PythonError` contract at compile time; all surfaces return `Result[…, PythonError]` mapped through `bridge_error_expr`; `kwarg` erases only to the existing `(str, Object)` shape; `py_call_keyed`/`py_call_attr_keyed` reuse `python::call_object`/`call_attr` over the same tracked store. No exposed handles, no data-dependent unwraps in the new codegen.
- **Ownership/cleanup equivalence, proven end to end.** Raw methods lower as `FunctionType::all_borrow`; receivers/arguments pass by `&`. I ran both ignored native package tests against local CPython at head — `typed_raw_api_builds_runs_and_releases_ordinary_objects` and `raw_coroutine_api_builds_and_runs_on_the_owned_loop` both build, run, and pass their `live_objects` equality and `:released` assertions (2/2, 21.3 s).
- **Owned loop, all five conditions.** `run_coroutine_blocking` routes through `async_runtime::ensure_started` (`coroutine_ops.rs:4-11`); coroutine suite 4/4 (identity, concurrency, checked Python failure, shutdown cancel/join), owned-loop async runtime 14/14, object-ops 6/6 including the dict-mapping-key regression — all re-run and passing at head.
- **Demo, docs, metadata.** The demo runs end to end and prints exactly the README-documented `Python raw API demo: SIFR:9:resources=zero`. `docs/python-interop.mdx` accurately documents the closed conversion set, automatic release, and owned-loop coroutines. The retained-intrinsics surface covers exactly the three intrinsics, the adapter-reachability inventory covers both keyed adapters with their real consumer, and the capability row's named owners (including cancellation) correspond to suites I executed.

## Validation actually run at head

Release + dev compiler builds; all four fixtures compiled with exact diagnostic-count/span checks; `cargo test -p sifr_runtime --features python` coroutine/object-ops (10/10) and async_runtime (14/14); `cargo test -p sifr_lsp -- python_declaration` (26/26, i.e. the ledger's 24 declaration tests + 2 fingerprint units); driver frontend bridge test (generated Rust reuses `from_str`, `py_call_attr_keyed`, `__sifr_declaration_object_result`); both ignored native CPython package tests (2/2); `demos/m16_raw_api/run.sh`; direct `benchmark_source` probe covering all mode/scenario routing and error paths; shipped harness runs of the didOpen and diagnostics isolated cases; `run_benchmarks.py --validate-only` and `--self-test`; `check_trend_policy.py`; manifest SHA-256 recomputation; `check_hir_maintainability_guardrails.py` PASS; stdlib manifest-schema, intrinsic-allowlist, and adapter-reachability guards PASS; `cargo fmt --check` clean; `cargo clippy --workspace -- -D warnings` clean; file-size cap verified on every touched hand-maintained file (largest: 890).

## Findings

None. No BLOCKER, MAJOR, or MINOR findings remain.

## Non-findings (observations, no action required)

- A hypothetical future `lsp.did_open_diagnostics` case declared `package` would pass `benchmark_source` yet never use the package (`run_did_open_diagnostics` owns its own workspace). No current manifest entry can trigger this, validation pins the directory shape, and the budgets doc now states the didOpen contract explicitly — latent authoring hazard only, not a defect in the shipped merge unit.
- The harness's hardcoded `"diagnostics_count": 0` output field predates this diff (carried from pass 4's observation).
- The release-profile binary requires `SIFR_SYSROOT`/installed layout (I hit and confirmed this, then used the dev profile) — pre-existing `sifr_sysroot` behavior, unrelated to this diff.
- Duplicate keyword names in raw `call`/`call_method` resolve last-wins via dict construction — pre-existing raw-path semantics identical to `py_call`, unchanged here.
- Working-tree bookkeeping outside the frozen diff: an empty untracked pass-5 review artifact placeholder and a content-dirty `third_party/ruff` submodule (gitlink unchanged in the range). Neither is part of the merge unit; I left both untouched.
- The authoritative merge-profile gate must still be rerun before merge, exactly as the ledger already states ("Repeated whole-diff review, the authoritative merge-gate rerun, and PR merge remain before Wave 5 can close") — that is accurate process description, not an open defect.

## Verdict

The pass-4 remediation is exactly what was prescribed and nothing else: the didOpen case no longer constructs a dead locked package while retaining its historical package-less measurement, the mode-validation contract still guards every case, the durable performance reference now documents the required `workspace_mode` semantics accurately, and the ledger describes all of it truthfully. Every prior finding across four passes is verifiably closed at the head, no benchmark threshold or baseline was weakened, no product shortcut remains, and the full M16 feature body — single conversion authority, sealed identity, checked errors, automatic-release equivalence proven in compiled binaries, owned-loop raw coroutines across success/failure/concurrency/cancellation/shutdown — holds under fresh inspection and re-execution.

VERDICT: SATISFIED
