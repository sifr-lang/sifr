Review complete. I focused on the 7 areas you called out and traced the diff against the actual sysroot/stdlib code paths.

**No blocking findings.** The diff satisfies each focus area:

- **CPython differential**: `verification/areas/cpython_differential/checks/generated_suite.py:189-197` builds/runs the release binary with explicit `--sysroot`, and 4 hand-seeded fixtures + 5 generator templates in `checks/generated_programs.py` now build JSON via `dumps(from_array([from_int(...)]))`. Only `hand_seeded_merge.py:74` still runs via `cargo run` without an explicit `--sysroot`, which is consistent with running from workspace context — timeout bump 90→240s (`hand_seeded_merge.py:24`) tracks the new APIs' compile cost. Not a blocker.
- **Analysis parses via frontend helper**: `sifr_analysis/src/host/editor_facts.rs:6,16` and `stdlib_navigation.rs:7,113` now use `sifr_frontend::{parse_source, parse_source_module}`; `parse_source_module` is added at `sifr_frontend/src/graph_cache_and_queries.rs:262-267` and re-exported via `lib.rs:17`. `grep sifr_syntax::parse_module crates/sifr_analysis/` returns nothing.
- **Formatter guard durability**: `check_formatter_rules_manifests.py:186-193` still enforces the `.gitmodules` URL + branch match, the `merge-base --is-ancestor` seed↔HEAD relationship, seed subject, and seed paths. Loss of `branch --contains` is safe because seed ancestry + `.gitmodules` branch pin still constrain HEAD to a descendant of the pinned seed on the declared branch.
- **Native-link trust**: `sysroot_trusted_native_links` at `sifr_driver/src/build/materialize.rs:310-324` only unions `aws_lc_0_41_0_crypto` when `crates` carries a `tls`/`http` feature OR `retained_direct_dependencies` starts with the three rustls package spec prefixes. `aws-lc-sys 0.41.0` in `Cargo.lock:210` matches the literal. Tests at `materialize.rs:601-641` confirm untrusted links still fail (`static=crypto`) and http selection inherits the trust.
- **E2E dependency helpers**: `fixture_dependency_paths.rs` collapses to pure `env!("CARGO_MANIFEST_DIR")` sibling lookups with `.expect(...)` and no env/ancestor discovery. `harness_behavior_tests.rs:477-484` adds explicit "empty deps do not pull runtime" coverage against `fixture_cargo_toml::generate_cargo_toml` and the empty-input paths (lines 33-100, 103, 111, 119) indeed skip runtime insertion. `batch_execution.rs:82,363` now `env_remove("CARGO_TARGET_DIR")` so tmp-dir target paths remain stable under merge-gate overrides.
- **Direct `list[int]` interop bridge**: `sifr_codegen/src/rust_interop_direct.rs:40-49,314-342` produces `into_iter().map(SifrIntBridge::from).collect::<Vec<_>>()` for owned and `&_.iter().copied().map(...).collect::<Vec<_>>()` for borrowed, matching the callee signatures in `sifr_stdlib/src/collections.rs` (owned `Vec<SifrIntBridge>` for `set_from_list`, `&[SifrIntBridge]` for `set_len`/`set_contains` via Deref coercion from `&Vec<_>`). The corresponding stdlib declarations at `stdlib/_sifr/collections.sifr` only use `own` and default(borrow); no `mut_borrow` list[int] adapter exists, so the currently-generated `Ref{mutable:false}` covers every real callsite.
- **Docs/status**: `internal_docs/sifr_sysroot_and_stdlib_architecture.md:462-505` documents the new borrowed list[int] bridge and the exact sysroot TLS trust set; `plans/issues/active/ad-hoc-sifr-sysroot-stdlib-toolchain.md` reflects closeout-locally-validated status with the 2026-07-06 merge-gate run.

**Advisory-only observations (not blocking)**:
- `sysroot_trusted_native_links` `starts_with("rustls = ")` prefix match (`materialize.rs:315-319`) is coupled to rendered TOML formatting from `sifr_stdlib_model/src/features.rs`. A future formatting change fails closed (legitimate TLS build rejected), not open, but a `SysrootCrate` enum or structured package name comparison would remove the coupling.
- The `aws_lc_0_41_0_crypto` literal (`materialize.rs:321`) is version-locked to `aws-lc-sys 0.41.0` in `Cargo.lock` with no automated cross-check; a bump to 0.42.0 would silently break TLS builds until the literal is edited by hand.

I'm satisfied with this diff.
