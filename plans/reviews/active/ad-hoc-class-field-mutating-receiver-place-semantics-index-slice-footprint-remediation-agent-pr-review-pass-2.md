## Review: class-field receiver-place index/slice footprint remediation — pass 2 @ `85be31b17` (base `9c99ef43b`)

I made **no modifications** to any repository file, and ran **no** Cargo build/test/clippy, Sifr compilation, corpus sweep, performance probe, broad validation, or subagent/reviewer. All conclusions come from reading the diff at this exact head and the surrounding code.

### Diff surface at this head
- `crates/sifr_lowering/src/lower/method_receiver_places/footprint.rs:108`, `:121` — the two `collect_footprint(object, …)` additions (unchanged from pass 1).
- `crates/sifr_lowering/src/lower/method_receiver_footprint_tests.rs` (new, 111 lines) + `mod.rs:91-92`.
- `crates/sifr/tests/e2e/fail/mutable_receiver_dynamic_index_base_overlap.sifr` (new, 18 lines).
- `crates/sifr/tests/e2e/pass/class_field_dynamic_index_base_disjoint.sifr` (new, 51 lines) + one manifest entry.
- `plans/reviews/active/…-pass-1.md` (record only).

### Pass-1 finding closure — verified closed correctly

**1. `index_and_slice_arguments_accept_disjoint_unresolved_bases` proves sibling-field identity, not root-only behavior.** (`method_receiver_footprint_tests.rs:78-111`)
I traced both statements against the actual traversal. For `self.source.snapshot().values[:1]`: `root_binding_id` (`method_receiver_places.rs:614-622`) walks only `Name`/`FieldAccess`/`Index`/`Slice`, so it returns `None` through the `MethodCall`, and **no** `Dynamic` is pushed (`footprint.rs:118-119`). The new recursion at `:121` reaches the `FieldAccess` arm (`:240-249`) → `extract_footprint_place` fails on the `MethodCall` base → `root_binding_id` again `None` → `MethodCall` arm (`:162-172`) → `collect_callable_field` returns `false` (`snapshot` is a method, not a callable field, `:65-67`) → `collect_footprint(self.source)` hits the `extract_place` fast path (`:99`) → `Place{self, [Coordinator.source]}`. The mutable receiver place is `Place{self, [Coordinator.owner]}` (`extract_place` succeeds — `Coordinator` is neither optional nor recursive, `method_receiver_places.rs:407-416`). `places_overlap` (`:24-28`) is false: same root, neither projection list a prefix of the other. Identical for the `[0]` index line via `:108`. So the test is a **live** guard: had the recursion produced `Dynamic(self)` or the wrong field identity, it would fail. Root-only precision would fail it. Both the index and slice arms are covered, and the receiver/base fields are true siblings under one root.

**2. The pass fixture is well-typed, ownership-correct, and observably asserted.** (`class_field_dynamic_index_base_disjoint.sifr`)
- Ownership: `replace(self, own values: list[int])` receives `self.source.snapshot().values[:1]`, a slice — a freshly-constructed list, so an owned temporary is the correct convention.
- Typing precedents all exist in-corpus: returning a class-typed field by value (`generic_class_field_access.sifr:6`, `generic_class_unrelated_non_clone_param.sifr:6`), comparing an `int | None` result against an int literal (`class_field_mutating_receiver_contexts.sifr:124-125`), and list-literal equality (`generic_chain_str.sifr:7`).
- Observable receiver state: `apply()` return (`== 7`), `owner.value` (`== 7`, set by the index call) and `owner.values` (`== [7]`, set by the slice call) are all asserted, so both widened call sites are runtime-checked, not merely compiled. Structure matches the immediate precedent `class_field_callable_invocation_disjoint_sibling.sifr`.
- Registration: exactly one occurrence repo-wide (`create_pr_e2e_manifest.json:16`). Manifest names are loaded into a `BTreeSet` (`fixture_compilation.rs:100-107`), so ordering is irrelevant and duplicates would be silently collapsed — there are none. The name matches `path_to_name` for the file, so `filter_fixtures_by_selection`'s unknown-fixture check (`:136-145`) is satisfied. No expected-stdout entry is needed (assert-only fixture, `expected_stdout` is `Option`). The fail fixture correctly needs no manifest edit (directory scan), and `tests/e2e/fail/` is now 566 files, consistent with the reported 566/566 with no hardcoded count baseline to update.

**3. The `own` change on the negative slice regression does not weaken it.** (`method_receiver_footprint_tests.rs:59`)
The receiver/argument overlap loop at `method_receiver_places.rs:217-223` is gated only on `MutableReceiverTarget::Place`; it does not consult `conventions`, so the argument's convention cannot affect whether `expression_overlaps` runs or what it reports. The assertion still pins both `OWN_DOUBLE_MUTABLE_BORROW` **and** `primary_range == "self.pick().values[:1]"`, and `expression_overlaps` remains the sole producer of that code at that range (`:219`, `:227`, `:240`, `:270` are its only call sites, all funneling through `report_overlap`). Pre-fix the base is unresolvable and the bound is a literal, so the footprint is empty and no diagnostic is emitted at that range — mutation sensitivity is preserved exactly as before the `own` edit. `own` only removes an unrelated borrow-into-field diagnostic that the `.any()` assertion would have tolerated anyway; it makes the test cleaner, not weaker.

**4. Original fix, negative regressions, fail fixture, module split, adjacent paths — all still correct at this head.**
The two-line change is byte-identical to pass 1. `Dynamic(root)` is still pushed ahead of each recursion and remains load-bearing (it subsumes every same-root `Place`, `:20`, so the recursion can never widen a name-rooted base, while for a resolvable `FieldAccess` base the recursion yields the strictly narrower place). `extract_place` does not handle `Index`/`Slice` (`:398-428`), so the `Index`/`Slice` arms are only reached for genuinely non-place subscripts. Recursion is on strict sub-expressions; duplicate footprints are inert under the existence test at `:18-21`. The fail fixture's `col=28` is right (8 indent + `return ` = 15 + `self.update(` = 12 → arg at 28). `indexed_storage.rs` (54 lines) does no independent footprint collection, and `expression_overlaps` has no callers outside `validate_call_overlaps`, so no adjacent traversal needs the same patch. File sizes: `footprint.rs` 279, `method_receiver_places.rs` 684, `mod.rs` 254, test module 111, fixture 51 — all under 900.

---

### Findings

**1. Non-blocking — the second behavior widened by this diff (a nested subscript's inner index operand) still has zero test coverage in either direction.**
`crates/sifr_lowering/src/lower/method_receiver_places/footprint.rs:108` (and `:121`)

The recursion adds footprints in exactly two previously-dropped shapes. The non-name-rooted base (`f().field[…]`) is now covered from both sides (two unit regressions + fail fixture on the reject side; one unit test + pass fixture on the accept side). The other shape — the inner operand of a nested subscript — is not covered at all.

Concretely, for `self.owner.update(other[self.owner.index][0])`: the outer `Index`'s object is `other[self.owner.index]`, `root_binding_id` resolves to `other`, so `Dynamic(other)` is pushed and does not match the `self`-rooted receiver; pre-fix the inner index `self.owner.index` was dropped entirely and the call was **accepted**. Post-fix, the `:108` recursion descends into `other[self.owner.index]`, re-enters the `Index` arm, and collects `Place{self, [Owner.owner, …index]}`, which prefix-overlaps `Place{self, [Owner.owner]}` → the call is now **rejected**. I believe the new rejection is correct and consistent with the policy already applied to `self.owner.update(self.owner.index)`, and the reported 950-pass lowering suite plus 566/566 fail corpus indicate nothing in-corpus regressed. But this is a real accept→reject flip introduced by the diff, and nothing in the repository pins it — a future refactor could silently drop the inner-operand recursion (e.g. by hoisting the `Dynamic(root)` push into an early-return when the root resolves) and every test at this head would still pass.

Pass 1 named this shape explicitly (`x[i][j]` / `self.update(other[0][1])`) alongside the non-name-rooted base; only the latter was closed. Suggested closure is one additional unit test in the new module — a reject case such as `self.owner.update(other[self.owner.index][0])` asserting `OWN_DOUBLE_MUTABLE_BORROW` at the argument range, and/or an accept case such as `self.owner.update(other[self.source.index][0])` — no fixture or manifest change required.

Non-findings I considered and dismissed: the pass fixture and the new positive unit test both pass pre-fix (by design — they are over-rejection guards, which is precisely what pass 1 asked for, and the negative side already carries mutation sensitivity); near-duplication between the new unit test and the pass fixture (different layers — lowering acceptance vs. full codegen/runtime observability — matching the `class_field_callable_invocation_disjoint_sibling` precedent); the manifest entry's non-alphabetical placement (names are set-loaded, order is not semantic, and the placement follows the workstream grouping); the `values` field-name reuse across `Inner`/`Owner` in the fixture (overlap decisions there turn on `Coordinator.owner` vs `Coordinator.source`, so the collision cannot mask an error); the locally duplicated `range_for` helper (pervasive repo idiom); and the `SpecializedIndexedStorage` receiver-index path (pre-existing, out of scope).

---

**NOT SATISFIED** — one non-blocking actionable finding. The pass-1 finding is closed correctly on the shape it named as the primary gap; the remaining item is the untested sibling shape from the same widening.
