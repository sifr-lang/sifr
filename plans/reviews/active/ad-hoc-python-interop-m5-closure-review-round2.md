M5 closure verification results:

**Diagnostic precision (context-only obligation)** — `annotations_and_function_lowering.rs:827-843` now picks the requirement by `MustUseObligationKind`, so a `ContextOnly` binding says "must be consumed by \`with\` before function exit". The new test `python_context_expression_tests.rs:43` `context_only_obligation_rejects_value_that_is_never_entered` uses the `cleanup=context` `Transaction` opaque and asserts the exact phrase; the evidence JSON `negative_matrix[0]` cites this test — pointer accurate, test passes.

**Ordinary Sifr error → canonical SifrBoundaryError coverage** — New test `context_ops_tests.rs:227` `ordinary_sifr_error_truthy_exit_is_ignored_and_recorded` builds a `SifrExitCause { kind: OrdinaryError, sifr_type: "ValidationError", message: "invalid value" }` (canonical `SifrExitCauseKind::OrdinaryError` → label `"ordinary-error"`), routes through `context_exit_sifr_cause`, and asserts the Python `__exit__` receives `exc_type.__name__=="SifrBoundaryError"` plus `cause_kind=="ordinary-error"`, `sifr_type=="ValidationError"`, `message=="invalid value"`, `tb is None`. It then records ignored suppression and checks the evidence row `primary_cause=="ordinary-error:ValidationError"`. The still-present `ignored_non_python_suppression_is_structured_cleanup_evidence` companion was rekeyed to the canonical `"timeout:TimeoutError"` (previously the non-canonical `DeadlineExceeded`). Evidence JSON `outcome_matrix[5]` and `outcome_matrix[4]` now point to `ordinary_sifr_error_truthy_exit_is_ignored_and_recorded` and `originating_python_error_can_be_suppressed_and_exit_runs_once`.

**Prior M5 closure evidence remains sound** — Fixture `context_codegen_smoke.sifr` adds `python_error_transaction` returning 23; total arithmetic is 0+7+11+13+17+23=71 and the runner marker was updated to `sifr-python-interop:sqlite-context:total=71`. `run.py` now enforces `validate_sync_context_evidence` (capability, all seven canonical causes, ≥3 cleanup/negative rows, live-marker lock) and self-tests the missing-cause branch. All cited tests exist and pass: `failing_exit_poison_releases_manager_once_and_retains_cleanup_error`, `python_exception_replay_preserves_exact_triple_across_nested_exits`, `cleanup_failure_keeps_python_primary_replay_for_outer_exit`, `cause_classification_uses_canonical_resolved_types`.

**Guardrails / regression** — `scripts/check_file_size_guardrails.py` PASS (largest touched file 864 lines). `scripts/check_hir_maintainability_guardrails.py` PASS. `cargo clippy --workspace -- -D warnings` clean. `cargo fmt --check` clean. `verification/areas/python_interop/run.sh --self-test` OK. `cargo test -p sifr_lowering --lib --skip test_e2e_pass` 665/665 pass. `cargo test -p sifr_codegen --lib python_context` 7/7 pass. `cargo test -p sifr_runtime --features python context_ops_tests` 8/8 pass.

Round-1 already returned SATISFIED and flagged the two nits fixed here plus workflow tidy-ups (unchecked `M5` and wave-5 plan boxes at `plans/issues/active/ad-hoc-declaration-first-python-interop.md:142,343`, and this round-2 review record) as "docs commit … does not gate a functional M5 close." Those remain outstanding but are, by the round-1 verdict, non-blocking for closure of the code/evidence.

VERDICT: SATISFIED
