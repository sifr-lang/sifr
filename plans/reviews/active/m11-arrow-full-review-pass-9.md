All verification is complete. Here is the final frozen-diff closure review.

---

# M11 Arrow C Data Interface — final closure review, pass 9 (`origin/main...HEAD` at 0c604a023)

Full frozen-diff review of the branch (145 files, +8,132/−1,128 over 10 commits, merge base 61155e200), the M11 contract section (`plans/issues/active/ad-hoc-declaration-first-python-interop.md:1648-1728`), all eight prior review artifacts, and the two tracking commits. Method: five independent adversarial area reviews (runtime/ABI, lowering/ownership, codegen/driver, certification/CLI, evidence/docs) plus direct verification of every candidate blocker/major against the code, live probes through the fresh post-remediation release compiler, and guardrail runs. No repository files were modified; probes ran from `/tmp/m11p9` and the empty untracked `m11-arrow-full-review-pass-9.md` stub was left untouched.

## Frozen-diff integrity and closure-tracking truthfulness — verified

- PR #2991 is OPEN and MERGEABLE; its head commit equals local HEAD 0c604a023.
- The delta since d727cd692 (the commit pass 8 reviewed and the commit the authoritative merge gate ran at, per 32f7f1bc1 being docs-only relative to code) is exactly four documentation files: the plan, the pass-7 and pass-8 artifacts, and the roadmap. Zero code changed after the reviewed/validated state.
- All pass-8 merge-time bookkeeping items were done honestly: the pass-7 artifact was committed with its placeholder replaced by the true result (create-PR pass, **merge gate fail** at 368b23606) and marked `VERDICT: SUPERSEDED BY PASS 8`; the plan header and M11 section (lines 42-53, 1671-1690) cite exactly the fresh authoritative gate numbers (3772.09s, E2E 674/674 signature 1f8b1cadc4f48ec8, interop 20/20, diagnostics 175/175, 261 hardening variants); all M11 checkboxes and the roadmap are updated; the profile files confirm the merge-profile interop lane count of 20.
- `third_party/ruff` gitlink is unchanged vs main (`git diff origin/main HEAD -- third_party/ruff` empty); the working tree carries one unstaged local hunk that must not be staged at merge.
- Guardrails at HEAD: file-size **PASS (2,744 files)** — matching the closure claim — HIR and driver maintainability **PASS**, `git diff --check` clean, no TODO/FIXME/placeholder additions anywhere in the diff.

## Acceptance criteria — all three MET

- **No copy switch / never certify uncertain copying.** Confirmed independently across four surfaces: the `@python.arrow` grammar rejects any keyword except `schema` (`lower/python_interop/arrow.rs:166-172`); certification rejects `copy_performed`/unverified pointer identity at fixture validation, artifact load, and artifact write (`python_cli.rs:370-374`, `arrow_certification.rs:144-149`); the evidence script derives copy from pointer identity so uncertainty maps to rejection (`arrow_evidence.py:251`); the raw-API contract was deliberately flipped to conservative `copy_possible = true`, moving every zero-copy claim behind certification. Runtime re-verifies exact producer identity per acquisition (`arrow_ops.rs:193-211`).
- **Ownership transfers once and remains moved on consumer failure.** Confirmed: `prepare_arrow_argument` removes the store entry before the consumer call (commit-before-call); requested schemas are removed before the producer call; reconciliation preserves the first error and never re-materializes (`sifr_stdlib/src/python/arrow.rs:68-76`). Statically, adversarial probing of walrus, conditionals, comprehensions, generators, async loops, closures, nested defs, augmented assignment, `super()`/static/constructor/`__call__`, protocol receivers, field/tuple/subscript stores, loop releases, and branch merges found every smuggling shape either rejected at lowering with a stable `PYZC`/`OWN` code or provably unable to produce a binary. My own live probes reconfirmed the key rejections (`SIFR-PYZC-0001` for borrowed-affine operator return and owned-affine operator params, `SIFR-OWN-0003` for ordinary borrowed returns) and that the exempted non-affine operator shape checks, builds, and runs correctly.
- **Unconsumed resources release exactly once.** Confirmed on all mainline paths: single-guard get-check-remove store operations (no TOCTOU), double release → structured `SifrPythonClosedArrowCapsule`, paired rollback on partial acquisition, token-nonce forgery protection, instrumented exact-count tests. One narrow edge exception exists (P9-1 below) — a leak (zero releases), never a double release.

The certification identity/trust model held up under adversarial reading: the artifact binds environment digest (interpreter identity, SOABI, platform, pyproject/uv.lock digests, per-import distribution probes), exact producer module/type, kind, schema mode, sorted distributions re-probed live at certify/build/check, and a SHA-256 fixture digest; every drift axis is rejected field-by-field with `deny_unknown_fields`; trust remains root-owned and only the session root's artifact is ever loaded. Emitted Arrow wrapper code is entirely `Result`-structured with zero `unwrap`/`expect`/`panic!`; the 675-fixture emit-safety test passed the fresh gate, and my emit probe of the previously failing fixture produced zero occurrences. Documentation matches the implemented grammar and CLI exactly, with no copy-switch or reduced-version terminology.

## Findings

**Blockers — none. Majors — none.**

Two area agents initially proposed majors; both were verified against the code and downgraded with cause:

- The runtime candidate (P9-1 below) requires a producer already in Closing/Poisoned/Closed state or a mid-run attach failure — states unreachable from well-typed programs given consuming close, non-send identities, and static borrow rules — and its worst case is a loud-at-shutdown leak in a call that already returned a structured error.
- The certification candidate (P9-2 below) mutates nothing on a consistent tree, matches the default session semantics of every existing command including `sifr check`, and cannot touch certification artifacts, trust, manifests, or the Python environment; the strict machine-checked non-mutation contract is M13's scope.

**Minors** (none gating; all are follow-up-worthy)

- **P9-1 — requested-schema entry can leak (zero releases) on pre-transfer runtime errors.** `acquire_foreign_with_schema` consumes the stdlib identity (`sifr_stdlib/src/python/arrow.rs:156-157`; `into_arrow_key` takes the key, `resource_identity.rs:58-67`, so the drop releases nothing), but `take_requested_schema` runs only after `clone_handle` inside `attach` (`arrow_ops.rs:278-281`). If `attach` fails or the producer is Closing/Poisoned/Closed (`foreign_object.rs:63-84`), the schema entry is orphaned in `ARROW_STORE` — its release callback never runs and shutdown live-object validation fails. Fix: take the schema entry before `clone_handle`, or restore it on pre-call errors.
- **P9-2 — `sifr python certify` / `--check` hardcode `CargoLockMode::Normal` with no lock-mode flags** (`python_cli.rs:101,114`), so on a drifted manifest the nominally read-only recheck can rewrite `Cargo.lock` or touch the network via `cargo metadata`, with no `--locked/--frozen` escape hatch (every other package command has one, `cli_model_and_entrypoint.rs:679-691`). Recommend defaulting `--check` to `Locked`.
- **P9-3 — pre-existing user-triggerable compiler ICE, one new spelling reachable.** Field assignment inside an operator-dunder body panics codegen (`SIFR-INTERNAL-0001`, `function_like_lowering.rs:100` — file untouched by this branch; reproduced live with no Arrow/borrow involvement). Fail-closed (no binary), but the d727cd692 exemption makes `self.stash = other` one more spelling that reaches it. Recommend lowering-level rejection or FieldAssign support.
- **P9-4 — diagnostics-quality cluster around the operator exemption (extends known P8-1).** Indirect store-then-return of a borrowed non-affine operator param falls through to raw rustc `SIFR-BUILD-0005` (main parity, never unsound); mixed-class operator call sites hit a pre-existing E0369 emission gap; the d727cd692 regression test asserts `lower_ok` only, with no emission/e2e coverage; f-strings interpolating affine resources fail at rustc instead of a clean diagnostic (M10 parity). A hardening refinement: the exemption predicate (`parameter_conventions.rs:149`) should also keep tracking when the type carries a must-use obligation, so exact-once cleanup never rests solely on emission-side `!Clone`.
- **P9-5 — fail-open guards on unreachable driver invariants.** `build/python_interop.rs:91-97` silently continues past a target-less Arrow declaration and falls back for a missing `certification_target` (both unreachable for valid input); `python_runtime.rs:92` + `project_codegen.rs:126` would silently drop the certification cache-identity fragment on a (practically infeasible) serialization failure. Contrary to the no-fallback rule; should hard-error.
- **P9-6 — certification workflow robustness.** Non-atomic artifact write (`arrow_certification.rs:103`); packaging silently tolerates a corrupt artifact / drops escaping fixtures instead of validating up front (`arrow_certification.rs:256-276`); no timeout on fixture execution (`python_cli.rs:341-347`); distribution identity is name+version, mitigated by the environment digest but weakest without a uv lock. All fail closed at load time.
- **P9-7 — test gaps.** No direct null-payload capsule fixture (defensive rejection layers exist; the evidence JSON "covers" claim slightly overstates); the `RELEASE_CALLS` counter in `arrow_ops/tests.rs:10` is incremented but never asserted; no runtime test where the consumer raises and ownership is then asserted moved (covered statically and via partial-consumption paths).

**Advisories** (recorded, non-gating): `checked_ref` unbounded lifetime signature (`abi.rs:344-355`); `unsafe impl Send` on ABI structs could be `cfg(test)`-gated; inherent FFI trust on capsule pointer sizes (matches arrow-rs practice, deliberately shallow validation); `reconcile_arrow_argument` drops the cleanup error when the consumer also failed; `finish` error path defers `drain_pending_releases`; fnv1a64 environment digest is drift detection, not a security binding (fixture digest is SHA-256); executable fixtures can themselves write (inherent to executable evidence); `--check` with no artifact reports a read error rather than "zero targets"; CLI usage errors reuse `PYZC_INVALID_DECLARATION`; bridge-producer coverage is unit-fixture-only and device evidence is synthetic (both honestly labeled in the evidence JSON); one compiled example covers pandas+PyArrow+Polars rather than three binaries; four files sit at 894-899 lines under the 900 cap; pass-8's internal "675 pass fixtures" figure vs 674 fixture files on disk is an immaterial artifact-internal count; carried pass-6/7 items (protocol owned-affine declaration bypass — re-confirmed soundness-inert with every escalation path independently rejected; while-condition loop gap; cross-module inherited-method export ordering) remain pre-existing and inert.

## PR #2991 closure-readiness

**PR #2991 is closure-ready.** The frozen diff at 0c604a023 is exactly the state the authoritative merge gate validated (code-identical to 32f7f1bc1/d727cd692 plus tracking docs), every acceptance criterion is met, all remediation commits were verified to close their targets without regressions, closure tracking is truthful, and no blocker or major exists. At merge: do not stage the dirty `third_party/ruff` submodule content, and the empty untracked pass-9 stub should be filled with this report if the artifact convention is kept. The minors above (P9-1, P9-2, and P9-3 foremost) are recommended as small follow-up issues, not merge conditions.

VERDICT: SATISFIED
