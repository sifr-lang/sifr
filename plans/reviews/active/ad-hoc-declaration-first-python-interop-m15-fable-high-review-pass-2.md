All evidence is in. `Planned` status feeds only reporting surfaces (`python_check.rs:31` maps it to "Deferred"; codegen never branches on it), so the duplicate-target delta is a conservative status-reporting regression, not a correctness or certification issue. Composing the final review now.

---

# M15 LSP Declaration Authoring — Fresh Full-Diff Review (pass 2, commit 1885275fb vs main)

I reviewed the complete 47-file diff against the M15 plan section, AGENTS.md, the internal architecture contracts, and the actual compiler/package/LSP code, with three parallel deep-dive verifications (symbol attribution end-to-end, environment/digest/deferral parity, driver probe behavior preservation) plus local re-runs: sifr_lsp 59/59, sifr_driver python_interop 16/16, sifr_analysis 48/48 (incl. the new direct `plan_preserves_declaration_module_file_identity`), sifr_package python 63/63, `cargo fmt --check` and `clippy -D warnings` clean on all touched crates, HIR guardrails pass, and every touched hand-maintained file under the 900-line cap (max 887).

## Pass-1 findings: all seven are genuinely resolved

1. **Exact symbol attribution (was MAJOR 1) — resolved.** Matching is now exact `(FileId, name)` equality (`crates/sifr_lsp/src/python_declarations.rs:118-122`). Completion items carry the *defining* file (`crates/sifr_analysis/src/host/implementation.rs:206` via the symbol index, which maps each symbol to its own module's file; imports index by module name, so `from a import sqrt` can never collide), and non-symbol candidates carry `sifrFile: null` and are skipped (`python_declarations.rs:608-615`). Hover uses the resolved `symbol_name` (`crates/sifr_analysis/src/host/semantic_editor.rs:25-28`) plus the definition-resolved file. The substring (`sqrt_table`) and last-dotted-segment (`Class.method` → `close`) mechanisms are structurally gone — protocol methods record dotted `function_name` (`crates/sifr_codegen/src/python_interop_plan.rs:218`) that no bare completion label can equal. Both pass-1 scenarios are test-pinned (`python_declaration_tests.rs:81-117, 120-160`, the cross-module test asserting on label *and* `/data/sifrFile`).
2. **Live digest validation (was MAJOR 2) — resolved.** The LSP now runs the live environment probe and computes `digest_python_environment_probe(...).hex` (`python_declarations.rs:371-374`), validating certifications against it (`:398`) and bindings against `authoring_environment_digest()`, which is computed from that same live probe in `PackagePythonRuntime::from_probe` (`crates/sifr_driver/src/build/python_runtime.rs:53-54`) — digest-for-digest identical to `sifr check` (`crates/sifr/src/python_runtime_context.rs:106-133`). The tautology is gone, and `certification_drift_is_checked_against_the_live_environment_digest` writes a schema-valid artifact with `"environment_digest":"stale"` and asserts the mismatch diagnostic — a test the old self-referential code would have failed to produce.
3. **Library deferral parity (was MAJOR 3) — resolved.** Same gate (`runnable_app_paths().is_empty()`, `python_declarations.rs:354-357` vs `crates/sifr/src/check_and_package_commands.rs:116-122`), same resolver (`resolve_python_environment_for_check`), same outcome on deferral: no error, `runtime: None`, statuses "deferred" (`:365-370`), matching check's clean pass. Test-covered.
4. **Canonical graph/root/trust and protocol-certification parity (was MINOR 4) — resolved.** The LSP now runs `PackageSession::discover` → `load_package_graph_snapshot` → `resolve_python_bridge_graph` → `resolve_python_environment_for_check` (`python_declarations.rs:325-364`), inheriting multiple-selection rejection, non-root `[python]` rejection, and trust validation from the identical package code (`crates/sifr_package/src/python/environment.rs:131-190`); errors are rendered as diagnostics, not swallowed. Arrow/DLPack certification errors surface via the *same* driver function the compiler uses, `validate_protocol_certifications_for_plan` (`python_declarations.rs:183-186` ↔ `crates/sifr_driver/src/build/python_interop.rs:109`). Trust rejection is test-pinned (`SIFR-PYTRUST-0005`, probe count 0).
5. **Per-environment/per-target caching and inter-probe cancellation (was MINOR 5) — resolved.** Target inspections are memoized by `(package_root, external_fingerprint, target)` and survive source edits (`invalidate_source` clears only plan entries, `python_declarations.rs:75-83`); environments are cached separately; cancellation is checked between every target probe (`:263`). Tests assert probe/environment counters stay at 1 across repeat requests and non-target edits, and that a cancelled in-flight request stops with 0 probes.
6. **Declaration-scoped diagnostics with single ownership (was MINOR 6) — resolved.** Diagnostics carry the declaration's module and HIR span (`crates/sifr_driver/src/build/python_interop.rs:297-306`), are attached only to the declaring document (`python_declarations.rs:96-114, 425-462`), and package-level diagnostics go to one deterministic owner document (`:295-305`). Test asserts the line-3 anchor in the declaring file and absence in the other open file.
7. **Measured evidence (was MINOR 7) — resolved.** The verification runner now executes four real cargo test commands and prints only `focused_commands=4 passed` (`verification/areas/python_interop/runner/lsp_declaration_authoring.py`); the previously-claimed sifr_analysis coverage now exists as a direct test; the plan's "13/13" and "16/16" claims match what I measured; the suite is wired as blocking in all four profiles; the aiosqlite claim is backed by the in-repo `fixtures/async_context/aiosqlite_session.sifr` fixture in the blocking `async-context-examples` suite.

## Driver probe refactor: behavior-preserving, opaque fix faithful

The structured split (`inspect_python_target` / `apply_python_target_inspection` / `probe_python_interop_plan`) *is* the driver's own build path — one authority, which the LSP calls with a cache layered on top. Match arms, ordering, codes, and messages are byte-identical to main for every declaration kind in the normal one-declaration-per-target case. Commit 2279d2cc2 restores exactly main's five-kind signature-validation set `{Function, Buffer, Arrow, Dlpack, DlpackStream}` (`python_interop.rs:252-263`), so opaque constructors are again validated for type-ness only — the aiosqlite `@python.opaque(type=aiosqlite.Connection, ...)` shape — and `opaque_target_does_not_validate_constructor_signature` (`:510-535`) pins precisely that boundary (inspectable constructor with a required parameter → zero diagnostics, Verified). Bridge-marking relocation and the `selected_by: root_package_id` change are inert, as pass-1 already established.

## Actionable minors (no blockers, no majors)

1. **Hover can still attach Python status to an exact-same-named non-declaration symbol.** Hover's file attribution rides on the token-text `definition()` scan (first matching token across files in FileId order, `crates/sifr_analysis/src/host/implementation.rs:700-737`) with a current-file fallback (`crates/sifr_lsp/src/requests/hover.rs:17-22`). A parameter/local named `sqrt` in the module declaring Python `sqrt` (compiles cleanly), or a pure-Sifr `sqrt` in a higher-FileId module, gets the "Python target … verified" hover block. Completion — the surface named by the acceptance criterion — is sound, and the exposure requires exact-name collision rather than pass-1's any-substring, so this is a follow-up (semantic definition resolution or a symbol-kind check before enrichment), plus a cross-module same-name *hover* test, which is the one attribution scenario without coverage.
2. **Duplicate-target probes regress to "Deferred" vs main.** `apply_python_target_inspection` resolves the probe by first match (`python_interop.rs:167-173`), so when two declarations bind the same Python target, the second probe entry stays `Planned` → "Deferred" in `sifr python check` (`python_check.rs:31`), and the LSP's last-wins status map (`python_declarations.rs:468-472`) then shows *both* as deferred. Main verified all matching probes. Reporting-only and conservative (nothing is falsely certified; codegen doesn't branch on the status), but it's a real divergence from main and a small LSP/check parity gap in that corner. Relatedly, a shared *failing* target now fans out one diagnostic per declaration occurrence×match instead of main's one-per-probe-with-first-reason — deliberate for per-file attribution, worth noting as a multiplicity change.
3. **Binding-artifact digest drift lacks a regression test.** The watcher test writes malformed `{}`, which the old tautological code also rejected; no test writes a schema-valid `sifr.python-bindings.json` with a stale `environment_digest`. The certification half has exactly that test; add the binding twin. Similarly, `library_without_root_environment_selection_defers_python_status` asserts absence of `SIFR-PYENV-0001` while the historical bug emitted `SIFR-PYENV-0003`, and its fixture actually defers via the trust branch — tighten to assert an empty diagnostics list.
4. **Narrow editor-healthy/check-fails windows remain.** The LSP skips the CLI's certified-distribution version cross-check and `set_binding_identity` (`crates/sifr/src/package_python_certifications.rs:20-41` has no LSP counterpart), and only resolves the environment when declarations exist and the workspace has no compiler errors (`python_declarations.rs:172`) — a `[python]`-configured package with zero declarations gets no environment diagnostics in the editor where check would validate. Both are conservative-silence gaps, not false certification; document the scoping or close them in a follow-up.
5. **The "pure refactor" commit 1b1f17ba0 slightly changed CLI failure behavior.** Cargo-metadata failures now render bare `map_cargo_failure` diagnostics — dropping the `action`/`current_dir`/`args_redacted`/`lock_mode`/`network_mode`/`exit_status` args and `bounded_excerpt` truncation of the old `cargo_failure_diagnostic` (`crates/sifr/src/diagnostic_rendering_and_run.rs:843-883`) — and spawn/parse failures now exit `EXIT_USER_DIAGNOSTIC` instead of `EXIT_USAGE_OR_CONFIG` (`check_and_package_commands.rs:341-359`). Secret redaction is preserved. Restore the context args and exit-code classification, or record the change as intentional.

## Verdict

All three pass-1 majors and all four minors are substantively resolved with real, behavior-pinning regression tests; the driver refactor preserves main's semantics including the repaired opaque boundary; docs, architecture, plan claims, and blocking verification wiring are accurate and in place. The five minors above are follow-up material within Wave 5's remaining closure work, none violating the milestone's acceptance criteria (no path offers an unsupported or unrelated declaration as certified via completion; remaining parity gaps are conservative-direction and narrow).

**SATISFIED**
