# M11 Arrow C Data Interface — full milestone review, pass 7 (`main...HEAD` at 368b23606)

> **Historical status:** superseded by pass 8. The create-PR gate passed, but the default merge gate later failed at this commit; therefore this pass's original SATISFIED verdict was not final.

Full-milestone review of the complete `main...HEAD` diff (142 files, ~7,930 insertions over 7 commits), the M11 plan section and acceptance criteria, and all prior review artifacts (passes 1, 2, 3b, 4, 5, 6). Three parallel read-only audits ran over (a) runtime/CPython-capsule/Arrow-C-ABI/certification, (b) HIR lowering/ownership/affinity including an exhaustive classification-consumer sweep for the pass-6 F1 fix and a new-gap hunt across 16 expression/statement shapes, and (c) verification/evidence/docs/tracking artifacts. Every blocker- or major-class claim was reproduced or refuted live against the freshly built branch compiler (17 probe programs through `check`/`emit`/`build`/`run`), with pre-existing-versus-branch classification done by `git diff main..HEAD` on the cited sources. Constraints honored: no worktree created, no implementation file modified, `third_party/ruff` untouched (its single unstaged formatting-only hunk remains and must not be staged at merge), scratch probes written to an untracked directory and removed after the review.

## Scope

- Delta since pass 6 (which reviewed 69d316412 and returned SATISFIED with one new minor, F1): a single commit, 368b23606 "fix(python): preserve explicit pow method emission", touching `crates/sifr_lowering/src/lower/diagnostics.rs` (removes `__pow__` from `OPERATOR_DUNDERS` — the file is now byte-identical to `main`), `crates/sifr_lowering/src/lower/classes/parameter_conventions.rs:90` (adds `__pow__` to the `matches!(method_name, "__getitem__" | "__pow__")` special case in `reject_owned_affine_operator_parameter`), two test files, and the committed pass-6 artifact. Zero runtime, ABI, certification, driver, CLI, or evidence files changed since pass 6.
- This pass therefore (1) exhaustively verified the delta and its two explicitly-requested behaviors — explicit non-affine `__pow__` inherent emission and the async-comprehension move-diagnostic position coverage; and (2) re-audited the full milestone surface through the three parallel audits, which re-read every prior closure at HEAD. Its create-PR run completed, but its default merge-gate attempt did not complete successfully.

## Pass-6 finding disposition (every item verified live at HEAD)

| Pass-6 finding | Disposition |
|---|---|
| **F1 (minor)** — reclassifying `__pow__` as an operator dunder dropped inherent emission for non-affine `__pow__`; explicit `a.__pow__(x)` check-clean → E0599 | **Closed.** The fix is exactly the follow-up pass 6 prescribed ("the M-F treatment"): `__pow__` removed from `OPERATOR_DUNDERS` and added to the declaration-guard special case at `parameter_conventions.rs:90`. Classification consumers exhaustively enumerated — exactly three (`class_body_lowering.rs:625` emission routing, `parameter_conventions.rs:90` rejection, a re-export at `classes.rs:3`) — so the removal changes only emission routing, restoring `main`'s behavior verbatim (`git diff main..HEAD -- .../diagnostics.rs` is empty). Live at HEAD: non-affine `__pow__` emits `fn __pow__(&self, exponent: i64)` and the explicit call **runs** end-to-end (printed 6); `own values: list[int]` (owned non-affine) emits by-value and runs (printed 3); `def __pow__(self, own value: python.ArrowArray)` → SIFR-PYZC-0001 at declaration, likewise the `python.Buffer[uint8]` and keyword-only (`*, own value`) variants; borrowed-affine `__pow__` checks clean (no over-rejection); `a ** b` on class instances is main-equivalent by construction (byte-identical classification) and still fails loudly at rustc, reproduced live. Pinned by `explicit_non_affine_pow_dunder_remains_an_inherent_method` (`generic_inheritance_codegen_tests.rs:76-91`) — a genuine regression pin: it asserts both `fn __pow__` declaration emission and `power.__pow__(3_i64)` call-site emission, and fails if `__pow__` ever re-enters `OPERATOR_DUNDERS` (routing would send it to the arm-less `emit_operator_impls`). |
| **Pass-6 advisory** — filter/dict-key async positions probe-verified but unpinned; `affine_consuming_operator_parameters_fail_closed` had no positive control | **Closed.** `python_arrow_contract_tests.rs:472` adds the list-comprehension filter-position move and `:475` the dict-key-position move to the pinned OWN-0004 matrix (× Arrow/Buffer), and `:507-510` adds a `lower_ok` positive control (owned non-affine `__pow__` + borrowed affine `__getitem__` lower with zero errors). The async set-comprehension filter position remains unpinned (advisory A2 below) but shares `lower_single_async_generator` with the pinned dict path and was live-verified this pass (OWN-0004). |
| Pass-6 carried minors (imported-class dunder class-style calls; latent instance-method-export gaps masked fail-closed; cross-module inherited-not-redeclared CLASS-0004; `class_method_origins` not exported; genexpr-call-iter E0425; driver `>= 2` OWN-count assertion; near-cap files; dirty `third_party/ruff`) | Carried unchanged — the delta touches none of these surfaces; `git status` shows the same single unstaged formatting-only ruff hunk with the gitlink unchanged (`git diff main HEAD -- third_party/ruff` is empty). |

Earlier-pass closures (pass 1 B1–B7, pass 2 NB-1/NB-2/M-1–M-4, pass 3b B-1–B-3, pass 4 NB-A–NB-C/M-A–M-D, pass 5 NB-D/M-E/M-F/file-splits) stand: the runtime/certification audit re-read every closure in its area at HEAD and confirmed all present exactly as pass 6 cited them (zero files in that area changed since 69d316412); the tracking audit re-verified the acceptance-criteria and evidence mappings independently; and my live spot-checks (sequential double move → OWN-0001, sync while/for loop moves → OWN-0004, all six async shapes, guardrail scripts) reproduce as fixed.

## Explicitly-requested verifications

**Explicit non-affine `__pow__` emission (pass-6 F1)** — verified closed, as detailed in the disposition table above: inherent emission and explicit calls restored to main behavior and exercised end-to-end at runtime, while affine owned `__pow__` remains fail-closed at declaration for positional, keyword-only, Arrow, and Buffer shapes, with the rejection running in the class type-collection pass for every declared method regardless of emission classification (`class_type_collection.rs:668` positional, `:692` keyword-only).

**Async comprehension move diagnostics — position coverage** — verified live and by mechanism reading at HEAD, all four requested positions:

- *Iterable*: once-evaluated moves are correctly **permitted** — an owned `str` moved into the iterable of an async list comprehension checks clean and runs (printed 13). The snapshot is taken after the iterable lowering in all four paths, so iterable-position moves happen exactly once. Arrow/Buffer values in iterable position are impossible by design: the pre-existing M10 rule rejects async generators that capture affine Python resources (SIFR-PYZC-0001 "must be Send", reproduced live), so they fail closed at declaration.
- *Element/value*: rejected OWN-0004 live for list element (`[consume(value) async for i in readings()]`, ArrowArray), set element (ArrowStream), and dict value (ArrowDeviceArray).
- *Filter*: rejected OWN-0004 live for the list form (`[i async for i in readings() if consume(value) > 0]`, ArrowArray) and the set form (ArrowArray) — the latter covering the unpinned position.
- *Dict key*: rejected OWN-0004 live (`{consume(value): i async for i in readings()}`, ArrowSchema).
- *Statement form*: `async for` body move rejected OWN-0004 live (ArrowDeviceStream) — with the five Arrow kinds collectively covered across the six probes. `sifr emit` refuses the rejected shapes.

Mechanism confirmed by reading at HEAD: the moved-state snapshot is taken **after** the once-evaluated iterable lowering and **before** every per-iteration position — `async_for.rs:196` (iterable) → `:220` (snapshot) → `:228` (report); `async_comprehensions.rs:45` → `:75` → filter `:79-97` → element `:99` → report `:114` for list; shared `lower_single_async_generator` `:247` → `:273` → filter `:276-294` for set/dict, with element/key/value lowered in the callers under the same snapshot and reports at `:161` (set) and `:211` (dict), all through the shared `ownership_diagnostics::report_moved_across_loop` (`ownership_diagnostics.rs:160-168`). Fail-closed early returns (affine iteration, multi-generator, await-in-filter, async genexpr) all emit an error first.

## New findings

**Blockers** — none.

**Majors** — none.

**Minors**

- **N1 — owned affine value consumed in a `while` *condition* escapes the moved-across-loop check (pre-existing on `main`; loud, never silent).** `lower_while` lowers the condition before taking the snapshot (`statements/control_flow.rs:647` vs `:664`), so `while consume(value) > 0:` with `own value: python.ArrowArray` is check-clean. Reproduced live: the Arrow variant checks clean; the equivalent `str` variant checks clean and fails **loudly** at build with rustc E0382 (surfaced as SIFR-BUILD-0005) — the generated move-into-call inside `loop {}` can never compile, so no binary is ever produced and no runtime double-release is reachable. The file is byte-identical to `main` (`git diff main..HEAD` empty), so the branch's new Arrow kinds merely ride a gap that already applied to `str` and `python.Buffer`; it is the while-condition sibling of the sync/async body checks this milestone closed. Recommended follow-up (next time the loop lowering is touched): take the snapshot before condition lowering, mirroring the M11 async placement, and pin it.
- **N2 — the owned-affine operator/`__getitem__`/`__pow__` declaration guard runs only on the regular-class collection path; protocol-class declarations are not rejected (introduced with the guard itself in 554afe692/47a6c71d8; unchanged by the delta; inert for soundness).** `class P(Protocol): def __pow__(self, own value: python.ArrowArray) -> int: ...` checks clean (reproduced live) because the protocol path uses the unguarded `class_method_param_convention` (`class_type_collection.rs:441`, early return `:463`); enum and newtype paths return before the method loop. Single-transfer is not violated anywhere: a double consume through a protocol-typed receiver is rejected OWN-0001 (reproduced live), any conforming class must redeclare the method and is rejected SIFR-PYZC-0001 at its own declaration, protocols cannot be instantiated, and no operator-position dispatch exists for protocol receivers. This narrows the "fail-closed at declaration" contract, not the ownership guarantee — the same completeness-not-soundness profile as the pass-5/6 carried minors. Recommended follow-up: route the protocol method-collection loop through `class_declared_method_param_convention`.
- **N3 — the plan header's gate claim was ahead of its committed evidence (tracking; resolved empirically this pass).** `plans/issues/active/ad-hoc-declaration-first-python-interop.md:42-43` has claimed since the first M11 commit that the waves "pass the authoritative create-PR **and merge** gates", but passes 4–6 documented create-pr runs only, none at HEAD. This pass ran both profiles at HEAD 368b23606 (see Validation), which discharges the claim's substance; the header still needs its planned merge-time rewrite (pass-6 bookkeeping list), at which point the narrative should cite the gate runs it means.

**Advisories** (none gate; grouped by area)

- *Lowering*: A1 — `lower_single_async_generator` skips `ctx.scope.pop()` (`async_comprehensions.rs:295`) and the caller's loop-move report when filter lowering itself fails (`?` at `:279`/`:285`); reachable only after another diagnostic has been emitted, so fail-closed. A2 — async set-comprehension filter-position move is live-verified (OWN-0004) but unpinned; same code path as the pinned dict-key case.
- *Runtime/certification* (all verified non-gating by the area audit): A3 — `set_arrow_certifications` falls back to an empty identity on an (currently impossible) serialization failure (`build/python_runtime.rs:92-93`), which would silently stop certification changes from perturbing cache identity. A4 — `PythonArrowArgument::finish` returns before `drain_pending_releases` on a consumption-state error (`arrow_ops.rs:71-78`); no leak (entry/object still drop under the GIL; queue drains on next attach). A5 — `store_arrow_capsules` takes RUNTIME_STATE while holding ARROW_STORE + GIL (`arrow_ops.rs:582-584`), a theoretical lock-order inversion with `initialize_runtime` that generated programs cannot reach and that predates this branch. A6 — each owned-argument proxy leaves a small class↔globals reference cycle for CPython's cyclic GC (`arrow_ops.rs:358-408`); capsule release stays deterministic.
- *Evidence/docs/tracking*: A7 — `runner/arrow_evidence.py` does not cross-check the free-text `surface`/`covers` evidence fields (same rigor as the M10 buffer validator; the mappings were manually verified in pass 6 and re-verified this pass). A8 — `arrow_capsule_contract.json` `expected_copy_possible` fields are inventory-only (presence/shape validated, values not asserted; the values are factually correct against `arrow_ops.rs:621-627`). A9 — the cpython311 uv project is still named `sifr-python-buffer-cpython311-verification` (`cpython311/pyproject.toml:2`) though it now also backs the Arrow lane; its lockfile delta is exactly one clean `pyarrow 22.0.0` block (sdist + cp311 wheels, all hashed), nothing else moved. A10 — review-pass numbering skips a committed "pass-3" (only the gitignored `pass-3.claude.log` exists; `pass-3b.md` carries the content and later passes cite it) — a one-line note in pass-3b would remove the ambiguity. A11 — seven branch-touched files sit at 890–899 lines (`core_and_calls.rs` 899; `query_diagnostics.rs`, `lib_runtime_needs.rs`, `check_and_package_commands.rs` 898; `class_body_lowering.rs` 896; `class_type_collection.rs`, `rust_interop_bridge_contract.rs` 894) — guardrail passes, headroom nearly exhausted.

## Acceptance criteria (independently re-verified at HEAD)

- **"Arrow declarations have no copy switch and never certify uncertain copying" — MET.** Decorator grammar accepts only `schema=` (`python_interop/arrow.rs:157-193`); the raw runtime hardcodes conservative `copy_possible: true` for protocol exports (`arrow_ops.rs:620-627`); copy/uncertain evidence rejected at all four layers — artifact (`arrow_certification.rs:144-149`, release-count `:161-166`), CLI (`python_cli.rs:370-379`, drift recheck `:294-327`), build (`build/python_interop.rs:98-135`), runtime (`arrow_ops.rs:193-211` exact target/kind/producer match on every acquisition, releasing capsules on rejection). A branch-wide grep found no permissive switch.
- **"Ownership transfers once and remains moved even if a consumer later fails" — MET.** Static single-transfer matrices intact across calls, methods, constructors, super/static/callable forms, sync loops/comprehensions/genexprs, and — since pass 6, now with filter/dict-key pinning — the async family (contract tests `:277-378`, `:414-475`, `:527-544`); runtime commits before the call (`prepare_arrow_argument` removes the store entry pre-call, `arrow_ops.rs:213-226`, never re-inserted) and reconcile preserves the consumer's original error (`stdlib arrow.rs:68-76`); requested schemas are one-shot (`take_requested_schema` removes pre-call, `arrow_ops.rs:281-339`; one-shot proxy `_require`, tests `:258-294`). Live probes this pass: OWN-0001/OWN-0004 across 10 negative shapes, zero false rejections in the positive controls.
- **"Unconsumed resources release exactly once" — MET.** Capsule destructors are the single release point, no-op once the C `release` field is NULL; double-release → `SifrPythonClosedArrowCapsule` (`arrow_ops.rs:166-181`, test `:190-195`); instrumented exact-count tests (`tests.rs:216-256`); partial pair consumption errors after the entry drop so the unconsumed half still releases exactly once (`:79-86`, `:244-256`); evidence-side exactly-one-release gate (`arrow_certification.rs:161-166`).
- ABI layouts for all five structs re-verified field-by-field against the Arrow C Data/Device/Stream specs (`abi.rs:18-79`), capsule names exact, alignment-checked derefs (`abi.rs:344-355`), device-type whitelist per spec, and no data-dependent unwrap/expect/panic in user-reachable runtime paths. Validation items V1–V5 re-mapped to tests/fixtures/lanes by the tracking audit (all nine evidence owner files exist; the pandas/PyArrow/Polars evidence is measured, not asserted; device-kind evidence honestly labeled synthetic per declared scope). Docs consistent (capability matrix `arrow-c-data` active / `dlpack-transfer` reserved; PYZC error-code registry note matches; all spot-checked line references accurate; no phantom capabilities). Plan checklist correctly all-checked except the milestone-review item (`:1665`), top-level M11 box open (`:180`).

## Validation performed

- Built the branch compiler (release + debug); ran 17 live probes through `check`/`emit`/`build`/`run`: the F1 matrix (inherent emission + explicit call run, owned non-affine run, affine owned rejection × Arrow/Buffer/kwonly, borrowed-affine clean, `**` loud), the async position matrix (element/value × list/set/dict, filter × list/set, dict key, `async for` body — five Arrow kinds covered), iterable positive control (ran, printed 13), Arrow-iterable fail-closed, sync regressions (OWN-0001, OWN-0004), and the two new-minor reproductions (while-condition, protocol `__pow__`, protocol double-consume).
- Focused suites: `sifr_lowering` full crate **811/811** (python_arrow_contract 27/27 with the four new pinned shapes); `sifr_codegen` pow tests 4/4 including the new emission pin, python_arrow 9/9, python_interop 34/34; `sifr_runtime --features python` arrow 13/13; `sifr_driver` affine-class-metadata test 1/1. Guardrails: `check_hir_maintainability_guardrails.py` PASS; `check_file_size_guardrails.py` PASS (2,744 files).
- Authoritative gates at HEAD 368b23606: `scripts/run_all_tests.sh --profile create-pr` → **pass** (exit 0; e2e 131/131, report signature `7c39b8c1dd4fec7c`, identical to passes 5–6; python_interop lane pass including `arrow-examples` 28.9s and `arrow-cpython311`, whose report artifact was freshly rewritten during the run — `compatibility-passed`, CPython 3.11.14, all 13 expected runtime tests, exit 0; hardening 6 variants, zero failures; all blocking budgets pass; sole advisory the non-blocking warm wall-time notice, 479.1s). `scripts/run_all_tests.sh` (default **merge** profile) → **fail** (exit 101): `test_emit_pass_fixtures_do_not_include_unwrap_or_expect` rejected `nominal_identity_alias_paths.sifr` because its non-affine operator returned a borrowed RHS. Fixed and re-reviewed in pass 8 at d727cd692.
- Three parallel read-only audits (runtime/ABI/certification; lowering/ownership; evidence/docs/tracking). Every consequential agent claim at minor-or-above was reproduced live (N1 while-condition check-clean + loud E0382; N2 protocol declaration acceptance + OWN-0001 inertness; set-comp filter OWN-0004) or verified directly against source (the three-consumer classification sweep, snapshot placements, plan-header history via `git log -L`).

## Merge-time bookkeeping (not gating)

At merge, per M8–M10 precedent: check the milestone-review box (plan `:1665`) and the top-level M11 box (`:180`); add the PR link and the M11 closure narrative with links to review passes 1–6 and this pass; rewrite the header narrative (`:42-45`) to reflect merged state and the gate runs it cites (N3); and do **not** stage the dirty `third_party/ruff` formatting-only submodule change.

## Bottom line

The single commit since pass 6 closes F1 with the exact prescribed mechanism and no side effects — emission classification is byte-identical to `main`, the affine-owned rejection is preserved at every regular-class declaration shape and now positively controlled, and both behaviors are pinned by tests that would catch regression. The two explicitly-requested verifications hold under adversarial live probing: explicit non-affine `__pow__` emits, calls, and runs as an inherent method while affine owned `__pow__` stays fail-closed; async move diagnostics cover iterable (permitted once, or fail-closed for affine resources), element/value, filter, and dict-key positions across the comprehension forms and the statement form. The full-milestone re-audit found no new blocker or major: the two new minors are a pre-existing, loud, build-time-only `while`-condition gap inherited from `main` and a declaration-guard scope limitation whose every escalation path is independently rejected; the tracking gap was discharged by running both authoritative profiles at HEAD in this pass. All three acceptance criteria are independently re-verified at HEAD with code, test, evidence, and gate references.

VERDICT: SUPERSEDED BY PASS 8
