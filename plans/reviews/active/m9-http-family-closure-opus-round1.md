Reviewing evidence in the codebase, I can now give my read.

## M9 HTTP Family closure review

### Evidence verified

- **Public boundary correctly split**: `stdlib/sifr/http.sifr:1-13` re-exports validation helpers from `_sifr.http`; pure Sifr wrappers (`Method`, `Status`, `HeaderMap`, `RequestHead`, `ResponseHead`, `HttpRequest`, `HttpResponse`, `BodyStream`) live in `stdlib/sifr/http.sifr:24-264`.
- **Native declarations minimal and typed**: `stdlib/_sifr/http.sifr:12-51` declares only 8 native helpers backed by `sifr_stdlib.http.*`. `crates/sifr_stdlib/src/http.rs:83-181` implements them and depends only on `sifr_runtime::interop::SifrIntBridge` — no reverse dependency into `sifr_runtime` transport.
- **Prefix dispatch/preamble/fallback gone**: `crates/sifr_codegen/src/intrinsics/registry/url_http.rs`, `crates/sifr_codegen/src/preamble/url_http_runtime.rs`, and `crates/sifr_retained_intrinsics/src/http.rs` are absent from their respective directories (verified by directory listings). No `_sifr.http`/`sifr.http` references remain in codegen intrinsics or preamble sources.
- **Retained manifest closure**: `internal_docs/stdlib_retained_compiler_intrinsics.toml:104-112` records `_sifr.http::header_helpers` as `state = "closing"` with M9a removal criteria attached, matching the phase's design rule at `plans/issues/active/ad-hoc-stdlib-native-boundary-completion.md:52-54` (`closing` is the correct transitional state before M13).
- **Dependency snapshot correctness**: `crates/sifr_stdlib_manifest/tests/network_http_dependency_snapshots.rs:341-397` locks module-only `sifr.http` to `sifr_stdlib[http]` (no raw `http = "1.4.1"`, no cookie crate, no percent-encoding); `url-header-cookie` snapshot at `:191-206` matches; `http-transport` snapshot at `:208-252` keeps the raw crate promotion when codegen requires it explicitly.
- **Migration closure guard**: `scripts/check_stdlib_migration_closure.py:100-107` includes all eight `http_*` helpers in the observed-surface allowlist. Guards are wired into the create-PR profile at `verification/runner/sifr_verify/profile_runner.py:352-366`.
- **Transport substrate is documented verification-owned**: `docs/network_http.md:58-68`, `internal_docs/network_http_architecture.md:5-8`, `verification/areas/stdlib_parity/reports/network_http_http_transport_traceability.md:7-13`. `sifr.http_transport` is compile-time rejected via `SIFR-IMPORT-0009` (`crates/sifr/tests/e2e/fail/network_http_sifr_http_transport_internal.sifr:1-2`).
- **Transport semantics proven**: HTTP/1.1 + HTTP/2 client/server loopback in `verification/areas/runtime_platform/fixtures/http_transport_loopback.rs`; HTTPS/H2 ALPN loopback in the same fixture; SETTINGS/HPACK/GOAWAY at `crates/sifr_runtime/src/http.rs:672-714`; RST_STREAM cancel mapping at `:717-767`; malformed-response mapping at `:604-632`; independent request/response body limits at `:634-669`; timeout support at `:48-64` used by all six transport entrypoints.
- **Design-rule alignment for keeping `sifr_runtime/src/http.rs`**: The phase's own rule (`plans/issues/active/ad-hoc-stdlib-native-boundary-completion.md:40-42`) explicitly reserves runtime ownership for "low-level process, network, TLS, HTTP, Python, and runtime state." M9's task list says "Keep HTTP client runtime substrate in `sifr_runtime` only where reusable" — the Hyper/H2 substrate at 768 lines is reusable transport, not stdlib behavior.

### Answers

1. **Blocking gaps before M9 can be marked merged/complete?** No. The three acceptance criteria (HTTP behavior stdlib-owned, no test harness in stdlib impl, no HTTP prefix intrinsic in active dispatch) are all met with primary evidence. The one nuance is that the M9 task text at `plans/issues/active/ad-hoc-stdlib-native-boundary-completion.md:951` names "redirect" among semantics to prove, while `docs/network_http.md:60` correctly defers redirect to the future production HTTP client capability — this is a phrasing tension in the acceptance list, not missing evidence, since the docs explicitly place redirect policy outside this substrate. Non-blocking; worth a one-line clarification in the phase text when M9 is marked merged, but not a gate.

2. **Public HTTP client/server transport APIs required?** No. The documented verification-owned transport boundary is acceptable and consistent with the phase's own runtime-ownership rule (line 40-42) and the M9 task wording ("Keep HTTP client runtime substrate in `sifr_runtime` only where reusable"). Production client/server APIs are explicitly deferred to `network-http-serving-scale-capability` and the future production HTTP client per `internal_docs/network_http_architecture.md:37-41`. Forcing them into this phase would violate the "keep changes focused on the requested milestone" rule and duplicate the deferred capability.

3. **Is `_sifr.http` declaring only native helper ops acceptable?** Yes, and forcing request/response/body classes into `_sifr.http` would be incorrect. The phase's own design rule (`:33-36`) says `_sifr.*` is the private declaration source boundary for trusted sysroot Rust interop; pure Sifr classes with no Rust interop belong in `stdlib/sifr`. `RequestHead`/`ResponseHead`/`BodyStream` are pure Sifr wrappers around validated native primitives, so keeping them in `stdlib/sifr/http.sifr` is the correct application of the source-origin rule.

4. **Docs/guards/retained manifest/dependency snapshots sufficient?** Yes. Retained manifest carries the `closing` row with removal criteria; migration closure guard covers all 8 helpers; dependency snapshots lock both module-only and codegen-required transport paths and are pinned in tests; docs (public + internal + traceability) all describe the same boundary consistently; `SIFR-IMPORT-0009` rejection covers the `sifr.http_transport` shape.

### Verdict

**No blocking findings. I am satisfied to mark M9 complete** and update the phase tracker row from "in progress" to "merged" with the M9a evidence already captured. The lone item worth doing at closeout is a one-line clarification in the M9 acceptance text noting that `redirect` (and other client-policy items already listed as "intentionally outside" in `docs/network_http.md:60`) is owned by the future production HTTP client capability, not this substrate — non-blocking editorial cleanup.
