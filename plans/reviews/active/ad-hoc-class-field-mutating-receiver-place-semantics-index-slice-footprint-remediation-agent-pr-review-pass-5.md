# Review: PR #3094 — index/slice argument footprint remediation (final evidence pass) @ `01bf3aff1` (base `9c99ef43b`)

I made **no** repository modifications and ran **no** Cargo build/test/clippy, Sifr compilation, corpus sweep, broad validation, or performance probe, and spawned no subagent. All conclusions come from reading the diff, the surrounding ownership/place code, the pass 1–4 records, PR metadata, and the supplied run artifacts.

## Findings

**1. Non-blocking — PR #3094's Validation/Review section is stale: it still declares the authoritative gate unrun, and records neither the transient LSP failure nor the retries that cleared it.**
PR body (`gh pr view 3094`), "Validation" bullet: *"authoritative `scripts/run_all_tests.sh --profile create-pr`: pending an uncontended host window"*, and "Review": *"A final evidence review will run after the authoritative gate."*

The authoritative retry has since completed green (`/private/tmp/class-field-index-slice-01bf3aff1-create-pr-pass2.log`, `target/validation_lane_reports/create-pr.latest.json`, wall 1309.02s, 24/24 lane steps `status=pass`, e2e `140 pass tests completed (140 passed, 0 failed)`, `report_signature=ac6d879686517f2c`), and the first run's single blocking failure was cleared by the focused official retry (`target/verification/areas/developer-tooling-lsp-smoke-retry-results.json`, 6/6 variants pass, `blocking_failures: 0`). None of that is in the PR record a merger reads. Closure: update the PR body's Validation/Review section with the green create-pr result (wall time, e2e 140/140 + signature `ac6d879686517f2c`), the transient `lsp-protocol-smoke` failure (`returncode -9`, "LSP did not exit after stdin close"), and its 6/6 retry.

**2. Non-blocking — the committed pass-4 record quotes a stale lowering test count as exact-head evidence.**
`plans/reviews/active/ad-hoc-class-field-mutating-receiver-place-semantics-index-slice-footprint-remediation-agent-pr-review-pass-4.md:34` — *"the reported exact-head results (six focused tests green, … **953 passed/1 ignored** …)"*.

At `a813b9971` (= this head's code) `sifr_lowering` reports **954 passed; 0 failed; 1 ignored** (pass-2 log line 1529), which the PR body also states. 953 is the count at the pass-3 head `0b6bebdfc`, which carried five focused tests rather than six (954 − 6 new = 948 baseline; 948 + 5 = 953). The number does not affect any conclusion — all six focused tests are individually confirmed green (pass-2 log lines 1135, 1137, 1138, 1143, 1144, 1146) — but the branch's evidence record cites a count that contradicts the authoritative run. Closure: correct the figure to 954 in that record (or in a follow-up record).

No blocking findings.

## Evidence verified

- **Exact head and tree identity.** `HEAD = 01bf3aff14b925fe73e5cb08692021e1cc7e0af0`, matching `headRefOid`; base `9c99ef43b` is an ancestor; `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. `git diff a813b9971 01bf3aff1` is exactly one added file — the pass-4 review record — so the implementation/test tree is byte-identical to the pass-4-approved code. Working tree has no tracked modifications (only an untracked 0-byte `…pass-5.md` placeholder, which I did not write to).
- **Authoritative retry is genuinely green at this head.** `create-pr.latest.json` shows every `lane_steps` entry `status=pass` with `budget_status=pass`, including all blocking ones (`python_interop` 495951/600000, `e2e_pass_suite` 388464/600000, `crate_tests` 148291/600000, `developer_tooling_checks` 89099/180000, `runtime_platform_suites` 67880/120000); zero non-`pass` `case_timings`; `hardening_summary` 0 failures. The only advisory is `warm wall-time budget exceeded` (`within_warm_budget: false`) — a non-enforcing target on a contended host, not a gate. The pass-2 log is a byte-exact superset of the canonical `create-pr.latest.log` (prefix-verified via `cmp`; the extra 2231 bytes are the trailing stdout copy of the same report), so the log I was given is the run that produced the JSON.
- **E2E.** `140 pass tests completed (140 passed, 0 failed)`, `report_signature=ac6d879686517f2c`, run against `create_pr_e2e_manifest.json`, which holds exactly 140 unique names including the new `class_field_dynamic_index_base_disjoint`. The annotated-fail corpus is 566 files, matching the reported 566/566 (note: `test_e2e_fail` is not part of the create-pr lane — `sifr_cli_full` is `modes: ["full"]` — so the new fail fixture is covered at the merge gate and by the same source pinned in unit test 1; that is profile design, not a defect here).
- **Transient LSP failure is unrelated to this diff.** Run 1 failed only at `developer_tooling_checks` (`lsp-protocol-smoke`, `returncode -9` after `workspace/executeCommand`, "LSP did not exit after stdin close") and aborted before `crate_tests`/`e2e_pass_suite` ever ran. The diff touches only `sifr_lowering` receiver-overlap footprint collection plus tests — no LSP, driver, CLI, or runtime code — and the shape is a process-exit timeout, not a diagnostic mismatch (`mismatches: []` everywhere in the retry). The same case passed twice at this exact head (retry 15762ms; in-lane 80894ms).
- **Fix correctness (re-derived, not inherited).** `extract_place` (`method_receiver_places.rs:398-429`) has no `Index`/`Slice` arm, so both subscript forms always reach `footprint.rs:104`/`:111`; `root_binding_id` (`:614-622`) returns `None` through a `MethodCall` base, which is exactly the hole — pre-fix the base contributed nothing. The two added `collect_footprint(object, …)` calls (`footprint.rs:108`, `:121`) close it. The `Footprint::Dynamic(root)` push is retained ahead of each recursion and stays load-bearing (`:20` matches any same-root place, so recursion can never widen a name-rooted base; for a resolvable `FieldAccess` base it yields a strictly narrower place). Recursion is on strict sub-expressions (terminates), duplicates are inert under the existence test at `:18-21`, and the `match` has no `_` arm.
- **Coverage matrix, both arms, both directions.** Unresolved base: reject index (unit test 1 + `mutable_receiver_dynamic_index_base_overlap.sifr`, `col=28` correct), reject slice (test 2), accept (test 3 + `class_field_dynamic_index_base_disjoint.sifr`, which asserts `apply() == 7`, `owner.value == 7`, `owner.values == [7]`). Nested subscript: reject index (test 4), reject slice (test 5), accept with both halves live (test 6, `replace` mutates `self.values` so the receiver target exists). All six ran green in the authoritative retry.
- **Records honest, tracking consistent.** Passes 1–4 each state their read-only constraints and their findings track the actual head progression; every prior finding is closed in code, and only finding 2 above misstates a figure. Commits after `a813b9971` contain review records only. Absence of a `plans/` roadmap/issue edit matches the immediate precedent `ac31b0908`, which likewise shipped code + fixtures + manifest with records landed separately.
- **Guardrails.** `footprint.rs` 278, `method_receiver_places.rs` 684, `mod.rs` 252, new test module 205, fixtures 51/18 — all under 900; the lane's own `file-size guardrails: PASS (3077 files, limit 900 lines)` and `lowering maintainability guardrails: PASS` confirm this at head.

Non-findings considered and dismissed: root-only coarseness of `Footprint::Dynamic` and `Place{self}` over-approximation from a `self`-rooted `MethodCall` base (pre-existing policy, now applied uniformly to subscripts); duplicate `Dynamic` pushes on nested subscripts; `range_for` duplication (idiom in 13 other test modules); non-alphabetical manifest placement (`BTreeSet`-loaded); `values` field-name reuse across fixture classes; missing `__init__` in unit sources and the fail fixture; the `SpecializedIndexedStorage` receiver-index path (pre-existing, out of scope); `warm wall-time budget exceeded` (advisory-only on a contended host).

**NOT SATISFIED** — the diff, its coverage, and the authoritative evidence are all sound and merge-ready on substance; two non-blocking evidence-record corrections (stale PR Validation section, and the 953→954 count in the pass-4 record) remain actionable.
