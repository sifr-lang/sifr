**Findings (severity-ordered)**

1. **[HIGH — scope/verifiability] Guard script is not in this diff.** `scripts/check_stdlib_migration_closure.py` is referenced by the new `verification/policy/guardrails.json` entry, by `verification/runner/sifr_verify/profile_runner.py:297-298`, by three claims in `plans/issues/active/ad-hoc-sifr-sysroot-stdlib-toolchain.md` wave 4 evidence, and (implicitly) by the architecture doc's promise that "the migration closure guard fails if those retired intrinsic names reappear in the active dispatcher" — but the script itself is not part of the diff shown. The review question explicitly asks about "false positives/negatives in the new guard" and "missing retired names"; neither is answerable from this diff. The primary artifact of wave 4 is missing from what is under review. `git status` in the header shows it as `?? scripts/check_stdlib_migration_closure.py` (untracked), so the branch has it in the working tree but not staged; if this diff is meant to represent the wave 4 change set, the script must be included so its retired-name list, dispatcher-arm detector, deleted-registry regex, and stale-phrase list can be inspected.

2. **[MEDIUM — durable-doc taxonomy] "Callback" surface bucket is dropped from the private-declarations row.** `internal_docs/sifr_sysroot_and_stdlib_architecture.md` (row-27 replacement) now reads: "…retained runtime/resource leaves remain compiler-owned and allowlisted." The wave intent (per the request and per the architecture doc's own "Runtime/resource/callback surfaces follow the Rust interop certification gate" line ~735 elsewhere in the file) uses the three-part taxonomy resource/runtime/callback. Dropping "callback" from the row makes the row's taxonomy narrower than the certification-gate paragraph it must line up with, and readers cannot tell from the row alone whether callback leaves fall under retained-allowlisted or under a third bucket. Recommend making the row say "retained runtime/resource/callback leaves" for consistency, or add a one-line note explaining the omission.

3. **[LOW — evidence claim vs. diff scope] Wave 4 evidence lists validation over surfaces not in this diff.** The wave 4 evidence bullet in `plans/issues/active/ad-hoc-sifr-sysroot-stdlib-toolchain.md` includes `cargo test -p sifr_codegen intrinsics`, `coverage_matrix readiness`, `check_stdlib_native_intrinsic_allowlist.py`, and `check_sysroot_stdlib_resource_certification_gate.py`. None of those surfaces are touched by this diff, so those runs are just regression-suite smoke rather than closure-specific evidence. Not wrong, but as written it can be read as "the closure guard was validated end-to-end," when the closure-specific evidence collapses to `python3 scripts/check_stdlib_migration_closure.py` and `--self-test` — which cannot be re-verified in review because the script is absent (see finding 1). Consider tightening the evidence bullet to separate closure-specific runs from broader guardrail smoke.

4. **[LOW — wiring ordering, cosmetic] Profile runner adds the closure guard immediately after the allowlist guard, both under the same implicit "stdlib native intrinsic" logical group, but without a group-level heading.** `verification/runner/sifr_verify/profile_runner.py:297` inserts `print("Running stdlib migration closure guard")` between the allowlist self-test call and "Running audit fixture smoke suites". Functionally fine; a passing/failing operator reading the log gets separate messages. No change required — flagging only in case future maintainers group these into a shared "stdlib guardrails" phase.

**Scope confirmation**

- Retained runtime/resource/callback surfaces are correctly not migrated in this diff.
- Retained-allowlist TOML (`internal_docs/stdlib_retained_compiler_intrinsics.toml`) is a distinct file from the deleted `stdlib_native_surface_ownership.toml`; the doc keeps the former and drops references to the latter, which is the intended split.
- Guardrails.json entry format matches the sibling `stdlib-native-intrinsic-allowlist` entry (same `args: []`, `timeout_seconds: 60`, `report: stdout`); no formatting drift.
- Deleted-registry wording appears to be fully removed from `sifr_sysroot_and_stdlib_architecture.md` within the three edited windows; cannot confirm the rest of the file or other durable docs from the diff alone — that guarantee is what the (missing) guard script is supposed to provide.

**VERDICT: NEEDS_CHANGES**

The primary blocker is finding 1: the closure guard script itself is not in the diff, so the review cannot validate the guard's retired-name coverage, dispatcher-arm detection, or false-positive behavior — all of which the wave's premise rests on. Finding 2 (dropped "callback" taxonomy) is a small but real doc drift worth fixing in the same wave.
