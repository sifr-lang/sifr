All seven parallel investigations are complete. Here is the consolidated closeout review.

# Ad Hoc Stdlib and Compiler Boundary Rearchitecture — Phase-Wide Implementation Review

Scope: `d2306d3b31c8..bf2ad72d0` (M1–M6 merged + closure draft), 226 files touched.

## Milestone verification (all independently confirmed)

**M1 — Runtime observability boundary.** The exact-shape bridge lives at `crates/sifr_stdlib/src/runtime_observability.rs:6-61` with `Result<(), String>`, five tracing levels, accepted/rejected counters with bounded labels, and no panic surface on user data. `stdlib/_sifr/runtime.sifr:8-15` declares `runtime_emit_diagnostic` via `@rust(sifr_stdlib.runtime_observability.emit_diagnostic)`; the public wrapper at `stdlib/sifr/runtime.sifr:51-56` delegates to it. Compiler lowerer, dispatch arm, and fallback signature are all removed; manifest observability entries are gone; `metrics`/`tracing` are absent from `retained_direct_dependency_packages` (both in the TOML and in `crates/sifr_stdlib_manifest/src/features/dependency_plan.rs:266-282`). The standalone regression at `crates/sifr/tests/runtime_observability_boundary.rs:54-76` drives `check/emit/build/run` and asserts both positive (`sifr_stdlib::runtime_observability::emit_diagnostic` present) and negative (`metrics::`/`tracing::` absent).

**M2 — Authoritative E2E dependency planning.** `grep -rn 'infer_dependencies\|infer_dependency' crates/ verification/ scripts/` returns zero live hits; all seven prior E2E scanner/planner files are deleted. `SysrootDependencyPlan` is stored per fixture and group (`e2e_support/harness_model.rs:69,83`), and batches are keyed by `DependencyFingerprint { dependency_inputs, resolved_plan }` and refused if plans differ (`fixture_compilation.rs:267-270`). The M2 inventory drives typed-authority tests at `e2e_support/dependency_plan_authority_tests.rs:4-153`, and negative tests `missing_metadata_is_not_repaired_from_generated_rust` (:222) and `fixtures_with_different_production_plans_cannot_repair_each_other` (:238) prevent unioning/repair. Equivalence test `compiled_fixture_plan_matches_production_build_report` (:173) matches E2E and production build report identically; `cache_identity_changes_with_the_production_dependency_plan` (:261) proves invalidation.

**M3 — Typed CompilerIntrinsicId.** `CompilerIntrinsicId` (17 variants at `crates/sifr_ir/src/hir_nodes.rs:117-136`) and `HirExpr::IntrinsicCall { intrinsic, args, ty, call_range, arg_ranges }` (:516-523). Identity routes only through `HirFunction.compiler_intrinsic`, `ExternalDefs.compiler_intrinsics`, `LowerCtx.compiler_intrinsics`, and `ReExportMaps.compiler_intrinsics`; `FunctionType` (`crates/sifr_type_system/src/types/definitions.rs:246-253`) is signature-only. Codegen dispatch at `crates/sifr_codegen/src/intrinsics/registry.rs:25-69` is a total match over all 17 IDs. Raw-name dispatch is gone from `HirExpr::Call { func: String, .. }`; local shadowing reconciled at `mod_impl.rs:744-749`. `@compiler_intrinsic` is sysroot-only (`compiler_intrinsics.rs:27-79`), requires ellipsis-only body, and is rejected for user/package with structured diagnostics. First-class value rejection at `core_and_calls.rs:213-222`. All lowering sites listed in the plan emit `HirExpr::IntrinsicCall` directly. Collision tests exist for local, nested, method, and imported callables. The `bytes_to_hex_strict` bridge is fully migrated: adapter at `sifr_stdlib/src/bytes.rs:13-21`, declaration at `_sifr/bytes.sifr:13-15`, live caller `hashlib.sifr:182`.

**M4 — Collections residue removal.** All eight `counter_*` intrinsics are gone (signatures, dispatch arms, lowerers, registry-only tests). `stdlib/sifr/collections.sifr:44` retains `class Counter[T: Hashable]` and `from_list` at :240. `serde`/`serde_json` are absent from `retained_direct_dependency_packages` and from `retained_dependency_specs`. `defaultdict_new/get/set` residue is gone; typed defaultdict language semantics remain via lowering. Manifest row `sifr.collections::typed_defaultdict_language_semantics` and `sifr.bytes::primitive_constructors` (three IDs, no `bytes_to_hex_strict`) reflect the split. Public bytes wrappers at `stdlib/sifr/bytes.sifr:21-30` execute checked bodies verified by `stateless_private_codegen_tests.rs:245-272`.

**M5 — Fallback signature architecture deletion.** `crates/sifr_retained_intrinsics/` is deleted; `rg 'sifr_retained_intrinsics' crates Cargo.toml Cargo.lock` returns 0 hits. `re_export_intrinsic_fallbacks`, `resolve_retained_fallback`, `get_intrinsic_module`, `intrinsic_io` are absent from `crates/`. `_sifr/{io,test,task}.sifr` placeholders are removed. `fallback_signature_modules` is absent from `ALLOWED_SURFACE_FIELDS`. Bootstrap negative tests `missing_private_stdlib_member_is_a_structured_bootstrap_failure` and `missing_private_stdlib_module_...` (`bootstrap_tests.rs:208,233`) enforce `STDLIB_BOOTSTRAP_FAILURE` on missing declarations. The executable guard `_deleted_fallback_architecture_failures` at `check_stdlib_native_intrinsic_allowlist.py:146-175` runs unconditionally and its `--self-test` (:717-737) materializes a fixture with restored crate + forbidden tokens and asserts rejection.

**M6 — Reachability, guards, and recertification.** `scripts/check_stdlib_native_adapter_reachability.py:55-142` enumerates public `sifr_stdlib` adapters, cross-references `@rust(...)` targets from `stdlib/**/*.sifr`, and requires each substrate to have consumer-file evidence. Live count reproduces `403 = 399 + 4` per `internal_docs/stdlib_native_adapter_reachability.toml:7-25`. The retained-intrinsic guard performs set equality across typed IDs, dispatch variants, source declarations, manifest entries, and owned files (no substring/count-only comparisons). Retained-dep orphan check scans `sifr_codegen/**/*.rs` for live `StdlibFeature::X` and rejects orphan rows. Schema v2 backfills `lowering_files`/`codegen_files`. All seven required negative tests are present (`@compiler_intrinsic` user rejection, collision, removed `_sifr.*` imports, first-class use, missing declaration, orphan retained deps, deleted API/schema fields). Installed vs source-tree recertification at `verification/areas/sysroot_release/runner.py:174-233` builds `stdlib_boundary_recertification.sifr` twice, executes both binaries, and diffs normalized Cargo shapes against a reviewed snapshot; wired into merge profile. All four guards are declared in `verification/policy/guardrails.json:54-79` and invoked via `profile_runner.py:352-366` for every profile lane.

## Cross-cutting audit

- **File-size guardrail**: No touched file exceeds 900 lines; two exactly at 900 pass the strict `>` in `check_file_size_guardrails.py:169`.
- **Single sources of authority**: One `SysrootDependencyPlan`, one `CompilerIntrinsicId` enum, one retained-glue manifest.
- **Validation masking**: The three "inference" mentions in the harness are anti-masking guards, not masks.
- **Forbidden tokens**: `sifr_retained_intrinsics`, `re_export_intrinsic_fallbacks`, `resolve_retained_fallback`, `fallback_signature_modules`, `intrinsic_io`, `retained-fallback-signature-glue` appear only inside deletion-guard lists that actively reject them.
- **Panics**: No `.unwrap()/.expect()/panic!/assert!` in the runtime bridge.
- **Cargo/lockfile drift**: Only intentional removals (`sifr_retained_intrinsics` package + consumers); `metrics/tracing/serde/serde_json` correctly retained as internal `sifr_stdlib` deps because they are no longer generated into user projects.
- **Dependency direction**: `scripts/check_source_crate_dependency_direction.py` is modified (removing the deleted crate from its universe), not deleted; enforcement preserved. No lowering→codegen or ir→codegen edges.
- **Installed sysroot masking**: `runner.py` isolates per-label probe caches (:196-198), forces `CARGO_NET_OFFLINE=true`, and byte-parity-checks Cargo shapes.
- **Public API**: `sifr_driver::lib` additions are additive; no removed public re-exports without justification.

## Non-blocking observations (not required for phase closeout)

1. `plans/reviews/active/stdlib-compiler-boundary-phase-closeout-review-round1.md` is currently a 0-byte placeholder — this artifact is what you're populating with this review.
2. The deleted-token substring guard uses unanchored `if token in text`; over-matching is preferable here, but a future anchoring pass would tighten it.
3. `plans/phases/06_stdlib_architecture.md:285-336` and `plans/phases/13_type_system_completion.md:533` still describe deleted `counter_*` names in prose; the phase blueprint puts these updates in M6 scope but they were not touched. Consider a follow-up doc pass.
4. `DELETED_COLLECTION_RESIDUE_ROOTS` and `DELETED_FALLBACK_PATHS` in `check_stdlib_native_intrinsic_allowlist.py:74-92` reference `crates/sifr_retained_intrinsics`; the guard is filesystem-existence based so this is correct today, but a comment noting "must-not-exist" intent would help future readers.
5. Manifest schema does not require every retained surface to list its own `lowering_files/codegen_files`; the shared `typed-intrinsic-dispatch-core` surface owns them centrally. Accepted per phase spec.

## Summary

Every M1–M6 acceptance criterion is met with executable enforcement, not prose. Guard scripts use set equality or exact structural matching, not substring counting. Every architectural invariant (single dependency-plan authority, typed intrinsic identity, no raw-name dispatch, no fallback recovery, no source scanning, adapter reachability, installed/source parity) is backed by tests or self-tests that would fail on regression. Public API is preserved. No user-triggerable panics were introduced. No first-party file exceeds the 900-line cap. The full local merge gate remains the only outstanding step, which the phase blueprint explicitly defers past this review.

VERDICT: SATISFIED
