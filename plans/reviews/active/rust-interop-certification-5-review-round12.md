## Finding 1 — consuming-cleanup rule vs. published `callback_subscription_core` positive fixture: **CLOSED**

- Fixture repaired: `verification/areas/rust_interop/fixtures/callback_subscription_core/positive/signal_subscription_cancel_shutdown.sifr:27` is now `async def verify_signal_subscription_cancel_shutdown(own subscription: SignalSubscription)` (diff shows exactly this one-line change; `fixture.json` is untouched, so it remains `expected_result: pass` / `status: passing` — the finding was closed by fixing the source, not by downgrading the claim).
- Mechanism verified, not just the text: `borrowed_params` is populated only from params whose `convention.is_borrowed()` and whose type is `OwnershipKind::Move` (`annotations_and_function_lowering.rs:616-623`). An `own` param is not inserted, so the consuming-receiver check in `methods_lambdas_and_comprehensions.rs:226-238` takes the `HirExpr::Name { .. } => {}` arm instead of emitting `OWN_BORROWED_PARAMETER_ESCAPES`. The class side still satisfies the policy: `close=async_close` with exactly one `async def aclose(own self) -> Result[None, …]`.
- Guard references the exact fixture: `crates/sifr_lowering/src/lower/rust_interop_tests.rs:680-691` `include_str!`s that precise path and asserts `ownership_errors.is_empty()` after filtering `lower_module`'s `Err` set to `OWN_BORROWED_PARAMETER_ESCAPES` only — so unrelated prelude diagnostics don't mask or falsely trip it.
- The relaxation is not vacuous: round-11's own probe on this same file (before the `own` edit) surfaced `SIFR-OWN-0003` for `subscription`, so the ownership check demonstrably runs to completion on this source despite the `ThreadsafeCallback` artifact. A regression to a borrowed parameter would still be caught.
- `opaque_resource_matrix/positive/resource_close_aclose_matrix.sifr` is in the strict full-lowering list (`rust_interop_tests.rs:658-666`), which requires `lower_module` to return `Ok`. Its cleanup consumes an owned local (`resource: ResourceMatrix = await open_resources()` → `await resource.aclose()`), and the earlier borrow via `run_resources(resource)` is permitted by the same `Name` arm.

## Finding 2 — reserved message aliases initialize only `message`: **CLOSED**

- Both arms now expand to the full field set in `crates/sifr_codegen/src/rust_interop_error_mapping.rs`:
  - `Type::Alias` path (`:19-30`) maps `message_error_fields(fields)` over every name.
  - `Type::Class` reserved-name path (`:47-60`) likewise uses `message_error_fields(fields)` rather than a hardcoded `message`.
- `message_error_fields` (`:261-277`) returns **all** declared field names when every field resolves to `Str` and one is named `message`; otherwise `None`. That contract is exactly what the gate `opaque_self_state_error_is_representable` (`rust_opaque_validation.rs:137-152`) enforces (has `message: str`, all fields `str`), so representable ⇒ every emitted initializer field is covered. The two are now aligned; no separate name-based rejection is needed.
- Reported repro is covered end-to-end: all three sites round-11 saw E0063 at funnel through this function — the `Closed` and `Poisoned` arms via `bridge_declared_error_expr` (`rust_interop_direct.rs:161-170` → `rust_interop_panic.rs:45-52` → `bridge_error_contract_expr`), and the bridge `map_err` via `rust_interop_panic.rs:138`. There is no other reserved-alias construction site (`class_method_emitter.rs:357/477` are `__init__` `Self { … }` builders, unrelated).
- Unit test pins it: `reserved_message_errors_initialize_every_declared_string_field` (`:315-337`) asserts `HttpError { message: …to_string(), detail: …to_string() }` for the exact reported shape.
- No one-field regression: with `fields = [("message", Str)]`, `message_error_fields` yields `["message"]`, and `runtime_diagnostic_string_errors_map_to_the_declared_error` (`:296-313`) still asserts the unchanged `DiagnosticError { message: … }` rendering. The `PythonError` shadow case still bails before the reserved arm (`shadow_python_error_does_not_use_the_runtime_struct_mapping`), and the non-reserved multi-field path (`:100-116`) uses the same helper, so behavior is uniform across all seven reserved names (`is_message_error_alias`, `:119-130`).

No actionable correctness issues found within this scope. One non-blocking observation: the callback guard asserts only the absence of `OWN_BORROWED_PARAMETER_ESCAPES` rather than full `Ok` lowering, unlike the other five fixtures; that is weaker coverage, but it is the correct shape given the isolated-lowering prelude diagnostics and is demonstrably non-vacuous for the rule it guards.

VERDICT: SATISFIED
