## Findings

**HIGH — Python context-entered borrow escapes via a `for` iterable (`crates/sifr_lowering/src/lower/statements/control_flow.rs:717`).**  
`lower_for` never runs the entered-borrow guard on the iterable expression, and the loop target is registered with neither `python_context_borrows` nor `live_must_use_bindings`. Consequently a `@python.opaque(cleanup=context)` entered value can be moved through a list literal (or any aggregate) into a fresh loop variable and then returned or aliased out of the enclosing `with` block — the exact "storage-escape" shape the round-2 review escalated as blocking for `ListComp`/`SetComp`/`DictComp`. Verified by adding a one-shot `#[test]` around the wave-3 `CONTEXT_OPAQUE_PREFIX` that constructs `for t in [transaction]: return t` inside a `try:/with make_transaction() as transaction:` and asserted the lowering must error: `lower_module` returned `Ok(_)` with no `PYCTX_INVALID_DECLARATION` diagnostic, panicking on the "expected rejection" branch. Root path: `for_stmt.iter` at control_flow.rs:717 is lowered without any `pyinterop::reject_python_context_borrow_*` call; `ctx.scope.define(target_name, elem_ty)` at :846 does not populate `python_context_borrows` or invoke `record_must_use_binding`; `transfer_return_ownership` (`return_lowering.rs:129-157`) then finds neither table populated for the loop var and lets it escape. `Stmt::AsyncFor` (control_flow.rs handled via async_for) shares the mechanism, so the same shape will regress into wave 8 unless the same guard is added.

Round-2 blocker is otherwise closed and independently reviewed: the ListComp/SetComp/DictComp/GeneratorExpr generator iter/filter recursion at `python_interop/context.rs:40-69` is comprehensive; the coverage tests at `python_interop_tests.rs:598-621` cover element/iter/filter/lambda/FString shapes; all HIR consumers (`traversal_impl.rs`, `flow_graph/effects.rs`, `nonlocal_support.rs`, `hir_snapshot_tests.rs`, codegen dispatch/validation) were updated to the new `HirWithItem`/`HirWithItemKind`; the codegen wave-4 gate is intact via a hard `CodegenError` in `stmt_support_emitter/async_with_and_for.rs:17-21`; `mark_moved_with_flow` (`mod_context.rs:383-401`) refuses to move a name registered in `python_context_borrows`; multi-item nested with save/restore of `previous_context_borrows` is proven by `python_with_supports_multiple_context_items_with_distinct_borrows`; native-`with` lowering is untouched (`Native` kind still flows through `try_lower_simple_with_stmt`/`try_lower_with_stmt_for_ir`); file-size guardrail holds (`control_flow.rs` at exactly 900 lines, all other touched files well under). But the confirmed for-statement gap is a real escape and violates the wave-3 acceptance criterion "opaque entered values … cannot escape, move, or close independently."

VERDICT: NOT SATISFIED
