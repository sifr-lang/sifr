# M15 LSP Declaration Authoring — Frozen-Diff Milestone Review (pass 8, commit 416ee9e3d vs origin/main)

**Verdict: SATISFIED.** The pass-7 minor is genuinely and verifiably closed: full diagnostic publication now schedules only documents whose `document_diagnostics` structurally succeeds, the publishability predicate provably preserves load-diagnostic documents while excluding exactly the analysis-unavailable ones, and the strengthened close-owner regression reproduces pass 7's empirical scenario end-to-end and is mutation-tested discriminating in both error directions (abort-on-unpublishable and skip-valid-documents). The pass-6 major remains closed, every prior finding remains closed, the residual ledger and the remediation's own documentation are accurate as written, and both acceptance criteria hold. No blocker, major, or minor remains open — only nit-grade items for the ledger's next touch.

## Scope and commit

Frozen PR #2995 diff, `origin/main..416ee9e3d` (60 files, +3565/−373). The delta since pass 7 (5c6f19973) is exactly two commits: `4c74771a7` (the remediation — the four-line publishability filter in `publish_all`, the three-line `can_publish_document_diagnostics` predicate in `session.rs`, the strengthened close-owner regression plus a shared `open_excluded_document` helper extracted from the election regression, the pass-7 closure paragraph in the plan, and the checked-in pass-7 artifact) and `416ee9e3d` (docs-only: the post-pass-7 gate paragraph in the plan, 8 insertions to one file). Every line of that delta was read. The untouched remainder of the diff carries the pass-1 through pass-7 verification: no file outside `diagnostics.rs`, `session.rs`, the notifications test file, and the plan changed since pass 7 — in particular `python_declarations.rs` (the election and its filter), `analysis_workspace.rs` (`can_analyze_document` and the loader-failure paths), `notifications/mod.rs`, and all driver/package/frontend surfaces are byte-unchanged since the pass-7 review, so nothing reopens the pass-6 major closure or any earlier-audited behavior.

Inputs reviewed: the M15 plan section, acceptance contract, closure paragraphs, and residual ledger (`plans/issues/active/ad-hoc-declaration-first-python-interop.md:2091-2290`, including the two new paragraphs at `:2267-2283`); `internal_docs/python_interop_declaration_architecture.md` (ownership paragraph at `:616-622`); `internal_docs/architecture.md`; `docs/python-interop.mdx`; and all seven prior M15 Fable review artifacts. Worktree hygiene: the dirty `third_party/ruff` submodule is the identical semantically-inert whitespace-only reformat observed at passes 4–7. No implementation or documentation files were modified by this review; all three temporary mutations were fully reverted and the suite re-run green at HEAD, confirmed by `git status` (only the pre-existing submodule drift remains).

Per instruction, no new full gate was started. The authoritative create-pr run recorded in the plan at **780.26s** on the remediation commit `4c74771a7` is accepted as validation evidence: Python interop 19/19, LSP 71/71, analysis 48/48, package 139/139, driver 382/382 plus 33 expected generated-build ignores, runtime-platform 28 variants with one capability-gated skip, and E2E 131/131 with signature `7c39b8c1dd4fec7c` and a warm 42/42 cache; every per-step blocking budget passes with warm wall time the only non-blocking advisory. `416ee9e3d` touches only the plan file, so that gate covers HEAD's code exactly. The ledger paragraph recording it (`:2275-2283`) matches these numbers clause-for-clause.

## Commands and evidence

- `cargo test -p sifr_lsp` at HEAD → **71/71 pass** (4.60s), matching the ledger and the recorded gate exactly (the remediation modifies two existing tests and adds a helper; the count is unchanged from pass 7).
- **Mutation 1 — filter removal** (temporarily replaced the filtered collection at `crates/sifr_lsp/src/diagnostics.rs:34-38` with the pre-fix `session.document_uris()`, then reverted): `closing_python_package_diagnostic_owner_republishes_on_remaining_document` **fails** at its sibling-republication assertion — byte-for-byte the pass-7 minor's failure shape (the excluded document's job aborts the drain before `z.sifr`'s handoff) — while `unanalyzable_document_cannot_own_python_package_diagnostics` still passes (the election filter is untouched by this mutation), confirming the two regressions pin independent layers.
- **Mutation 2 — predicate weakening** (temporarily dropped the `!self.load_diagnostics(uri).is_empty()` disjunct from `can_publish_document_diagnostics`, leaving only `can_analyze_document`, then reverted): the **full suite stays green, 71/71** — the load-diagnostic-preserving direction is verified by inspection (below) but pinned by no test. Recorded as a nit.
- **Mutation 3 — over-strict predicate** (temporarily forced the predicate to `false`, then reverted): the close-owner regression **fails** (no republication reaches the remaining document), so the test also guards the skip-valid-documents direction; an over-strict filter cannot land silently.
- Post-revert baseline: `git diff` clean of source changes, `cargo test -p sifr_lsp` → 71/71.
- `cargo fmt --check` → clean (exit 0); `cargo clippy -p sifr_lsp -- -D warnings` → clean. File sizes: `diagnostics.rs` 167, `session.rs` 759, `python_declaration_tests.rs` 166, `notifications/mod.rs` 204 lines — all under the 900-line cap.
- Static verification of the predicate/consumer match, publication-path coverage (`publish_all`'s only callers are the watcher batch at `notifications/mod.rs:115` and didClose at `:199` — both filtered), job-queue hygiene, and progress coherence, detailed below.

## Disposition of the pass-7 minor and all prior findings

**Pass-7 MINOR 1 (didClose owner handoff silently fails while an unanalyzable document is open) — CLOSED.** All three remediation pieces verified independently:

- **`can_publish_document_diagnostics` (`crates/sifr_lsp/src/session.rs:166-168`) matches `document_diagnostics`' structural success precondition exactly.** `document_diagnostics` (`crates/sifr_lsp/src/diagnostics.rs:126-167`) succeeds on the early return at `:132-139` iff the document's load diagnostics are non-empty — the predicate's first disjunct — and otherwise requires `with_document_analysis` to succeed, which `can_analyze_document` (`crates/sifr_lsp/src/analysis_workspace.rs:151-162`, byte-unchanged since pass 7) models per the pass-7 equivalence proof, modulo only the transient snapshot-superseded cancellation that is not a publishability property. The disjunct is load-bearing, not defensive: documents carrying non-empty load diagnostics — project-open failure attaches the project diagnostics to every member document with `host: None` (`analysis_workspace.rs:300-308`), and a per-document overlay failure inserts diagnostics while evicting the URI from `files_by_uri` (`:349-359`) — are precisely documents where `can_analyze_document` is false, so without the disjunct `publish_all` would silently skip republishing real load errors (mutation 2 confirms no test would catch that today). Conversely, loader-excluded documents like the fixture's nested `a/aux.sifr` get an **empty** load-diagnostics entry (`:333-335`, `:363-365`), so both disjuncts are false and they are correctly filtered. Net: predicate false ⟹ `document_diagnostics` fails; predicate true ⟹ it succeeds (modulo the pre-existing transient window) — **no valid document is skipped**.
- **The filtered `publish_all` (`crates/sifr_lsp/src/diagnostics.rs:34-38`)** narrows scheduling only; `flush_ready`'s abort-on-first-error drain (`:82`) is unchanged, but no unpublishable document can now enter the queue through either `publish_all` caller. Queue hygiene holds around it: `close_document` removes the closed URI's pending jobs before republication (`session.rs:138-145`), so no stale closed-document job can poison the didClose drain, and an excluded document's job scheduled by its own didOpen/didChange/didSave (`publish_document` schedules unconditionally, `diagnostics.rs:24`) is always popped before its error propagates, so it cannot persist into a later drain except behind a same-drain transient failure — a double-transient, self-healing window squarely inside the ledger's accepted open/change/save family and pre-existing before M15.
- **The strengthened close-owner regression (`crates/sifr_lsp/src/notifications/tests/python_declaration_tests.rs:6-54`)** now opens the excluded document before the close (`:8-9`), asserts its URI sorts below the owner's — the exact ordering that triggered pass 7's abort — and drives `textDocument/didClose` through the real notification handler over an in-memory connection, asserting both the explicit empty-diagnostics clear for the closed owner (`:40-46`) and a publication carrying `SIFR-PYZC-0001` on the remaining document (`:47-53`). This is pass 7's scratch reproduction, checked in. Mutation-tested discriminating in both directions (mutations 1 and 3 above). The election regression (`:56-68`) retains its full pass-7 semantics through the shared `open_excluded_document` helper (`:70-88`): same nested fixture, same sort assertion, same analysis-unavailable and owner-keeps-diagnostic assertions.

The pass-7 failure mode — the closed owner's clear published, then the drain aborted on the excluded document before the eligible sibling's handoff, with the package error displayed nowhere — is structurally gone, and the two documentation statements pass 7 flagged as falsified are now **true as written** rather than conditioned: the architecture doc's "Closing the current owner clears it and republishes the package diagnostic on the next eligible document" (`internal_docs/python_interop_declaration_architecture.md:620-621`) and the ledger's close-handoff clause now hold in the excluded-document scenario, with the dedicated regression covering exactly that combination.

**Pass-6 MAJOR 1 — remains CLOSED.** `python_declarations.rs` and `analysis_workspace.rs` are byte-unchanged since pass 7; the election regression passes at HEAD and under mutation 1; the pass-7 equivalence proof for `can_analyze_document` carries forward unmodified. The two publishability predicates are coherent by design: election uses the narrower `can_analyze_document` (a load-diagnostic-only document short-circuits in `document_diagnostics` before `python_declaration_snapshot` and can never publish package diagnostics, so it must not own them), while publication adds the load-diagnostics arm (such a document must still publish its load errors).

**Passes 1–5:** every finding remains resolved as dispositioned in passes 4–7. The two-commit delta touches no surface they covered — completion/hover/definition attribution, the driver OR-merge, the missing-lock deferral matrix, fail-closed promotion, live bridge discovery, workspace-member aggregation, kind-neutral shared-target wording, and the cargo-failure mapping are all byte-unchanged since pass 5/6. The pass-5 didClose handoff regression is the very test strengthened here and passes at HEAD.

**Pass-7 nits:** carried, still nit-grade. The redundant `host.is_some()` conjunct and its undocumented invariant are unchanged; pass 7's second nit (the election regression asserting via `document_diagnostics` rather than a publication drain) is now materially mitigated — the publication-drain interaction it hid is exactly what the strengthened close regression exercises.

## New findings

No blocker, major, or minor.

### Nits (no individual remediation demanded; ledger at next touch)

- **The load-diagnostic-preserving disjunct of `can_publish_document_diagnostics` is untested** (`session.rs:167`): mutation 2 (dropping `!load_diagnostics(uri).is_empty()`) leaves all 71 tests green, so a future "simplification" to bare `can_analyze_document` would silently stop republishing load-failure diagnostics on watcher-batch and didClose publication. Inspection-verified correct today; a small regression opening a load-failing project document through `publish_all` would pin it.
- **Ledger wording** (`plans/issues/active/ad-hoc-declaration-first-python-interop.md:2273-2274`): "using the same publishability boundary as owner election" is loose — publication's predicate is a strict superset of the election's (it adds the load-diagnostics arm, correctly). The sentence's preceding clause states the exact behavior, so the record is not misleading, but "same" overstates the sharing (what is shared is `can_analyze_document`).
- Pass-7's carried nits above, for absorption at the ledger's next touch.

## Acceptance and ledger assessment

1. **"LSP results agree with compiler/check results for the same package snapshot" — HOLDS.** The pass-7 delivery gap — correct diagnostics computed for the correct owner but their didClose republication silently aborted while an excluded document was open — is closed and mutation-pinned; the pass-6 computation-level violation remains closed; every previously-audited agreement direction is untouched by the delta and stands as verified in passes 4–7. What remains is exactly the ledger's accepted transient family (per-document open/change/save windows, double-transient drain leftovers), now with the close transition genuinely guaranteed as the documentation states.
2. **"Completion never offers an untyped or unsupported declaration as certified" — HOLDS.** The delta touches only publication scheduling and tests; the status vocabulary, fail-closed promotion in the shared driver function, and the non-promotion/mixed-rejection regressions are byte-unchanged since pass 5 and unaffected.

**Ledger accuracy: high.** Both new paragraphs were verified clause-by-clause: the pass-7 finding description, the remediation description ("schedules only documents that can provide analysis or load diagnostics" — exact), the strengthened-regression claim (exact), and the 780.26s gate enumeration (matches the accepted authoritative run figure-for-figure, including analysis 48/48 and the warm 42/42 cache). The architecture doc's ownership paragraph is now fully true without conditioning. The single blemish is the nit-grade "same publishability boundary" phrasing above. Quantitative claims re-measured this pass are exact: LSP 71/71; all four touched files well under the 900-line cap.

## Verdict

**SATISFIED.** The pass-7 minor is closed exactly as its remediation direction demanded — unpublishable documents are excluded from full-publication scheduling via a predicate that provably matches `document_diagnostics`' success precondition while preserving load-diagnostic documents, and the close-owner regression now covers the combined excluded-document-plus-didClose scenario end-to-end, mutation-verified discriminating in both error directions. The pass-6 major and all pass-1 through pass-5 findings remain closed, progress and job-queue behavior are coherent under the filter, the documentation the fix was obligated to make true is true, the residual ledger is accurate, both acceptance criteria hold, and the accepted authoritative create-pr gate (780.26s on the remediation commit, all blocking lanes green) covers HEAD's code exactly. No unresolved blocker, major, or minor remains; the residual items are nit-grade and recorded for the ledger's next touch.
