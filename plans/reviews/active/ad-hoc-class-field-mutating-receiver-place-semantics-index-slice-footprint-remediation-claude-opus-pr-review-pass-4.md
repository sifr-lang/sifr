## Review: class-field receiver-place index/slice footprint remediation — pass 4 @ `a813b9971` (base `9c99ef43b`)

I made **no modifications** to any repository file (the 0-byte untracked `plans/reviews/active/…-pass-4.md` placeholder remains untouched and empty), and ran **no** Cargo build/test/clippy, Sifr compilation, corpus sweep, performance probe, broad validation, or subagent/reviewer. All conclusions come from reading the diff at this exact head and the surrounding code.

### Diff surface at this head
- `crates/sifr_lowering/src/lower/method_receiver_places/footprint.rs:108`, `:121` — the two `collect_footprint(object, …)` additions (byte-identical to passes 1–3).
- `crates/sifr_lowering/src/lower/method_receiver_footprint_tests.rs` (new, 205 lines; +32 vs. pass 3) + `mod.rs:91-92`.
- `crates/sifr/tests/e2e/fail/mutable_receiver_dynamic_index_base_overlap.sifr` (18), `crates/sifr/tests/e2e/pass/class_field_dynamic_index_base_disjoint.sifr` (51) + one manifest entry.
- Three `plans/reviews/active/…pass-1/2/3.md` records.

### Pass-3 finding 1 — closed

`Receiver` in `nested_index_and_slice_arguments_accept_disjoint_inner_indexes` now carries `values: list[int]` (`method_receiver_footprint_tests.rs:181`) and `replace` assigns `self.values = values` (`:189`). `body_contains_receiver_mutation` matches `HirStmt::FieldAssign { object, .. } if object == "self"` (`classes/class_semantics.rs:13,20`), so `Receiver.replace` is promoted to `MutableBorrow` (`method_receiver_analysis.rs:66-73`); `prove_mutable_place(self.receiver)` succeeds (`Coordinator` is neither optional nor recursive, `method_receiver_places.rs:407-416`; `Coordinator.accepted` inherits `MutableBorrow` through the fixpoint at `:48-59`), so `receiver_target` is `Some(MutableReceiverTarget::Place)` and the `:217-223` loop **does** call `expression_overlaps` on the slice argument at `:199`. It remains disjoint: `Slice` → `Dynamic(other)` (root ≠ `self`), `:121` recursion → `Dynamic(other)`, `Place{other}`, and `Place{self,[Coordinator.source, Source.index]}`, none of which prefix-relates to `Place{self,[Coordinator.receiver]}` under `places_overlap` (`footprint.rs:24-28`). The slice half is now a live over-rejection guard, and its liveness is independently corroborated by test 5 passing on the same class shape.

### Pass-3 finding 2 — closed

`nested_slice_argument_collects_inner_index_footprint` (`:145-174`) asserts the reject independently. Traced: `extract_place` has no `Index`/`Slice` arm (`method_receiver_places.rs:398-428`), so both levels reach `footprint.rs:111`/`:104`. Outer `Slice` pushes `Dynamic(other)` (non-matching) and its only bound is `IntLiteral(1)`; the sole path to `self.receiver.index` is the `:121` recursion, which re-enters the `Index` arm and collects `Place{self,[Coordinator.receiver, Receiver.index]}` via the `:99` fast path — a strict prefix-overlap with the receiver place. Delete `:121` and the footprint is exactly `[Dynamic(other)]` → accepted → the test fails, matching the reported mutation result. The assertion pins both `OWN_DOUBLE_MUTABLE_BORROW` (= `SIFR-OWN-0002`) and `primary_range` at the full argument range `other[self.receiver.index][:1]` — which is precisely `arg_ranges[0]` as passed by `report_overlap` (`:346-352`) — and that needle occurs exactly once in the source.

**Type-correctness / non-inertness of both new sources:** the dynamic tuple subscript `other[…]` in both index and slice form appears in test 6, which asserts *zero* errors via `.expect(…)`, so that construct cannot be injecting a spurious diagnostic into tests 4–5; conversely, test 5's reject can only be produced by `expression_overlaps` (the sole producer of that code at that range, via the four call sites `:219`, `:227`, `:240`, `:270`, all funneling through `report_overlap`), and it requires `receiver_target = Some(Place)`, which proves the `replace` mutability chain end-to-end. No shared-borrow or mutable-argument path is involved: `own` is not `is_mut_borrow`, so `mutable_arg_places[0]` is `None` and the `:236-247`/`:250-279` loops stay dormant — the reject is unambiguously the receiver/argument check.

### Prior findings and the rest of the diff — still correct

- **Fix completeness/conservatism.** The two lines are unchanged. `Dynamic(root)` is still pushed ahead of each recursion and stays load-bearing (it subsumes every same-root `Place` per `footprint.rs:20`, so the recursion can never widen a name-rooted base; for a resolvable `FieldAccess` base it yields the strictly narrower place). `Index`/`Slice` carry no sub-expressions beyond `object` + `index`/`start`/`stop`/`step`, all now traversed; the `match` has no `_` arm; recursion is on strict sub-expressions; duplicate pushes are inert under the existence test at `:18-21`. `expression_overlaps`/`places_overlap` have callers only within this module, and `indexed_storage.rs` does no independent collection, so no adjacent traversal needs the same patch.
- **Coverage matrix is now complete in both directions for both arms:** unresolved-base reject — index (test 1 + e2e fail fixture), slice (test 2); unresolved-base accept — test 3 + e2e pass fixture; nested reject — index (test 4), slice (test 5); nested accept — test 6 (both arms live). Each of `:108` and `:121` is pinned by two distinct rejects.
- **Fixtures/manifest.** Fail fixture `col=28` is correct (8 indent + `return ` = 15 + `self.update(` = 12 → argument at col 28); `tests/e2e/fail/` is 566 files, matching 566/566. Pass fixture asserts `apply() == 7`, `owner.value == 7`, `owner.values == [7]`, so both widened call sites are runtime-observable; it is registered exactly once in `create_pr_e2e_manifest.json` (140 entries, verified unique, no count field, no other manifest lists pass fixtures — matching the `class_field_callable_invocation_disjoint_sibling` precedent).
- **Decomposition/size.** New `#[cfg(test)]` module correctly gated and alphabetically placed at `mod.rs:92`; `footprint.rs` 278, `method_receiver_places.rs` 684, `mod.rs` 252, test module 205, fixtures 51/18 — all under 900. `check_hir_maintainability_guardrails.py` only checks banned monoliths + checklist doc, both unaffected.

### Findings

None.

Non-findings I considered and dismissed: the reported mutation evidence, read strictly ("removing line 121 fails exactly its reject"), understates the pinning — removing `:121` must also fail `slice_argument_traverses_unresolved_base_footprint`, since that base's `root_binding_id` is `None` and its only bound is a literal; the pinning is therefore stronger than claimed, not weaker, and no repo change follows. Also dismissed: no e2e fail fixture for the slice or nested rejects (unit tests pin the lowering behavior; one fail + one pass fixture matches the immediate precedent); accept tests lacking mutation sensitivity (by design — they are over-rejection guards); root-only `Footprint::Dynamic` coarseness and `Place{self}` from a `self`-rooted `MethodCall` base over-approximating (both pre-existing policy, now merely applied consistently to subscripts); missing `__init__`/never-instantiated classes in unit sources and the fail fixture; `range_for` duplication (idiom in 13 other test modules); `values` field-name reuse across classes; non-alphabetical manifest placement (`BTreeSet`-loaded); absence of a `plans/` update in the code commits (precedent lands records separately); and the `SpecializedIndexedStorage` receiver-index path (pre-existing, out of scope).

On evidence: everything I traced statically is consistent with the reported exact-head results (six focused tests green, slice mutation failing its reject, 953 passed/1 ignored, fail corpus 566/566, native pass fixture, clippy/fmt/guardrails). The final full-lowering run and create-PR profile are still unclaimed, so that evidence must be confirmed before merge — that is a gate on the merge, not an actionable defect in the diff.

**SATISFIED** — zero actionable findings; both pass-3 findings are fully closed and all prior findings remain closed.
