## Rust Interop Verification Matrix Hardening — Final Implementation-Readiness Review

Read-only. No files modified (`git status --short` unchanged; the manifest mutation I used to probe forward-compatibility was restored and verified byte-identical).

### What I independently verified as correct

**Inventory is exact.** Recomputed from the checked-in data, every number in the new `## Closeout Inventory` matches: 34 fixture rows / 34 compatibility rows / 34 manifests, all `schema_version: 2`; 47 `passing` and 21 `planned` evidence directions with zero other statuses; 47 *distinct* `(test_file, test_name)` provenance keys (no sharing); categories 17 `supported` / 5 `supported-through-bridge` / 1 `unsupported-by-design` (`direct_crate_negative_type`) / 11 `future-owned-by-separate-phase`; execution kinds 13 `cargo-probe` / 4 `compiler-diagnostic` / 10 `contract-only` / 7 `runtime-observed`.

**hardening_1.** `rust_interop_checks` is in the fixed sequence (`profile_runner.py:70`), fails closed on empty selection (`:515-518`), deletes and revalidates its own result JSON (`:521-533`), and `validate_rust_interop_result` (`:115-160`) requires schema/area/`bless:false`/blocking/positive-variants/zero-failures/exact suite set/summary. All four profiles select all four suites; `validate_selected_area_suites` (`profiles.py:171-188`) rejects both a missing suite and a missing area. Self-test asserts the step is scheduled for all four (`selftest.py:280-300`) and covers 12 result-JSON mutations plus malformed JSON.

**hardening_2.** Frozen table encoded verbatim (`check_fixture_matrix.py:130-136`); the self-test sweeps all 5×4 tier/kind pairs plus the tier-1→contract-only downgrade. `diagnostic_crate_rationale` is exact-key/`linked:false`/`executed:false` validated, rejected on non-diagnostic rows, required on diagnostic rows with crates, and cross-checked across matrix/compat row/`fixture.json` (`:598-615`, `check_compatibility_matrix.py:149`). `check_tiers.py --self-test` writes temp matrix/TOML and covers control + missing/duplicate/mismatch/invalid-name/empty-list. Both named tier-1 rows are now real `cargo-probe` bound to `test_build_*_cargo_probe` / `test_check_*_cargo_probe` in `package_rust_interop_build_tests.rs`.

**hardening_3.** No README loophole survives: the only README reads left are existence checks (`check_fixture_matrix.py:198`) and scenario-package README existence (`_scenario_checks.py:128`); nothing parses prose. `_provenance_checks.py` enforces exact field set, `step == crate_tests`, unique blocking suite in that profile's `crate_test_membership`, mode-enabled, package-scoped path with no escape, exactly-once Rust test (comment/string/raw-literal aware, `_rust_test_evidence.py:289-384`), `--ignored` command symmetry, cfg-feature reachability incl. `#[path]` remaps, `--skip` filter collision, `runtime-observed`⇔`crates/sifr_runtime/` binding, positive `cargo-probe` requiring an explicit probe marker, weakest-mandatory-profile derivation, and single-owner test reservation. `_rust_test_outcomes.py` additionally requires the bound test to actually assert the declared diagnostic code (resolved through `DiagnosticCode::` constants) or runtime state. Non-passing records are forbidden from carrying `validation` at all. Distribution: 40 create-pr + 7 merge bindings; the 7 merge ones are the `--ignored` generated-build suite, correctly the weakest blocking profile that runs it.

**hardening_4.** `_is_rejection_context` and all lexical markers are gone from source (only historical mentions in `plans/reviews/**` and the plan's own "removes …" bullet). Rejection is structural: exact 3-tick `sifr-rejected` fence, suffix-correct same-line marker with inline-code-span suppression (`check_stale_drafts.py:144-153`), near-miss fences reported as malformed, unclosed rejected fences reported, `python`-fence rule preserved, `archive`/`reviews` exclusions unchanged. 20 self-tests, including "nearby lexical words", "adjacent rejection prose", "marker on another line", and MDX-vs-HTML marker asymmetry. All 15 in-scope stale mentions carry markers and the checked-in scan passes.

**No new panic/fallback/skip/network path.** The only production behavior change is `#3022`'s package-bridge probe planning (`probe_planning.rs:47-99`), which fails closed with `RUST_CARGO_METADATA` + `return None` when the sysroot runtime crate is unresolved. The `return Ok(())` early-outs in `rust_interop_probe.rs:50-54` are pre-existing moved code, not new skips. The one `.expect` in the range is inside `#[cfg(all(test, debug_assertions))]` and belongs to unrelated `#3021`. No network in any new test.

**Gates re-run on this tree:** four checker self-tests (68/4/6/20 cases), area 8 variants / 0 failures, `sifr_verify --self-test` all 8 lanes incl. "Rust interop profile execution self-test", file-size PASS (2828 files), HIR guardrails PASS, `git diff --check` clean, create-pr `--emit-plan` shows the selection and the blocking budget.

---

### Findings (prioritized)

**1 — Non-blocking, but fix the plan text now: the required-suite derivation will break all four profiles the moment Phase 40 registers `stable-candidate`.**
`required_rust_interop_suites()` (`verification/runner/sifr_verify/profiles.py:191-203`) derives the *required* set from every suite in `verification/areas/rust_interop/manifest.json`, and `validate_selected_area_suites` (`profiles.py:171-178`) demands it of every non-`selected-areas-only` profile. `_rust_interop_profile_self_test` (`verification/runner/sifr_verify/selftest.py:288-294`) tightens this further to *exact equality* per profile. Phase 40 plans the opposite: register the suite in the area manifest (`plans/phases/40_stable_channel_ga_promotion_and_release_governance.md:398-399`) and add it to the **release** profile only (`:416-419`), with the exit gate scoped to release (`:459-460`). I confirmed the failure empirically — appending a fifth suite to the manifest made `load_profile` raise `omits required Rust interop verification suites: stable-candidate` for create-pr, merge, nightly, *and* release. Either scope the required set to the four structural suites, or correct Phase 40 `milestone_40_1` to add `stable-candidate` to all four profiles. This is the exact seam this closeout PR is already editing (`:58-62`), so it is the cheapest place to resolve it.

**2 — Non-blocking: the blocking create-PR step budget is no longer backed by a current measurement.**
`verification/profiles/create-pr.json:15` sets `rust_interop_checks: {budget_ms: 5000, enforcement: "blocking"}`, added in `#3018` per `hardening_1`'s "measured warm run" requirement. `#3022` then moved the bulk of the work into the step: measured on this tree, `tiers`+`stale-drafts` = 0.65 s while `matrix`+`compatibility-matrix` = 2.54 s (provenance parsing, `_provenance_checks.py` added in `20a9f55b4`), for a 3.15–3.42 s warm step. Headroom went from roughly 7× to roughly 1.5× against a *blocking* budget, and neither `#3022` nor `#3023` re-measured or widened it; I found no recorded measurement in the area README or the hardening review artifacts. Re-measure and either widen the budget or record the current warm number as the justification.

**3 — Non-blocking, low: the durable PR attribution is a contiguous range that includes an unrelated PR.**
`plans/phases/39_rust_interop.md:5` and `:275` and `plans/roadmap.md:82` all say "PRs #3018-#3023". `#3021` in that range is `docs: finalize class field receiver place plan`, unrelated to this work. The issue's own progress table (`plans/issues/active/rust-interop-verification-matrix-hardening.md:240-244`) lists the correct five: #3018, #3019, #3020, #3022, #3023. Cite the five, or write "#3018-#3023 excluding #3021".

**4 — Blocks archival (process, not implementation): the review artifact is empty.**
`plans/reviews/active/rust-interop-verification-matrix-hardening-final-review-round1.md` is 0 bytes. `hardening_5` requires recording the final review, and the other four items each cite a concrete artifact. This must be populated before the closeout PR is opened; archiving with an empty sign-off record would leave the item's own evidence column unbacked.

**Minor observation (no action required).** The successor's minimum common gate (`plans/issues/active/rust-interop-runtime-ecosystem-certification.md:325-337`) omits `check_stale_drafts.py --self-test`, which `hardening_4` created. It is not a real coverage gap — the area runner executes it as a case (`stale-drafts/rust-interop-stale-drafts-self-test`) and `areas run --area rust_interop` is the first line of that gate.

### Archival safety

Safe once findings 1–4 are handled and the heavy gates pass, with one mechanical prerequisite: five durable locations reference the issue by path and must be repointed to `plans/issues/archive/` in the archiving commit — `plans/phases/39_rust_interop.md:5`, `:275`, `plans/roadmap.md:82`, `plans/phases/40_stable_channel_ga_promotion_and_release_governance.md:53`, and `plans/issues/active/rust-interop-runtime-ecosystem-certification.md:8` and `:52`. There is no repo-wide markdown link checker that would catch a stale path (only `check_docs_error_code_links.py`, which is scoped to diagnostic pages). All 11 `future_owner` values point at the certification issue, which stays active, so the compatibility-matrix `future_owner` existence check is unaffected. The hardening issue's own `sifr-rejected` fence and inline markers leave the stale-draft scan scope on archival (`SKIP_PARTS = {"archive", "reviews"}`), which is correct and non-breaking.

The successor-unblocking claim itself is honest and does not overclaim: the certification issue's entry statement (`:7-15`, `:56-58`) asserts only that `hardening_1`–`hardening_4` merged and that `certification_0` may start, and explicitly keeps the row sequence blocked; the retained `certification_0` responsibilities in the closeout text match its four entry-criteria bullets.

NOT SATISFIED
