# M9 remediation Wave 3 — Codex 5.6 Sol high review pass 4

Reviewer configuration: `gpt-5.6-sol`, high reasoning, fast service tier, read-only full-wave review.

## Verdict: blocked

1. **High — native cancellation still bypasses async callback drain and provisional rollback.** Generated wrappers submit using the parent carrier and only drain call-scoped callbacks afterward (`crates/sifr_codegen/src/python_interop_async/callback_frame.rs:426`). Active cancellation resumes the task's abort fallback from inside submission (`crates/sifr_runtime/src/python/async_cancellation.rs:24`), so the task can be dropped before cleanup at callback-frame line 450 or provisional rollback (`crates/sifr_codegen/src/python_interop_async/conversions.rs:198`). `AsyncioCallback::drop` then leaks an active target (`crates/sifr_runtime/src/python/callbacks/asyncio.rs:301`); foreign callback drop can synchronously block the executor while draining (`crates/sifr_runtime/src/python/callbacks/foreign.rs:224`). Mask cancellation for every async callback cleanup/rollback, not only `lifetime=result`, then resume it after terminal cleanup. Add generated call-scoped foreign/asyncio and receiver-registration cancellation tests.

2. **High — compiled fixtures still do not prove typed `HandlerError` translation or capture release.** Both fixtures store the returned typed result without inspecting its variant (`verification/areas/python_interop/fixtures/callback/reconciliation.sifr:61`, `verification/areas/python_interop/fixtures/callback/sync_context_reconciliation.sifr:52`). Their `typed-observed` marker only proves Python received `SifrCallbackError` (`verification/areas/python_interop/fixtures/callback/python_bridges/callback_reconciliation.py:61`); it does not prove the generated wrapper returned the original typed Sifr error. Likewise, invoking escaped shells only proves rejection after closure, not destruction of captured handler state. Make the Sifr fixtures pattern-match `HandlerError`, and expose an independently observable capture-drop counter/weak reference after owner cleanup.

3. **High — compiled cancellation/finalization/shutdown evidence remains absent.** The retained Pub/Sub case performs successful registration followed by explicit `aclose` (`verification/areas/python_interop/fixtures/pubsub/declaration_callback.sifr:29`); it never cancels retained-result finalization or overlaps rollback with shutdown. The evidence catalog cites only runtime unit coverage for shutdown (`verification/areas/python_interop/fixtures/callback/callback_evidence.json:17`) and omits the reconciliation fixtures from its live evidence. Add a compiled `lifetime=result` case that cancels the generated wrapper while registration/rollback is held, verifies cancellation completes only after capture release, and exercises runtime shutdown/finalization interaction. Also add compiled late cancellation during failed async context entry rather than relying solely on codegen and carrier unit tests.

The reviewer also confirmed that pass-3's implementation blockers were structurally resolved: failed async context entry now releases late cancellation unconditionally, retained foreign callback identities are owner-local, and async wrappers await foreign callback drain.
