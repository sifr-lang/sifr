NEEDS_CHANGES

Primary blocker: `scripts/check_stdlib_manifest_schema.py:255-268` depends on `origin/main` being a resolvable ref, but `.github/workflows/local-first-validation.yml:40` uses `actions/checkout@v4` with default `fetch-depth: 1` and no explicit fetch of main — on `pull_request` events this ref won't exist and every PR CI run will fail the guard. Fix the checkout (add `fetch-depth: 0` or an explicit `git fetch origin main:refs/remotes/origin/main` step) or make `_base_manifest()` fetch the ref itself. Secondary blocker: `_is_pr_reference()` at `scripts/check_stdlib_manifest_schema.py:271-273` is a naive substring match that accepts values like `"PR #TBD"` — tighten to a numeric PR id or URL. Also: surface git's stderr on the failure branch (line 263), expand self-test coverage to exercise all 5 allowed transitions and the new-row positive/duplicate-id paths (line 375), and reorder `_validate` so closure validation runs before the empty-surface early return (line 88).
