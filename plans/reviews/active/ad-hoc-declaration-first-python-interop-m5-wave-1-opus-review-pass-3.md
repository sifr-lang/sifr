## Closure review — full working-tree diff vs main

I traced the wave's surface end-to-end. All three areas the user called out are complete and consistent with the rest of the diff.

**Recursive entered-aggregate rejection.**
`context.rs:172-193` unwraps `Result[T, PythonError]`, and when `T` is not a bare `Type::Class` it calls `ctx.must_use_obligation_for_type` (`mod_context.rs:229-253`), which recursively descends `List/Tuple/Union/Dict/Result`. Any leaf that is an opaque with cleanup ≠ `Drop` yields `Some`, so `list[Session]`, `dict[str, Session]`, `Option[Session]`, etc. all emit `PYCTX_INVALID_DECLARATION` with the "entered aggregates cannot hide semantic cleanup obligations" wording. Regression is `context_enter_rejects_aggregate_hiding_close_required_opaque_result` (`python_interop_tests.rs:334-363`). Bare `Type::Class` still routes through the pre-existing distinct-opaque rejection tested by `context_enter_rejects_distinct_close_required_opaque_result`.

**Diagnostic catalog and generated docs.**
`SIFR-PYCTX-0001` is now Active: registered in `python_interop_codes.rs:22`, added to `ACTIVE_DIAGNOSTIC_CODES` (`registry.rs:700`), described in `python_interop.rs:230-238`, removed from `reserved.rs` (verified: 0 hits there, 1 hit in `python_interop.rs`). `docs/errors/SIFR-PYCTX-0001.md` is generated and consistent with the registry's message template and representative fixture. `docs/errors/diagnostic-codes.md` and `internal_docs/diagnostic_codes.md` both move the row from Reserved to Active. `code_catalog.json` has one entry (`PYCTX_INVALID_DECLARATION`, `docs/errors/SIFR-PYCTX-0001.md`, renderer_support `human/json/compact`). `code_baseline_coverage.json:1573-1582` adds the single expected coverage row against `frontend_internal_diagnostic_rules_cases`. The synthetic compact baseline gains `E SIFR-PYCTX-0001 <unknown> invalid Python context declaration: synthetic invalid context protocol` and its `16 errors → 17 errors` header increments correctly. `baseline_metadata.json` `source_hash` for that fixture (`fa5bebb1…`) still matches the `main.sifr` SHA — verified with `shasum`.

**Durable wording correction to `demos/m4_demo/README.md`.**
Milestone-taxonomy phrasing is removed ("M4 Recursive…" → "Recursive…"; "M4's runnable demo" → "This runnable demo"; "the milestone" → "the feature set"). The fenced `bash demos/m4_demo/run.sh` block was replaced with a prose "From this directory, run `bash run.sh`." — the `run.sh` file still exists and is unmodified.

**Independent-mergeability spot checks.**
- `PythonCleanupPolicy::Context` is the only newly-active cleanup atom; `async_context` and `async_close` still route through `reserved_cleanup` (`python_interop.rs:368-378`).
- `must_use_obligation_for_type` returns a typed `MustUseObligation` struct whose `Display` prints the same `Python opaque {name} ({cleanup:?})` label used before, so every `format!("{obligation}")` call site (`class_body_lowering.rs:520-522`, `annotations_and_function_lowering.rs:832-834`, `control_flow.rs:349-351`) prints identically to the prior string form. The `HashMap<String, MustUseObligation>` migration is complete: every consumer (`return_lowering.rs`, `control_flow.rs`, `annotations_and_function_lowering.rs`, `class_body_lowering.rs`, `mod_context.rs`) reads through `.get(...).cloned()`/iteration, no residual `String`-typed reader.
- `return_lowering` no longer inlines the "mark moved" else-if; the identical behavior is centralized inside `transfer_return_ownership`'s `HirExpr::Name` arm — for `CloseLike` the mark still fires, for `ContextOnly`/`AsyncContextOnly` a `PYCTX_INVALID_DECLARATION` is emitted with the "rather than returned or aggregated" phrasing exercised by `context_only_obligation_cannot_transfer_through_return`. Aggregate/comprehension recursion still fans out.
- Direct construction of `ExitCause` outside sysroot-private declarations is refused in `core_and_calls.rs:337-346`; direct calls to `Self.__exit__` are refused in `methods_lambdas_and_comprehensions.rs:262-270` via `python_context_exit_methods`, populated in `python_interop.rs:collect_python_opaque_classes` (unified entrypoint replacing the previous inline loop in `mod_impl.rs`). `method_consumes_receiver` was extended so `@python.context.exit` also implies consumption.
- Stdlib introduces `ExitCauseKind`/`ExitCause`/`ExitDecision` in `_sifr.python` and re-exports through `sifr.python_core` and `sifr.python`; import ordering is alphabetical and matches the existing block conventions.

**No stale generated/diagnostic metadata or unrelated behavior change.**
The reserved-code block dropped exactly one line (`SIFR-PYCTX-0001` PYCTX). No other reserved atoms were touched. The public `docs/errors/diagnostic-codes.md` and `internal_docs/diagnostic_codes.md` moved the same code atomically; no orphan rows remain. Baseline metadata source_hash matches the on-disk `main.sifr`; the check-compact baseline is the only baseline file touched and its entry is in sorted registry order. No unrelated crates, demos, or fixtures are modified beyond the M4 README wording.

SATISFIED
