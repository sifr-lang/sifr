PR-ready.

Public signal behavior preserves the `Awaitable[Result[Signal, SignalError]]` shape (async def returns the same external type), with `_signal_from_number` correctly mapping 2→SIGINT and 15→SIGTERM. Cargo deps in `crates/sifr_stdlib/Cargo.toml:93` (`signals = ["dep:sifr_runtime", "dep:tokio"]`) are gated behind the feature and validation confirms tokio's signal/rt/macros features are wired. Stale compiler paths are fully excised — module removal in `registry.rs:10`/`registry.rs:95-107` and `retained_intrinsics/src/lib.rs:17,33,82`, deletion of both `signal.rs` files, and the negative test `signal_intrinsics_are_owned_by_compiled_stdlib_declarations` at `registry_core_tests.rs:272` pins the retirement. Closure guards add all three intrinsic names to `RETIRED_INTRINSICS` in `check_stdlib_migration_closure.py:216-218`. Docs correctly narrow to `callback_subscription_core` in `stdlib_retained_compiler_intrinsics.toml:198` and the reason field explicitly disclaims broader ecosystem certification.
