### VERDICT: PASS

The wave preserves the M11 invariant (resource-sensitive surfaces remain blocked by the matrix + certification-issue owner pin) and completes M12's cleanup goal (broad ownership registry deleted; retained compiler-native glue tracked by the M12-wave-1 intrinsic allowlist). The gate is now stricter — any category or ownership drift on the four protected rows fails loudly — while removing the redundant TOML inventory.
