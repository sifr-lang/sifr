## Independent Final Review — Diagnosis Milestone

**Scope reviewed read-only** at `HEAD ee59fc054` vs `origin/main 94a5fec67`. No tracked file, git ref, or GitHub state was modified (`git status` shows only the pre-existing untracked 0-byte `…-review-pass-12.md`). Probes ran in `/tmp/rev13*`; corpus builds reused the repo's gitignored `sifr_output/` artifact dir and the existing `target/debug/sifr` (built at the cited baseline `649334330c`; the only `crates/` deltas between that SHA and `HEAD` are rust-interop files, none on the dict/linked-list lowering or codegen paths).

### What verified clean

- **Rebase resolution.** Both tracker edits are pure one-line insertions. `plans/phases/index.md:56` and `plans/roadmap.md:87` retain the upstream `REVIEWER-RESTORE` row, and the upstream `#3058` relocation of `GENC-NAN`/`PKG-RUST`/`PERF-HOST` to `../issues/active/` survived intact; the new `DICT-AUG` rows sit in the established ALG-CORPUS → DICT-AUG → GENC-NAN slot in both files with resolving paths. Nothing upstream was dropped or reverted. Review passes 1-3 are tracked, unmodified, and each ends `SATISFIED`. The cited reproduction SHA `649334330c` is still an ancestor of `HEAD`, so the evidence pointer survives the merge.
- **Static inventory, independently recomputed.** 411 top-level corpus fixtures; 22 fixtures reference `nodeNext`; 20 import `helpers/list_node` (including the two preserved check failures `0002`/`0086`); `0141`/`0160` are the only local `def nodeNext` copies. `18 + {0002,0086} = 20 importers`, `18 + {0141,0160} = 20 latent`, 22 total — the doc's partition at `plans/issues/active/ad-hoc-algorithmic-full-corpus-preexisting-failures.md:114`/`:144-159` is exact. No `.sifr` fixtures exist outside `src/`, so 22 is the whole universe of that shape.
- **Mechanisms are technically supported.** Group 1: `supports_total_order` (`crates/sifr_lowering/src/lower/type_bounds.rs:206`) falls through to `_ => false` for `List`, `Tuple` already recurses, and `Float` is absent — the "admit `list[T]` recursively, keep sets/dicts excluded on semantic grounds" framing is correct; all six fixtures use bare `.sort()`. Group 2 reproduced: `xs == []` → `cannot compare … 'list[int]' and 'list[Any]'` from the gate at `crates/sifr_type_system/src/check.rs:394`, ahead of `equality_comparable` at `:431`, so "specialize literal HIR in lowering, don't weaken the gate" is the right constraint. Group 3: `container_literal_specialization.rs:200-256` rebuilds the defaultdict alias with the *original* `key_ty`, confirming "preserves an `Any` key in HIR". Group 4: `0036_valid_sudoku.sifr:23-24` reads `rows[r]` before the write at `:28-29`. Group 5 codegen: `option_binding_requires_mut_for_ir` (`crates/sifr_codegen/src/stmt_support_emitter/condition_lowering.rs:152-172`) already emits `Some(mut x)` for recursive-field classes but returns `false` for params absent from `local_binding_types`, and its `borrowed_params` early-return correctly keeps `&Option` destructures out — so "a single codegen fix, no helper source change" is accurate and correctly scoped. Group 6: `0377_combination_sum_iv.sifr` does carry an unannotated `dfs` after `return`.
- **Latent class reproduced.** `0206`, `0141`, `0021` each `check` clean and fail `sifr build` with `error[E0596]: cannot borrow node.next as mutable`. The corpus runner really is check-only (`verification/areas/algorithmic_compatibility/runner.py:556-568` invokes `sifr check` and nothing else).
- **DICT-AUG.** Problem statement reproduces exactly: `values: dict[int,int] = {}; values[1] += 1` checks clean and emits `if let Some(__elem) = values.get_mut(&(1_i64))`. Four ACs match the four named paths; the 2026-10-31 fail-closed expiry is stated in the issue Status and identically in both trackers. `TYPE_CONTAINER_ELEMENT_CONFLICT` = `SIFR-TYPE-0008` exists (`crates/sifr_diagnostics/src/codes/registry.rs:45`); `leetcode-full` is in `nightly.json:185` and absent from `release.json`; `check.rs` 876 and `minmax_sorted_sum.rs` 859 lines confirm the wave-1/2 new-module constraint. Diff touches only `plans/` — no unrelated scope.

### Findings

**F1 — HIGH. The latent check-pass/build-fail inventory is not exhaustive, its single-defect framing is wrong, and the acceptance gate cannot catch what is missing.**

`plans/issues/active/ad-hoc-algorithmic-full-corpus-preexisting-failures.md:46-51` states the diagnosis "also found a disjoint set of 20 fixtures that pass `sifr check` but fail `sifr build`/`sifr run` **through the same** owned recursive-field extraction codegen defect", and `:139-142` fixes that set at 20. Both are understated. Verified counterexample:

- `verification/areas/algorithmic_compatibility/corpora/leetcode/src/0001_two_sum.sifr` — `sifr check` → `no errors found`; `sifr build` → `error[SIFR-BUILD-0005]`, with `error[E0277]: the trait bound Box<dyn Any>: Clone is not satisfied` and `error[E0308]: expected i64, found Box<dyn Any>`.
- Different root cause, not the `nodeNext` shape: the empty dict literal `prevMap = {}` at `0001_two_sum.sifr:4` is never specialized from the later `prevMap[n] = i`, so `emit` produces `let mut prevMap: HashMap<i64, Box<dyn ::std::any::Any>>` and the proven-subscript read emits `prevMap.get(&diff).cloned()`. It is adjacent to diagnosis groups 2/3 (`Any` container specialization) but is a distinct, unrecorded latent class. `0001_two_sum` is not among the preserved 20 check failures and is not in the 22-fixture `nodeNext` set.
- Sizing: an authoritative `sifr build` sweep over the first 81 fixtures returned 5 check-failures, 68 build-clean, and 8 build-failures — 7 documented `nodeNext` fixtures plus `0001_two_sum`. An `emit`+`rustc` pass over the first 40 check-passing fixtures found the same single extra failure. I did not complete a full 411-fixture build sweep (each `sifr build` is ~9 s and the host is under heavy concurrent load), and that is part of the finding: **no full-corpus build sweep exists in the evidence chain.** Pass 11's completeness argument rests on an 8-fixture spot-check of other owned-optional-class fixtures, which by construction could not surface a dict-typing class.

Consequence: the acceptance criterion at `:258-260` requires build+run only for "all 22 corpus fixtures exercising the affected owned recursive-field extraction pattern". Wave 7 closeout can therefore declare the corpus green, restore `leetcode-full` to the release profile, and leave `0001_two_sum` — the corpus's first fixture — emitting non-compiling Rust. That is exactly the outcome `:197-199` says the waves exist to prevent, and it is a live "if it compiles, it works" breach.

*Correction:* run a complete-corpus `sifr build` (or `emit`+`rustc`) sweep and record the resulting latent set in full, partitioned by root cause rather than as "the same" defect; add the empty-dict-literal `Any`-value class as its own group/wave or as a separately tracked issue alongside DICT-AUG; and widen `:258-260` from the 22-fixture pattern to a full-corpus build/run requirement at closeout, so the green-corpus criterion is measured by the same lane that would expose these failures. Update `:46-51` and `:139-142` to stop asserting a closed 20-fixture single-defect latent set.

**F2 — LOW. Evidence continuity has an unexplained gap at passes 8-10, and an empty artifact is staged to be swept in.**

`:161-172` cites passes 1-3, then 4-7, then jumps to 11. Passes 8, 9, and 10 exist only as 0-byte gitignored `*.agent.log` files, so a future auditor cannot distinguish "produced no output" from "review with unaddressed findings suppressed". Add one clause stating passes 8-10 produced no reviewable output. Separately, `plans/reviews/active/ad-hoc-algorithmic-full-corpus-preexisting-failures-agent-review-pass-12.md` is a 0-byte untracked file in the working tree — delete it before the PR so a `git add -A` cannot commit an empty artifact, which passes 4, 5, 7, and 11 all flagged as a standing rule.

### Validation evidence

The reported `scripts/run_all_tests.sh --profile create-pr` exit 0 with all blocking lanes green and every lane under budget is consistent with a docs-only diff; the warm wall-time advisory under concurrent unrelated builds is correctly treated as non-blocking. It does not bear on F1, which is a completeness gap in the diagnosis content rather than a gate failure — and by construction the check-only corpus lane cannot surface it.

VERDICT: CHANGES REQUESTED
