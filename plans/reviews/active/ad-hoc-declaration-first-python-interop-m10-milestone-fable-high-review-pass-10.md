# M10 Milestone Closure Review — Pass 10 (PR #2990, `codex/m10-milestone-closure` @ df2bb8cc7, Fable high, read-only, post-commit)

**Summary: the milestone meets the bar and the closure ledger is accurate.** The frozen diff at `origin/main...HEAD` is exactly the surface passes 2–9 collectively reviewed, now committed: commit 1dbe86007 (71 files, +1961/−449) is the pass-9-reviewed remediation delta made permanent, and the final commit df2bb8cc7 is verified documentation-only — 4 lines in the milestone issue flipping the M10 and milestone-review checkboxes and rewording the closure sentence. No source, test, evidence, or vendor file changed after the attested merge-gate run, so the validation results (exit 0 in 2685.23s, E2E 674/674 signature `1f8b1cadc4f48ec8`, interop 18/18, diagnostics 175/175, 261 hardening variants, codegen 864/864, lowering 784/784+1, driver 360/360+33 with generated builds 33/33, performance 8/8, guardrail over 2720 files, warm-time advisory only) transfer to HEAD unchanged.

**Methodology.** I read the full milestone issue (both pages: M10 tasks/acceptance/validation and the complete review ledger), AGENTS.md, the follow-up issue `ad-hoc-class-field-mutating-receiver-place-semantics.md`, Fable-high passes 2–7 and 9 in full (pass 8 is the 146-byte invalid timer stub, disregarded as instructed), the complete 588-file diffstat, the exact content of both final commits, and the diffs for every non-code surface (docs/python-interop.mdx, both interop architecture docs, architecture.md, retained-intrinsics TOML, roadmap, exit evidence, buffer/dataframe evidence runners, budgets.json, .gitignore/.gitattributes, Cargo.toml). I directly inspected the highest-risk implementation files (`error_contracts.rs`, `python_interop.rs` five-field predicate, `source_names.rs`, `class_field_emitter.rs`, `class_method_receiver_analysis.rs`, `mutating_methods.rs`, `python_interop_common.rs`, `class_upcasts.rs`, `try_error_carrier.rs`, `statements.rs` union recording, `field_and_stdlib_rewrites.rs`/`recursive_exprs.rs`/`stmt_expr_method_and_question_mark.rs` suppression sites, buffer lowering, `scope.rs`/`mod_context.rs` taint machinery) and ran six short behavioral probes in `/tmp/m10rev10` (deleted afterward) with `target/debug/sifr`, which was built at the frozen-implementation commit time and is newer than every source file. No suites were rerun; no agents, timers, or background tasks were spawned; no repository file was modified.

**Independent confirmation of the M10 remit at HEAD:**

- **Mutable field-place semantics** (the pass-4→7 blocker family): native probe `self.helper.merge(self.stock)` prints 2 — the mutation lands on the original place. The convention threads end-to-end (per-parameter conventions in `class_type_collection.rs`, binding mutability in `class_body_lowering.rs`, check-side `reject_immutable_method_mut_borrow_arguments` at `mutating_methods.rs:41`, root-name mutation marking, per-argument clone suppression with restore-if-unconsumed at all three emitter sites). Immutable-rooted mut arguments fail at check: my probe reproduced `SIFR-OWN-0005` at the exact argument span, matching the fail fixture.
- **Canonical nominal identity / same-basename types**: cross-assignment `CsvError → ConfigError` rejected at check; the 114-line `nominal_identity_alias_paths.sifr` fixture exercises aliased stdlib inheritance, `super()`, operators, and compiler-prefixed generics natively.
- **Union error carriers**: `record_try_error_types` recurses into union members (`statements.rs:68`); native probe with reversed handler order dispatched the same-basename `ConfigError` correctly; builtin/mixed/aliased fixtures are in the E2E suite; preamble error structs carry `PartialEq/Eq/Hash` so shared carrier enums keep ordinary-value traits (b74b6a65c).
- **Catch-all identity**: `handler_is_catch_all` keys on `Type::is_builtin_error_base` (identity `None`, no parent, exactly `message: str`) at all three codegen consumer sites plus lowering; probe confirmed imported same-basename `Error` is not a catch-all (SIFR-RESULT-0005). The basename tail-match upcast fallback is deleted; only its negative test mentions "basename".
- **PythonError separation and feature gating**: the duplicate-safe five-field contract predicate (`python_interop.rs:12` — length + all-present + all-canonical, so duplicates cannot pass) guards all declaration families; `_sifr.python.PythonError` is off `GLOBAL_RUST_NOMINAL_IDENTITIES` with `assert_ne!` guards; bridge-field injection requires the exact contract *and* actual interop declarations in the module (`class_field_emitter.rs:7-28`); runtime-feature detection covers both `::sifr_stdlib::python::` and `::sifr_runtime::python::` (used from `lib_modules_and_codegen.rs:752` and `entrypoints.rs:174`). Native probe: a user `class PythonError(Error)` with arbitrary fields in a non-interop program builds and runs (prints 3) — the pass-2 blocker's exact shape.
- **Typed buffers, exact release, writable least-authority**: writable `Self` acquisition is rejected (`buffer.rs:167-170`); writable-producer parameters that can carry Python identity require `own` (`python_interop.rs:636-648`); both writable-alias fail fixtures exist and are registered in `buffer_evidence.py`'s negative matrix; dataframe evidence pins numpy `copy_bridges=False` with a fail-closed policy-drift self-test. The buffer runtime/lowering surfaces were unchanged by the post-pass-7 delta and are covered by the attested interop 18/18.
- **Receiver-analysis decomposition and file size**: `class_emitter.rs` 786, `class_method_emitter.rs` 747, with the four new focused modules (156/155/22/49 lines); `class_method_receiver_analysis.rs` is a clean recursive `&mut self` promotion with cycle protection.
- **Poisoned-binding suppression**: `ErrorTaint` is a proof token constructible only in the diagnostic-emitting path and `error_taint_since` requires the error count to have grown — suppression cannot fire without a real emitted diagnostic, so no invalid program can be silently accepted.
- **Documentation/evidence/tracking**: docs and both architecture contracts now document the five-field error contract, sealed `Object` identity (no basename impersonation), read-only `Self`, and `own`-producer rules; exit evidence activates `PYCB`/`PYZC` as active families; the roadmap correctly states PR #2989 merged with closure on PR #2990; `budgets.json` changes are RSS-ceiling-only (67→80 MiB across 18 LSP query entries, justified against unmodified main's own measurement; time thresholds untouched). The vendor bulk (~136K lines) is mechanical `Cargo.lock`/`build/`/`.vscode` tracking for checksum-complete clean checkouts with matching `.gitignore`/`.gitattributes` rules. `Cargo.toml` adds only a dev-profile `opt-level = 1` for `sifr_codegen`, matching sibling crates.
- **Ledger accuracy**: the Status paragraph and milestone-review bullet record Codex passes 1–14 and Fable passes 2–9 with pass 8 explicitly marked invalid, link the follow-up issue (satisfying pass 7's filing requirement), and quote the merge-gate numbers exactly as attested. The M10 checkbox links PR #2990 described as "the reviewed and validated M10 closure PR" — accurate as phrased, since the checkbox flip necessarily ships inside the closure PR itself.

Blockers distinguished from pre-existing gaps: the one known silent-wrong-result defect (class-field mutating *receiver* clone, `self.helper.bump()`) reproduces on the shipped pre-M10 release, is outside M10 causation, and is properly scoped in the committed follow-up issue. All fail-closed parity gaps carried from passes 6–9 (same-root read E0502, proven-index E0282, poisoned-initializer ICE cosmetics) are documented and never silent.

## Blocking findings

None.

## Non-blocking findings

1. **MEDIUM (carried from pass 9, pre-existing interaction, fail-closed)** — spurious `SIFR-INTERNAL-0001` when a poisoned binding feeds a later assignment initializer; the root diagnostic is preserved and check exits nonzero, but users see "internal compiler error" for an ordinary typo. Fix direction recorded in pass 9.
2. **MEDIUM (pre-existing, correctly filed)** — the class-field mutating-receiver clone defect tracked in `ad-hoc-class-field-mutating-receiver-place-semantics.md`; should be picked up immediately after closure since M10's mut-arg feature makes dual-mutation shapes newly compilable.
3. **LOW (carried)** — same-basename diagnostics remain unqualified on type-checker-formatted paths; my probe printed `expected 'Error', got 'Error'` for the cross-identity assignment. Fail-closed but confusing for exactly the programs M10 legitimizes.
4. **LOW (carried)** — `crates/sifr_runtime/src/python.rs` sits at exactly the 900-line cap; the next touch forces a mid-change split. Consider splitting proactively in M11.
5. **LOW (carried)** — fail-closed check/build parity gaps in the mutability-analysis follow-up bucket: mut argument alongside a same-root read (rustc E0502) and proven-index mut arguments (rustc E0282); plus invalid parameter annotations are not poisoned, unlike ann-assign locals.

## Verification

I inspected: the full milestone issue including the M10 tasks/acceptance/validation lists and complete review ledger; AGENTS.md; the follow-up issue; Fable passes 2–7 and 9 in full plus the invalid pass-8 stub; the 588-file frozen diffstat; the complete content of df2bb8cc7 (single-file, 4-line docs change) and the stat/scope of 1dbe86007 (matches the pass-9-reviewed delta); the non-code diffs for docs, architecture contracts, exit evidence, evidence runners, budgets, roadmap, ignore/attribute rules, and Cargo.toml; and the implementation of every remediation family (error contracts, source-name escaping, bridge gating, catch-all identity, union carrier recording, upcast fallback deletion, buffer writable least-authority, receiver analysis, clone suppression, error-taint suppression). I ran six behavioral probes in `/tmp/m10rev10` with the HEAD-fresh `target/debug/sifr` (three native builds, three check-mode), all confirming attested behavior, then deleted the probe directory. The attested merge-gate results were accepted without rerun as instructed. Repository state is unchanged: `git status --porcelain` shows only the pre-existing dirty `third_party/ruff` submodule (gitlink still 8111415495, content left as found) and the pre-existing empty untracked pass-10 placeholder, which I did not write to; this review is delivered here. No agents, background tasks, timers, or long-running validations were spawned.

VERDICT: SATISFIED
