Verdict: **PASS**

The M10 wave 4 TOML sysroot stdlib interop migration is clean; no blockers found.

**Adapter-policy compliance:** `_sifr.toml` declares `toml_parse_tokens` as a direct `@rust(sifr_stdlib.toml.toml_parse_tokens, panic=trusted_no_panic)` binding with matching signature shape (`str → Result<Vec<String>, String>` maps to `str → Result[list[str], ParseError]` via the standard `Display → ParseError` error conversion). No callee injection, no `bridge.` routing, no converter/pipeline metadata; `completed_private_declarations_follow_adapter_policy_syntax` covers it (crates/sifr_driver/src/stdlib/stateless_private_codegen_tests.rs:42). Adapter reshaping (ParseError → TOMLDecodeError, flat token stream → TomlValue tree) is correctly placed in the sifr wrapper (stdlib/sifr/tomllib.sifr:126-233), not in the sysroot crate.

**Public API/error shape stability:** `loads`, `load`, `load_handle`, and `TomlValue` (fields, constructor, `is_*` / `as_*` / `at` / `get` / `keys` / `values` / `items`) are byte-for-byte preserved. `TOMLDecodeError` `line`/`column` defaults still resolve to `0` via the compiler-generated builtin-error preamble (crates/sifr_codegen/src/lib_modules_and_codegen.rs:529), matching the old intrinsic's behavior.

**Dependency plumbing:** `TOML_DEPS` is now `&[]` (crates/sifr_stdlib_model/src/features.rs:299); generated Cargo manifests for `sifr.tomllib`/`_sifr.toml` no longer leak a direct `toml = { version = ... }`. Both compiler-side (`generated_stdlib_features.rs`, `features.rs`) and e2e-support (`fixture_cargo_toml.rs`, `fixture_dependency_paths.rs`) plan and emit the `sifr_stdlib` `"toml"` feature, verified by `planned_sysroot_stdlib_features_are_minimal_for_representative_modules`, `stateless_sysroot_leaves_do_not_emit_direct_third_party_dependencies`, `test_generate_cargo_toml_tomllib_uses_stdlib_toml_feature`, `test_generate_test_runner_cargo_toml_uses_stdlib_toml_feature`, and `test_build_project_includes_reachable_support_module_stdlib_crates_in_manifest`. `sifr_stdlib` enables `preserve_order` at its own dependency edge, so ordering behavior is preserved.

**Safety / bounds:** `sifr_stdlib::toml::toml_parse_tokens` bounds input length (1 MiB), recursion depth (128), and token count (100 000); errors return via `Result<_, String>`. No `unwrap`/`expect`/`unsafe`; recursion is depth-guarded before descent; `panic=trusted_no_panic` is defensible. Rejection paths are exercised by `toml_leaf_reports_parse_errors` and the panic-free / parse-safety e2e fixtures.

**Compiler ownership retirement:** `crates/sifr_codegen/src/intrinsics/registry/toml.rs` is deleted, the `"toml_parse"` arm is removed from `lower_intrinsic_rendered`, and `toml_intrinsic_is_owned_by_compiled_stdlib_declaration` guards both `toml_parse` and `toml_parse_tokens` from re-entering the active intrinsic registry. `sifr_stdlib_model::intrinsic_toml` correctly re-shapes to `Result[list[str], ParseError]`. The stateless private codegen test asserts the emitted Rust routes through `sifr_stdlib::toml::toml_parse_tokens` with `ParseError { message: __sifr_bridge_error.to_string() }`.

**Test coverage:** Unit (`sifr_stdlib::toml::tests`), private codegen (`toml_private_declarations_codegen_through_sifr_stdlib`), planner (`features_tests.rs`), fixture (`test_generate_*_cargo_toml_*`), and e2e (`stdlib_tomllib.sifr`, `cpython_tomllib_subset.sifr`, `structured_data_formats.sifr`, `parse_safety_error_paths.sifr`, `panic_free_stdlib_errors.sifr`) are all present and referenced in the phase-tracking record.

**Non-blocking observation** (not a review gate):
- `internal_docs/stdlib_native_surface_ownership.toml:79` still sets `current_owner = "compiler intrinsic registry: crates/sifr_codegen/src/intrinsics/registry/toml.rs with direct toml dependency planning"` while that file has been deleted. `migration_blocker` and `deletion_stage` are updated, but the M10 wave 3 URL migration reset `current_owner` in the same file to the sysroot-stdlib phrasing; matching that pattern here would keep the ownership index self-consistent. (Several older migrated surfaces — `_sifr.uuid`, `_sifr.math`, `_sifr.html`, `_sifr.calendar`, `_sifr.platform` — carry the same pre-existing stale `current_owner`, so this is longstanding debt rather than something introduced here.)
