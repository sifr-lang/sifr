# 12K-B47 — remaining cost and baseline provenance

Assessment complete. **Recommend one bounded startup attribution experiment,
not another allocation or formatter patch on the present evidence.** B46 removed
the approved discarded-work mechanisms; all nine current performance limits
still fail. The source identifies remaining construction and residency owners,
but does not attribute the numerical excess to one sufficient repair.

Owner: [3776](https://github.com/sifr-lang/sifr/issues/3776). Police owns the
next scope/policy decision. PR: none. Merge: none. Assessment blocker: none.

Independent HTTPS clone `/private/tmp/sifr-b47.axK5oy/sifr`, branch
`codex/item12k-b47-axK5oy`, documentation base/observed remote main
`4b4cc339964baeeb6641e57dc669fef700a5fa24`. Examined source is the retained exact
Git object `df0056ad1b9d7fcbaacda3a3f0809da66279ea84` (B46), over
`93111f3426245b3ba5484195b8154d3bfc769769` (B44). B46 record is
`bcbd5c8c71d1cb74ef849bd5c74fa104e149dcd5`; B45 record is
`826679ca3a6f23af4f52ec88b132542e99372700`. Neither examined source is delivered
to main by this assessment.

Root, exact paths and allowed commands were registered with parent and Police
before clone/authored writes. Own index, refs, object store, no alternates or
target; sibling `tmp` and `evidence`. Commands bind private TMPDIR and unset
CARGO_TARGET_DIR. Parent's intentionally dirty phase/Python documents and all
predecessor worktrees, indices, caches and evidence are read-only. Parent TOP/B47,
AGENTS, full architecture, both named skills and full B45/B46 reports were read.
The explicit saved-assessment override supersedes their generic test/review/merge
sequence. No Opus request is made or past verdict re-adjudicated.

## Current evidence and the unchanged decision

The offline [comparison](ad-hoc-emitted-rust-b47-artifacts/comparison.json)
binds all **111** current raw process receipts, actual commands, metric roles,
accepted/rejected attempts, producer/checker identity and the nine comparisons.
The owned reader `/private/tmp/sifr-b47.axK5oy/evidence/saved_assessment.py`
imports no compiler/benchmark/checker code and launches no target.

Authenticated 430 selected paths: current terminal/inventory/report/release,
B45 report and its three companion anchors, B46 protected inputs/new outputs,
all raw receipts and referenced sidecars. Each raw RSS/instruction/cycle integer
agrees with its stderr; medians, MAD and population CV agree with saved results.
B45 arithmetic/history is reused, without another 899/910-file predecessor scan.
Full read receipts are outside Git at `evidence/read-receipts.json`.

| Current anchor | SHA256 |
| --- | --- |
| B46 terminal | `302f407be75cfaadc5f72ca714298f4a7f94163eed4d8588be552f815532a87d` |
| B46 inventory, 852 entries | `02550656c6982d7cfa147bbfc535fcded9085ae5eb97ee3b986db12d469bd164` |
| Representative summary | `a426755033dcba50135d2e53810f427d7a34862e1883b78972678c2180660bef` |
| Budget disposition | `5ab04a80725c205c70c9f8d6c1d8a69d90d82b22748713d323cd687e1b3414cf` |
| Producer INPUT `representative.budget.latest.json` | `947d19d275a8257afe0c39be2eb27cf2b6c8186ba4daccdf9be7c4b338e24c3f` |
| Canonical log containing actual checker errors | `cb9fb993c192b9d1a279d731534c0ed977101083127780e88b98363e66c7535f` |
| Sealed release, all 51 supervised commands/both reviews | `c136ec3137707156b8eb7c78accf21b3695d61bd82f545b010b750fec687b18f` |

One invocation `performance-representative-1788887990611838000`: all ten cases
accepted, six rules and producer PASS; actual SAME-ID checker exit1 in60.672ms,
canonical root exit1 in196.331495666s without timeout. The JSON above is producer
input, **not a separate checker report**. Sealed 287 process identities/68 groups
absent, no cleanup signals; that release is reused, not newly sampled.

111 means 48 command receipts plus63 query receipts. Formatter-large attempt1
has six receipts and CV0.029975590969, rejected; original internal attempt2 has
six and CV0.011954417818, accepted. Other nine cases accepted attempt1; accepted
CVs are below0.02. No external retry or extra accepted acquisition is inferred.

| Failing metric | B44 | B46 | Enforceable limit |
| --- | ---: | ---: | ---: |
| Single build RSS bytes | 185466880 | 190169088 | 181649408 |
| Project check instructions | 20705027673 | 19453233660 | 13938915299 |
| Project check RSS bytes | 185597952 | 185024512 | 179601408 |
| Single check instructions | 20651201663 | 19394638768 | 13879420485 |
| Single check RSS bytes | 185139200 | 186023936 | 181436416 |
| JSON diagnostic instructions | 20645274502 | 19389012613 | 13874003870 |
| JSON diagnostic RSS bytes | 185253888 | 185352192 | 180748288 |
| Formatter corpus instructions | 46400530 | 45331927 | 38059926 |
| LSP aggregate RSS bytes | 150945792 | 155418624 | 108593152 |

Instruction comparisons actually use lower bounds19444966650,19365249772,
19379235694,45172711 respectively, all above their limits. The work rule remains
`max(ceil(baseline*1.02), baseline+2000000)`, with median minus
`min(3*MAD, .005*median)`; RSS uses
`max(ceil(baseline*1.10), baseline+33554432)`. No waivers.
Additional-modules build, formatter-large, unchanged-file query and warm
diagnostics query remain PASS controls. Corpus RSS and LSP instructions also
remain passing. Roughly6% lower check instructions is observational, not an
exact causal saving or a successful budget repair.

RSS is Darwin maximum resident set size **bytes**, not KiB, peak footprint,
summed allocation bytes or a leak measurement. Seven command cases exclude
their warmup from five measured processes. Query RSS/latency uses one3+20
aggregate; instructions use20 independent complete3+1 processes. For LSP,
20edits/iteration means460 versus80edits. Its Python client starts the exact
own `sifr lsp --stdio` server, initializes and opens before timed iterations,
then shuts down/waits. Retain the actual launcher/server boundary; its retired
instruction field is not equivalent to compiler-internal per-query work.
Sources: `query_processes.py:19-67`, `lsp_query_bench.py:182-236`,
`developer_tooling/lsp_protocol.py:24-43`, `process_metrics.py:17-64`.

## Decisive remaining ownership map

All current line references are at exact B46. The
[source crosswalk](ad-hoc-emitted-rust-b47-artifacts/source-search.json) binds
79 selected baseline/current paths, reuses B45's history and records the actual
loaded source inventories. This is a source assessment, not another review.

| Boundary | Actual remaining construction/retention | Consumer requirement and causal limit |
| --- | --- | --- |
| Common check/build startup | `build/entrypoint.rs:379` calls full `compile_stdlib`; `stdlib/bootstrap.rs:37-50,62-124,323-488,562-567` loads, parses, lowers, canonicalizes and emits every registered module before application lowering | Tiny checks still pay this common path. Correct inventory validation, public/private trust, export identity, defaults and semantic failures are required. Demand-independent Rust emission/metadata construction is a concrete attribution target, not proof that deleting it preserves the bootstrap contract or clears19.4B |
| Bootstrap metadata copies | `bootstrap.rs:327-340` clones accumulated emission maps, including generic HIR templates; `:454-487` retains generic/class templates and fields; `:773-781` clones a class then clears method/operator bodies | These remain after B46's HIR-map-copy removal. Templates/signatures/constant mappings support generic inference and later policy emission. No duplicate whole-HIR-map claim; temporary template/copy cost is unmeasured |
| Private contract construction | `stdlib/interop.rs:14-27` clones each qualifying private HIR/source into a pending collection; `:30-61` builds full private contracts and source metadata at the end | Temporary pending HIR coexists with final shared HIR. `sysroot_interop.rs:28-48,155-181` needs sysroot trust/source context and currently checks inventory presence, but attaches selected application contracts. No repeated unrelated backend probes: B44 removed that fanout. Exact cost of the remaining full private plan is unknown |
| CLI retention/copy | `stdlib/cache.rs:5-24`, `types.rs:6-23`, `entrypoint.rs:91-101,379,404` retain global bundle, clone the requested full bundle, then clone definitions for lowering | Shared `Arc` HIR is not deep-copied. Emitted strings, definitions, generic templates and contract metadata still have owned clones. Single/project checks (`build/api.rs:99-105,133-134`, `entrypoint.rs:137-149`) return diagnostics and do not consume application Rust; package checks have real ExecuteAll interop consumers (`api.rs:119-130`) and cannot simply use a weaker check path |
| Editor residency | `host/construction.rs:12-25` and `overlay_updates.rs:17-40` request definitions plus auxiliary stdlib sources; `stdlib/tooling.rs:60-84` reloads source text; global initialization still retains code/HIR/interop | B46 definitions projection already avoids cloning the full bundle. Full typed inventory serves later demand; auxiliary source maps serve navigation and immutable snapshots. It is not valid to prune global data by the first document or delete old snapshot answers |
| Additional editor-only startup | `sql_editor_runtime.rs:49-64` creates `ComponentHost` even with empty profiles; `sifr_compiler_component/src/host.rs:83-101` constructs/retains a Wasmtime Engine | This demand-independent owner is absent at baseline and is a plausible separate residency contributor. No component is thereby proven executed, and no bytes are attributed to it. Ordinary no-query CLI returns before Engine creation (`sql_application_queries.rs:139-165`), so it cannot automatically explain the common CLI residual |
| Formatter directory invocation | `formatter_discovery.rs:36-58,99-108,110-128` validates rules once and lazily reuses per-rule sparse matchers; `check_and_package_commands.rs:582-591,695-724` shares the invocation owner | B46 reuse is present; no per-file recompilation defect remains here. Required selection, per-path matching, validation and actual formatting still cost work. No source-proven sufficient tiny matcher repair is established |

The new full shared HIR inventory remains for canonical selected demand
(`stdlib_codegen_metadata.rs:7-12`, `stdlib_interop_demand.rs:46-79,100-124`).
B46's no-HIR emission metadata, unit module-body result, once-only final
single/project/test assembly selection, immutable visitor and source-ordered
projection are intact. The removed discarded plans are not residual defects.

For LSP, current independent work-process RSS154812416–156172288 is already
near aggregate155418624 despite80 versus460edits. That weighs against long
edit history being the sole cause; it proves neither a leak nor no leak.
`frontend_reuse.rs:55-60,158-160` prunes unshared entries, workspace trace remains
bounded and protocol events retain20. Preserve current project semantics,
source/definition maps, cancellation/version identity and retained snapshots.
B46's passed snapshot test is reused, not run again.

Required semantic additions differ from waste. Current structural/generic
identity, adapters, default factories, const specialization, SQL declarations,
and source-based trust must remain correct. A shared cache can retain legitimate
state even when a particular command does not consume every representation.
Full startup codegen, metadata copying and pending-contract duplication are
specific candidates for measurement; none is assigned a cost fraction here.

## Baseline source, toolchain, configuration and backend crosswalk

The actual work artifact comes from producer
`b59fb265cf58327e0018778b39e5cd5001bd6edd`, written by
`67ee71150285f1ee90cff5d28a7758f04ced937f`; SHA256
`56f91e711d78b4efb7e759e20868a3edec67c95f5b70368f08ddb5e70f98cc26` is unchanged.
Old wall-time `baselines.json` is not this authority.

- Static loaded inventory: baseline90 modules (62public/28private),494368bytes,
  17112lines; B4689 (63/26),498665bytes,17105lines. Two SQL declaration modules
  added; public bytes and private bytes/collections removed;51sources changed,
  36unchanged. Counts derive from actual manifest constants, not every file in
  `stdlib/`. Similar source volume does not imply equivalent semantic work.
- Both sources build benchmark helpers in `target/debug`; driver/lowering/
  type-system/codegen dev optimization settings are unchanged (opt-level1 for
  the three compiler crates and Ruff parser). Native generated programs use
  release, a separate boundary. Root release LTO/codegen-unit settings agree.
- Baseline workspace declares Rust1.93/edition2021 and has no
  `rust-toolchain.toml`; contemporary owner record mentions Rust1.94 during
  integration, not an authenticated exact capture binary. Current source pins
  Rust1.98.0/edition2024/resolver3. B46 saved metadata gives rustc1.98.0
  `(88d9e12ae 2026-08-18)` and Cargo1.98.0. Exact baseline executable/toolchain
  remains unbound without raw capture; do not treat the manifest floor as its
  measured rustc version.
- Ruff gitlink changed from `e024f2a4870568734a3b215226570aab87e396a2` to
  `f19957111640fdee8055bfe5b6aa854259344473` (0.15.12 to0.16.4 fork line).
  Dependency lock changed; ignore/globset and other compiler dependencies
  differ. Current `.cargo/config.toml` also pins Syntaqlite's SQLite version.
  Wasmtime/component/SQL/structural crates are additional backend/host owners;
  a workspace member alone is not proof it executes in a benchmark.
- `sysroot.toml` itself is unchanged, but loaded source and implementation
  inventories differ. B46's source-tree sysroot/toolchain metadata has the
  development zero content digest; this is not a content-equivalence proof.
  B46 selected-only attachment/probes and separate bridge cache remain; no
  pre-application all-backend probe regression is restated.

Formatter source-level command is still absolute
`<root>/target/debug/sifr fmt --check --no-cache
<root>/verification/areas/performance/formatter_project`, cwd root. The two
`.sifr` files and explicit-large control are identical in baseline/B46 Git
objects. `.gitignore` is identical. The corpus now has a20-byte `sifr.toml`
containing `[source] root="."`, absent at baseline; current format config is
resolved from cwd (`check_and_package_commands.rs:564-570`), so the nested source
manifest is not evidence of a new formatting option. Keep this provenance
difference explicit without assigning it the excess.

Baseline `check_and_package_commands.rs:618-665` used substrings over absolute
paths and could exclude every file if the checkout ancestor matched `/tmp/`.
Current root-relative validation and sparse matching implement stronger,
required behavior. Escaped/compound rules conservatively reach the matcher;
both real files are formatted in the validated current invocation. Formatter
45331927 instructions versus38059926 therefore has multiple concrete changed
inputs/owners. No evidence singles out another mandatory-literal micro-edit,
and old zero-file status cannot be manufactured from B19/B20 later roots.

LSP baseline generated a temporary Cargo package/lock/**sifr.toml**, not a
manifestless file. `ddcecec119601724487adef2e1f888fc65a646de` moved it to the
canonical `query_projects/lsp/sifr.toml` plus `src/main.sifr`, with a pure source
rename. This is proven project-preparation drift, not quantified RSS attribution
or permission to revert the fixture. Keep the actual aggregate/current project.

## One bounded old-capture lookup: UNKNOWN

[Provenance](ad-hoc-emitted-rust-b47-artifacts/provenance.json) and outside-Git
`evidence/lookup-scope.json` record the exact searches/command receipts. Target:
`f8c09d47e29ecbdfe2c8d9efb451db40481cc519a0d8c96a2cda247c80051713`.

Searched archive host-variance148-176 and its actual owner3100/PR3101/PR3116
links; issue body/sixcomments, PR3101 body/no comments and commit metadata,
PR3116 body/twocomments. Inspected exact67ee/b59 metadata, the referenced
21895-byte phase blob and21363-byte governed budget blob, and targeted locator
matches in67ee's phase/reviews/performance data. No raw capture asset/path is
named; `target/performance/m3` is absent from that Git tree.

Within the explicitly named historical stores, inspected matching evidence
filenames and terminal index pointers, B19/B20 index locator fields, B41's
authentication/source-matrix records and immediate performance-directory
entries. They expose later B19diagnostic/B20bench-1788825805-84570 only; B41
has no performance directory. No target digest/b59/capture/baseline locator
matches. Metadata/path/size preceded selected content reads; there was no
plausible raw candidate to hash (zero raw candidates hashed), hence no match.

No arbitrary home/object traversal, unreferenced asset enumeration, B13 bulk
preservation scan, expired410 retry, rebuild, recapture or zero-file control.
Original absolute formatter root, raw argv and protected capture inputs remain
**UNKNOWN**. This is bounded non-recovery, not proof bytes do not exist elsewhere.

Separate policy disposition: all nine limits remain enforceable. Proven input,
toolchain and semantic drift warrants Police's comparability decision; it does
not itself authorize rebaselining, a waiver or rollback. Historical formatter
CV cause remains unknown and is not an automatic delivery prerequisite.

## One next action: bounded current startup attribution

The full [diagnostic registration](ad-hoc-emitted-rust-b47-artifacts/diagnostic-registration.json)
specifies commands, controls, predictions, units and first-failure stop limits.
No measurement is launched by B47.

Currently installed public capability was read directly: xctrace16.0(17F113),
`/Applications/Xcode.app/Contents/Developer/usr/bin/xctrace`; `list templates`
includes Time Profiler, `list instruments` includes Allocations, and current
`help record/export` documents launch, combined instrumentation, time bounds,
TOC and XPath exports. All seven metadata/help receipts are preserved. Actual
recording success/symbolization/export schemas have not been asserted.

Propose one independently owned exact-df0056 source clone with the authenticated
B46 arm64 binary copied into its private `target/debug` (no build), SHA256
`1d34f40dde09932a852be1420a72493de548de9c14def52b792c45a528248424`.
Use own exact-source sysroot and TMPDIR. Exactly four process samples in order:
`--version`, `check <absolute arithmetic.sifr>`, same check, `--version`.
Each is launched through Time Profiler plus Allocations. No warmup, retry,
baseline executable, LSP/formatter target, changed source or extra representative.

Discrimination: both check traces must resolve whether bootstrap emission/
metadata/private-plan construction or parsing/lowering/canonicalization is the
leading source family. Allocation/free/live events separate temporary churn
from state retained before process exit. Version is an executable-startup
negative control, not an exact arithmetic subtraction. Report CPU stack weight
in declared time units and heap bytes/counts, **never instruction fractions or
RSS-equivalent bytes**. Disagreement, missing units/stack attribution or load-only
evidence is an explicit inconclusive result and stop, not permission for more
launches. This experiment prioritizes the common check startup; it does not
claim to resolve the separate formatter or editor-only residency owner.

Fixed limits: one setup120s/zero targets; four recorders, four launched compiler
processes (2check+2control), four TOC and four data exports;60s target/recording
caps,90s recorder deadline plus30s owned cleanup each,120s exports,720s overall.
At most2GiB new owned storage,4GiB free required. First mismatch, permission
prompt, instrument/target/export failure, timeout, insufficient symbols or
storage breach stops; no alternate profiler/build/LLDB custody sequence.
Record owned descendant identities and reap/verify release before proceeding.
Event counts are observations under a fixed template/time cap, not an invented
public fixed-frequency setting. Successful/failed evidence stays owned/sealed.

## Delivery dependency distinction and terminal handoff

The actual merge profile selects `performance:representative`
(`verification/profiles/merge.json:680-688`). Its runner binds the producer and
budget consumer (`performance/runner.py:187-226`). Thus a candidate carrying
these failures cannot knowingly pass the required merge profile. Clearing its
applicable limits is a real qualification dependency; this diagnostic is one
proposed route to finding a sufficient repair, **not itself a mandatory gate**.
If a smaller complete builtin-registration delivery demonstrably avoids an
irrelevant mechanism, Police may choose it. B47 neither proves that sufficient
nor demands importing the entire held stack to settle historical uncertainty.

Coordinator resequencing stands: B21/B26 three-file builtin closure and B25's two
unchanged list-repeat failures are actual scoped obligations. B26's prior full
unit was the smallest then demonstrated, not proof that every preserved feature
is causally necessary for SQL. No fresh bulk study or next-item code is added.

Keep all other owners/records/counters open: B26/B27's retained264 companions,
90native/411check, Python30, exact65SQL, all-area/toolchain/original-quality/full
delivery obligations;12D/E/F/full12/final12A and whole-phase review remain later
phase work. They are **not independently asserted as SQL prerequisites** merely
because they share a phase or PR3717 association. The pre-existing test-runner
`artifacts.rs:67-79` empty InteropBuildPlan has no runtime attachment consumer;
B46 plan retention does not implement it, and B47 does not repair it.

Original12K4FAILED+1RESOURCE, B13two/B14one/B15two failures, exhausted
B20initial+B22remediation, separate B42/B44 initial SAT, B42admission/HOSTtimeout,
B44budgetFAIL and B46initial+sole-remediation SAT/onebudgetFAIL remain unchanged.
No third review, hidden rerun, baseline/fixture/test weakening or main-delivery
claim. Relevant documentation checks only: diff whitespace and source-file-size
guardrail; no tests/self-tests/builds/Cargo/uv/compiler/profiler/host sampling,
Opus, create-PR/merge gates, PR or merge. Only this report and its four JSON
companions are authored in Git, with evidence/readers outside it.

Own record branch is committed/pushed and the actionable report/hash links are
published to owner3776. `evidence/terminal.json` and `evidence/inventory.json`
bind final record/publication/checks. Callback goes to parent and Police for
immediate adjudication. **Assessment blocker: none. Stop after terminal handoff;
do not start the proposed experiment, builtin/list-repeat repair or next item.**
