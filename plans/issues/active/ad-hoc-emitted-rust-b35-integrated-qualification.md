# 12K-B35: integrated module/custody/output coverage qualification

Date: 2026-09-08. Owner: performance / issue3776. State: OFFLINE PASS, PRELAUNCH FROZEN.

## Scope and exclusive ownership

Owned independent fresh-main clone `/private/tmp/sifr-b35.a3TVXg/sifr`, branch
`codex/item12k-b35-integrated-qualification`, base
`b92faf990877d2f553c70b2d29994d82ac60d4f0`. Separate index/object store, no
alternates/shared target. Private sibling `tmp` and external `evidence` (E).
Parent's intentional dirty phase/foreign Python ledger and all predecessor
files/indexes/refs/raw are read-only. Complete parent TOP B35 registration
and full275-line B34 scoped contract are the execution authority. Standing user
authorization and coordinator conditional host reservation cover this one proof;
send committed/pushed prelaunch identities, then proceed without routine ack.

B33 terminal SHA256
`218d0b5cb0b7f4c012117a6b37c5007b6c66529642506f220b2c618055e5ae5b`, frozen
manifest `81597f166178476699cb73a6ca7b13c980624125a5603c89c6b7cafc5e185a31`.
B34 terminal `2bbf3f58ded1555c74d5cfc30a81f68f2b299e1b3b9485b9cdee2f0880c45a31`,
manifest `f54cda93e7518f6d234fd12afd30b3c74098e4020692a3ebb808e3ec7688127e`.
All52 combined terminal/manifest-bound source/input paths authenticate. Both
static contracts and phase records are main-merged, native owners closed.

Complete B34 helpers and inputs are owned copies; only executable owned paths
and the run identity relocate. B33 native module equality and all alias file
roundtrips, immutable custody identities, typed address/transition guards and
canonical exclusive-file output producer/consumer are preserved.

## Adopted B34-F1 repair

B34's exact-SHA review recorded cleanup iteration over growing target/group sets
as infrastructure follow-up B34-F1. Coordinator explicitly adopted it into this
pre-proof integration. `coverage_cleanup.py` owns stable target/group scheduling,
repeated bounded fresh discovery, and the final fresh full-table absence decision.
`run_coverage.py` uses these shared functions in watcher and terminal cleanup.
Each signal callback still uses unchanged `Custody.cleanup_allowed` with a fresh
capture, all original member/ancestor identities and sticky taint/rejection.
Newly discovered roles enter subsequent bounded passes; omission from an earlier
snapshot never proves absence. Final group checks use the set obtained after
the final full observation. Rejection still makes completion INCONCLUSIVE.

`coverage_cleanup_tests.py` injects only synthetic rows/clocks/signals and tests
set growth, late sole target, second-target rejection, foreign membership,
changed ancestor, wrong group, unknown lifecycle, bounded survivors and final
fresh absence negatives. The actual output consumer rejects newly surviving
groups. All349 inherited cases remain in the sole encompassing suite, including
the combined module/custody/output decision. Python3.9 parsing includes new files.
B34-F2/F3/F4 are not implemented. No new native-generation, subsecond, atomic
process snapshot or check-and-signal guarantee is claimed.

Owned registered files: E/`coverage_{symbols,address,module,module_fakes,module_tests,
custody,custody_tests,output,output_tests,cleanup,cleanup_tests}.py`,
`lldb_coverage.py`, `run_coverage.py`, `coverage.lldb`, `record_b35.py`,
`identity-contract.json`, `module-inventory.json`, `inputs/`, `sifr-experiment09`,
and generated authentication/offline/frozen/prelaunch/raw/terminal receipts.
External root `registration.md` records exact paths/commands before use.

## Named validation and immutable execution inputs

Only this encompassing suite family (preserve original receipts on changed-code
in-scope correction; no live process tests):

```bash
env -u CARGO_TARGET_DIR TMPDIR=/private/tmp/sifr-b35.a3TVXg/tmp PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 /private/tmp/sifr-b35.a3TVXg/evidence/coverage_symbols.py --self-test
```

Immutable binary SHA256
`a4386baecd7576b256dc6d68eb2e240952f591fa0a468df55f60080189c4b392` copied from
B32; complete13-identity contract
`3ebc80789a4d9843badee45fb69587b12dd62101f0211a6b09d3a59896a1972d`.
Ten inherited runtime/installed identity/workload/config paths authenticate,
including unchanged two-file formatter_project, root/project sifr.toml,
host/process helpers, LLDB Python API and dyld. No build or compiler mutation.

After suite PASS, `record_b35.py freeze` binds all final tested sources, raw
inputs, runtime/config inputs, receipt and ownership registration. Commit/push
the prelaunch registration before callback and sole live command:

```bash
env -u CARGO_TARGET_DIR TMPDIR=/private/tmp/sifr-b35.a3TVXg/tmp PYTHONDONTWRITEBYTECODE=1 /opt/homebrew/bin/python3 /private/tmp/sifr-b35.a3TVXg/evidence/run_coverage.py
```

LLDB argv: `/usr/bin/lldb --no-lldbinit -b -s E/coverage.lldb`. Target argv:
`E/sifr-experiment09 fmt --check --no-cache OWNED_CLONE/verification/areas/performance/formatter_project`.
Run ID `12K-B35-a3TVXg-output-1`. One debugger session/target/setup, flags134,
300s total including30cleanup,180admission/30setup/60target,256events,1GiB evidence,
4GiB free floor, distinct outer/embedded clock ownership and authenticated custody.
Canonical owned stdout/stderr files only; require one completed mode3-to-mapped-
mode0 handover, all8 ordered entries, exit0, exact2-file completion and zero-
process release, retaining full raw identity/custody/output-route evidence.
First live failure is terminal: no repair/retry/fallback/guard bypass, record
precise later issue3776 owner and stop without Opus/merge. No historical retry.

Success only: `git diff --check BASE HEAD`, Homebrew Python
`scripts/check_hir_maintainability_guardrails.py` and
`scripts/check_file_size_guardrails.py` in the same private environment; external
maintained source sizes below900. One exact-SHA initial Opus plus at most one
remediation covers frozen external apparatus AND live evidence, read-only/no
tests/new requirements. Second-review new mechanism defect becomes later work
and stops. Docs-only PR/main merge plus phase record, then STOP. No compiler,
fixture, lockfile, workflow, build/Cargo/Clippy/CV/counter/Sifr gates or next code.

Historical B32 object identity, conflicting process row/cause and cross-process
ordering remain UNKNOWN; prior proof INCONCLUSIVE/output completion UNREACHED.
B31 handover/all8/exit0 with missing observer streams is not a PASS. B24 causal/
full unchanged representative budget, SQL/B27 delivery including builtin fix/
approved65, and the original emitted-Rust phase remain open regardless of B35.

## Frozen prelaunch registration

One named suite invocation: PASS365/365 (all349 inherited plus16 cleanup cases),
zero failures, zero live process tests. Python3.9 syntax parsing PASS. Receipt
E/`offline-result.json` SHA256
`fede088d3704204876713448c58e7bb6e36aac40e1c074b1600e54497d1a2866`.
All tested-source digests reauthenticated by the freeze step.

E/`frozen-manifest.json` SHA256
`7606a96e060b17f91931549cdc77388d959264f4c26ee05c65de9a25e3dd0175` binds full
final external source/input/receipt/runtime/config and ownership registration.
E/`prelaunch.json` SHA256
`9dd81f40fa6ef5bd1de9ae0ab2c22e0664bbc5aa7e0338bb2ebfdea8f09e3032` binds exact
commands, unchanged binary, private output run identity, limits, ancestor config
and frozen manifest. Live proof has not yet run; all claims above remain static.
This registration must be committed and pushed before callback and actual launch.
