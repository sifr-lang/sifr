# 12K-B26: complete joint emitted-Rust delivery assessment

Date: 2026-09-08. Owner: emitted-code phase; this is dependency coordinator
Item68's assessment, not another policy or compiler worker. This item changes
documentation only. No compiler, fixture, workflow, lock, harness, build,
benchmark acquisition, behavioral test, Clippy or Sifr gate was executed.

## Decision

Deliver one changed-input integration of the complete retained emitted-code
contracts and the exact approved item65 policy, after the separately owned
performance qualification blocker has been resolved. Do not require65 to merge
first: its failed gate requires H/I/M1, while the emitted integration's full
distribution coverage must preserve65's approved replacement for expired-waiver
policy. This is a qualification dependency, not a compiler call into governance.

B21 correctly proved that builtin registration has a three-file independent
source closure. B25 demonstrated that this is insufficient for independent
delivery: its full affected crate run fails two unchanged list-repeat tests.
H and I likewise stopped on shared qualification prerequisites; M1 requires I
for the original async native cases, and65 actually fails H/I/M1 signatures.
Separate source patches are possible, but the available evidence does not
establish separately mergeable, fully qualified prerequisites. Requiring their
main merges before allowing a combined candidate would recreate the cycle.

The selected delivery scope is the existing complete original integration
contract, plus65 and B25's strengthened assertions. This is the smallest
**demonstrated complete delivery unit under those accepted contracts**; it is
not a claim that every12B line is a syntactic dependency of a singleton repeat.
The narrower semantic closures and the reasons for the additional qualification
inputs are distinguished below. No mathematical impossibility of another future
extraction is claimed. A smaller candidate that drops the accepted corpus,
support-quality or full-profile obligations is not a complete delivery of this
chosen unit. Retained Item12's broader residual work remains separate.

The proposed next integration is12K-B27, not a retry of98480 or4c7068b under a
new name. B26 neither grants nor spends its proposed review/gate allowance.
B22/B23/B24 remain separate mechanism owners. They are not implemented here,
and none of their unqualified source is imported into this documentation PR.

## Authenticated inputs and limits

The initial source-assessment main snapshot is
`3a7bf16a722912eedc63e1b6d3942b62b65784ce`. Before the first Opus request,
main advanced to `91004bfb154b23980380863eaa1965ddc69099e4`; B26 normally
merged that base and incorporated its relevant coverage changes below. The
final B26 review base is91004bfb, with only the three scoped Markdown files
in the candidate diff. The independent owned clone is
`/private/tmp/sifr-b26.hQtWkE/codebase`, branch
`codex/item12k-b26-joint-delivery-assessment`, with sibling `tmp` and `evidence`.
The parent's two dirty Markdown files and all predecessor clones, indexes,
targets, artifacts and local-only refs are read-only. The authoritative B26
registration was read from the parent's uncommitted top section; its historical
600KB phase file was not copied into main.

| Input | Full source / record identity | Delivery state |
| --- | --- | --- |
| B21 assessment | candidate `e7d8b64b31dea715a7710e309dc03ff14ab19e55`; merge `d7671085653a0d634449cf112e9420b4b790b925`; record merge `3a7bf16a722912eedc63e1b6d3942b62b65784ce` | [3787](https://github.com/sifr-lang/sifr/pull/3787) and [3788](https://github.com/sifr-lang/sifr/pull/3788) merged; assessment only |
| B25 | source `64847befe1722df5848a7ca99e913431f5797d0b`; record `7f3330c4599cd2e59c977e4cb771487b9d343b31` | remote source/record branches preserved; no PR/review/gate/merge |
| 65 | source `4c7068b36904e216b02778f5664d2b8fd1159a6a`, base `6bd085f40e42c04b1d81a09ce1660a392541bd1e` | [3785](https://github.com/sifr-lang/sifr/pull/3785) open draft, source reviewed, gate failed |
| H | source `9b52ac20094608c8a31f252db99e49ef7c963384`; record `b6e6210a97598fb631b929b2d4daf4012b41bb16` | [3697](https://github.com/sifr-lang/sifr/pull/3697) open draft |
| I | source `f6e8afd964bb214a44c50271dcb2014ee8e828b4`; record `19ad69969a672d7b741122ded4dd879f2bdaf9ab` | [3698](https://github.com/sifr-lang/sifr/pull/3698) open draft |
| M1 including12J/R1 | source `d726ffc11258c49f0185fd2d49697988cf90972c`; record `a7e13eb45006eac925417491b89a932af5df2595` | [3700](https://github.com/sifr-lang/sifr/pull/3700) open draft |
| 12B including builtin12C | full `8e532f15895e7005fae8c658739ba3c3a6818c18`; original builtin fix `3f422b01633d23c2bc8d8ce8ca59057c6e56adea` | [3694](https://github.com/sifr-lang/sifr/pull/3694) open draft |
| Original integration/B13/B14/B15/B18 | full source reference `98480c78587d6cbd99a7079d10c71825360bd468`, fork with main `156157242b0995c01c4fff03575624b5c471c0d8` | [3717](https://github.com/sifr-lang/sifr/pull/3717) open draft |
| B19 diagnostic-only successor | full `fb66ed00d7672b9f5c623e3291d1b8498d50ec45`, following B13 record `4763c8c99a95eeac7f9e272b851e0f814d33bb78` | not a cause-correction or delivery pass |
| B20 | source `ac4c277b30c6cac046ab1746fe4020c04df7eaf1`; full record `96d051e4c16771ae1902f6e8aa77ecd836d45fa6` | [3783](https://github.com/sifr-lang/sifr/pull/3783) unapproved/open draft |
| External corpus | `8bcbe7ab7939e5c8362c10f61a80e368022cc372`, base `7fcb9fd1eaf3e0cf9bf51e8858276b7927a83baf` | [leetcode48](https://github.com/sifr-lang/leetcode/pull/48) open; retained explicitly |
| Excluded residual Item12 | `8ad089a9458f35fcfa228e93fe44f4d69731828b` | no silent import, completion or disposal |

Full B21 assessment was read at
[ad-hoc-builtin-registration-delivery-assessment.md](ad-hoc-builtin-registration-delivery-assessment.md).
SQL candidate `4f53a1ce39f612e5e8c26b8802e5ef798c07d026` was read-only fetched
from B21's `refs/assessment/sql` into the owned clone, preserving the original
local-only reference. Its Clippy log SHA256 is
`d13d32cf4a9190517172cd86c1cb204d660a641c14ae550c56f9202ce2be6b75`;
this remains the builtin registration defect, not permission to repair SQL.

B26's external `source-audit.json` authenticates18 retained commits, nine PR
identities,558 paths across main and retained source blobs, and the following
raw receipts. The companion [exact input inventory](ad-hoc-joint-emitted-rust-delivery-inputs.md)
enumerates277 original integration delta paths (five historical issue records,
124 generated companions),28 item65 delta paths and three B25 paths. It also
records all16 gitlinks. These are source references, not reviewed new code.

| Receipt | SHA256 | What it establishes |
| --- | --- | --- |
| `/private/tmp/sifr-b25.MUmZ3C/evidence/terminal.json` | `53b1f40689c660c36e43a9ceb58024aa6ea908d9ed990d8e64635d3e82194fe3` | actual B25 stop and both unchanged test/producer blobs |
| B25 `focused.log` | `2a95c66bb2d02a30312cb8b1a810994cb2eade243327b044333e3b51f2a0c33a` | two builtin tests pass |
| B25 `codegen.log` | `7976234006e427308dfb721c4945b3c6e57d19248f8c71b4e70fc5ecb0879e59` | 1409 pass, two list-repeat failures; driver/Clippy/fmt unreached |
| `/private/tmp/sifr-item65-delivery.zoLQU3/terminal.md` | `afa5c0dbdf8e6acc169bf0ebb4d16cba4d14d88d8c9f04b7620c62d63348db45` | actual terminal, per-file digests; [durable publication](https://github.com/sifr-lang/sifr/pull/3785#issuecomment-5577596125) |
| 65 `validation.md` | `037ab56a7f57ca12856bf9aeb6d6daa5b9410e4e3d783d3a55e22fcfa3ff6e48` | pre-review focused evidence; its older zero-gate statement is superseded by terminal |
| 65 `merge.log` | `19388f08a16e4bf6a66a8de68843370a9311f3e665670b96b0d9cfc820cd4d08` | actual failed gate,2887.09s |
| 65 `python-interop-results.json` | `ec75bab1639395b7e5b89a6d31febf65dbb562062192b41009a771272b8188f5` | 30 variants,26 pass/four fail |
| `/private/tmp/sifr-b13-integration.oBoJJW/gate-outcome.98480c78587d6cbd99a7079d10c71825360bd468.json` | `57415124d13a0c6429e4e4020d7544235bf5b7fbb1bd3830364b57527b2d2235` | ten areas pass, performance fails, remaining lanes unreached |
| Same root `input-review-mapping.98480c78587d6cbd99a7079d10c71825360bd468.json` | `b1dcfa5f7349a3ba261149e984616deaadf7167d973943c2804bdb2b2d41e279` | exact B13/B18 review and277-path mapping |

65 has no terminal.json, inventory manifest or terminal-record commit. Its three
dirty local documents are not approved source. The transient failed Rust files
are absent; retained stderr and the Sifr binding archive (SHA256
`f6397580895bcdfc3c3e779ef789eb5215c55d9b1df60f82705e50ae817b743f`)
are the available historical evidence. No failed Rust was regenerated. Before
claiming any older artifact missing, consult R3
`/private/tmp/sifr-storage-r3.TigHgT/{preservation-map.json,additional-preservation-map.json}`
and R2 `/private/tmp/sifr-capacity-r2.G8tOjZ/preservation-map.json` and
`additional-verified/preservation-map.json`. No cleanup is part of B26.

## Root causes, callers and complete semantic boundaries

### Builtin registration and list repetition

B25 retains the complete B21 token/registry contract: private catalog-backed
`BuiltinError`, total canonical identity, partial string lookup for other callers,
all four typed nominal collectors, both production registration callers,
module-shadow/global-Rust identities and both binary/test-project consumers.
Use B25's three source paths for its expanded round-trip, repeated-registration,
custom/qualified/lookalike/empty-name rejection, absent-definition and global
WorkerError/WorkerRuntimeError assertions. Merge these assertions into the
integrated nominal file; do not overwrite M1's module bindings or constructor
type discovery with B25's smaller file.

The list failures are **valid missing optimization contracts**, not evidence
that either assertion should be weakened. Main/B25 producer
`stmt_support_emitter/stmt_expr_binop.rs` has blob
`efda96c1a677b95f8c33dc1f1e90575007c4ca5f`. Test blobs are
`b1e0d5560c50daedfab81d3dddc2e9a90a486968` and
`d5dd9ac4f142fcf6668313b1b95ef4275214a0e9`. Both tests require
`std::iter::repeat(SifrInt::from_i64(0))`; the performance test also requires
`collect::<Vec<_>>()`, and both reject an output-extend loop. Main's common
list/bytes branch always clones a temporary source collection and loops with an
exact-integer counter. This is the same defect recorded under Naming cleanup
validation findings, not introduced by the builtin token change.

12B's existing `singleton_repeat.rs` selects a statically singleton lowered
`RustExpr::Vec`, evaluates each operand once in source order for both operand
orders, and emits `std::iter::repeat(element).zip(SifrRange::new_known_nonzero(0,
count,1)).map(...).collect::<Vec<_>>()`. Counts remain exact `SifrInt`; negative
and zero counts yield empty output without a host-width cast. A general list,
bytes or string still uses its supported general lowering; no blanket
singleton rule is inferred for an arbitrary variable collection.

The exact helper chain is `stmt_expr_binop` → `lower_singleton_repeat_for_ir`
→ `materialize_reusable_value_for_ir` in
`nested_subscript_assignment_helpers.rs` → existing
`clone_moved_names_in_borrowed_aggregate`, `ownership_plan::materialize_owned_value`
and `last_use_move_exprs` from body analysis. The retained body-analysis delta
requires every occurrence in a statement to be accounted for before moving a
reusable name. Count reuse therefore cannot be delivered by copying only the
singleton helper. The general string/list/bytes operand materialization and
order changes belong to the same repeat-count contract. The existing
`corpus_repair_repeat_count_reuse` and
`corpus_repair_repeat_count_reuse_effectful_and_nested` cover reusable counts,
loops, nested expressions, effectful calls and both evaluation orders.

12B's complete accepted corpus contract additionally owns sentinel reads,
checked-place/disjunctive reads, exception capture scope, optional nested
mutation, comprehension ownership, recursive storage/mutability and reachable
method retention. These are traced through the `checked_place` submodules,
`body_analysis`, `place_emitter`, `ownership_plan`, `lower_expr`, structured
statement emitters and their named corpus/reusable/nested-container regressions
in the inventory. They are **corpus qualification dependencies**, not invented
H/M1 prerequisites. The external90-source migration and411 check-only corpus
remain coupled to those accepted mechanisms. Retained12's unrelated whole-phase
cleanup is neither needed to call the repeat helper nor approved for import.

### H: declaration-owned fields across every generated file

65's binding-authoring E0560 errors are eight initializers in
`src/binding_authoring/math_python.rs`: generated PythonError declares
`python_error`, consumers initialize `sifr_generated_python_error`.
H's complete source delta starts before9b52's last remediation commit:
`dba6a8f7075ea071058654c85ed1e46e4d1272fa` establishes the registry and
`9b52ac20094608c8a31f252db99e49ef7c963384` closes bridge materialization.
Taking only9b52's parent diff omits the mechanism.

`generated_rust_canonicalizer::canonicalize_generated_rust_project` builds the
field registry keyed by module/nominal owner. `field_name_cleanup/{registry,
rewrite}.rs` resolve imports/re-exports, generic and nested owners, local typed
receivers, method returns, Result/Err payloads, struct initializers/patterns and
member reads. `identifier_canonicalizer` keeps field and value namespaces
separate, including shorthand. Same-owner collisions and keywords are injective;
external fields keep their spelling; an unresolved generated owner errors.

Driver `format_generated_binary_project` must assemble `bridge_modules` first,
then canonicalize main, support and bridge modules in one family. All
`GeneratedBinaryProject` constructors in build/project, Python, sysroot and
Rust interop tests initialize the new field. Bridge-source keys become module
identities rather than paths. The cache key includes finalized bridge sources;
materialization maps their identities to paths and uses
`format_canonical_generated_rust` without recanonicalizing fields independently.
Binary listings retain their original boundary. The test-runner orchestrator
uses the same shared-field then format-only ordering. All those callers and
contract tests are in the exact inventory; they cannot be reduced to PythonError
or one bridge producer. H's core registry, rewrite, materializer and formatter
blobs compare equal to98480. Its21 affected companions are production outputs,
covered by the final whole264-pair generation, not copied as current evidence.

### I plus B13/B14/B15: macro declarations, demand and support quality

65's three failing callback examples (`asyncio-roundtrip`,
`callback-reconciliation`, `pubsub-retained-async-close`) reference inaccessible
`SIFR_GENERATED_SIFR_TASK_CANCELLATION`. I's full implementation includes
`2109ac57c9b474ceffd3efea317be5f82c739042` plus final f6e8's parser cleanup;
f6e8's last diff alone is not the repair. `task_local_support` parses only the
compiler-owned `tokio::task_local!` grammar, retaining attributes, type, identity
and cancellation operations. Visibility, defined-name collection, item-name
parsing and pruning in `stdlib_filter/implementation.rs` share this parser and
split declarations for independent demand. Binary and test-project support
assembly remain consistent. Unknown macros/nested owners are not blanket-exported.

The standalone I repair has a five-source-path closure plus its one affected
companion and architecture record; B13 is not necessary merely to parse its
macro. For the selected full quality contract, however, I's blanket crate-visible
support and wildcard imports encounter the actual B13 representative failures.
B13 makes only externally demanded declarations and required signature types
public inside a private support module, retains helper/import privacy, and emits
explicit consumer imports. It adds the `consumers` argument at both support
assembly sites. `TaskLocal.ty` becomes crate-visible to close visible-interface
types; the macro grammar itself is otherwise identical to I.

B13's importer (`stdlib_filter/support_import_refs.rs`) and canonicalizer
`item_dependencies`, `project_support_pruning` and `support_import_cleanup`
must use B14's `format_capture` parser and scoped-reference collector. A format
rewrite to `println!("{ACTIVE}")` must retain the constant and import; lexical
shadows, type/value namespaces, width/precision and explicit named format slots
must stay distinct. B13's continuation regression locks the shared parser
semantics. B14 alone produced two stale companions, which B15 regenerated;
B13 subsequently regenerated118 companions. The final stack has124 changed
companions overall. These counts overlap; do not add them or retain only the
two B15 files. Full freshness remains264 pairs.

B13's representative debt is a source-attributed baseline over12 surfaces:
522 retained diagnostics/19 codes after the targeted support warnings and six
proved interface warnings were retired. Its JSON source hashes, Markdown
derivation and B18 taxonomy correction form one qualification contract. They
cannot be copied as proof for changed generated bytes, blessed from failing
Clippy output or expanded into retained12's all-surface completion. Reconcile
newly changed input hashes before Clippy using the same source-proof rule.

### M1: required message, ancestry, storage and consuming conversion

65's `httpx2-client` (`main.sifr:56`) and `aiosqlite-session` (172/201/232)
reject PythonError raised into Result[None,Error] with RESULT0003. The accepted
M1 contract explicitly requires a constructor-supplied `str` message for every
error. This user adjudication supersedes message-less or integer-message forms;
do not reopen it or substitute Display/default strings. PythonError retains
exactly its five fields and no duplicate message or extra constructor argument.

M1 requires its preceding12J/R1 implementation, not just d726's36-path delta.
The complete source closure is enumerated in the inventory's M1 rows:

- Lowering class collection/body/descriptor passes seed and validate message
  storage, forbid field-default/non-string messages, forward inherited
  constructors, and check explicit `super().__init__` argument types. Declaration
  diagnostics and imported-class identity preserve the actual owner rather than
  an `Error` basename. `required_error_message_tests` uses insta; its dev
  dependency and the matching Cargo.lock edge are required test inputs.
- IR `class_ancestry::semantic_parent_chain` preserves the semantic Error marker
  separately from physical data-parent storage. Frontend callable/slot/
  specialization/query exports and driver stdlib bootstrap consume that ancestry;
  codegen class fields, signatures, entrypoints, union helpers and module
  generation use the same identity. This closes imported and mixed-marker cases.
- `error_refs` and its checked-place/conversion submodules collect declaration
  and reference demand. `ModuleSupportDemand` merges it by canonical identity
  and renders conversions once in shared support. `MessageStorage` follows
  owned embedded parent fields; `class_upcasts` converts directly to root Error
  before losing a child's message through a non-error data parent. There is no
  synthesized Display fallback. Existing compiler-invariant hardening suggestions
  and unproved reference-only SQL storage reachability remain separate follow-ups.
- `project_stdlib_nominals` collects constructor-only expression types and
  `module_bindings` accounts for imported shadows. Both binary and test-project
  consumers use those names. These intersect B25's nominal plan and B13's support
  assembly and must be semantically combined. `python_interop_common` removes
  its duplicate special conversion path. Runtime channel error constructors,
  `stdlib/sifr/sync.sifr`, parallel-map demo and E2E callers supply messages.
  The opaque negative fixture retains its assertion with an unrepresentable
  payload under the new contract. Native ordinary/project/test-project, Python
  five-field and async-error-channel regressions remain required.

The M1 frontend/IR/conversion implementations match98480; lowering differs only
in the separately owned TypeVar diagnostic expectation correction. Keep the
approved exact diagnostic codes/ranges and corrected wording; do not waive the
original two #3667 failures. M1's local native tests passed historically, but
original async suites reached I's E0425 and their runtime assertions were
unreached. The integrated Python30 pass is distinct later evidence.

### Item65 policy remains externally owned and exact

Retain all28 source-delta paths listed in the inventory: both publication/recovery
workflows, live approval CLI/policy, historical waiver adapter and self-tests,
release-plan/signoff/bootstrap/orchestrator consumers, both evidence schemas,
materializers, publication scripts, workflow-contract tests, submodule ownership
adaptation and synchronized architecture/distribution/incident documentation.
The approved policy requires GitHub-recorded approval by designated User
`yaseralnajjar` (10493809), permits self-review, disables admin bypass, and binds
repository, operation, run, attempt, initiator and evidence digest. It covers
bootstrap-alpha/index/recovery, GA, normal, rollback and incident roll-forward.
Live waiver selection is retired; historical evidence retains event-time validity.
Governance scripts come from the workflow revision; exact source submodule
checkout ownership remains enforced. Preserve missing/wrong approver, identity,
bypass and retired-waiver negatives. No release approval, publication, settings
mutation, waiver renewal or policy reinterpretation is part of B26/B27 delivery.

65's reused11/11 eight-suite proof came from unchanged policy inputs at
`3c481750c977d182464cdd90932843eabaedbc48`; its final readiness4/4 passed.
The complete gate then failed the compiler signatures above. Its
[initial SATISFIED review](https://github.com/sifr-lang/sifr/pull/3785#issuecomment-5577167381),
raw SHA256 `72ceeb192cdca02762d22e302c652641fdaeffe0ebac4f2fa715bb872c1f1670`,
is reusable policy reasoning only after byte/consumer/schema comparison.

## Current-main merge and qualification boundaries

The exact lineage is4eef8a2bb4dbc24fb7c1d1213652be379047f4b1 →98480 →4763 →fb66
→96d051. It establishes custody, not safe extraction. Source comparison shows:

1.98480→4763 changes only phase records.98480 retains approved B13/B14/B15/B18.
2.4763→fb66 adds only diagnostic evidence retention in performance
   `controlled_sampling.py`, `run_benchmarks.py`, `sample_evidence.py` and records.
   The approval is for that diagnostic patch, not complete causal acceptance.
3.fb66→96d051 adds B20's CLI discovery changes (Cargo manifest/lock,
   `check_and_package_commands.rs`, `formatter_discovery.rs`, `main.rs` and its
   integration tests) and records. The eager malformed-ignore regression remains
   unapproved and must not enter B27 as an approved source input.
4.Main since fork156157 includes later Python runner dependency/CFFI checks,
   parsed submodule YAML contracts, the UV pin/checksum invariant, Rusqlite
   lock-context tests and B21 docs. Copying98480's tree would reverse these.
   Apply the branch's fork-to-tip semantic delta to fresh main, preserving all
   later main inputs. Apply65 against its own exact base; it intentionally adapts
   the newer submodule checker, rather than restoring the old regex checker.
   Its immutable checksum provenance comment also survives.
5.Integrate Cargo.lock's existing dependency edges (`tempfile` for codegen native
   capture tests, `insta` for lowering snapshots), without unrelated upgrades.
   Preserve the union of profile memberships, commands, modes and blocking flags:
   MySQL/SQLite smoke+full in the stack must not lose main's full memberships or
   duplicate IDs. All31 unique full-mode commands still execute. Keep the stack's
   27 coverage negative cases, including missing and stale SQL target assertions,
   together with all newer main Python-delivery mutations described below. Main's
   resolved SQL classifications are preserved rather than replaying their old
   failing gate. The stateless Python metadata test's structural borrowed/raw.0
   propagation assertion and portable-project Clippy corrections also remain.

Pre-review current-main update91004bfb adds item53's canonical
`validate_python_delivery_coverage` in `verification/runner/sifr_verify/profiles.py`,
its `profile_assignment_matrix.py` consumer and dynamically enumerated negative
mutations in `coverage_matrix_readiness_self_test.py`. They require every
non-live Python manifest suite in at least one delivery profile; an opt-in live
profile cannot satisfy that requirement. The stack's extra stale-SQL-target
negative must be merged into this updated test list, not replace it. Item58's
HTTPX2 protocol documentation, Python README and tier2 package-identity comment
also survive. The nine changed main paths and exact blobs are captured in
B26 `main-update.json`. This update changes qualification inputs and was assessed
before review; it does not change the retained compiler/formatter root analysis.
The inventory's original main column remains explicitly the3a7bf16 snapshot.

The full98480 gate passed production92,13 guards, Python30, diagnostics184,
algorithmic representative12, developer tooling42 and generated quality9, among
its ten passing areas. It failed `formatter-corpus-001-project-check` controlled
work sampling (CV0.064257/0.026115/0.048016, threshold0.02). That same formatter/
performance source is on assessed main. Therefore the blocker is relevant to
the new full gate even if B20 is excluded. No evidence identifies the cause as
OS startup or proves the B20 discovery patch resolves sampling.

B19's fixed startup controls were inconclusive. B20's first build warmup timed
out; its launcher omitted private TMPDIR and its wrapper left a descendant
overlapping the next launch. The two launches were the same first case. No
formatter or budget qualification occurred. These are separate facts:
discovery regression→B22, timeout descendant lifecycle→B23, remaining causal
and controlled representative qualification→B24/owner3776. Timeout cleanup
does not establish why the first command timed out. No acquisition is repeated
in this assessment and no presumed host intervention is registered as a fix.

The later nine areas at98480 (distribution, sysroot, workspace, packages,
stdlib, regression, fuzz, ecosystem, SQL) and both toolchain stages were
unreached.65 stopped even earlier, after Python. Neither partial run qualifies
those areas, full E2E, all31 crate commands or the final combined candidate.

## Attributable review and gate history

| Owner | Review count / verdict | Gate history retained |
| --- | --- | --- |
| 12B/12C | two reviews consumed, final SATISFIED;12C incorporated | two FAILED |
| H | initial + remediation consumed, final SATISFIED | one FAILED |
| I | one initial SATISFIED, zero remediation | one FAILED |
| Original12J/R1 | exhausted NOT SATISFIED reviews | zero gates |
| M1 | one initial SATISFIED, zero remediation | zero gates |
| Original12K | initial + remediation EXHAUSTED | four FAILED + one RESOURCE_TERMINATED143; zero pass |
| B13 | one initial SATISFIED, zero remediation | two FAILED cumulatively |
| B14 | initial + remediation EXHAUSTED, SATISFIED | one FAILED |
| B15 | initial + remediation EXHAUSTED, SATISFIED | two FAILED |
| B19 | one initial, diagnostic-patch SATISFIED; full acceptance unmet | zero gates; failed formatter acquisition retained |
| B20 | initial NOT SATISFIED, zero remediation | zero gates; INVALID_ISOLATION_ABORTED acquisition |
| B25 | zero reviews | zero gates; full codegen FAILED |
| 65 | initial SATISFIED, zero remediation | one FAILED |

Sources are each exact terminal above and the retained98480/fb66/96d051 phase
records, including B13's SHA-keyed input-review mapping. B18 was a docs merge
into the stack, not main. None of these counters is reset by a record commit,
renamed task, available disk, warmed cache or new invocation ID. There is no
passing exact-SHA complete merge evidence for an uncreated B27 candidate.

## Sequential next-item registrations — proposed, not dispatched

### Existing B22: explicit-file discovery remediation

Input: B20 exact sourceac4c and initial NOT SATISFIED review; change only its
formatter discovery/error-loading boundary and associated tests. Preserve root
relative matching, globs/components/negation and explicit-file exclusion bypass.
Malformed ignore rules must not be eagerly fatal for a bypassed explicit file;
directory selection must keep its defined diagnostics. Add negative malformed-rule
cases to the existing unit and CLI modules before running their registered
commands: `cargo test -p sifr --bin sifr formatter_discovery` and
`cargo test -p sifr --test formatter_discovery`, then formatting/diff/HIR/file-size.
These commands select the existing four unit and two CLI regressions plus the
new cases. Retain B20's one initial; at most its one remediation review remains.
A new mechanism defect at that review becomes a later item and terminal stop.
No isolated full Sifr gate is useful on the known-blocked inherited stack;
propose qualification in B27's single complete gate, explicitly at dispatch,
without claiming a standalone main merge. B22 is needed if the eventual causal
correction retains B20 discovery. If B24 demonstrates a different correction,
exclude B20 and keep B22 separately open; do not assert unconditional dependency.

### Existing B23: timeout descendant lifecycle

Input: authenticated B20 terminal/process receipts and the unchanged wrapper
blob `7dc3270d0226f68ead596c347d15c38cc21fa33b` at B20. Scope is
`verification/areas/performance/run_benchmarks.py`'s `run_subprocess` ownership
boundary and its deterministic self-test support, not formatter semantics.
Each timed command must own, terminate and reap its descendant group on timeout
before another sample starts; success/error paths and raw evidence survive.
Add deterministic child/grandchild timeout, no-overlap, ordinary-success and
nonzero-exit cases to the existing `--self-test` entrypoint. If a responsibility
split is required by the900-line limit, the registered new helper is
`verification/areas/performance/benchmark_process.py`, containing only timed
process ownership and its deterministic checks. No other runtime responsibility
or path expansion is registered.
Commands: `python3 verification/areas/performance/run_benchmarks.py --self-test`,
`python3 verification/areas/performance/check_budgets.py --self-test`, diff/HIR/
file-size. No benchmark acquisition or Sifr gate for this Python harness scope.
Propose one initial exact-SHA review and at most one remediation; no allowance
is granted here. Correct future launcher TMPDIR independently of the wrapper.
This prerequisite makes a future timed acquisition's isolation valid; it is not
evidence that the original latency or CV cause is fixed.

### Existing B24: attributable cause and controlled qualification

Owner3776 must first register one bounded experiment distinguishing formatter
configuration/discovery, parse/format work and startup/accounting, using preserved
B19/B20 samples. Inputs include B23's qualified lifecycle and B22 only if its
discovery is retained. The exact experiment command cannot honestly be specified
before the owner selects and records its instrumentation, cases, observation
criteria and stop condition; that registration is the first required action,
not permission for speculative research or another unchanged acquisition.
Required eventual commands already supported by the repository are benchmark
and budget `--self-test` and
`uv run --project verification --locked python -m sifr_verify areas run --area performance --suite representative`.
That suite owns `benchmark-subset` followed by `budget-subset`, the same invocation
ID and original case selection/thresholds. `--suite benchmark-subset` is not a
supported substitute. Require attribution plus the unchanged formatter and full
representative/budget acceptance; a lucky stable sample alone does not establish
cause. Keep all invalid/failed attempts. Proposed review/gate limits must be
fixed at its dispatch from its actual file scope; no inherited allowance reset,
no automatic full gate, no baseline/workload/retry relaxation. A demonstrated
additional mechanism requires separate scope before implementation.

### Proposed B27: complete joint qualification and delivery

Dependencies: merged B26 assessment; a terminal, causally supported B24 result
with qualified necessary B22/B23 inputs; fresh main; all exact source inputs in
the inventory; external65 owner confirmation that the candidate preserves its
approved bytes and consumer contracts.65 main delivery is not a prerequisite.
One independent owner integrates and qualifies the unit; no second Item68 worker.

Candidate construction: start from newly fetched main; reconcile the exact
fork156157→98480 delta,65's base6bd085→4c7068 delta and B25's three-file assertion
completion. Preserve current-main fixes listed above. Carry only relevant
architecture and scoped issue updates, not historical dirty ledgers. All source,
test, schema, workflow, lock, profile, companion and submodule paths are explicitly
enumerated by the inventory. B24's newly qualified delta is a separate required
input with its own exact path manifest and SHA before the integration freezes;
its not-yet-created source cannot be given a fictitious identity here.

Do not independently cherry-pick H/I/M1 last commits. Do not import fb66/96d051
wholesale. Regenerate affected output with the candidate compiler and verify all
264 pairs, preserving every Sifr assertion. Retain corpus8bcbe7ab and the
90-source check/run contract, and exclude residual Item12 source8ad089. The
current main→corpus difference is deliberate and separately reviewed; all other
15 gitlinks stay exact unless an explicitly owned later input requires a change.

Acceptance includes every semantic/negative boundary above, complete required
message/storage and async cleanup/cancellation behavior, no introduced runtime
panic or diagnostic suppression, exact policy65 behavior, complete original
qualification below, source/compiler/corpus provenance and one approved final
candidate. No fixture assertion, profile membership, lint/debt ownership,
budget, certification or ignored-test policy can be weakened to obtain a pass.

Finish all implementation and generator-owned output before testing/review.
Register any new negative test names inside these existing filters before their
execution. The existing supported targeted commands are:

```bash
cargo test -p sifr_codegen corpus_repair
cargo test -p sifr_codegen test_list_repeat_lowers_without_vec_mul_shape
cargo test -p sifr_codegen single_element_list_repeat_uses_std_repeat_not_extend_loop
cargo test -p sifr_codegen project_field_identity
cargo test -p sifr_driver project_field_identity
cargo test -p sifr_codegen generated_rust_canonicalizer
cargo test -p sifr_codegen task_local_support
cargo test -p sifr_codegen implicit_format_capture
cargo test -p sifr_codegen support_assembly_codegen_tests
cargo test -p sifr_codegen test_generate_rust_multi_with_metadata_preserves_trait_impl_visibility
cargo test -p sifr_lowering required_error_message
cargo test -p sifr_driver required_error_message
cargo test -p sifr_driver async_python_error_channel
cargo test -p sifr_driver --lib tests::project_build_check::build_project_preserves_module_scoped_builtin_error_shadow_identities -- --exact
cargo test -p sifr_driver --lib tests::test_runner::test_project_preserves_module_scoped_builtin_error_shadow_identities -- --exact
cargo test -p sifr_codegen
cargo test -p sifr_driver
cargo test -p sifr_ir
cargo test -p sifr_lowering
cargo test -p sifr_frontend
cargo test -p sifr_type_system
cargo clippy --workspace -- -D warnings
cargo fmt --check
python3 scripts/check_hir_maintainability_guardrails.py
python3 scripts/check_file_size_guardrails.py
git diff --check <exact-base> <exact-candidate>
```

The full crate commands can cover their contained targeted groups once when
the report proves nonzero selection and each required result; do not duplicate
unchanged successful evidence just to print each filter. In particular,
`corpus_repair` includes builtin and repeated-value positive/negative cases.
Do not use obsolete `item12b_` filters that select zero after naming cleanup.

Compiler-owned generation/setup commands (not run in B26):

```bash
cargo build --locked -p sifr
python3 scripts/check_demo_emitted_freshness.py --sifr target/debug/sifr --update
python3 scripts/check_demo_emitted_freshness.py --sifr target/debug/sifr
uv run --project verification --locked python -m sifr_verify.generated_cargo_setup --profile merge --revision <frozen-pushed-candidate>
```

The generated setup prepares all92 graphs at that exact revision. Inspect the
source-attributed12-surface/14-Rust-file baseline concordance before Clippy;
adapt the preserved B13 verification helper only for owned paths and current
proof inputs. No stale generated source or current-output blessing qualifies it.

All five original Python suite identities remain required:
`dependency-versions`, `binding-authoring`, `callback-examples`,
`async-declaration-examples`, `async-context-examples`. Their supported filtered
commands are `uv run --project verification --locked python -m sifr_verify areas
run --area python_interop --suite <suite>` (the last two may share one call).
Filtered commands can fail the unchanged promotable-certification boundary
despite passing native examples; do not call that an outer pass and do not use
`--allow-partial-certification` as delivery evidence. Prefer one complete
`uv run --project verification --locked python -m sifr_verify areas run --area
python_interop` command covering all30 variants and the five suites without
duplicate acquisitions. Preserve native build/run assertions, frozen binding
immutability and bytecode checks, cancellation and retained-owner drained close,
async original exception/suppression and cleanup evidence. Main's newer CFFI and
dependency runner checks invalidate98480's whole-Python proof on the new input.

Other complete original qualification commands:

```bash
uv run --project verification --locked python -m sifr_verify areas run --area generated_code_quality --suite representative
uv run --project verification --locked python -m sifr_verify areas run --area algorithmic_compatibility --suite leetcode-full
uv run --project verification --locked python -m sifr_verify areas run --area distribution_release --suite epoch-bootstrap --suite qualification --suite evidence-custody --suite incident-governance --suite protected-drill --suite stable-prepare --suite stable-publish-primitives --suite stable-publication
```

Native90 uses the preserved driver
`/private/tmp/sifr-b13-continuation.kr53D8/evidence/native_qualification.py`,
SHA256 `868526c5d203498bc432ab4d4fa768924c041a403ef1eae4fd354760e2516be6`.
Make an owned evidence copy with only workspace/compiler/temp/output path
adaptation; invoke `python3 <owned-evidence>/native_qualification.py
<owned-evidence>/native-qualified`. Its exact selection is all90 `.sifr` paths
in corpus7fcb9fd1→8bcbe7ab; each gets `check` then `run`, original assertions,
source SHA256, compiler binary SHA256 and both repository SHAs. Keep its bounded
three workers/two Cargo jobs and owned TMPDIR.411 `leetcode-full` remains
check-only; neither result substitutes for the other. No corpus code is written
by B26 or silently excluded from the future qualification.

Eight65 distribution suites may reuse11/11 exact unchanged input evidence after
the approved policy owner authenticates the complete source/consumer/schema/
toolchain closure; otherwise execute their named command once on changed inputs.
The final whole profile must still reach them. Similarly, historical H/I/M1,
12B/B13/B14 reviews support unchanged mechanism reasoning, not current native
execution or affected interaction approval. Unreached lanes have nothing to reuse.

After all required prerequisites and exact-SHA integration approval, the proposed
sole gate is `scripts/run_all_tests.sh --profile merge`. Skip create-pr only when
merging that SHA in-session. No second gate, resume, warmed replay or renamed
unchanged-candidate retry. A subsequently available complete pass may be reused
only after exact SHA and complete input/coverage authentication.

The full profile contract is all20 area entries,13 guards, production92, all31
unique full-mode crate commands and both toolchain steps. This includes the
complete Python30, representative generated quality9, diagnostics, core,
runtime, algorithmic representative, developer tooling, performance producer
and dependent budget, distribution and sysroot, workspace/packages, stdlib,
regression, fuzz, ecosystem and SQL. Stdlib includes module-merge-check,
audit-fixtures, complexity-resource, module-inventory, default crate,
`--no-default-features --features json,unicode,i18n,http` and `--all-features`.
The normally ignored `cargo test -p sifr_driver --lib -- --ignored
--test-threads=1` and `cargo test -p sifr --bin sifr -- --ignored
--test-threads=1` remain, as do all ordinary full-mode commands. E2E uses the
normal empty fixture manifest (historically726 fixtures), not a filtered slice.
Actual selected and executed current counts govern; no older count permits
dropping a newly present case. The profile supplies these commands once; do not
append another broad gate. B27 does not close retained12's all-surface audit.

Proposed review allowance: one initial Opus exact-base/candidate integration
assessment and at most one remediation covering actual changed interactions,
source/fixture/schema/current-main reconciliation and all two-repository inputs.
Reuse exhausted predecessor approvals within their original scope. The reason
for a new allowance is genuinely changed causal/performance inputs plus the
first combination of preserved compiler contracts with approved65/newer main;
renaming unchanged98480 or4c7068 is insufficient. The user/orchestrator must
explicitly authorize this concrete allowance at dispatch after B24 supplies its
terminal; this assessment cannot manufacture it. A new second-review mechanism
defect is recorded as a later item and stops delivery. Repeated identical
blockers require adjudication. Provider failures are not approvals; skill retry
limits apply. No final review is committed into the SHA it approves.

One owner has a new clone, branch, index, private TMPDIR and external evidence.
Leave CARGO_TARGET_DIR unset; initialize the exact16 submodules only in that
clone and verify corpus/ruff/native dependency objects. No predecessor target,
source or evidence ownership transfers. Arrange a compiler window with
coordinator `01a07d84-7c62-77f0-b7c7-ecf310829a11`. Before long work inspect
free disk and private target; an idle private target over20GiB may be cleaned
only by its owner under AGENTS. If safe owned cleanup cannot supply enough space,
record the resource blocker and stop. A cold first pass is not performance proof.

On success, preserve exact candidate/base, compiler hash, corpus SHA, command
receipts and external Opus evidence; merge the qualified corpus48 before the
Sifr integration gitlink, verify both remote merges and update only scoped
phase/dependency records. Reconcile older PRs as superseded/delivered only when
their complete accepted content is proved present. No policy/settings/release
actions follow. Then stop;12D/12E/12F/retained12/docs-only12A remain later owners.

## B26 validation and handoff contract

B26 runs only exact-base/candidate diff hygiene, HIR maintainability, file-size,
and read-only commit/PR/blob/receipt/proposed-command identity checks. Its one
initial exact-SHA Opus assessment and at most one remediation follow the named
skills; no predecessor or whole-phase review is reopened. Only the assessment,
input-inventory Markdown and scoped emitted-phase record may change.

All necessary source inputs and authenticated historical receipts for this
assessment are available. There is no B26 assessment blocker. Future integration
is blocked by unresolved causal/controlled qualification (B24 with relevant
B22/B23 inputs) and the ungranted new integration allowance described above;
no65-main-first approval or invented missing artifact is required. After B26
docs merge and the record-only phase update, send the parent and coordinator
full base/candidate/merge SHAs, PRs, evidence locations/digests, remaining scope
and zero-owned-process receipt, and stop without starting any registered item.
