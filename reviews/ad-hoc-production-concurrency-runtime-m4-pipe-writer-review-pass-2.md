RESULT: PASS

The follow-up changes to the M4 sync stdin pipe writer wave are correct and the non-blocking async-silent-ignore concern from pass 1 is genuinely remediated. The implementation is type-correct, panel-free in data paths, scope-honest, and the validation evidence reproduces locally (I re-emitted both fixtures and confirmed the generated async helpers, gating, and ordering).

What was verified

- **Async deferral is the right shape.** `lib/sifr/process.sifr:369-389` keeps `async_run`/`async_output` as `Awaitable[Result[..., ProcessError]]` and threads `command.stdin_mode` (and `command.has_stdin_data` for `async_output`). The emitted helper bodies put the guards *inside the async function* (`process_async_runtime.rs:75-89,213-244`), so they manifest as `Err(ProcessError(...))` on `.await` instead of raising synchronously or returning the wrong shape. The compiled output confirms this: the generated `async fn __sifr_process_async_run/output -> Result<..., ProcessError>` checks `if stdin_mode != "inherit" { return Err(ProcessError { message: "async process stdin mode requires owned pipe support".to_string() }); }` (and the parallel `has_stdin` guard with `"async process stdin bytes require owned pipe support"`). Stdlib registration matches: `crates/sifr_stdlib/src/process.rs:215-243` declares 6/7-arg async intrinsics returning `Awaitable[Result[Status|Output, ProcessError]]`. Intrinsic lowerers in `intrinsics/registry/process_async.rs:33-53` enforce 6/7-arg arities and pass `stdin_mode`/`has_stdin` through `Box::pin`.

- **The `process_async_runtime.rs` split is coherent and ordering/gating are preserved.** `preamble.rs:17-21` brings `process_async_runtime::*` into crate scope; `lib_modules_and_codegen.rs:583-591` orders `build_process_status_items()` → `build_process_async_items()` → `build_process_child_items()`, so `__sifr_process_exit_signal` (defined in `process_runtime.rs:10-77`) is declared before `__sifr_process_status_from_exit` (now declared in `process_async_runtime.rs` and emitted as the first item from `build_process_async_items`) which is in turn declared before the async run/output futures that call it. The widened gate `let needs_process_status = stdlib_needs_process_status || stdlib_needs_process_async;` (`lib_modules_and_codegen.rs:416`) still pulls in the signal helper whenever async is needed. Stdlib filter still strips both helper symbols in all three sites (`stdlib_filter/implementation.rs:330-335` text scan, `:373-374` AST visit, `:407-409` item identification). I spot-checked emission for `process_sync_output_text.sifr` (no pipe-writer table or async helpers) and `process_spawn_pipe_writer.sifr` (writer table + 3 helpers present, no async helpers).

- **Generated runtime stays panic-free in user data paths.** All async helper I/O flows through `process_map_err` (`process_async_runtime.rs:20-37`) producing typed `ProcessError`; status conversion uses `__sifr_process_status_from_exit` which only matches on `Option<i64>` and equality. Sync pipe writer/close (`preamble/process_child_pipes.rs:404-485`) takes the writer out across the I/O call, returns it via `ok_or_else` / `?`, and reinserts on success; mutex poisoning still recovers via the accepted `unwrap_or_else(|err| err.into_inner())` pattern.

- **Guardrails and docs are honest.** `process_runtime.rs` 537 LOC, `process_async_runtime.rs` 410 LOC, `process_child_pipes.rs` 485 LOC, `intrinsics/registry/process.rs` 849 LOC, `intrinsics/registry/process_handles.rs` 80 LOC — all under 900; `python3 scripts/check_file_size_guardrails.py` and `check_hir_maintainability_guardrails.py` reproduce PASS locally. `verification/stdlib/concurrency_runtime_m4_process_traceability.md:18` correctly broadens the deferral language to cover **both** `Command.stdin_bytes(...)` and non-inherit `Command.stdin(...)` modes; the host matrix still marks Windows `host-limited`; both manifests include `process_spawn_pipe_writer`; the ledger entry in `issues/ad-hoc-production-concurrency-runtime-platform-substrate-execution.md:816-845` records the wave including the async-deferral remediation. `cargo fmt --check`, `cargo check -p sifr_codegen -p sifr_stdlib -p sifr_driver -p sifr`, and JSON parse of both manifests reproduced PASS.

- **Fixture coverage matches the new claim.** `crates/sifr/tests/e2e/pass/process_async_run_output.sifr:33-39` exercises the new async `Stdio("pipe")` rejection path alongside the pre-existing `stdin_bytes` rejection at `:25-31`; both assert on `"owned pipe" in e.message`, matching the typed messages emitted by the helper.

Non-blocking notes (worth a follow-up, do not block)

1. `reviews/ad-hoc-production-concurrency-runtime-m4-pipe-writer-review-pass-2.md` is still a 0-byte placeholder. Either populate it with this PASS or drop it before opening the PR so the diff stays clean.
2. The ledger entry at `issues/ad-hoc-production-concurrency-runtime-platform-substrate-execution.md:843` only lists pass-1's PASS. Add a pass-2 PASS line referencing this review (and the async-deferral remediation it verified) before the PR is opened.
3. `process_async_runtime.rs`, `process_runtime.rs`, `preamble/process_child_pipes.rs`, and `intrinsics/registry/process.rs` each re-define `process_error_expr` / `process_map_err` / `ok_expr` / `err_expr` privately. This matches the existing per-module convention so it's fine for this wave, but a shared `preamble/process_common.rs` helper module would remove four near-identical copies (low priority; do not bundle into this PR).
4. Pass-1's other non-blocking notes (Sifr-level `PipeWriter._closed` flag on `write_all` failure; missing-pipe vs already-taken fixture split; empty/long-sequence writes) are still unaddressed. As pass-1 already classified them non-blocking and typing is preserved either way, deferring to a later wave is acceptable.
5. `__sifr_process_pipe_close` still emits `let mut __pipe = __maybe_pipe.ok_or_else(...)?;` with `__pipe` never read; the underscore prefix suppresses `unused_variables` and rustc doesn't fire `unused_mut` on a fully unused binding, so it compiles cleanly today. A `let _pipe = __maybe_pipe.ok_or_else(...)?;` would be marginally tidier and survive future lint tightening, but is not a correctness issue.
