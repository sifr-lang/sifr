

This is better. The **"Memory: Sifr 82% less"** format is the right balance — it tells users *what* metric (memory) and *what* the result is (82% less), while tooltips preserve technical precision for those who want it.

Keeping "Peak RSS" in tooltips/headers rather than the visible label is correct: it's an implementation detail users don't need upfront.
