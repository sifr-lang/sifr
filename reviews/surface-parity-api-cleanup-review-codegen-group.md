YES, all five fail at the "run" stage (returncode 1) after "check" passes (returncode 0), meaning the generated Rust code compiles and runs but produces incorrect results — pure codegen/runtime bugs, not language-policy issues.

[exit_code]=0
