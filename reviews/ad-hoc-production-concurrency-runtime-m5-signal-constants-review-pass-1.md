PASS: The lowering restricts the expression fallback to `ConstructorCall` HirExprs only, and codegen narrowly emits a private factory function — neither path opens broad arbitrary module side effects. `lib/sifr/signal.sifr` defines `SIGINT`/`SIGTERM` as pure Sifr-owned `Signal(name, number)` values; the docs, host matrix, traceability, and inventory consistently mark them as portable value-model evidence while keeping `ctrl_c`/`terminate` streams, Unix-only constants, delivery semantics, and task context propagation in-progress or host-limited. The fixture directly imports `SIGINT`, `SIGTERM`, `Signal`, and `strsignal`, binds owned values, reads field data before the consuming `strsignal` call, and matches the existing `signal_value_model_basic`/`signal_strsignal_basic` ownership pattern. Both create-pr and merge manifests insert `signal_constants_basic` in the same position after `signal_strsignal_basic`. The execution ledger records the wave and includes the full create-pr PASS line (warm wall-time advisory `216.65s` vs `<=2m`, platform golden `pass=6, skip=1`, e2e `119 passed, 0 failed, cache_hits=31/33, report_signature=0df4819d3daf7aa4`).
