PASS

Rationale:

- **Lowering targets the Tokio async path, not sync shell.** All three new intrinsics in `crates/sifr_codegen/src/intrinsics/registry.rs:680-691` dispatch through `process_async::lower_process_async_shell_*`, which build owned arg lists and call `__sifr_process_async_run` / `__sifr_process_async_output` / `__sifr_process_async_output_timeout` - the same Tokio current-thread helpers used by the argv async APIs (`registry/process_async.rs:62-95`). No `process_shell_run` or `std::process::Command` is emitted in the shell path, and validation confirmed this via emitted-code grep.
- **Shell selection is explicit and uniform.** `async_shell_process_owned_args` hard-wires program `"sh"` with `["-c", script.clone()]`, empty env, no cwd, and `"inherit"` stdio - matching the sync shell convention and consistent with the Windows host-limited stance documented in `verification/platform/supported_host_matrix.md:30`.
- **Stdlib surface is awaitable, not blocking.** `process.rs:418-444` registers each intrinsic with `Type::Awaitable(...)` and `lib/sifr/process.sifr:559-575` exposes `async_run_shell` / `async_output_shell` / `async_output_shell_timeout` as plain async functions without `@blocking_io` or `@shell_exec`, so they are genuine awaitable APIs rather than direct calls into the sync shell path (which would have tripped `SIFR-ASYNC-0007`). This is explicitly called out in the traceability update.
- **Coverage is adequate for the milestone.** The new `crates/sifr/tests/e2e/pass/process_async_shell_exec.sifr` exercises nonzero status, split stdout/stderr, stdin piping via `cat`, fast-path timeout completion, and timeout exceeded with `kind == "timeout"`/`timed_out` evidence. `lowers_process_timeout_intrinsics_via_registry` now asserts the shell-output-timeout intrinsic renders `Box::pin(__sifr_process_async_output_timeout(...))` with `"sh"`, `vec!["-c", script.clone()]`, `stdin.clone()`, `has_stdin`, and `timeout`. Both create-pr and merge E2E manifests register `process_async_shell_exec`, and `scripts/run_all_tests.sh --profile create-pr` is green (110 E2E pass; report_signature=ef971c12870cf2e0).
- **No regressions to existing classifications.** Sync `run_shell` / `output_shell` keep their `@blocking_io` + `@shell_exec` decorators and direct-async rejection diagnostics; the traceability table updates clarify that the async shell APIs are separate awaitable surfaces.

Minor non-blocking observations (worth a follow-up, not a merge gate):
- The async shell functions intentionally omit `@shell_exec`, which is documented but means shell-effect tracking is not extended to async callers. If the project later wants effect-level visibility into shell execution regardless of blocking-ness, that decorator design would need to evolve.
- Windows shell semantics remain host-limited; this is correctly marked in the host matrix and not in scope for this patch.
