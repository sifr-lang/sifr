Acknowledged — that was the background `grep` I had launched earlier; it exited 1 because the substring was not found, which is the result I already confirmed synchronously via the Grep tool. No action needed; the overreach check stands: none of `WorkspaceSession`, `WorkspaceSnapshot`, `SourceProvider`, `DirtyScope`, or `can_replace_module_in_project` appear anywhere under `crates/`, so M0 stays within scope.
