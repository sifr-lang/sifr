# Review: semantic/diagnostic code taxonomy — DIAG-11 expressions/operators (raw HIR)

## Scope
Top expression chunk in `crates/sifr_hir/src/lower/expressions.rs`:
- unsupported expression forms (yield, etc.)
- matrix binop (`@`)
- raw diagnostics for unsupported comparison operators
- `SIFR-TYPE-0012` / `TYPE_UNSUPPORTED_EXPRESSION_FORM`

Reused `TYPE_UNSUPPORTED_OPERATOR` for operator-specific failures.

---

## Findings (ordered by severity)

### No required fixes remain. All checks pass.

---

## Detailed validation

### 1. `TYPE_UNSUPPORTED_EXPRESSION_FORM` — SIFR-TYPE-0012

**Code definition** (`codes.rs:45`):
```rust
pub const TYPE_UNSUPPORTED_EXPRESSION_FORM: Self = Self::new("SIFR-TYPE-0012", Severity::Error);
```
Correctly declared as Error severity in the TYPE family.

**Registry entry** (`codes.rs:701-711`):
- `owner_module: "sifr_hir::lower::expressions"` — matches the lowering module
- `message_template: "unsupported expression form: {form}"` — single `form` arg
- `declared_args: [arg!("form")]` — message+json format
- `dedupe_args: ["form"]` — dedupes on the `form` field
- `representative_fixture_path` — e2e fixture at `crates/sifr/tests/e2e/fail/unsupported_yield_expression.sifr`
- State: Active

**Emit site** (`expressions.rs:104-111`):
```rust
_ => {
    expression_diagnostics::unsupported_form(
        ctx,
        "unsupported expression type",
        expr.range(),
    );
    None
}
```
The `form` parameter is a static string literal `"unsupported expression type"`. The diagnostic is emitted at the full `expr.range()`, which for yield expressions covers the `yield 1` span. The fallback arm is appropriate — it handles any expression kind the HIR does not yet lower.

**Diagnostic helper** (`expression_diagnostics.rs:6-12`):
```rust
pub(super) fn unsupported_form(ctx: &mut LowerCtx, form: &str, range: TextRange) {
    ctx.error_with_code_at(
        DiagnosticCode::TYPE_UNSUPPORTED_EXPRESSION_FORM,
        format!("unsupported expression form: {form}"),
        range,
    );
}
```
Uses `ctx.error_with_code_at` (structured, code-bearing) rather than the plain `ctx.error` fallback.

**Unit test** (`expressions_tests.rs:730-742` — `test_unsupported_expression_form_has_type_code`):
```rust
error.code == Some(DiagnosticCode::TYPE_UNSUPPORTED_EXPRESSION_FORM)
&& error.message == "unsupported expression form: unsupported expression type"
&& error.primary_range == Some(range_for(source, "yield 1"))
```
Verifies correct code, correct rendered message, and correct primary range.

**E2E fixture** (`unsupported_yield_expression.sifr`):
```sifr
# expect-error[col=10]: SIFR-TYPE-0012
# Test: unsupported expression forms use structured expression-form diagnostics.

def main():
    x = (yield 1)
```
Column 10 points at the `(` of `(yield 1)`, which contains the yield expression. The e2e test framework verifies this against the structured diagnostic output.

**Docs** (`docs/errors/SIFR-TYPE-0012.md`):
- Correctly generated by `gen-error-docs`
- Owner, template, fixture, and declared-args fields all match the registry entry

**Taxonomy**: "Unsupported expression form" is correctly classified under TYPE family (static type/inference compatibility), not FLOW (control flow) or other families. The form describes a syntactic construct that Sifr's type-checker cannot handle at the HIR level.

**Internal docs** (`internal_docs/diagnostic_codes.md`):
- `SIFR-TYPE-0012` row is Active, Error severity, with correct owner and template

---

### 2. Matrix multiplication (`@`) — reused `TYPE_UNSUPPORTED_OPERATOR`

**Emit site** (`expressions.rs:353-356`):
```rust
Operator::MatMult => {
    expression_diagnostics::matrix_multiplication(ctx, binop.range());
    return None;
}
```

**Diagnostic helper** (`expression_diagnostics.rs:27-33`):
```rust
pub(super) fn matrix_multiplication(ctx: &mut LowerCtx, range: TextRange) {
    ctx.error_with_code_at(
        DiagnosticCode::TYPE_UNSUPPORTED_OPERATOR,
        "matrix multiplication operator (@) is not supported".to_string(),
        range,
    );
}
```
Uses `TYPE_UNSUPPORTED_OPERATOR` (SIFR-TYPE-0005) — appropriate because `@` is an operator and the TYPE family handles operator/operand compatibility errors. The message is specific to matrix multiplication.

**Unit tests** (`expressions_tests.rs`):
- `test_matrix_binop_has_unsupported_operator_code` (line 714): verifies code `TYPE_UNSUPPORTED_OPERATOR`, message `"matrix multiplication operator (@) is not supported"`, range over `1 @ 2`
- `test_matrix_augassign_has_unsupported_operator_code` (line 700): verifies same code and message for augmented assignment `value @= other`

Both reuse `TYPE_UNSUPPORTED_OPERATOR`. This is correct — matrix multiplication is an unsupported operator.

---

### 3. Unsupported comparison operator (in `lower_compare` catch-all)

**Emit site** (`expressions.rs:509-517`):
```rust
_ => {
    expression_diagnostics::unsupported_operator(
        ctx,
        "comparison",
        "unsupported comparison operator",
        comparator.range(),
    );
    return None;
}
```
The `_` catch-all fires for `CmpOp` variants that `type_check_comparison` does not support. The diagnostic reuses `TYPE_UNSUPPORTED_OPERATOR` with operator `"comparison"` and operand_types `"unsupported comparison operator"`. This is appropriate — the failure is semantically an unsupported operator.

Note: The existing `in`/`not in` handling (lines 424-493) also emits `TYPE_UNSUPPORTED_OPERATOR` with operator `"in"`/`"not in"`. These are distinct code paths, both correctly using the same code.

---

### 4. Primary range correctness

| Diagnostic | Range target | Assessment |
|---|---|---|
| `unsupported_expression_form` | `yield 1` (full yield expr) | Correct — the entire unsupported expression form |
| `matrix_binop` | `1 @ 2` (full binary expr) | Correct — the `@` operator expression |
| `matrix_augassign` | After `) -> None:\n    ` + `value` | Correct — the augmented assignment target |
| `in_operator_non_collection` | The comparator (e.g., `2`) | Correct — the non-collection operand |
| `unsupported_comparison` | `comparator.range()` | Correct — the unsupported comparator expression |

All primary ranges point to the relevant source construct, not to some unrelated span.

---

### 5. Taxonomy check

- `SIFR-TYPE-0012` (unsupported expression form) — TYPE family, expression-level error
- `SIFR-TYPE-0005` (unsupported operator) — TYPE family, operator/operand error

Both are correctly classified under TYPE ("Static type compatibility and inference diagnostics"). There is no ambiguity between TYPE and FLOW — unsupported expressions at the HIR lowering level are type errors (the expression form is not a supported type-carrying construct), not control-flow errors.

---

### 6. No fallback behavior

- `lower_expr` fallback arm calls `unsupported_form()` then returns `None` — structured diagnostic, then None is propagated.
- `lower_binop` for MatMult calls `matrix_multiplication()` then returns `None`.
- `lower_compare` for unsupported ops calls `unsupported_operator()` then returns `None`.
- In `lower_compare`'s `in`/`not in` paths, when `contains_element_type()` returns `None`, `unsupported_operator()` is called but lowering continues (returns a `ContainsOp` node with `Type::Bool`). This is deliberate — the `ContainsOp` node is valid HIR even when type-checking fails; the diagnostic is supplementary.

No `.unwrap()`, `.expect()`, or panicking paths in user-facing code.

---

### 7. Documentation and schema

- `docs/errors/SIFR-TYPE-0012.md` exists, is generated by `gen-error-docs`, and has correct content.
- `docs/errors/diagnostic-codes.md` lists SIFR-TYPE-0012 with correct severity, summary, and link.
- `internal_docs/diagnostic_codes.md` has the full registry row for SIFR-TYPE-0012.
- `DIAGNOSTIC_REGISTRY` test (`registry_skeleton_is_internally_consistent`) passes for all new/changed entries.
- `active_diagnostic_docs_pages_exist_with_exact_casing` test passes — all active diagnostics have doc pages.

---

### 8. Code-reuse correctness

`TYPE_UNSUPPORTED_OPERATOR` is reused for:
- Matrix multiplication (operator: `@`)
- `in`/`not in` on non-collections (operator: `in`/`not in`)
- Unsupported comparison operators (operator: `comparison`)

This is appropriate — all are "unsupported operator" failures in the TYPE family. Creating a separate `TYPE_UNSUPPORTED_EXPRESSION_FORM`-equivalent for operators would be redundant. The operator messages are specific enough to distinguish matrix multiplication from other operator failures.

---

## Summary

All four items in scope pass:

| Check | Result |
|---|---|
| `TYPE_UNSUPPORTED_EXPRESSION_FORM` code, registry, emit site, test, fixture, docs | Pass |
| Matrix multiplication reuse of `TYPE_UNSUPPORTED_OPERATOR` | Pass |
| Unsupported comparison operator reuse of `TYPE_UNSUPPORTED_OPERATOR` | Pass |
| Primary ranges | Pass |
| Taxonomy (TYPE family) | Pass |
| No fallback behavior | Pass |
| Docs/schema consistency | Pass |
| `cargo check -p sifr_hir -p sifr_diagnostics` | Pass (per user validation) |
| `cargo clippy` | Pass (per user validation) |
| `cargo fmt` | Pass (per user validation) |

**No required fixes remain.**
