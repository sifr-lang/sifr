I've reviewed the working tree. The slice correctly installs a per-spawn Unix process group, escalates TERM->KILL against the group, reaps the immediate child, and honestly scopes non-Unix coverage as pending. The fixtures genuinely exercise descendant leakage (background subshell writes a marker after the parent dies), traceability and manifests are updated, and the unit assertions cover the new generated code.

PASS

Findings (none blocking - recorded for situational awareness):

- `crates/sifr_codegen/src/intrinsics/registry/process.rs:494-518` and `crates/sifr_codegen/src/preamble/process_async_runtime.rs:323-358, 510-545`: the inline `kill -TERM`/`kill -KILL` helper invocations are wrapped in `let _ = ...?;`, so failure to *spawn* `kill` (e.g., a non-POSIX host without the binary on PATH) propagates as a `ProcessError` instead of a typed timeout `Status`. Acceptable on the supported Unix hosts but worth a follow-up note in the trace if you ever target stripped containers without `kill(1)`.
- `crates/sifr_codegen/src/intrinsics/registry/process.rs:573-672`: only `process_output_timeout` and `process_shell_output_timeout` lowerers get `process_group(0)` before spawn. Stdlib `run_timeout` routes through `process_output_timeout` (`lib/sifr/process.sifr:413-415`), so the sync run-timeout path is implicitly covered - the new fixtures only exercise the shell paths, but the code path is shared. Adding an argv-based descendant fixture later would tighten coverage but isn't required for this slice.
- `crates/sifr_codegen/src/intrinsics/registry/process.rs:494-517` and the async equivalents: descendants that re-`setsid` or `nohup` would escape the group. The traceability copy honestly says "covers background descendants" without claiming escape-proof behavior, so this is consistent - just flagging it as a future hardening boundary if you ever want to escalate to PID-namespace or `kqueue PROC` based supervision.
- `crates/sifr_codegen/src/intrinsics/registry/process.rs:494-517`: the 50 ms TERM->KILL grace is intentionally aggressive and matches the test's 350 ms safety window; under heavy CI load this is still plenty because KILL is unblockable. No action needed.
- `crates/sifr_codegen/src/preamble/process_async_runtime.rs:457-548`: the async output-timeout path drops the captured stdout/stderr pipes implicitly when the timeout arm returns. Since the helper KILL closes the descendant write ends and we return empty bytes for timeout, this is correct, but a comment near the drop boundary might help future readers (optional - current style elsewhere in this preamble also relies on implicit drop).
- `crates/sifr/tests/e2e/pass/process_timeout_group_cleanup.sifr` and the async sibling: the `except ProcessError as e: assert e.message == ""` pattern means any actual error surfaces as a clear assertion failure (since the message is non-empty). Consistent with neighboring fixtures.
- `verification/stdlib/concurrency_runtime_m4_process_traceability.md`: the rewritten Status / Sync timeout / Async timeout rows accurately describe the new behavior and explicitly mark non-Unix as pending. Status line correctly tags this slice as "under review" matching the ledger entry in `issues/ad-hoc-production-concurrency-runtime-platform-substrate-execution.md:438`.
- Stylistic: inlining multi-line Rust as `RustExpr::Ident(...)` strings is consistent with the surrounding async preamble pattern; no new precedent set. Worth noting that the new sync registry path now adopts the same trick, so longer-term decomposition would help, but not in scope for this slice.
