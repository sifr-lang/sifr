PASS

Pass 1 blockers — verification:

1. **Missing encodings/locale/dotted CPython import fixtures.** PRESENT. `crates/sifr/tests/e2e/fail/bare_cpython_encodings_import.sifr`, `bare_cpython_locale_import.sifr`, and `bare_cpython_dotted_codecs_import.sifr` exist; each asserts `expect-error[col=6]: SIFR-IMPORT-0008`. `crates/sifr_stdlib/src/lib.rs:109-116` now resolves reserved CPython roots (including dotted children such as `codecs.utf_8`, `encodings.utf_8`) to `sifr.encoding`/`sifr.unicode`/`sifr.i18n`; a unit test at `crates/sifr_stdlib/src/lib.rs:226-244` covers the dotted form. Execution ledger reports `cargo test -p sifr --test e2e test_e2e_fail` pass for 375 fixtures (issues/ad-hoc-production-text-i18n-platform-substrate-execution.md:230).

2. **Itemized unsupported Python-shaped surface classifications.** PRESENT. `verification/stdlib/text_i18n_substrate_inventory.md:34-47` carries per-API rows for `codecs.register`/`unregister`/`register_error`, stream classes (`codecs.open`, `EncodedFile`, `StreamReader`, `StreamWriter`, `StreamReaderWriter`, `StreamRecoder`), `encodings.*`, CPython codec C APIs, `locale.setlocale`, `localeconv`, `strcoll`/`strxfrm`, implicit preferred text encoding, `gettext.install`/global `_`, and `gettext.textdomain`/`bindtextdomain` — each with owner milestone, terminal state, stability, CPython evidence (file + test class), and a regression-fixture/revisit-rule link. JSON mirror at `verification/stdlib/text_i18n_substrate_inventory.json:53-145`.

3. **Dependency decision records.** PRESENT. New `verification/stdlib/text_i18n_dependency_decisions.md` records one row per crate family with the full required field set (Sifr abstraction, Unicode/version alignment, panic/unsafe audit, typed error mapping, license/MSRV/binary/platform impact, deterministic local test strategy, supply-chain signal). JSON entries at `verification/stdlib/text_i18n_substrate_inventory.json:213-318` now include the matching structured fields per crate (`sifr_abstraction`, `unicode_alignment`, `panic_unsafe_audit`, `typed_error_mapping`, `test_strategy`, `supply_chain_signal`). The inventory cross-links to the detailed record at `verification/stdlib/text_i18n_substrate_inventory.md:105`.

4. **Exact Tier 0/Tier 1 alias table.** PRESENT. `verification/stdlib/text_i18n_substrate_inventory.md:60-78` enumerates accepted labels per encoding with a per-row justification. JSON mirror at `verification/stdlib/text_i18n_substrate_inventory.json:146-162` (`accepted_aliases`).

5. **Reserved `open(...)` and mode-literal diagnostic wording.** PRESENT. `verification/stdlib/text_i18n_substrate_inventory.md:82-86` and `.json:163-181` reserve `SIFR-IO-0801` (statically visible text-mode `open` without `encoding=`), `SIFR-IO-0802` (nonliteral/dynamic `open(...)` mode), and `SIFR-ENCODING-0803` (dynamic `errors=` handler) with canonical message + help text. M1 has a derivable contract.

6. **No-global-state policy as M0 evidence.** PRESENT. `verification/platform/platform_contract.md:38-40` adds a `## No Global State` section enumerating the enforcement points; `verification/platform/platform_contract.json:50-62` records the same as a `policies.no_unsynchronized_process_global_mutation` block with an `enforced_by` array. The text/i18n inventory cross-references the platform policy at `verification/stdlib/text_i18n_substrate_inventory.md:107-109` and a structured `no_global_state_policy` block at `verification/stdlib/text_i18n_substrate_inventory.json:319-324`.

7. **External review PASS missing on the platform contract / pass-1 + remediation in ledger.** PRESENT. Pass-1 outcome and remediation list recorded at `issues/ad-hoc-production-text-i18n-platform-substrate-execution.md:129-132`; post-remediation focused validation at lines 227-235 (e2e fail suite, JSON validity, platform golden, fmt, HIR guardrails, file-size guardrail all pass). This pass-2 file is the external review `PASS` on the shared platform contract artifacts.

Non-blocking observations (carried/new):

A. `verification/platform/golden/manifest.json` still references three `.sifr` files (`http_body_text_after_text_i18n.sifr`, `subprocess_text_explicit_encoding.sifr`, `locale_host_limited_formatting.sifr`) that do not exist on disk; they are correctly skipped today because `blocked_until` is non-empty. When `SIFR_PLATFORM_CLOSED_MILESTONES` is updated to unblock them, the runner will surface a missing-file error rather than a substrate diagnostic. Add stub sources (with `@cpu_heavy/@blocking_io` annotations and the expected stdout) before flipping `blocked_until`, or record a manifest-level note that those sources land in the providing milestone PRs.

B. The platform-golden runner relies on substring matches over both the diagnostic code (`SIFR-IMPORT-0008`) and the suggestion text (`sifr.encoding`) per `verification/platform/golden/manifest.json:30-32`. Any future wording change to the redirect note will break the merge gate via the substring match. Consider pinning to the stable code only and adding a comment explaining the coupling, or extracting the suggestion strings from a central constant.

C. Substrate inventory still uses `deferred-to-phase-adapter`, `deferred-to-phase-encoding-expansion`, and `deferred-to-phase-message-format-backends` (`verification/stdlib/text_i18n_substrate_inventory.md:29, 39-40, 55, 101`). The phase-doc nomenclature is `deferred-to-adapter-phase`. Either rename these for vocabulary consistency with the issue text, or add a single explanatory note that these are the `deferred-to-phase-X` template instantiations and which follow-up issue tracks each one.

D. The JSON `dependencies` array does not include the local `.mo` parser entry that the MD records ("Translation catalogs | local audited `.mo` parser | n/a | n/a | accepted for M4"). It is first-party code rather than a crate, so omission is reasonable, but consider adding a `first_party_runtime_components` JSON block so M4 reviewers can audit the same shape (panic/unsafe scope, typed error mapping, deterministic test strategy) without falling back to the MD only.

E. `verification/stdlib/text_i18n_m0_traceability.md:15` keeps the external-review requirement aimed at "these artifacts" without naming the review filename. Once this pass-2 file is committed, link it from the traceability matrix so the M0 checkbox flip can cite a concrete review artifact.
