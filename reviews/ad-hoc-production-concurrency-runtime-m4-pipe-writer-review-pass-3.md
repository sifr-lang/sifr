RESULT: PASS

Post-rebase verification of the M4 sync stdin pipe writer wave (PR #2356) over `origin/main` carrying PR #2354 (async process run timeout) and PR #2355 (async timeout ledger). Pass 1 and pass 2 PASS verdicts still hold: the rebase preserved the public stdlib surface, the stdin-mode guards on async run/output/timeout helpers, the typed-error sync stdin writer pipeline, the build_process_async_items helper-emission gating, the docs/manifests honesty, and the file-size guardrail. No blocker remained.

What was verified

- **Public stdlib threads `stdin_mode` into all three async entry points.** `lib/sifr/process.sifr:370-402` passes `command.stdin_mode` to `process_async_run` (6 args), `process_async_run_timeout` (7 args with `seconds` appended), and `process_async_output` (7 args with `has_stdin_data` appended). Sync `spawn` at `lib/sifr/process.sifr:251-267` rejects `command.has_stdin_data` with the `"stdin_bytes"` typed `ProcessError` before touching the intrinsic, and threads `stdin_mode` into the 8-arg `process_spawn` call alongside stdout/stderr modes.

- **Stdlib intrinsic metadata matches the stdlib call sites.** `crates/sifr_stdlib/src/process.rs:94-108` records `process_spawn` as 8 params including `stdin_mode: Str`. The async block at `:216-258` records `process_async_run` (6 params), `process_async_output` (7 params with trailing `has_stdin: Bool`), and `process_async_run_timeout` (7 params with `stdin_mode: Str` then `timeout_seconds: Float`). `process_child_stdin`, `process_pipe_write_all`, and `process_pipe_close` are registered with the expected `Result<i64|None, ProcessError>` shapes at `:109-153`.

- **Codegen lowerers enforce matching arities and preserve timeout ordering.** `crates/sifr_codegen/src/intrinsics/registry/process_async.rs:33-65` checks arity 6/7/7 for run/run_timeout/output, builds the owned 6-arg base (program/args/env/cwd cloned, has_cwd by value, stdin_mode cloned), and appends arg 6 last for both run_timeout (`timeout_seconds`) and output (`has_stdin`). `crates/sifr_codegen/src/intrinsics/registry/process_handles.rs:26-80` enforces arity 8 for `process_spawn` and arity 1/2 for the pipe handle and write/close shims.

- **Generated async preamble preserves split, guards, and gating.** `crates/sifr_codegen/src/preamble/process_async_runtime.rs:209-501` still exports `build_process_async_items(needs_run, needs_run_timeout, needs_output)` and unconditionally emits the shared `__sifr_process_status_from_exit` helper before each requested async helper. All three bodies start with `process_async_stdin_mode_guard()` (`:75-90`) which returns `Err(ProcessError("async process stdin mode requires owned pipe support"))` whenever `stdin_mode != "inherit"`. The run-timeout body additionally validates finiteness and sign (`:236-265`) and races a cancel-safe `__child.wait()` against `tokio::time::sleep(__duration)` with a kill+typed-timeout-`Status` path on the timer arm. The output body adds a `has_stdin` guard returning `"async process stdin bytes require owned pipe support"` (`:306-315`). Item ordering at `crates/sifr_codegen/src/lib_modules_and_codegen.rs:583-602` keeps `build_process_status_items()` → `build_process_async_items(...)` → `build_process_child_items()`, so `__sifr_process_exit_signal` is declared before the async helpers call it; `needs_process_status` is widened by `|| needs_process_async` at `:424`. Stdlib filter visits/patterns at `crates/sifr_codegen/src/stdlib_filter/implementation.rs:320-419` cover the new pipe writer symbols and the async run/run_timeout/output entry points in all three call sites. Emission check on `process_async_run_timeout.sifr` shows the expected `if stdin_mode != "inherit" { return Err(...); }` guard, the timeout finiteness guard, and the `__sifr_process_status_from_exit` / `__sifr_process_exit_signal` references in the `select!` arm.

- **Sync stdin writer is typed-error-only with no user-triggerable panic path.** `crates/sifr_codegen/src/preamble/process_child_pipes.rs:182-272` extracts `__child.stdin.take()` via `ok_or_else` (typed "process stdin pipe is not available or already taken for child handle: {handle}") and registers the writer into `__SIFR_PROCESS_PIPE_WRITERS` using the accepted poison-recovery `unwrap_or_else(|err| err.into_inner())` pattern. `__sifr_process_pipe_write_all` (`:404-460`) removes the writer from the map across the I/O call, returns a typed error if missing, propagates `std::io::Write::write_all` failures via `map_err`, and reinserts the writer on success. `__sifr_process_pipe_close` (`:462-485`) removes the writer and lets it drop. Sifr-level `PipeWriter._closed` short-circuits double close at `lib/sifr/process.sifr:97-108` before reaching the native helper. Repeated `Child.stdin()` is short-circuited inside the same `take()` path so the second call gets the same typed error. None of the new helpers call `.unwrap()` / `.expect()` on data-dependent state.

- **Docs/manifests preserve PR #2354/#2355 evidence and do not overclaim.** Ledger lines `issues/ad-hoc-production-concurrency-runtime-platform-substrate-execution.md:820-869` retain the PR #2354 merge entry (`dd24a7c3...`), the async preamble split note, and the post-rebase create-pr lane evidence (`100 passed`, `0 failed`, `cache_hits=4/26`, `report_signature=458ad42c8c1b262c`). The wave entry explicitly disclaims streaming reads, async pipes, async communicate, cancellation, scoped supervision, and Windows support. `verification/stdlib/concurrency_runtime_m4_process_traceability.md:11-23,30-32,38-40,46-47` adds `process_spawn_pipe_writer` evidence rows for `Command`, `Stdio`, `PipeWriter`, sync `spawn/wait/Child.wait`, CPython family mapping, and create-pr/merge validation lanes, while keeping the deferral language for async owned pipes / streaming reads / scoped supervision. `verification/platform/supported_host_matrix.md:20` adds a sync stdin pipe writers row marking Windows host-limited. `verification/validation_lanes/create_pr_e2e_manifest.json:94` and `verification/validation_lanes/merge_e2e_manifest.json:109` include `process_spawn_pipe_writer`.

- **File-size guardrail clears the 900-line cap.** `wc -l` confirms `process_runtime.rs` 537, `process_async_runtime.rs` 501, `process_child_pipes.rs` 485, `intrinsics/registry/process.rs` 849, `intrinsics/registry/process_handles.rs` 80, `intrinsics/registry/process_async.rs` 65, `lib/sifr/process.sifr` 469, `crates/sifr_stdlib/src/process.rs` 306. `python3 scripts/check_file_size_guardrails.py` reproduces PASS (2186 files, limit 900).

Local reproduction

- `python3 scripts/check_file_size_guardrails.py` → PASS (2186 files).
- `cargo run -q -p sifr -- run crates/sifr/tests/e2e/pass/process_spawn_pipe_writer.sifr` → PASS (artifact cache hit).
- `cargo run -q -p sifr -- run crates/sifr/tests/e2e/pass/process_async_run_timeout.sifr` → PASS (artifact cache hit).
- `cargo run -q -p sifr -- run crates/sifr/tests/e2e/pass/process_async_run_output.sifr` → PASS (artifact cache hit).
- `cargo run -q -p sifr -- emit crates/sifr/tests/e2e/pass/process_spawn_pipe_writer.sifr` → emits `__SIFR_PROCESS_PIPE_WRITERS`, `__sifr_process_child_stdin`, `__sifr_process_pipe_write_all`, `__sifr_process_pipe_close`, and the 8-arg `__sifr_process_spawn` signature; surface rejects `Command.stdin_bytes` with the `"stdin_bytes"` typed message.
- `cargo run -q -p sifr -- emit crates/sifr/tests/e2e/pass/process_async_run_timeout.sifr` → emits the `stdin_mode != "inherit"` guard, the `process timeout must be finite and non-negative` guard, and the `__sifr_process_async_run_timeout` 7-arg signature.

Blockers

None.

Non-blocking notes (do not block merge)

1. Pass 1 note 2 is still open: when `process_pipe_write_all` fails mid-I/O the native writer is dropped from the map but `PipeWriter._closed` stays `False` in Sifr, so the next `close()` reports `"process pipe writer handle is closed or unknown"` instead of the friendlier `"process pipe writer is already closed"`. Both are typed; not a correctness issue.
2. Pass 2 note 5 is still open: `__sifr_process_pipe_close` binds `let mut __pipe = __maybe_pipe.ok_or_else(...)?;` and never reads `__pipe`. The double-underscore prefix suppresses `unused_variables` so it compiles cleanly, but `let _pipe = ...` would be marginally tidier under future lint tightening.
3. Pass 2 note 3 still applies: `process_async_runtime.rs`, `process_runtime.rs`, `preamble/process_child_pipes.rs`, and `intrinsics/registry/process.rs` each re-declare private `process_error_expr` / `process_map_err` / `ok_expr` / `err_expr` helpers. A shared `preamble/process_common.rs` would consolidate the four copies; low priority and not for this PR.
4. Pass 1 note 3 still applies: fixture coverage for `Child.stdin()` against an `inherit`/`null` stdin (the genuine "missing pipe" path), empty `write_all`, and a long sequence of writes is still absent. Current coverage exercises the typed double-close, double-extraction, and `stdin_bytes` rejection paths and is sufficient for this wave.
