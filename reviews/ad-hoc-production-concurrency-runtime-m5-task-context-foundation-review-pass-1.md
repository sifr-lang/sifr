FAIL:
1. The new `lib/sifr/task.sifr` source is not included in the patch packet — only its registration in `crates/sifr_stdlib/src/sources.rs` is visible. Gate criteria 1 and 2 (value-model scoping; `ContextKey[T]` typed default marker without dynamic `contextvars` behavior) cannot be verified from the patch alone. Concrete fix: include the full content of `lib/sifr/task.sifr` in the patch packet so the reviewer can confirm `Context`, `ContextKey[T]`, and `empty_context()` are defined as value-model types (no mutation surface, no implicit propagation, typed marker/default on `ContextKey[T]`).
2. The new pass fixture `crates/sifr/tests/e2e/pass/task_context_value_model_basic.sifr` and fail fixture `crates/sifr/tests/e2e/fail/task_context_propagation_rejected.sifr` are not included in the patch packet. Gate criterion 3 (fixtures cover importable value-model behavior and continued rejection of non-`None` `ctx=` propagation with `SIFR-TYPE-0002`) cannot be verified. Concrete fix: include both fixture sources (and any expected-diagnostic snapshots) in the patch packet so the reviewer can confirm the pass exercises only value-model imports/construction and the fail pins non-`None` `ctx=` rejection at the documented diagnostic code.
