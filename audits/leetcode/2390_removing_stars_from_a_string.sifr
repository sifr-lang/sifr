# LeetCode 2390: Removing Stars From A String

def removeStars(s) :
    res = []
    for c in s :
        if res and c == '*':
            res.pop()
        else:
            res.append(c)
    return ''.join(res)

def main():
    assert str(removeStars("leet**cod*e")) == 'lecoe'


