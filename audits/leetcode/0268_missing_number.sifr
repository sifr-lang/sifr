# LeetCode 268: Missing Number

def missingNumber(nums: list[int]) -> int:
    res = len(nums)

    for i in range(len(nums)):
        res += i - nums[i]
    return res

def main():
    assert str(missingNumber([3,0,1])) == '2'
    assert str(missingNumber([0,1])) == '2'


