# LeetCode 540: Single Element In A Sorted Array

def singleNonDuplicate(nums: list[int]) -> int:
    acc: int = 0
    for value in nums:
        acc = acc ^ value
    return acc

def main():
    assert str(singleNonDuplicate([1,1,2,3,3,4,4,8,8])) == '2'

