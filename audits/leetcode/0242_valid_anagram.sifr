# LeetCode 242: Valid Anagram

def isAnagram(s: str, t: str) -> bool:
    if len(s) != len(t):
        return False
    return sorted(s) == sorted(t)

def main():
    assert str(isAnagram("anagram", "nagaram")) == 'true'
    assert str(isAnagram("rat", "car")) == 'false'
