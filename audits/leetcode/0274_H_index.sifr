# LeetCode 274: H Index

def hIndex(mut citations: list[int]) -> int:
    length = len(citations)
    citations.sort()
    for i in range(length):
        if citations[i] >= length - i:
            return length - i
    return 0

def main():
    assert str(hIndex([3,0,6,1,5])) == '3'


