[package]
links = "sifr_source_compiled_identity"
name = "sifr_source"
version = "0.0.0"
publish = false
edition = { workspace = true }
rust-version = { workspace = true }
license = { workspace = true }

[dependencies]
ruff_text_size = { workspace = true }

[lints]
workspace = true

[build-dependencies]
sifr_identity = { workspace = true, features = ["build"] }
