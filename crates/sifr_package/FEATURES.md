# Sifr Package Feature Notes

Milestone 37.1 records feature metadata but does not activate or map Sifr features to Cargo features yet.

Future milestones must update this file when:

- a Sifr feature maps to a Cargo package feature;
- a backend-only Cargo feature can affect generated Rust;
- a feature influences trust validation, graph digests, or package source selection.
