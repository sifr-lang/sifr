# Builtin sum wrong arity.
# expect-error[col=21]: SIFR-CALL-0001
# Test: builtin sum still enforces its iterable-only arity when it is not shadowed.

def main():
    data: list[int] = [1, 2, 3]
    print(sum(data, data))
