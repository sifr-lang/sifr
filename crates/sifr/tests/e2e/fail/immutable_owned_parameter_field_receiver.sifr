# expect-error[col=5]: SIFR-OWN-0005
class Helper:
    items: list[int]

    def bump(mut self) -> None:
        self.items.append(1)


class Owner:
    helper: Helper


def mutate(own owner: Owner) -> None:
    owner.helper.bump()
