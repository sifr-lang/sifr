# Reference: phase_psp_iter_fix_1_reversed_iterator_not_reversible
# expect-error: SIFR-TYPE-0001: reversed() argument must be reversible, got 'Iterator[int]'
def main() -> None:
    nums: list[int] = [1, 2, 3]
    it: Iterator[int] = iter(nums)
    _rev = reversed(it)
