# expect-error[col=20]: SIFR-TYPE-0004

from enum import Enum

class Direction(Enum):
    NORTH = 1
    SOUTH = 2

    def same(self, other) -> bool:
        return True

def main():
    print(Direction.NORTH.same(Direction.SOUTH))
