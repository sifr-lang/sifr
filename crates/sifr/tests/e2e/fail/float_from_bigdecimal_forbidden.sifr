# expect-error: SIFR-DECIMAL-0006: float(bigdecimal_value) is not allowed; bigdecimal values are exact and cannot be converted to float
def main():
    d: bigdecimal = BigDecimal("1.5")
    x: float = float(d)
