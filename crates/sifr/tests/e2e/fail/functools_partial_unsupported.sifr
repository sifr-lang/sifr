# Reference: stdlib_parity_b2_functools_partial_unsupported
from sifr.functools import partial


def add(a: int, b: int) -> int:
    return a + b


def main():
    add_one = partial(add, 1)
    assert str(add_one(2)) == "3"
