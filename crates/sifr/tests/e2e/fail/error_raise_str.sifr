# expect-error: SIFR-TYPE-0001: raise requires an Error class instance — `raise "message"` is not allowed, use e.g. `raise ValueError("message")`

def broken() -> Result[int, ValueError]:
    raise "this is a string"

def main():
    _ = broken()
