class Local(NonSend):
    pass


class Inner[T]:
    value: T

    def __neg__(own self) -> T:
        return -self.value


class Wrap[T]:
    value: T

    def negated(self) -> T:
        return -self.value


def invalid(value: Wrap[Inner[Local]]) -> Inner[Local]:
    return value.negated()
