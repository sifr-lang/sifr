# expect-error[col=25]: SIFR-OWN-0002
class Helper:
    items: list[int]

    def absorb(self, mut other: Helper) -> None:
        self.items.append(1)
        other.items.append(2)


class Owner:
    helper: Helper


def conflict(mut owner: Owner) -> None:
    owner.helper.absorb(owner.helper)
