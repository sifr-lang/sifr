# Reference: stdlib_parity_rng_1_system_random_state_unsupported
from sifr.random import RandomState, SystemRandom


def main():
    system_random: SystemRandom = SystemRandom()
    _state: RandomState = system_random.getstate()
    _ = _state
