# expect-error: SIFR-MATCH-0002: match guard must be a bool expression, got 'int'
def classify(x: int) -> str:
    match x:
        case n if n + 1:
            return "truthy"
        case _:
            return "other"

def main():
    print(classify(5))
