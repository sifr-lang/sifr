# Reference: stdlib_parity_b2_operator_attrgetter_unsupported
from sifr.operator import attrgetter


class Point:
    x: int

    def __init__(self, x: int):
        self.x = x


def main():
    get_x = attrgetter("x")
    assert str(get_x(Point(3))) == "3"
