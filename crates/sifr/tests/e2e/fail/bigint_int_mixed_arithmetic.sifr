# expect-error: SIFR-TYPE-0006: cannot mix 'int' and 'bigint' in arithmetic; use bigint() or int() to convert explicitly
def main():
    x: int = 42
    y: bigint = bigint(100)
    z: bigint = x + y  # error: cannot mix int and bigint
