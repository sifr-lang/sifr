# expect-error: SIFR-IMPORT-0002: unknown import target: 'sifr.nonexistent'
# Test that importing from a nonexistent stdlib module fails
from sifr.nonexistent import foo

def main():
    x: int = foo()
    print(x)
