# expect-error: SIFR-RESULT-0002: `str` is not a valid error type in Result — use a class extending Error, e.g. `Result[int, ValueError]`

def broken() -> Result[int, str]:
    return 42

def main():
    _ = broken()
