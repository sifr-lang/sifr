# expect-error[col=21]: SIFR-STDLIB-0001
class Helper:
    values: list[int]

    def bump(mut self) -> None:
        self.values.append(1)


class Owner:
    helper: Helper | None

    def mutate(mut self) -> None:
        self.helper.bump()
