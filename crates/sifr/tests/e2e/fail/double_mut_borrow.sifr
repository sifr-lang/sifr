# expect-error: SIFR-TYPE-0001: cannot borrow 'items' as mutable more than once in the same call to 'swap'
def swap(mut a: list[int], mut b: list[int]):
    pass

def main():
    items: list[int] = [1, 2, 3]
    swap(items, items)
