# expect-error: SIFR-OWN-0010
class LocalCell(NonSend):
    pass


async def main() -> Result[None, ScopeFailure]:
    cell: LocalCell = LocalCell()

    @cpu_heavy
    def use_cell() -> int:
        assert cell is not None
        return 1

    handle = task.spawn_cpu(use_cell)
    await task.sleep(0.0)
    return None
