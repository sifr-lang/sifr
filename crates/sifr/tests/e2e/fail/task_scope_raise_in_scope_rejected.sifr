# expect-error[col=15]: SIFR-RESULT-0003

async def worker() -> int:
    return 1


async def main() -> Result[None, ScopeFailure]:
    async with task.scope() as scope:
        handle = scope.spawn(worker())
        raise ValueError("early exit")
    return None
