# expect-error: SIFR-OWN-0006
# Test: borrowed parameters cannot be rebound unless explicitly marked mut.

def borrowed_reassign(items: list[int]) -> int:
    items = [1, 2, 3]
    return len(items)

def main():
    print("unreachable")
