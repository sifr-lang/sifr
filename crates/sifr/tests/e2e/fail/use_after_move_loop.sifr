# expect-error: SIFR-TYPE-0001: value 's' is moved inside loop body; it would be unavailable on subsequent iterations
def consume(own s: str) -> int:
    return len(s)

def main():
    s: str = "hello"
    for i in range(3):
        result: int = consume(s)
        print(result)
