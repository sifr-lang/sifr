# expect-error: SIFR-ASYNC-0005
def compute_value() -> int:
    return 42


async def main() -> Result[None, ScopeFailure]:
    async with task.scope() as scope:
        handle = scope.spawn_cpu(compute_value)
        await task.sleep(0.0)
    return None
