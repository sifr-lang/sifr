# expect-error: SIFR-TYPE-0002
class LocalParent(NonSend):
    pass

class LocalChild(LocalParent):
    pass

def extract(values: dict[str, LocalChild]) -> list[LocalChild]:
    return values.values()
