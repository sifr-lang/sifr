# expect-error: SIFR-MATCH-0001: non-exhaustive match: type 'int' cannot be fully covered by literal patterns — add `case _:` to handle remaining values
def describe(x: int) -> str:
    match x:
        case 1:
            return "one"
        case 2:
            return "two"

def main():
    print(describe(3))
