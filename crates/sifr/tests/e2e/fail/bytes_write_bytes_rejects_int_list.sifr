# Bytes write bytes rejects int list.
def main():
    path: str = "/tmp/sifr_bytes_write_bytes_rejects_int_list.bin"
    fh = open(path, "wb")
    raw: list[int] = [1, 2, 3]
    _attempt: Result[None, IOError] = fh.write_bytes(raw)
    fh.close()
