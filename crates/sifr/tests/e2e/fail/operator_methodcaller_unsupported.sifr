# Reference: stdlib_parity_b2_operator_methodcaller_unsupported
from sifr.operator import methodcaller


class Greeter:
    def greet(self, who: str) -> str:
        return "hi " + who


def main():
    call_greet = methodcaller("greet", "sifr")
    assert call_greet(Greeter()) == "hi sifr"
