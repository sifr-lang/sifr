# expect-error: SIFR-CALL-0001: sqrt() takes at most 1 argument(s), got 2
# Test that calling a stdlib function with wrong number of arguments fails
from sifr.math import sqrt

def main():
    x: float = sqrt(1.0, 2.0)
    print(x)
