# expect-error: SIFR-OWN-0013
from sifr.ipc import require_serializable
from sifr.sync import Channel, ChannelSender


def main():
    channel: Channel[int] = Channel([], 1)
    sender: ChannelSender[int] = ChannelSender(channel)
    require_serializable(sender)
