# expect-error: SIFR-CALL-0003: greet() got multiple values for argument 'name'
def greet(name: str, greeting: str) -> str:
    return f"{greeting}, {name}!"

def main():
    print(greet("Alice", name="Bob"))
