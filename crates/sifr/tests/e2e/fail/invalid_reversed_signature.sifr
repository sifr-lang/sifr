# Reference: stdlib_parity_iter_fix_7_invalid_reversed_signature
# expect-error[col=9]: SIFR-PROTO-0002
class BadReversed:
    def __iter__(self) -> Iterator[int]:
        return iter([1, 2])

    def __reversed__(self) -> int:
        return 0


def main():
    _ = BadReversed()
