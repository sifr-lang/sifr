# expect-error[col=15]: SIFR-STDLIB-0001
class Helper:
    values: list[int]

    def bump(mut self) -> None:
        self.values.append(1)


def mutate_indexed(values: list[Helper]) -> None:
    values[0].bump()
