# expect-error: SIFR-CLASS-0003: enum 'Status' has duplicate value 200: variants 'OK' and 'SUCCESS'
# Test: enum with duplicate integer values
from enum import Enum

class Status(Enum):
    OK = 200
    SUCCESS = 200
    NOT_FOUND = 404

def main():
    s: Status = Status.OK
    print(s)
