# Reference: stdlib_parity_struct_0_json_dynamic_hooks_unsupported
from sifr.json import loads


def _object_hook(value: str) -> str:
    return value


def main() -> None:
    _decoded = loads("{\"a\":1}", _object_hook)
