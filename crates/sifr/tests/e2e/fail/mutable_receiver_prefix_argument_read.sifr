# expect-error[col=32]: SIFR-OWN-0002
class Helper:
    items: list[int]

    def observe_owner(self, owner: Owner) -> None:
        self.items.append(len(owner.helper.items))


class Owner:
    helper: Helper


def conflict(mut owner: Owner) -> None:
    owner.helper.observe_owner(owner)
