# expect-error: SIFR-TYPE-0001: unsupported operand type(s) for +: 'None | int' and 'int'
# Test: optional arithmetic is rejected without explicit narrowing

def add_one(x: int | None) -> int:
    return x + 1

def main():
    value: int = add_one(5)
    print(value)
