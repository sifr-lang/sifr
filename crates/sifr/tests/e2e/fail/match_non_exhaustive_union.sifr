# expect-error: SIFR-MATCH-0001: non-exhaustive match: type 'None | int' has uncovered variants: None — add matching case(s) or `case _:`
def describe(x: int | None) -> str:
    match x:
        case int():
            return "integer"
        # Missing None case - should error about non-exhaustive match

def main():
    print(describe(None))
