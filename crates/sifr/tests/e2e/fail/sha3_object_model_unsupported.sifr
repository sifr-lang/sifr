# Reference: stdlib_parity_rng_2_sha3_object_model_unsupported
from sifr.hashlib import HashObject, sha3_256_obj


def main() -> None:
    # sha3_256_obj currently stays on Result-based unsupported boundary.
    h: HashObject = sha3_256_obj("abc")
    _ = h.hexdigest()
