# Reference: stdlib_parity_ext_2_itertools_materialization_required
from sifr.itertools import accumulate


def main():
    values: list[int] = accumulate([1, 2, 3])
    assert str(values) == "[1, 3, 6]"
