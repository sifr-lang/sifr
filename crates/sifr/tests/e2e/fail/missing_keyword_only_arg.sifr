# expect-error: SIFR-TYPE-0001: cannot return borrowed parameter `name`: it is borrowed by default -- use `own name` to take ownership, or return `name.clone()`
def display(name: str, *, verbose: bool) -> str:
    if verbose:
        return f"Name: {name}"
    return name

def main():
    print(display("Alice"))
