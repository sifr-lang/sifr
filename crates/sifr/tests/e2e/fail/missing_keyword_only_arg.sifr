# expect-error: SIFR-OWN-0003: cannot return borrowed parameter `name`: borrowed parameters cannot escape -- add `own` at the signature boundary or return `name.clone()`
def display(name: str, *, verbose: bool) -> str:
    if verbose:
        return f"Name: {name}"
    return name

def main():
    print(display("Alice"))
