# Hashlib pbkdf2 hmac unsupported.
from sifr.hashlib import pbkdf2_hmac


def main() -> None:
    # pbkdf2_hmac remains explicitly unsupported in stdlib_parity_rng_2 scope.
    key: bytes = pbkdf2_hmac("sha256", "password", "salt", 4)
    _ = key
