# expect-error: SIFR-TYPE-0007: Callable type requires exactly 2 type parameters: [[param_types], return_type]

def consume(callback: Callable[[int], str, bool]) -> int:
    return 0

def main():
    print(consume(print))
