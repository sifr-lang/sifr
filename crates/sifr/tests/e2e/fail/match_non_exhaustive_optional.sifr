# expect-error: SIFR-MATCH-0001: non-exhaustive match: type 'None | str' has uncovered variants: None — add matching case(s) or `case _:`
def describe(x: str | None) -> str:
    match x:
        case str():
            return "has value"
        # Missing None case

def main():
    print(describe(None))
