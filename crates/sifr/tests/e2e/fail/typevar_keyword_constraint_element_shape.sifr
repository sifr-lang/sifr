# expect-error: SIFR-TYPE-0007: TypeVar constraints must be simple type names

from typing import TypeVar

T = TypeVar("T", constraints=(int, 1))

def echo(x: T) -> T:
    return x

def main():
    print(echo(1))
