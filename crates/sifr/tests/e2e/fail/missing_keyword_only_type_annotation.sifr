# expect-error: SIFR-TYPE-0004: parameter 'verbose' in function 'display' is missing a type annotation

def display(*, verbose) -> int:
    if verbose:
        return 1
    return 0

def main():
    print(display(verbose=True))
