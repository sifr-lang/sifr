# expect-error[col=17]: SIFR-TYPE-0007
# Test: generic classes reject the wrong number of type arguments.

class Pair[T]:
    value: T

def main():
    value: Pair[int, str] = Pair(1)
    print(value.value)
