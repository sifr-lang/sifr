# expect-error: SIFR-TYPE-0009: for loop tuple target expects iterable elements of tuple type, got 'int'

def main():
    values: list[int] = [1, 2, 3]
    for left, right in values:
        print(left + right)
