# expect-error: SIFR-TYPE-0002

async def worker() -> int:
    await task.sleep(0.0)
    return 1


async def main() -> Result[None, ScopeFailure]:
    async with task.TaskGroup(ctx="not-context") as group:
        handle = group.spawn(worker())
        result = await handle
    return None
