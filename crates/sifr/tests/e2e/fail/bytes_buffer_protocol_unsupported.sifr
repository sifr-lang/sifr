# Bytes buffer protocol unsupported.


class ByteLike:
    values: list[int]

    def __init__(self, values: list[int]) -> None:
        self.values = values


def main() -> None:
    payload: ByteLike = ByteLike([65, 66, 67])
    _ = payload.decode()
