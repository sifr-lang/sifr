# Test: mutable list variance is invariant (list[int] is not list[int | str])

def accepts_mixed(values: list[int | str]) -> int:
    return len(values)

def main():
    ints: list[int] = [1, 2, 3]
    size: int = accepts_mixed(ints)
    print(size)

# expect-error: expected 'list[int | str]', got 'list[int]'
