# expect-error: SIFR-TYPE-0001: function 'pick': missing argument 'x' with no default value
def seed() -> int:
    return 7

def pick(x: int = seed()) -> int:
    return x

def main():
    print(pick())
