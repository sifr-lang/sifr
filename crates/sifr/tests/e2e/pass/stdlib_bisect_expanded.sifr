# Test expanded sifr.bisect functions -- in-place API
from sifr.bisect import insort_left

def main():
    data: list[int] = [1, 3, 5, 7]
    insort_left(data, 4)
    assert str(data) == '[1, 3, 4, 5, 7]'

