# expect-stdout: positive
# expect-stdout: non-positive
def main():
    n: int = 5
    result: str = "positive" if n > 0 else "non-positive"
    print(result)

    m: int = -3
    result2: str = "positive" if m > 0 else "non-positive"
    print(result2)
