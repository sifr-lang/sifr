# Test: Field access after isinstance narrowing

class Dog:
    name: str
    age: int

def get_age(pet: Dog) -> int:
    return pet.age

def main():
    d: Dog = Dog("Rex", 42)
    assert get_age(d) == 42
