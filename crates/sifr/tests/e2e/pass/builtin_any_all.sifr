# Tests: any() and all() built-in functions

def main():
    mixed: list[bool] = [True, False, True]
    all_true: list[bool] = [True, True, True]
    assert any(mixed) == True
    assert all(mixed) == False
    assert any([False, False]) == False
    assert all(all_true) == True
