def main():
    items: list[int] = [1, 2, 3]
    items[-1] = 9
    assert str(items) == "[1, 2, 9]"

    items[-2] += 5
    assert str(items) == "[1, 7, 9]"

    del items[-1]
    assert str(items) == "[1, 7]"

    # Out-of-range negative delete is a safe no-op.
    del items[-5]
    assert str(items) == "[1, 7]"

    matrix: list[list[int]] = [[1, 2], [3, 4]]
    matrix[-1][-1] = 8
    assert str(matrix) == "[[1, 2], [3, 8]]"
