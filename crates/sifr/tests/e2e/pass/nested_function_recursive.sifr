# Test: Recursive nested function (common DFS/backtracking pattern)

def factorial_wrapper(n: int) -> int:
    def helper(x: int) -> int:
        if x <= 1:
            return 1
        return x * helper(x - 1)
    return helper(n)

def main():
    assert factorial_wrapper(5) == 120
