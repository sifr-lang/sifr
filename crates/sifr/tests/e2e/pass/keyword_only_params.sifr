def display(own name: str, *, verbose: bool = False) -> str:
    if verbose:
        return f"Name: {name}"
    return name

def main():
    assert display("Alice") == Alice
    assert display("Bob", verbose=True) == "Name: Bob"
