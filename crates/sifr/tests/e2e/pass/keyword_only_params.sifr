def display(own name: str, *, verbose: bool = False) -> str:
    if verbose:
        return f"Name: {name}"
    return name

def main():
    assert str(display("Alice")) == 'Alice'
    assert str(display("Bob", verbose=True)) == 'Name: Bob'
