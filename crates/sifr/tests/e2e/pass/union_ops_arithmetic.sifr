# Test: arithmetic on T|None

def add_one(x: int | None) -> int:
    return x + 1

def sub_one(x: int | None) -> int:
    return x - 1

def main():
    v: int | None = 5
    assert add_one(v) == 6
    assert sub_one(v) == 4
