# Test: module-level constants accessible from functions

PI: float = 3.14159
MAX_RETRIES: int = 3

def circle_area(r: float) -> float:
    return PI * r * r

def main():
    assert circle_area(5.0) == 78.53975
    assert MAX_RETRIES == 3
