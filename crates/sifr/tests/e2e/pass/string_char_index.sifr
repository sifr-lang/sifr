# expect-stdout: h
# expect-stdout: o
def main():
    s: str = "hello"
    c1: str | None = s[0]
    if c1 is not None:
        print(c1)
    c2: str | None = s[4]
    if c2 is not None:
        print(c2)
