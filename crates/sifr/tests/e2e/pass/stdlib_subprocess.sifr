
from sifr.subprocess import run, CompletedProcess

def main():
    try:
        result: CompletedProcess = run("echo ok")
        assert str("echo ok = " + result.stdout.strip()) == 'echo ok = ok'
    except IOError as e:
        _ = str("error: " + e.message)
