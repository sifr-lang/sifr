
from sifr.subprocess import run, CompletedProcess

def main():
    try:
        result: CompletedProcess = run("echo ok")
        assert "echo ok = " + result.stdout.strip() == "echo ok = ok"
    except IOError as e:
        print("error: " + e.message)
