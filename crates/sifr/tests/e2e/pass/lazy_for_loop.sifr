# Tests: for loop consumes generator lazily
def squares(n: int) -> list[int]:
    i: int = 1
    while i <= n:
        yield i * i
        i = i + 1

def main():
    output: list[int] = []
    for s in squares(3):
        output.append(s)
    assert output == [1, 4, 9]
