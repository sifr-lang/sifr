# Test: Forward reference - function uses class defined later
# expect-stdout: 42

def get_value(node: Node) -> int:
    return node.value

class Node:
    value: int

def main():
    n: Node = Node(42)
    print(get_value(n))
