
def read_file() -> Result[str, IOError]:
    raise IOError("file not found")

def main():
    try:
        data: str = read_file()
    except Error as e:
        assert str(f"caught: {e.message}") == 'caught: file not found'

    assert str("catch-all works") == 'catch-all works'
