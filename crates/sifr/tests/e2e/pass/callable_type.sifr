# Test: Callable type for higher-order functions

from typing import Callable

def apply(f: Callable[[int], int], x: int) -> int:
    return f(x)

def double(x: int) -> int:
    return x * 2

def add_one(x: int) -> int:
    return x + 1

def main():
    assert apply(double, 5) == 10
    assert apply(add_one, 6) == 7
