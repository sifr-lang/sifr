from sifr.itertools import accumulate

def main():
    data: list[int] = [1, 2, 3, 4, 5]
    result: list[int] = list(accumulate(iter(data)))
    outputs: list[int] = []
    for v in result:
        outputs.append(v)
    assert outputs == [1, 3, 6, 10, 15]
