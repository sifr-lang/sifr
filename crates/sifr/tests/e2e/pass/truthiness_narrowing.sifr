# Truthiness-based narrowing

def check(x: str | None) -> str:
    if x:
        return "has value"
    else:
        return "no value"

def main():
    result: str = check("hello")
    assert result == "has value"
