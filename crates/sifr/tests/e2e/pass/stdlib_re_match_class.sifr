# Test Match class
from sifr.re import Match

def main():
    m: Match = Match("hello", 2, 7)
    assert m.group() == hello
    assert m.start() == 2
    assert m.end() == 7
    sp: list[int] = m.span()
    assert sp[0] == 2
    assert sp[1] == 7
    assert m.to_str() == hello

