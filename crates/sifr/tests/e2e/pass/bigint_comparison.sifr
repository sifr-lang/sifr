def main():
    a: bigint = bigint(100)
    b: bigint = bigint(200)
    assert a == b == False
    assert a != b == True
    assert a < b == True
    assert a > b == False
    assert a <= b == True
    assert a >= b == False
    c: bigint = bigint(100)
    assert a == c == True

