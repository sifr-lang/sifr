
def process(x: int | None) -> str:
    if x is None:
        return "got none"
    return "got " + str(x)

def main():
    assert str(process(None)) == 'got none'
    assert str(process(42)) == 'got 42'
