def main():
    big: bigint = bigint(10) ** 20
    try:
        n: int = int(big)
        assert "should not reach here" == "caught overflow"
    except OverflowError as e:
        print("caught overflow")

