# F-string basic interpolation
def main():
    name: str = "Alice"
    age: int = 30
    msg: str = f"Hello, {name}! You are {age} years old."
    assert str(msg) == 'Hello, Alice! You are 30 years old.'
