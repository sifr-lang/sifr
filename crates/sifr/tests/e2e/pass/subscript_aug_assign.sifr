# Test: augmented subscript assignment
# expect-stdout: 5
# expect-stdout: 7

def main():
    counts: list[int] = [0, 0, 0]
    counts[1] += 5
    print(counts[1])
    counts[1] += 2
    print(counts[1])
