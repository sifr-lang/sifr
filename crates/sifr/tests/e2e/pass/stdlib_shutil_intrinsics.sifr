
from sifr.shutil import disk_usage

def main():
    usage: list[int] = disk_usage("/")
    assert "disk_usage len = " + str(len(usage)) == "disk_usage len = 3"
    assert "disk_total > 0 = " + str(usage[0] > 0) == "disk_total > 0 = true"
