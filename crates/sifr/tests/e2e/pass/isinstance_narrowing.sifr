# isinstance-based type narrowing

def describe(own x: int | str) -> str:
    if isinstance(x, int):
        return f"number: {x + 1}"
    else:
        return f"text: {x}"

def main():
    result: str = describe(42)
    assert result == "number: 43"
