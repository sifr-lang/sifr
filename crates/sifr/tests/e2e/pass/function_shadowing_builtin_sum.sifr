# Reference: narrowing_builtin_shadow_sum
# Test: user-defined functions should shadow builtin call lowering.

def sum(num1: int, num2: int) -> int:
    return num1 + num2

def main():
    assert str(sum(12, 5)) == '17'
    assert str(sum(-10, 4)) == '-6'
