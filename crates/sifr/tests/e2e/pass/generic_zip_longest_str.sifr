from sifr.itertools import zip_longest

def main():
    a: list[str] = ["a", "b", "c"]
    b: list[str] = ["x", "y"]
    result: list[list[str]] = zip_longest(a, b, "-")
    assert result == [["a", "x"], ["b", "y"], ["c", "-"]]
