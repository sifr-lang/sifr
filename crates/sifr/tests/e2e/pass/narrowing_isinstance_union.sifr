# Test: isinstance narrowing on union type with field access

class Dog:
    name: str

class Cat:
    name: str

def get_name(own pet: Dog | Cat) -> str:
    if isinstance(pet, Dog):
        return pet.name
    return "unknown"

def main():
    d: Dog = Dog("Rex")
    assert get_name(d) == Rex
