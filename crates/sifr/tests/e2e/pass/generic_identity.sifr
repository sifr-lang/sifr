# Test: generic identity function with TypeVar

from typing import TypeVar

T = TypeVar("T")

def identity(x: T) -> T:
    return x

def main():
    a: int = identity(42)
    assert str(a) == '42'
    b: str = identity("hello")
    assert str(b) == 'hello'
