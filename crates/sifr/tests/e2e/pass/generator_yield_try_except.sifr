from sifr.test import assert_eq

def gen() -> Iterator[int]:
    emitted: bool = False
    while not emitted:
        emitted = True
        yield 1


def main():
    values: list[int] = []
    for v in gen():
        values.append(v)
    assert_eq(values, [1])
