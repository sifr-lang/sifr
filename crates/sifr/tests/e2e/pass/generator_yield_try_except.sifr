from sifr.test import assert_eq

def gen() -> list[int]:
    try:
        yield 1
    except Error as e:
        yield 2


def main():
    values: list[int] = []
    for v in gen():
        values.append(v)
    assert_eq(values, [1])
