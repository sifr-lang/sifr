# Test: nested subscript assignment

def main():
    matrix: list[list[int]] = [[1, 2], [3, 4]]
    try:
        matrix[0][1] = 99
    except IndexError as e:
        return
    assert str(matrix[0][1]) == '99'
