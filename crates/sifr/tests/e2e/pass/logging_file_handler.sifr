
from sifr.logging import FileHandler

def main():
    path: str = "/tmp/sifr_log_fh_test.log"
    handler: FileHandler = FileHandler(path)
    handler.emit("INFO", "test", "hello from file handler")
    assert "handler emitted ok" == "handler emitted ok"
