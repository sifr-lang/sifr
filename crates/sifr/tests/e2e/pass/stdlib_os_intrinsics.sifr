
from sifr.os import getpid, cpu_count

def main():
    pid: int = getpid()
    assert str("pid > 0 = " + str(pid > 0)) == 'pid > 0 = true'

    cpus: int = cpu_count()
    assert str("cpu_count >= 1 = " + str(cpus >= 1)) == 'cpu_count >= 1 = true'
