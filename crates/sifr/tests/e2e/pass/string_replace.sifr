def main():
    s: str = "hello world"
    assert str(s.replace("world", "universe")) == 'hello universe'
    assert str(s.startswith("hello")) == 'true'
    assert str(s.endswith("world")) == 'true'
