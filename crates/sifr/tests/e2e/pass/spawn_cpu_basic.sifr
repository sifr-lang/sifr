@cpu_heavy
def compute_value() -> int:
    return 42


async def main() -> Result[None, ScopeFailure]:
    handle = task.spawn_cpu(compute_value)
    result = await handle
    assert str(result) == "Ok(42)"
    return None
