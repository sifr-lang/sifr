from enum import Enum

class Color(Enum):
    RED = 1
    GREEN = 2
    BLUE = 3

def main():
    c: Color = Color.RED
    assert str(c) == 'RED'
    c2: Color = Color.GREEN
    assert str(c2) == 'GREEN'
    assert str(c == Color.RED) == 'true'
    assert str(c == Color.GREEN) == 'false'

