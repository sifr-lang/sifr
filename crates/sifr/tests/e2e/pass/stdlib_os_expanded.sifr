# Test expanded sifr.os functions
from sifr.os import getcwd

def main():
    try:
        cwd: str = getcwd()
        print(len(cwd) > 0)
    except IOError as e:
        print("ERROR: " + e.message)

# expect-stdout: true
