# Tests bitwise operators: &, |, ^, <<, >>

def main():
    a: int = 5
    b: int = 3
    assert str(a & b) == '1'
    assert str(a | b) == '7'
    assert str(a ^ b) == '6'
    assert str(5 << 1) == '10'
    assert str(a >> 1) == '2'
