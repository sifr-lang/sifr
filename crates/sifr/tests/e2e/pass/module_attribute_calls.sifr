from sifr.math import fmod

def reverse_last_digit(x: int) -> int:
    return int(fmod(float(x), 10.0))

def main():
    assert str(reverse_last_digit(123)) == "3"
    assert str(reverse_last_digit(-123)) == "-3"
