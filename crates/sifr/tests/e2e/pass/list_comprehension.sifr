# Tests: list comprehension with and without filter

def main():
    nums: list[int] = [1, 2, 3, 4, 5]
    doubled: list[int] = [x * 2 for x in nums]
    assert str(doubled) == '[2, 4, 6, 8, 10]'
    big: list[int] = [x for x in nums if x > 3]
    assert str(big) == '[4, 5]'
