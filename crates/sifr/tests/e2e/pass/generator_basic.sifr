# Tests: basic generator function with yield

def count_up(n: int) -> Iterator[int]:
    i: int = 0
    while i < n:
        yield i
        i = i + 1

def main():
    nums: list[int] = list(count_up(5))
    assert str(nums) == '[0, 1, 2, 3, 4]'
