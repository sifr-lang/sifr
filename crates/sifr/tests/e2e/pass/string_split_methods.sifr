
def main():
    parts: list[str] = "a,b".split(",")
    assert str("parts_len = " + str(len(parts))) == 'parts_len = 2'

    words: list[str] = "one two three".split()
    assert str("words_len = " + str(len(words))) == 'words_len = 3'
