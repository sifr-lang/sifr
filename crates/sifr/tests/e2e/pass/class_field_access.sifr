
class Person:
    name: str
    age: int

    def __init__(self, name: str, age: int):
        self.name = name
        self.age = age

def main():
    p: Person = Person("Alice", 30)
    assert p.name == Alice
    assert p.age == 30
