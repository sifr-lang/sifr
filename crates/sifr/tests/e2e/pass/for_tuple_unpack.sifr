# Test: tuple unpacking in for loop

def main():
    pairs: list[tuple[int, str]] = [(1, "a"), (2, "b")]
    outputs: list[int] = []
    for i, s in pairs:
        outputs.append(i)
    assert outputs == [1, 2]
