# Tests: generator function returns a lazy iterator
def count_up(n: int) -> Iterator[int]:
    i: int = 0
    while i < n:
        yield i
        i = i + 1

def main():
    output: list[int] = []
    for x in count_up(3):
        output.append(x)
    assert output == [0, 1, 2]
