# expect-stdout: 0
# expect-stdout: 1
# expect-stdout: 2
# Tests: generator function returns a lazy iterator

def count_up(n: int) -> list[int]:
    i: int = 0
    while i < n:
        yield i
        i = i + 1

def main():
    for x in count_up(3):
        print(x)
