# Tests: sorted and reversed built-in functions

def main():
    nums: list[int] = [3, 1, 4, 1, 5]
    assert str(sorted(nums)) == '[1, 1, 3, 4, 5]'
    assert str(reversed(nums)) == '[5, 1, 4, 1, 3]'
