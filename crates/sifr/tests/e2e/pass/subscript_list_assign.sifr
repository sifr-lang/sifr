# Tests list subscript assignment: list[i] = val

def main():
    nums: list[int] = [1, 2, 3]
    try:
        nums[0] = 10
    except IndexError as e:
        return
    assert str(nums) == '[10, 2, 3]'
