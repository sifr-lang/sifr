# Tests that print() doesn't consume values - they can be reused

def main():
    s: str = "hello"
    assert str(s) == 'hello'
    assert str(s) == 'hello'
    nums: list[int] = [1, 2, 3, 4, 5]
    assert str(len(nums)) == '5'
