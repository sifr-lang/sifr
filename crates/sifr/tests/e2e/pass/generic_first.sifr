# Test: generic function operating on list[T]

from typing import TypeVar

T = TypeVar("T")

def first(items: list[T]) -> T:
    return items[0]

def main():
    nums: list[int] = [10, 20, 30]
    assert first(nums) == 10
    words: list[str] = ["hello", "world"]
    assert first(words) == hello
