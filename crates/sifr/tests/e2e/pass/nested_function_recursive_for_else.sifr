# expect-stdout: 0

def main():
    def rec(n: int) -> int:
        items: list[int] = [1]
        for i in items:
            pass
        else:
            if n > 0:
                return rec(n - 1)
        return 0

    print(rec(2))
