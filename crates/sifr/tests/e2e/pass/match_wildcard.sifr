def describe(x: int) -> str:
    match x:
        case 42:
            return "the answer"
        case _:
            return "something else"

def main():
    assert describe(42) == "the answer"
    assert describe(0) == "something else"
    assert describe(100) == "something else"

