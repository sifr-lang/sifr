def describe(x: int) -> str:
    match x:
        case 42:
            return "the answer"
        case _:
            return "something else"

def main():
    assert str(describe(42)) == 'the answer'
    assert str(describe(0)) == 'something else'
    assert str(describe(100)) == 'something else'

