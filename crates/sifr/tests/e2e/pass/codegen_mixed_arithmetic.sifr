# Tests that mixed int/float arithmetic auto-promotes int to float

def main():
    a: int = 10
    b: float = 3.5
    result: float = a + b
    assert str(result) == '13.5'
    c: float = 2.5
    d: int = 3
    result2: float = c * d
    assert str(result2) == '7.5'
