# Equality-based narrowing for literal types

def describe_shape(own shape: str) -> str:
    if shape == "circle":
        return "circle area"
    else:
        return "other shape"

def main():
    result: str = describe_shape("circle")
    assert result == "circle area"
