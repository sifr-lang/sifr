def main():
    x: int = 10
    x += 5
    assert x == 15

    y: float = 3.5
    y *= 3.0
    assert y == 10.5

    s: str = "Hello"
    s += "World"
    assert s == HelloWorld

    items: list[int] = [1, 2]
    items += [3]
    assert len(items) == 3
