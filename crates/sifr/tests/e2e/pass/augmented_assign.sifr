def main():
    x: int = 10
    x += 5
    assert str(x) == '15'

    y: float = 3.5
    y *= 3.0
    assert str(y) == '10.5'

    s: str = "Hello"
    s += "World"
    assert str(s) == 'HelloWorld'

    items: list[int] = [1, 2]
    items += [3]
    assert str(len(items)) == '3'
