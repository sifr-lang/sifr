# expect-stdout: o
# expect-stdout: l
def main():
    s: str = "hello"
    c1: str | None = s[-1]
    if c1 is not None:
        print(c1)
    c2: str | None = s[-2]
    if c2 is not None:
        print(c2)
