# Tests return type inference when -> annotation is omitted

def double(x: int):
    return x * 2

def greet(name: str):
    return "hello " + name

def main():
    assert str(double(5)) == '10'
    assert str(greet("world")) == 'hello world'
