# Tests bitwise augmented assignment operators

def main():
    x: int = 5
    x &= 3
    assert str(x) == '1'
    x |= 6
    assert str(x) == '7'
    x ^= 3
    assert str(x) == '4'
    x <<= 0
    assert str(x) == '4'
    x >>= 2
    assert str(x) == '1'
