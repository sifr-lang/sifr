# Optional type with is not None narrowing

def greet(name: str | None) -> str:
    if name is not None:
        return name.upper()
    else:
        return "stranger"

def main():
    result: str = greet("alice")
    assert result == ALICE
