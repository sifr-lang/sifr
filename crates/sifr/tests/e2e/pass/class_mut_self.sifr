# Test: &mut self for methods that mutate fields
# expect-stdout: 2

class Counter:
    count: int
    def increment(self) -> None:
        self.count += 1

def main():
    c: Counter = Counter(0)
    c.increment()
    c.increment()
    print(c.count)
