# Test: &mut self for methods that mutate fields

class Counter:
    count: int
    def increment(mut self) -> None:
        self.count += 1

def main():
    c: Counter = Counter(0)
    c.increment()
    c.increment()
    assert str(c.count) == '2'
