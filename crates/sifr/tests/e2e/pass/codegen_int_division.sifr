# Tests that int / int produces float (true division), not integer division

def main():
    a: int = 10
    b: int = 3
    result: float = a / b
    assert str(result) == '3.3333333333333335'
    c: int = 5
    d: int = 2
    result2: float = c / d
    assert str(result2) == '2.5'
