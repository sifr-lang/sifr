def main() -> Result[None, DivisionError]:
    x: bigdecimal = BigDecimal("1")
    y: bigdecimal = BigDecimal("0")
    try:
        result: bigdecimal = x / y
        assert False, str(result)
    except DivisionError as error:
        raise error
    return None
