/// Rust emitted from checked stdlib source, plus the source identity that produced it.
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct StdlibRustSource {
    pub module: String,
    pub source_path: String,
    pub source_sha256: String,
    pub rust: String,
}
